// M17 --- Матроиды: почему жадный алгоритм работает.

#import "../common.typ": *
#import "../notation.typ": *
#import "../diagrams/m17.typ": (
  greedy-counterexample, interval-scheduling, matroid-sources,
)

= Матроиды <chap:matroids>

Есть способ строить решение, который кажется почти наивным: на каждом шаге выбирать наилучший из доступных вариантов и не возвращаться к сделанному выбору.
Этот способ называется _жадным алгоритмом_, и к нему обычно приходят раньше, чем к более тонким идеям.

Наивность жадного алгоритма обманчива.
В большинстве задач он ошибается: решение, выгодное на каждом отдельном шаге, в совокупности может оказаться далёким от оптимального.

Но есть задачи, где жадный алгоритм всегда даёт точное решение, и таких задач немало.
По отдельности каждая из них выглядит удачей, но удача здесь не случайна: за всеми этими задачами стоит одна и та же структура, получившая название "матроид".
Там, где эта структура присутствует, жадный алгоритм точен при любых весах, а там, где её нет, он может ошибаться.

Какое свойство задачи превращает жадность из эвристики в алгоритм с гарантией?

#history-note[Происхождение понятия][
  Понятие матроида ввёл Хэсселер Уитни#footnote[H. Whitney. "On the Abstract Properties of Linear Dependence". American Journal of Mathematics, 1935.].
  Уитни установил, что линейная независимость векторов и ацикличность множеств рёбер графа удовлетворяют одним и тем же аксиомам.
  Термин "матроид" образован от слова "матрица" и означает структуру, родственную матрицам.
  Аксиомы матроида рассматривал также ван дер Варден (1937), однако закрепился термин Уитни.
]

#chapter-overview[
  Сначала формулируется общая задача: дано множество элементов с весами и семейство допустимых подмножеств.
  Требуется найти допустимое множество максимального веса.
  Затем разбираются три задачи, в которых жадный алгоритм даёт точное решение, и выделяются два общих свойства их допустимых семейств: наследственность и свойство замены.
  Семейство с этими свойствами называется матроидом.
  Приводятся три основных примера матроидов.
  Формулируется и доказывается теорема Радо--Эдмондса: жадный алгоритм оптимален на матроидах.
  Контрпример показывает, что без свойства замены жадный алгоритм может быть неоптимален.
  В конце рассматриваются применения: алгоритм Крускала, расписание с дедлайнами и интервальное расписание.

  После этой главы вы сможете:
  + формулировать аксиомы матроида и проверять их для конкретного семейства множеств;
  + приводить примеры графического, линейного и униформного матроидов;
  + формулировать теорему Радо--Эдмондса и объяснять, почему жадный алгоритм на матроиде оптимален;
  + объяснять, почему без свойства замены жадный алгоритм может быть неоптимален;
  + сводить задачу расписания с дедлайнами к задаче о матроиде.
]

== Задача выбора и жадный алгоритм

Возьмём набор проектов, из которых можно осуществить лишь некоторые: каждый проект приносит выгоду, но проекты ограничены в совместности.
Возьмём команду, которую набирают из сотрудников: ценность команды складывается из ценностей участников, а состав ограничен требованиями.
Задач такого рода много, и все они имеют одну и ту же форму.
Есть конечное множество объектов, каждому объекту приписана ценность, а среди подмножеств выделены _допустимые_ (_feasible_).
Нужно выбрать допустимое подмножество наибольшей суммарной ценности.
Эту общую постановку и рассмотрим.

#definition[Задача выбора][
  Дано конечное множество $E$.
  Задана весовая функция $w: E -> RR_(>=0)$.
  Подмножества из семейства $cal(F)$ называются допустимыми.
  Требуется найти допустимое множество максимального веса.
]

#note[Неотрицательные веса][
  Веса считаются неотрицательными.
  Если бы среди весов встречались отрицательные, задача во многих случаях вырождалась бы: пустое множество имеет вес нуль, и любой набор с отрицательным суммарным весом был бы заведомо неоптимален.
]

Естественный способ строить решение такой задачи --- жадный алгоритм.
Он просматривает объекты по убыванию ценности и включает каждый объект, если допустимость решения при этом сохраняется.

#definition[Жадный алгоритм][
  Жадный алгоритм упорядочивает элементы множества $E$ по убыванию веса и последовательно добавляет каждый элемент к текущему множеству, если допустимость при этом сохраняется.
]

Реализация жадного алгоритма коротка.
От задачи требуется лишь проверка независимости --- метод `is_independent`, остальное --- сортировка и перебор.

```rust /// Жадный алгоритм: элементы по убыванию веса,
/// включить, если независимость сохраняется.
pub fn greedy<M: Matroid>(m: &M, weights: &[u32]) -> Vec<u32> {
    let mut order: Vec<u32> = (0..m.n()).collect();
    order.sort_by(|&a, &b| weights[b as usize].cmp(&weights[a as usize]));
    let mut chosen = Vec::new();
    for e in order {
        let mut candidate = chosen.clone();
        candidate.push(e);
        candidate.sort_unstable();
        if m.is_independent(&candidate) {
            chosen = candidate;
        }
    }
    chosen }
```

Трейт `Matroid` --- это и есть допустимое семейство: число элементов `n()` и проверка независимости `is_independent`.
Вся специфика задачи спрятана в `is_independent`: для лесов это проверка ацикличности, для векторов --- линейная независимость.

#note[Цена оракула независимости][
  Время работы жадного алгоритма складывается из сортировки и не более $abs(E)$ обращений к проверке независимости, а цена одного обращения зависит от задачи.
  Для лесов ацикличность проверяет система непересекающихся множеств --- почти линейное время, для двоичных векторов независимость проверяет исключение Гаусса за кубическое время от размерности.
  Теорема Радо--Эдмондса, доказываемая ниже, гарантирует оптимальность при любой реализации такой проверки, но цену вызова она не контролирует.
]

Жадный алгоритм прост, но в общем случае не оптимален.
#example[Размен монет][
  Пусть в обращении есть монеты достоинством $25, 10, 1$ цент.
  Чтобы выдать 30 центов, жадный алгоритм выбирает монету 25, а затем добавляет пять монет по одному центу --- всего шесть монет.
  Между тем 30 центов можно выдать тремя монетами по 10.
  Жадный алгоритм неоптимален.
]

С жадными алгоритмами мы уже работали: алгоритм Крускала в главе о графах (@chap:graphs) и алгоритм Хаффмана в главе о кодах (@chap:codes).
Их оптимальность доказывалась в каждой главе отдельно.
За этими доказательствами стоит одна структура: существуют задачи, где жадный алгоритм оптимален для любых весов, и этот класс допускает аксиоматическую характеристику.

== Три задачи, где жадный алгоритм оптимален

Применим общую постановку к трём задачам.
Каждая приходит из своей области: одна --- из теории графов, другая --- из линейной алгебры, третья --- из простейшего счёта.
В каждой из них жадный алгоритм даёт оптимальное решение.
В конце раздела станет видно, что общего у этих задач.

Начнём с задачи из теории графов.
#example[Максимальный лес][
  Рассмотрим треугольник с рёбрами $a = (1, 2), b = (2, 3), c = (1, 3)$.
  Веса рёбер: $w(a) = 5, w(b) = 4, w(c) = 3$.
  Допустимыми объявляются ациклические наборы рёбер --- леса.

  Жадный алгоритм выбирает рёбра $a, b$.
  Ребро $c$ отбрасывает: его добавление создаёт цикл.
  Результат --- остов весом $5 + 4 = 9$.
  Другие остовы треугольника имеют вес 8 или 7, поэтому результат оптимален.
]

Вторая задача приходит из линейной алгебры.
#example[Базис максимального веса][
  Рассмотрим три вектора плоскости: $v_1 = (1, 0), v_2 = (0, 1), v_3 = (1, 1)$.
  Их веса равны $5, 4, 3$.
  Допустимыми объявляются линейно независимые наборы векторов.

  Жадный алгоритм выбирает векторы $v_1, v_2$.
  Вектор $v_3$ отбрасывает: все три вектора линейно зависимы.
  Результат --- базис веса $5 + 4 = 9$.
  Базисами плоскости являются любые пары данных векторов, и их веса равны 9, 8 и 7, поэтому результат оптимален.
]

Третья задача --- самая простая.
#example[Набор ограниченного размера][
  Даны четыре элемента с весами $5, 3, 4, 2$.
  Допустимы наборы размера не более двух.
  Жадный алгоритм выбирает элементы весов 5 и 4.
  Результат --- допустимое множество веса $9$.
  Любой допустимый набор содержит не более двух элементов, а сумма двух самых тяжёлых равна $5 + 4 = 9$, поэтому результат оптимален.
]

Три задачи задают три правила допустимости: ацикличность, линейную независимость и ограничение на размер.
Жадный алгоритм оптимален в каждой из них, и по отдельности это могло бы быть совпадением.
Общая причина одна --- устройство допустимых семейств, и сравнение этих семейств --- следующий шаг.

== Свойства допустимых семейств

Сравним допустимые семейства трёх задач.
Окажется, что во всех трёх выполнены одни и те же два свойства.

Первое общее свойство --- _наследственность_.

#definition[Наследственность][
  Семейство $cal(F)$ называется *наследственным*, если каждое подмножество допустимого множества допустимо, то есть $A in cal(F) and B subset.eq A ==> B in cal(F)$.
]

Наследственность означает, что допустимость не портится при удалении элементов: выбросив часть элементов из допустимого решения, нельзя получить недопустимое.
Наследственность объясняет и то, почему жадному алгоритму достаточно одного прохода.
Элемент, который нельзя добавить сейчас, не станет допустимым и позже: иначе добавленное множество содержало бы недопустимое подмножество.
Свойство легко проверить для всех трёх задач.
#example[Наследственность в трёх задачах][
  Подмножество леса --- лес: удаление рёбер не создаёт цикл.
  Подмножество линейно независимого набора линейно независимо: удаление векторов не создаёт зависимость.
  Подмножество набора размера не более $k$ имеет размер не более $k$.
]

Второе общее свойство тоньше.
Рассмотрим два допустимых множества $A, B$ с $abs(A) < abs(B)$.
Среди элементов $B setminus A$ всегда найдётся такой, который можно добавить к меньшему множеству, сохранив допустимость.
Это свойство --- _свойство замены_ (_exchange property_).

Представьте жадный процесс: мы идём по элементам и берём всё, что можно добавить, не нарушая допустимости.
Свойство замены --- гарантия, что такой процесс не зайдёт в тупик.
Пока существует допустимое множество большего размера, текущее всегда можно пополнить элементом из него.
Жадное построение не остановится на локальном максимуме, если есть куда расти.

#definition[Свойство замены][
  Семейство $cal(F)$ обладает *свойством замены*, если $A, B in cal(F) and abs(A) < abs(B) ==> exists x in B setminus A: A union {x} in cal(F)$.
]

Проверим свойство замены для трёх задач.

В задаче о лесе оно выполнено.
Если $abs(B) > abs(A)$, то найдётся ребро из $B setminus A$, добавление которого к меньшему набору не создаёт цикл.
В противном случае каждое ребро из большего набора соединяло бы две вершины одной компоненты связности меньшего леса, и в большем наборе было бы не больше рёбер, чем в меньшем.
Это же рассуждение использовано в доказательстве того, что леса образуют матроид.

#example[Свойство замены в задаче о лесе][
  В треугольнике рассмотрим допустимые множества $A = {a}, B = {b, c}$.
  Имеем $abs(A) = 1 < 2 = abs(B)$.
  Элемент $b in B setminus A$ можно добавить.
  Множество ${a, b}$ ациклично.
]

Для линейно независимых наборов свойство замены выполнено.
Если $abs(B) > abs(A)$, то в $B$ найдётся вектор, не лежащий в линейной оболочке (_linear span_) меньшего набора.
Иначе набор целиком лежал бы в пространстве размерности $abs(A) < abs(B)$, что невозможно для линейно независимого набора.

#example[Замена в линейном матроиде][
  На векторах плоскости $v_1 = (1, 0), v_2 = (0, 1), v_3 = (1, 1)$ возьмём независимые множества $A = {v_1}, B = {v_2, v_3}$.

  Имеем $abs(A) = 1 < 2 = abs(B)$.
  Оба вектора из $B setminus A$ подходят.
  Вектор $v_2 = (0, 1)$ не лежит в оболочке $v_1$ --- это ось абсцисс.
  Не лежит в ней и $v_3 = v_1 + v_2$.
  Поэтому и ${v_1, v_2}$, и ${v_1, v_3}$ линейно независимы.
]

Для наборов размера не более $k$ свойство замены проверяется непосредственно.
Если $abs(A) < abs(B) <= k$, то любой элемент из $B setminus A$ можно добавить.

Итак, в каждом из трёх семейств выполнены наследственность и свойство замены.
Именно эти два свойства определяют матроид.
Схема трёх задач, ведущих к понятию матроида, приведена на рисунке @fig:matroid-sources.

#figure(
  matroid-sources,
  caption: [Три источника --- один матроид.],
) <fig:matroid-sources>

== Определение матроида

Два свойства --- наследственность и свойство замены --- выделяют класс семейств, для которых жадный алгоритм оптимален.
Дадим этому классу имя.

#definition[Матроид][
  Пусть $E$ --- конечное множество.
  Пусть $cal(I)$ --- семейство его подмножеств.
  Пара $(E, cal(I))$ называется *матроидом*, если выполнены следующие условия:
  + *Непустота:* семейство $cal(I)$ содержит хотя бы одно множество.
  + *Наследственность:* $A in cal(I) and B subset.eq A ==> B in cal(I)$.
  + *Свойство замены:* $A, B in cal(I) and abs(A) < abs(B) ==> exists x in B setminus A: A union {x} in cal(I)$.

  Множества из $cal(I)$ называются *независимыми множествами*.
  Элементы множества $E$ называются *элементами матроида*.
]

Пустое множество всегда независимо, так как из непустоты семейства $cal(I)$ и наследственности следует $emptyset in cal(I)$.
Свойство замены означает, что любое независимое множество можно увеличивать, добавляя элементы из большего независимого множества, пока их размеры не сравняются.
Название "замена" отражает механику из линейной алгебры, где из базиса всегда можно выбросить один вектор и добавить другой, не потеряв свойства быть базисом.
В общей формулировке элемент из большего независимого множества подставляется в меньшее, и независимость при этом сохраняется.

Свойство замены не следует из наследственности.
#example[Наследственность без свойства замены][
  Пусть $E = {1, 2, 3}$.
  Пусть $cal(I) = {emptyset, {1}, {2}, {3}, {1, 2}}$.
  Наследственность выполнена: любое подмножество множества из $cal(I)$ тоже принадлежит семейству.

  Свойство замены нарушено.
  Возьмём множества $A = {3}, B = {1, 2}$.
  Выполнено $abs(A) < abs(B)$.
  Однако ни одно из множеств ${1, 3}, {2, 3}$ не принадлежит семейству.
  Семейство $cal(I)$ не является матроидом.
]

С матроидом связаны два стандартных понятия.

#definition[База][
  Максимальное по включению независимое множество называется *базой*.
]

#definition[Ранг][
  Размер базы называется *рангом*.
]

Из свойства замены следует, что все базы матроида имеют одинаковый размер.
Доказательство вынесено в упражнение.

Максимальное и наибольшее --- не одно и то же.
Максимальное множество нельзя увеличить, добавив элемент, а наибольшее содержит больше всего элементов.
В произвольной системе независимости максимальное множество может быть меньше наибольшего, но в матроиде они совпадают --- это следствие свойства замены.

#example[Максимальное, но не наибольшее][
  В системе независимости $cal(I) = {emptyset, {1}, {2}, {3}, {1, 2}}$ множество ${3}$ максимально: ни один элемент нельзя добавить, не покинув семейство.

  При этом наибольшее независимое множество --- ${1, 2}$, и оно больше максимального ${3}$.
  В матроиде такое расхождение невозможно: свойство замены увеличивает любую базу, пока её размер не сравняется с наибольшей.
]

В линейном матроиде база совпадает с базисом подпространства, натянутого на $E$.
В графическом матроиде базы --- остовные леса.
Если граф связен, базы --- остовные деревья.

#example[Ранг треугольника][
  В треугольнике с рёбрами $a, b, c$ базы --- пары рёбер, поэтому ранг матроида равен 2.

  Внутри подмножества ${a}$ наибольшее независимое подмножество --- оно само, и его размер равен 1.
  Внутри ${a, b, c}$ --- любая пара рёбер: три ребра зависимы, а любые два независимы.
]

#remark[Почему именно независимые множества?][
  Матроид можно задать не только через независимые множества, но и через базы, через функцию ранга и через оператор замыкания.
  Определение через независимые множества выбрано потому, что проверка независимости --- операция, которую жадный алгоритм выполняет на каждом шаге.
]

#check[Определение матроида][
  + Почему из непустоты семейства $cal(I)$ и наследственности следует, что $emptyset in cal(I)$?
  + Проверьте свойство замены для пары $A = {1}, B = {2, 3}$ в семействе всех подмножеств множества $E = {1, 2, 3}$ размера не более двух.
  + Базы графического матроида связного графа --- остовные деревья.
    Чему равен ранг этого матроида, выраженный через число вершин графа?
]

== Примеры матроидов

Три семейства из начала главы --- это матроиды.
Проверим аксиомы для каждого из них.

Начнём с графического матроида.
#definition[Графический матроид][
  Пусть $G$ --- граф.
  Пусть $E$ --- множество его рёбер.
  Независимыми объявляются ациклические наборы рёбер.
]

Независимость рёбер устроена так же, как независимость векторов.
Цикл --- графовый аналог нуля: пройдя по циклу, возвращаешься в исходную вершину, как линейная комбинация, равная нулю, возвращает в начало координат.
Поэтому ацикличность играет в графах ту же роль, что линейная независимость в алгебре.

#proposition[
  Семейство лесов графа образует матроид.
]

#proof[
  Проверим две аксиомы матроида по отдельности.

  *Наследственность* следует из того, что подмножество ациклического набора ациклично.

  *Свойство замены.* Пусть $A, B$ --- леса с $abs(A) < abs(B)$.
  Предположим, что ни одно ребро из $B setminus A$ нельзя добавить без образования цикла.
  Тогда каждое ребро $B$ соединяет две вершины одной компоненты связности меньшего леса.
  В каждой компоненте меньшего леса рёбер меньше, чем вершин, откуда $abs(B) <= abs(A)$, что противоречит условию $abs(A) < abs(B)$.
]

#example[Леса треугольника][
  В треугольнике с рёбрами $a, b, c$ независимы пустое множество, каждое одиночное ребро и каждая пара рёбер.
  Все три ребра зависимы: они образуют цикл.
  Базы --- остовные деревья, то есть пары рёбер.
]

Следующий пример --- линейный матроид.
#definition[Линейный матроид][
  Пусть $V$ --- векторное пространство, например $RR^n$ или $"GF"(2)^n$.
  Пусть $E$ --- множество его векторов.
  Независимыми объявляются линейно независимые наборы векторов.
]

#proposition[
  Семейство линейно независимых наборов образует матроид.
]

#proof[
  Проверим две аксиомы матроида.

  *Наследственность* следует из определения линейной независимости.

  *Свойство замены.* Пусть $A, B$ --- линейно независимые наборы с $abs(A) < abs(B)$.
  Набор $B$ не может целиком лежать в линейной оболочке меньшего набора, размерность которой равна $abs(A)$.
  Следовательно, в большем наборе есть вектор $x$, не принадлежащий линейной оболочке меньшего, и набор $A union {x}$ линейно независим.
]

Над $"GF"(2)$ --- как в главе о кодах (@chap:codes) --- векторы двоичные, и проверка независимости сводится к исключению Гаусса: новый вектор редуцируется относительно уже выбранных, и нулевая строка означает зависимость.

```rust
/// Линейно независимы ли выбранные двоичные векторы?
fn is_independent(vectors: &[Vec<u8>], set: &[u32]) -> bool {
    let dim = vectors[0].len();
    let mut basis: Vec<Vec<u8>> = Vec::new(); // редуцированный базис
    for &i in set {
        let mut row = vectors[i as usize].clone();
        for b in &basis {
            let pivot = b.iter().position(|&x| x == 1).unwrap();
            if row[pivot] == 1 {
                for c in 0..dim {
                    row[c] ^= b[c];
                }
            }
        }
        if row.iter().all(|&x| x == 0) {
            return false; // вектор --- комбинация остальных
        }
        basis.push(row);
    }
    true
}
```

Три вектора $(1, 0), (0, 1), (1, 1)$ независимы попарно, но все вместе зависимы: третий --- сумма первых двух.

#example[Векторы плоскости][
  Для векторов $v_1 = (1, 0), v_2 = (0, 1), v_3 = (1, 1)$ независимы пустое множество, все одиночные векторы и все пары векторов.
  Все три вектора зависимы: $v_3 = v_1 + v_2$.
  Базы --- базисы плоскости, то есть любые пары данных векторов.
]

Наконец, униформный матроид.
#definition[Униформный матроид $U_(k,n)$][
  Пусть $abs(E) = n$.
  Независимыми объявляются подмножества размера не более $k$.
]

#proposition[
  Семейство подмножеств размера не более $k$ образует матроид.
]

#proof[
  Проверим две аксиомы матроида.

  *Наследственность* проверяется непосредственно --- подмножество набора размера не более $k$ само имеет размер не более $k$.

  *Свойство замены.* Пусть $A, B$ --- независимые множества с $abs(A) < abs(B) <= k$.
  Тогда множество $B setminus A$ не пусто, и добавление любого его элемента не нарушает ограничение размера.
]

#example[Униформный матроид][
  В матроиде $U_(2,4)$ независимы все подмножества размера не более двух.
  Базы --- двухэлементные подмножества.
  Любое трёхэлементное подмножество зависимо.
  Задача выбора на $U_(2,4)$ --- выбор двух самых тяжёлых элементов.
]

Графический, линейный и униформный матроиды --- основные примеры.
Существуют и другие матроиды: трансверсальные, матроид расписаний.
Один из них --- матроид расписаний --- рассматривается в разделе о приложениях.

=== \* Матроид Фано

Возьмём в качестве элементов семь ненулевых векторов пространства $"GF"(2)^3$ и объявим независимыми линейно независимые наборы.
Перед нами линейный матроид ранга 3.
Занумеруем векторы числами от 1 до 7:
$1 = (1, 0, 0), 2 = (0, 1, 0), 3 = (1, 1, 0), 4 = (0, 0, 1), 5 = (1, 0, 1), 6 = (0, 1, 1), 7 = (1, 1, 1)$.
Зависимые тройки --- векторы с нулевой суммой: $123, 145, 167, 246, 257, 347, 356$.
Такие тройки называют "прямыми", а всю конструкцию --- плоскостью Фано: каждая пара точек лежит ровно на одной прямой.
Независимыми оказываются множества из не более двух точек и те тройки, которые не являются прямыми, а любые четыре точки зависимы.

Над полями другой характеристики представления нет.
Представление переводит точки $1, 2, 4$, не лежащие на одной прямой, в базисные векторы $e_1, e_2, e_3$, и каждый вектор-точку можно умножить на ненулевую константу.
Точка $3$ лежит на прямой с точками $1$ и $2$, поэтому её вектор пропорционален $e_1 + e_2$, а векторы точек $5$ и $6$ по тем же причинам пропорциональны $e_1 + e_3$ и $e_2 + e_3$.
Определитель векторов $e_1 + e_2, e_1 + e_3, e_2 + e_3$ равен $-2$.
В характеристике 2 он равен нулю, и тройка $356$ зависима, как и положено прямой.
При любой другой характеристике определитель отличен от нуля, и прямая оказалась бы независимой тройкой.
Матроид Фано представим над полем тогда и только тогда, когда характеристика поля равна двум.

Фано линеен, но не графичен.
Любые один и два элемента Фано независимы, поэтому его графическое представление было бы простым графом.
Ранг графического матроида простого графа равен числу вершин минус число компонент.
Связный граф ранга 3 имеет четыре вершины и не более $binom(4, 2) = 6$ рёбер, а несвязный граф того же ранга несёт ещё меньше рёбер.
У Фано элементов семь, так что графического представления не существует.
Каждый графический матроид, напротив, линеен: над $"GF"(2)$ его представляют столбцы матрицы инцидентности, у которой столбец каждого ребра содержит единицы в его концах, и набор столбцов зависим ровно тогда, когда рёбра содержат цикл.
Тем самым графические матроиды образуют строгую часть линейных.

== Корректность жадного алгоритма

Теперь у нас есть все ингредиенты: задача выбора, жадный алгоритм и понятие матроида.
Сформулируем результат, который их связывает.

#note[Инвариант жадного алгоритма][
  Жадный алгоритм поддерживает инвариант: построенное множество остаётся независимым.
  Анализ корректности основан на сравнении результата алгоритма с оптимальным множеством по позициям.
  Поддержание инварианта и пошаговое сравнение с оптимумом --- приём, применяемый и при верификации программ.
]

#theorem[Теорема Радо--Эдмондса][
  Пусть $(E, cal(I))$ --- матроид.
  Для любой неотрицательной весовой функции $w$ жадный алгоритм находит независимое множество максимального веса.
]

#proof[
  Сравним жадную базу с оптимальной по позициям и докажем от противного, что жадный вес не меньше.

  Все базы матроида имеют одинаковый размер.
  Если две базы имеют разные размеры, свойство замены увеличивает меньшую из них, что противоречит её максимальности.
  Обозначим общий размер баз через $r$.
  Жадный алгоритм возвращает базу, ведь если к множеству $G$ можно добавить элемент без потери независимости, алгоритм добавил бы его.
  Поэтому достаточно сравнить результат жадного алгоритма с базой максимального веса.

  Упорядочим элементы жадной базы $G$ и оптимальной базы $B$ по убыванию веса: $G = {g_1, ..., g_r}, B = {b_1, ..., b_r}$.
  Докажем, что на каждом шаге выполнено $w(g_i) >= w(b_i)$.

  Предположим, что на некотором шаге выполнено $w(g_i) < w(b_i)$.
  Множества $G_(i-1) = {g_1, ..., g_(i-1)}$ и $B_i = {b_1, ..., b_i}$ независимы, причём $abs(B_i) = i > i - 1 = abs(G_(i-1))$.
  По свойству замены существует элемент $x in B_i setminus G_(i-1)$, для которого множество $G_(i-1) union {x}$ независимо.
  Элементы $B_i$ упорядочены по убыванию веса, поэтому $w(x) >= w(b_i)$, а по предположению $w(b_i) > w(g_i)$, откуда $w(x) > w(g_i)$.

  Значит, жадный алгоритм рассмотрел элемент $x$ раньше $g_i$, и к этому моменту его множество --- подмножество $G_(i-1)$.
  По наследственности это множество вместе с $x$ независимо, поэтому алгоритм добавил бы $x$ --- противоречие.

  Итак, $w(G) >= w(B)$, то есть жадная база не легче базы максимального веса $B$.
  Любое независимое множество можно пополнить до базы, не уменьшив вес, поскольку веса неотрицательны, а свойство замены даёт добавляемый элемент, пока множество не станет максимальным.
  Следовательно, множество $G$ имеет максимальный вес среди всех независимых множеств.
]

Теорема Радо--Эдмондса относится к целому классу задач.
Если допустимые множества задачи образуют матроид, оптимальность жадного алгоритма не требует отдельного доказательства.
Обратное утверждение --- что вне класса матроидов жадный алгоритм может ошибаться --- рассматривается в следующем разделе.

#check[Корректность жадного алгоритма][
  + Почему результат жадного алгоритма на матроиде --- всегда база?
  + В каких двух местах доказательства теоремы Радо--Эдмондса работает свойство замены?
  + Почему элемент $x$ из оптимальной базы жадный алгоритм обязан был рассмотреть раньше элемента $g_i$?
]

== Обратное утверждение

Свойство замены --- не формальность: именно оно отделяет задачи, где жадный алгоритм оптимален, от задач, где он может ошибаться.
Семейство, в котором выполнена только наследственность, называется _системой независимости_.

#definition[Система независимости][
  Пусть $E$ --- конечное множество.
  Пусть $cal(I)$ --- семейство его подмножеств.
  Пара $(E, cal(I))$ называется *системой независимости*, если выполнена наследственность, то есть $A in cal(I) and B subset.eq A ==> B in cal(I)$.
]

Матроид --- это система независимости, в которой выполнено свойство замены.
Покажем, что без свойства замены жадный алгоритм может дать неоптимальный результат.

#example[Контрпример: жадный алгоритм неоптимален][
  Пусть $E = {a, b, c}$.
  Пусть $cal(I) = {emptyset, {a}, {b}, {c}, {b, c}}$.
  Это система независимости: наследственность выполнена.

  Свойство замены нарушено.
  Возьмём множества $A = {a}, B = {b, c}$.
  Выполнено $abs(A) < abs(B)$.
  Однако ни одно из множеств ${a, b}, {a, c}$ не принадлежит семейству.

  Зададим веса: $w(a) = 5, w(b) = 4, w(c) = 4$.

  Жадный алгоритм выбирает $a$.
  Затем рассматривает $b$, но ${a, b} in.not cal(I)$.
  Затем рассматривает $c$, но ${a, c} in.not cal(I)$.

  Результат --- множество ${a}$ веса 5.
  Оптимальное независимое множество --- ${b, c}$ веса 8.
  Жадный алгоритм неоптимален.
]

Схема контрпримера приведена на рисунке @fig:greedy-counterexample.
Элемент $a$ несовместим с остальными: жадный алгоритм выбирает его и получает вес 5.
Оптимальное множество --- набор ${b, c}$ весом 8.

#figure(
  greedy-counterexample,
  caption: [Контрпример к жадному алгоритму.],
) <fig:greedy-counterexample>

Контрпример показывает необходимость свойства замены.
Феномен общий: если система независимости не является матроидом, существует весовая функция, на которой жадный алгоритм неоптимален.

#proof-sketch[
  Докажем построением весовой функции по нарушающей паре.
  Пусть множества $X, Y in cal(I)$ нарушают свойство замены, причём $abs(X) > abs(Y)$: ни один элемент из $X setminus Y$ нельзя добавить к меньшему множеству $Y$.

  Назначим каждому элементу $Y$ вес $abs(Y) + 2$, каждому элементу $X setminus Y$ вес $abs(Y) + 1$, а остальным элементам нулевой вес.
  Элементы $Y$ тяжелее всех прочих ненулевых, и любой начальный отрезок $Y$ независим по наследственности, поэтому жадный алгоритм заберёт $Y$ целиком.
  Элементы $X setminus Y$ к $Y$ не добавляются по нарушенной замене, а элементы нулевого веса вес результата не меняют, так что вес жадного результата равен $abs(Y) (abs(Y) + 2)$.

  Остаётся сравнить этот вес с весом $X$.
  Каждый элемент $X$ весит не меньше $abs(Y) + 1$, а элементов в $X$ не меньше $abs(Y) + 1$, поэтому $w(X) >= (abs(Y) + 1)^2 = abs(Y) (abs(Y) + 2) + 1$.
  Жадный результат легче оптимального множества $X$.
]

== Матроиды в приложениях

Теорема Радо--Эдмондса сводит доказательство оптимальности жадного алгоритма к проверке двух аксиом матроида.
Покажем это на известных задачах.
Кажется, что перед нами разные алгоритмы: Крускал строит остов, жадный базис перебирает векторы, расписание сортирует работы по выигрышу.
На деле это один и тот же жадный алгоритм, запущенный на разных матроидах.

Алгоритм Крускала из главы о графах (@chap:graphs) --- жадный алгоритм на графическом матроиде.
В главе о графах он строит минимальное остовное дерево.
В терминах матроидов он находит максимальный лес.
Различие между этими задачами сводится к знаку весов.
Раз леса образуют матроид (раздел о примерах матроидов), оптимальность алгоритма Крускала следует из теоремы Радо--Эдмондса.

#example[Крускал как жадный алгоритм][
  В треугольнике рёбра имеют веса $5, 4, 3$.
  Жадный алгоритм идёт по рёбрам в порядке убывания веса.
  Ребро веса $5$ он берёт: лес из одного ребра ацикличен.
  Ребро веса $4$ тоже берёт: два ребра цикл не образуют.
  Ребро веса $3$ отбрасывает: вместе с двумя уже взятыми оно замыкает треугольник.
  Результат --- остов весом $5 + 4 = 9$.
  Это максимальное остовное дерево: любой остов треугольника состоит из двух рёбер, а два самых тяжёлых --- именно эти.
]

Вторая задача --- расписание с дедлайнами.
Она интересна тем, что матроид в ней скрыт: задача выглядит как задача об упорядочивании, хотя допустимые решения --- выбор множества.
Дано $n$ задач.
Задача $i$ характеризуется дедлайном и выигрышем:
$d_i in {1, ..., n}, p_i >= 0$.
Каждая задача выполняется за единицу времени.

#definition[Реализуемый набор (_feasible set_)][
  Набор задач называется *реализуемым*, если существует порядок выполнения, при котором каждая задача завершается к своему дедлайну.
]

Требуется найти реализуемый набор максимального суммарного выигрыша.
Реализуемость допускает простую характеристику: к моменту $t$ число задач с дедлайном не позже этого момента не должно превышать номер момента.

#lemma[Критерий реализуемости][
  Набор $X$ задач реализуем тогда и только тогда, когда для каждого момента выполнено условие:
  $ abs({i in X : d_i <= t}) <= t $.
]

#proof[
  Докажем критерий в обе стороны.

  *Прямо.* Если набор $X$ реализуем, то в любом допустимом расписании к каждому моменту выполняется не больше задач, чем номер момента.
  Задачи из $X$ с дедлайном не позже некоторого момента должны быть выполнены к этому моменту.
  Следовательно, число таких задач не превосходит $t$.

  *Обратно.* Пусть для каждого момента число задач из $X$ с дедлайном не позже этого момента не превосходит номер момента.
  Упорядочим задачи из $X$ по возрастанию дедлайнов и выполним их в этом порядке.
  Задача с дедлайном $d$ встаёт на позицию, не превосходящую числа задач с не более поздним дедлайном.
  По предположению это число не превосходит $d$.
  Значит, задача выполняется не позднее $d$.
  Набор $X$ реализуем.
]

#proposition[
  Реализуемые наборы задач образуют матроид.
]

Доказательство того, что реализуемые наборы удовлетворяют свойству замены, вынесено в упражнение.

#example[Расписание с дедлайнами][
  Даны три задачи.
  У задачи $A$ дедлайн 1 и выигрыш 50.
  У задачи $B$ дедлайн 1 и выигрыш 40.
  У задачи $C$ дедлайн 2 и выигрыш 30.

  Реализуемы наборы ${A, C}, {B, C}$.
  Набор ${A, B}$ не реализуем: задачи с дедлайном 1 нельзя выполнить обе.

  Жадный алгоритм по выигрышу выбирает $A, C$.
  Задачу $B$ отбрасывает.
  Результат --- набор ${A, C}$ с выигрышем 80, оптимальный.
]

Алгоритм Хаффмана из главы о кодах (@chap:codes) --- тоже жадный.
Однако матроидом он не покрывается: объединение двух символов изменяет само множество элементов, так что семейство допустимых множеств не задано на фиксированном множестве.
Поэтому оптимальность Хаффмана доказывается отдельно.

Рассмотрим, наконец, задачу, в которой матроид не возникает, --- интервальное расписание.

#example[Интервальное расписание --- не матроид][
  Пусть заданы интервалы времени.
  Допустимы попарно непересекающиеся наборы интервалов.
  Допустимые множества образуют систему независимости, но не матроид.

  Контрпример к свойству замены --- три интервала: $A = [1, 10], B = [1, 2], C = [3, 4]$.
  Возьмём $X = {A}, Y = {B, C}$.
  Имеем $abs(X) = 1 < 2 = abs(Y)$, и оба множества допустимы.
  + Ни один из интервалов $B, C$ нельзя добавить к меньшему множеству.
    Каждый из них пересекает $A$.
  По свойству замены хотя бы один элемент из $Y setminus X$ должен был добавиться к меньшему множеству --- и не добавляется.

  Свойство замены нарушено.
  Однако в невзвешенном варианте задачи --- когда каждый интервал ценен одинаково --- жадный алгоритм по времени окончания даёт оптимальное решение.
  Его корректность доказывается отдельно (глава о графах, @chap:graphs).
]

Конкретный набор интервалов приведён на рисунке @fig:interval-scheduling.
Длинный $A = [1, 10]$ пересекает $B = [1, 2]$ и $C = [3, 4]$, тогда как их пара допустима.

#figure(
  interval-scheduling,
  caption: [Интервалы из примера.],
) <fig:interval-scheduling>

Не во всех задачах выбора допустимые множества образуют матроид.
Если допустимые множества образуют матроид, корректность жадного алгоритма следует из теоремы Радо--Эдмондса.
В противном случае жадный алгоритм либо неоптимален, либо требует отдельного доказательства корректности.

== \* Дальше в матроиды

Матроиды --- самостоятельная область комбинаторики, и в этой главе затронута лишь её малая часть.
Перечислим направления, в которые теория развивается дальше.
Каждое заслуживает отдельной главы.

*Функция ранга.*
Ранг $r(A)$ --- размер его наибольшего независимого подмножества.
Ранг матроида --- частный случай этой функции: на всём множестве элементов $r(E)$ равен размеру базы.
Ранговая функция субмодулярна: $r(A union B) + r(A inter B) <= r(A) + r(B)$.
Минимизация субмодулярных функций полиномиальна, а максимизация NP-трудна.
Многие свойства матроида удобнее формулировать через ранг, чем через независимые множества.

*Циклы.*
Циклом (_circuit_) называется минимальное зависимое множество.
В графическом матроиде циклы --- циклы графа.
В униформном $U_(k,n)$ циклы --- подмножества размера $k + 1$.
Как и независимые множества, циклы задают матроид: у них свои аксиомы.

*Двойственный матроид.*
Базами двойственного матроида $M^*$ служат дополнения его баз, а ранг $M^*$ равен $abs(E) - r$, где $r$ --- ранг исходного матроида.
Для графического матроида получается кографический: независимы множества рёбер, дополнение которых содержит остовное дерево.
Базы двойственного матроида называются ко-базами.
В связном графе ко-базы --- ко-остова, то есть дополнения остовных деревьев до множества всех рёбер.

#example[Базы ко-остова][
  В треугольнике с рёбрами $a, b, c$ базы графического матроида --- пары ${a, b}, {a, c}, {b, c}$.
  Дополнения этих пар --- одиночные рёбра ${c}, {b}, {a}$, и они же --- базы двойственного матроида.
  Ранг исходного матроида равен 2, ранг двойственного равен $3 - 2 = 1$, и двойственный матроид здесь --- униформный $U_(1,3)$.

  Каждое одиночное ребро треугольника --- ко-остов: остовное дерево состоит из двух рёбер, и оставшееся ребро дополняет его до полного набора.
]

*Трансверсальные матроиды.*
В двудольном графе независимы подмножества одной доли, которые покрываются паросочетанием.
Обобщение теоремы Холла (глава о графах, @chap:graphs) --- теорема Радо о независимой системе различных представителей.

*Пересечение матроидов.*
Семейство множеств, независимых сразу в двух матроидах, допускает полиномиальный алгоритм.
Пересечение трёх матроидов --- NP-трудная задача.

*Матроиды и коды.*
Столбцы проверочной матрицы линейного кода образуют линейный матроид.
Двойственность матроидов соответствует переходу к дуальному коду (глава о кодах, @chap:codes).

== Итоги главы

Три задачи из начала главы --- остовные деревья, линейно независимые наборы и расписания с дедлайнами --- решались одним и тем же жадным алгоритмом, и совпадение оказалось не случайностью.
Общая причина --- в устройстве допустимых семейств: наследственность и свойство замены выделяют ровно те классы задач, где жадный алгоритм оптимален.
Система независимости с этими двумя свойствами называется матроидом, и графические, линейные и униформные матроиды --- его естественные примеры.

Теорема Радо--Эдмондса превращает наблюдение в критерий: жадный алгоритм оптимален на матроидах и может ошибаться на системах независимости, которые матроидами не являются.
Система независимости с нарушенным свойством замены --- контрпример: без свойства замены жадная стратегия уходит в неоптимальное решение.
Интервальное расписание напоминает, что граница несимметрична: жадный алгоритм там тоже точен, хотя матроидом задача не является.

Матроид расписаний показывает, что аксиоматика работает и там, где допустимые множества заданы дедлайнами, и связывает главу с алгоритмом Крускала из главы @chap:graphs.
Тот же аппарат протягивается к кодам: столбцы проверочной матрицы линейного кода образуют линейный матроид, а пересечение двух матроидов решается полиномиально, тогда как трёх --- уже NP-трудно.

Проверить две аксиомы проще, чем доказывать оптимальность каждого жадного алгоритма заново.
Дальше в книге структуры уступят место подсчёту: комбинаторика (глава @chap:combinatorics) займётся вопросом о том, сколько существует способов.

#check[Проверьте себя][
  + Сформулируйте аксиомы матроида.

  + Приведите пример системы независимости, не являющейся матроидом.

  + Докажите, что леса графа образуют матроид.

  + Почему жадный алгоритм на матроиде оптимален?

  + Почему интервальное расписание не является матроидом, хотя жадный алгоритм там оптимален?

  + Сформулируйте критерий реализуемости набора задач с дедлайнами.
]

#whats-next
Матроиды связаны с материалом первых глав: алгоритм Крускала (@chap:graphs) и коды (@chap:codes).
Следующая глава --- комбинаторика (глава @chap:combinatorics): если матроиды объясняют, почему жадный алгоритм работает, комбинаторика отвечает, сколько существует способов.
Дальнейшая теория матроидов включает функцию ранга, двойственные матроиды и пересечение матроидов.

== Упражнения

#tasklist("m17")[
  _Проверка аксиом._
  + Пусть $E = {1, 2, 3, 4}$, независимы подмножества размера не более двух.
    Является ли это семейство матроидом?
    Если да, назовите его.

  + Пусть $E = {1, 2, 3}$, независимы ${1, 2}, {3}$ и их подмножества.
    Является ли это семейство матроидом?
    Проверьте оба свойства.

  _Жадный алгоритм._
  + В униформном матроиде $U_(3,5)$ заданы веса 7, 2, 9, 4, 1.
    Что выберет жадный алгоритм и каков вес результата?

  + Придумайте граф с четырьмя вершинами и пятью рёбрами с весами.
    Найдите максимальное остовное дерево жадным алгоритмом.

  _Свойство замены._
  + Докажите, что все базы матроида имеют одинаковый размер.

  + Пусть семейство наследственно и все его базы имеют одинаковый размер.
    Следует ли из этого свойство замены?

  _Сложнее. \*_
  + Даны задачи с дедлайнами и выигрышами: $(1, 100), (2, 90), (2, 80), (1, 70)$.
    Найдите оптимальный набор жадным алгоритмом.

  + Докажите, что реализуемые наборы задач с дедлайнами образуют матроид.

  + #task-id("m17:t9") \* Рассмотрим граф с вершинами ${1, 2, 3, 4}$.
    Его рёбра: $a = (1, 2), b = (2, 3), c = (1, 3), d = (3, 4)$, где $a, b, c$ образуют треугольник, а $d$ --- "хвост" при вершине $3$.
    Перечислите все базы графического матроида этого графа и найдите его ранг.
    Чему равен ранг подмножества ${a, b, c}$?
  + Является ли матроидом семейство всех подмножеств размера ровно $k$?
    Проверьте наследственность и объясните, что именно ломается.

  + В семействе независимых множеств ${a}, {b}, {c}, {b, c}$ заданы веса $w(a) = 3$, $w(b) = w(c) = 2$.
    Что выберет жадный алгоритм, каков оптимальный набор и почему алгоритм ошибается?

  _Ранг._
  + Докажите, что в матроиде для любого множества $X$ все максимальные по включению независимые подмножества $X$ имеют одинаковый размер.
]


#project[Построение контрпримеров для жадного алгоритма][
  Теорема Радо--Эдмондса утверждает: на матроиде жадный алгоритм точен при любых весах.
  Обратное утверждение сильнее: если система независимости не является матроидом, то существуют веса, на которых жадный алгоритм гарантированно ошибается.
  Соберите работающий инструмент, который по системе независимости, заданной оракулом, либо подтверждает, что семейство является матроидом, либо предъявляет пару множеств, нарушающих свойство замены, и веса, на которых жадный алгоритм неоптимален.
  Проверьте его на матроидах --- инструмент должен подтверждать оптимальность жадного алгоритма --- и на системах независимости, не являющихся матроидами, где он обязан предъявить конкретную ошибку.

  #project-stage[Оракул и жадный алгоритм][
    Задайте систему независимости оракулом независимости и соберите жадный алгоритм, который упорядочивает элементы по убыванию веса и добавляет каждый элемент, если независимость сохраняется.
  ]
  #project-stage[Нарушение замены и веса][
    Проверьте наследственность отдельно: без неё семейство не является даже системой независимости.
    Затем найдите нарушение свойства замены перебором пар независимых множеств $A, B$ с $abs(A) < abs(B)$: нарушение --- это пара, к меньшему множеству которой не добавляется ни один элемент из $B setminus A$.
    По найденной паре постройте веса, на которых жадный алгоритм ошибается, прогоните его на этих весах и проверьте, что результат не оптимален.
  ]
  #project-stage[Коллекция примеров и случайные семейства][
    Соберите коллекцию примеров: униформный, графический, линейный и дедлайновый матроиды, где жадный алгоритм должен быть точен, и контрпримеры --- семейство $\{a\}, \{b\}, \{c\}, \{b, c\}$ и интервальное расписание, где нарушение есть и жадный алгоритм ошибается на предъявленных весах.
    Объясните, почему на одних нарушение есть, а на других нет.
    Затем прогоните инструмент по случайным наследственным семействам и измерьте, как часто встречается нарушение свойства замены и насколько жадный алгоритм может ошибаться.
  ]
  #project-stage[Функция ранга и субмодулярность][
    Добавьте функцию ранга подмножества: $r(A)$ --- размер наибольшего независимого подмножества $A$.
    Вычислите её перебором подмножеств $A$ по убыванию размера, до первого независимого.
    Проверьте субмодулярность $r(A) + r(B) >= r(A union B) + r(A inter B)$ на всех парах подмножеств.
    Убедитесь, что на матроидах она выполнена, а на семействе, не являющемся матроидом, найдите конкретную пару, где она нарушается.
    Объясните, почему рангу матроида субмодулярность обязана выполняться, а у произвольной системы независимости --- нет.
  ]
  #project-stage[Двойственность, циклы и трансверсальные матроиды][
    Двойственный матроид $M^*$: множество $A$ независимо в $M^*$, когда дополнение $E setminus A$ содержит базис $M$.
    Реализуйте это условием на ранге: $r(E setminus A) = r(E)$.
    Цикл --- минимальное зависимое множество: перечислите все циклы матроида и проверьте, что удаление любого элемента цикла делает множество независимым.
    Трансверсальный матроид: элементы независимы, когда их можно рассадить по группам так, чтобы разные элементы попали в разные группы.
    Реализуйте проверку поиском рассадки и продумайте, когда рассадка невозможна: какому подмножеству элементов не хватает групп.
  ]

  #project-outcome[
    инструмент, который по системе независимости либо подтверждает, что она является матроидом --- и тогда жадный алгоритм точен при любых весах, --- либо предъявляет конкретные веса, на которых жадный алгоритм ошибается.
    Это работающее воплощение обратного утверждения теоремы Радо--Эдмондса.
    В дополнение --- конструкции матроидов: двойственный матроид, перечисление циклов и трансверсальный матроид.
  ]
]
