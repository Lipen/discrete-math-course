#import "theme.typ": *
#show: template

#set page(
  paper: "a4",
  margin: (top: 2.5cm, rest: 2cm),
  header-ascent: 1em,
  header: [
    #set text(10pt)
    #smallcaps[*Домашнее задание \#1*]
    #h(1fr)
    #link(
      "https://github.com/Lipen/discrete-math-course",
    )[*Дискретная математика*]
    \
    *Множества и логика*
    #h(1fr)
    _$#emoji.leaf.maple$ Осень 2026_
    #v(0.25em, weak: true)
    #grid(
      columns: (1fr, auto, 1fr),
      column-gutter: 0.8em,
      align: horizon,
      line(length: 100%, stroke: 0.6pt + accent.lighten(60%)),
      context [
        #text(size: 9pt, fill: accent)[
          #counter(page).display() / #counter(page).final().first()
        ]
      ],
      line(length: 100%, stroke: 0.6pt + accent.lighten(60%)),
    )
  ],
)

#let pair(a, b) = $chevron.l #a, #b chevron.r$
#let card(x) = $abs(#x)$
#let Jaccard = math.op("Jaccard")
#let JaccardDist = $d_J$
#let Cosine = math.op("Cosine")
#let CosineDist = $d_cos$
#show table: set block(breakable: false)


#Box[
  "Множество есть многое, мыслимое как единое."
  #align(right)[--- Георг Кантор]
]


#Block[
  Двадцать лет назад станция "Палимпсест" замолчала посреди смены.
  В 03:47 внутренняя сеть отказалась подтвердить саму себя, а утренняя смена нашла тёплые коридоры, работающие терминалы и ни одного человека.
  Автоматика продолжала вести записи, хотя адресата у них давно не было.

  Экспедиция "Контур" подняла этот архив, от журналов наблюдений до протоколов условий, вплоть до показаний датчика, который разучился говорить "да" и "нет".
  Куратор экспедиции первым читает журналы и спешит с выводами.
  Инструкция к архиву не сохранилась, как не сохранился и тот, кто мог бы её написать.
  Разобраться в них можно, только сверяя одни записи с другими.
]


== Задача 1: Операции над множествами и разбиения #h(1fr)#TagCore

Первый журнал был самым тонким.
В нём сорок страниц, три признака на событие и ни одного пояснения, а легенда с расшифровкой пометок потерялась раньше, чем нашли сам архив.
От событий остались только идентификаторы вроде "pulse" или "flare", и вся информация о каждом заключена в том, к каким множествам оно отнесено.

#Given[
  Каждое событие отмечено хотя бы одним из трёх признаков.
  $A$, $B$ и $C$ --- множества событий, отмеченных соответствующим признаком:
  - $A = {"pulse", "flare", "drift", "echo", "fault"}$
  - $B = {"echo", "drift", "fault", "breach", "flare"}$
  - $C = {"pulse", "breach", "fault", "drift", "signal"}$

  Универсум $T$ равен $A union B union C$, а через $cal(P)(T)$ обозначается множество всех его подмножеств.
  Все дополнения в этой задаче считаются относительно $T$.

  $
    X & = A inter B inter C, \
    Y & = (A inter B) without C, \
    Z & = A without (X union Y).
  $
]

#tasklist("t1")[
  + Вычислите следующие множества и охарактеризуйте каждое одной фразой --- какими признаками обладают входящие в него события:

    #tasklist("t1.1", cols: 3, format: "letters", full: false)[
      + $A inter B$
      #colbreak()
      + $overline(A) inter B$
      #colbreak()
      + $(A inter B) without C$
    ]

  + Запишите формулой множество событий, обладающих признаком $B$ и не более чем одним из признаков $A$ и $C$ (ни одним или ровно одним).

  + При каком условии на произвольные множества $M$ и $K$ верно $M without K = K without M$? \
    Докажите необходимость и достаточность.

  + Установите истинность утверждений:

    #tasklist("t1.4", cols: 2, format: "letters", full: false)[
      + $emptyset in A$
      + $emptyset subset.eq A$
      + $emptyset subset A$
      + $"pulse" in A$
      + ${"pulse"} in A$
      #colbreak()
      + ${"pulse"} subset.eq A$
      + $A in cal(P)(T)$
      + ${A} subset.eq cal(P)(T)$
      + ${"pulse", "pulse"} without {"pulse"} = {"pulse"}$
      + $A symdiff emptyset = emptyset$
    ]

  + Разложите универсум на восемь элементарных областей, задаваемых принадлежностью к $A$, $B$ и $C$.
    На этом разложении докажите или опровергните, что ${X, Y, Z}$ является разбиением $A$.
]


== Задача 2: Меры сходства и расстояния #h(1fr)#TagChallenge

Второй журнал беднее первого.
Отдельных событий в нём не осталось, только эпизоды с их наборами характерных сигналов, и о близости эпизодов теперь судят по самим наборам.

#Given[
  Для двух непустых конечных множеств $X, Y$ определены две меры сходства --- индекс Жаккара и косинусная мера --- и соответствующие им расстояния.
  Индекс Жаккара --- доля общих элементов в объединении:

  $
    Jaccard(X, Y) = card(X inter Y) / card(X union Y), quad JaccardDist = 1 - Jaccard.
  $

  $
    Cosine(X, Y) = card(X inter Y) / sqrt(card(X) dot card(Y)), quad CosineDist = 1 - Cosine.
  $

  #table(
    columns: 2,
    align: (center, left),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    table.header([*Эпизод*], [*Характерные сигналы*]),
    [I], [pulse, echo, frost],
    [II], [echo, flare, frost, drift],
    [III], [pulse, glass, hum],
    [IV], [pulse, drift, glass, echo],
  )
]

#tasklist("t2")[
  + Постройте граф сходства: вершины --- эпизоды, ребро соединяет две вершины тогда и только тогда, когда $Jaccard >= 0.25$.

    + Вычислите $Jaccard$ для всех пар эпизодов и укажите все пары с максимальным и с минимальным значением.
    + Найдите компоненты связности --- группы вершин, связанные цепочками рёбер.

  + Проверьте два заявления куратора.

    + Куратор добавляет в два эпизода один и тот же сигнал, отсутствовавший в обоих.
      По его словам, сходство по Жаккару не изменилось --- общие сигналы и различия остались прежними.
      Проверьте утверждение на произвольной паре эпизодов из таблицы, сформулируйте, как изменяется $Jaccard$ при добавлении обоим эпизодам нового общего сигнала, и докажите это.

    + Куратор уверен, что пороговое правило даёт "хорошую" кластеризацию: похожие эпизоды попадут в одну группу, а непохожие --- в разные.
      Проверьте это на полученном графе: лежат ли все пары с максимальным $Jaccard$ в одной компоненте, а пары с минимальным --- в разных.

  + Закодируйте множество сигналов каждого эпизода двоичным вектором по объединению сигналов всех четырёх эпизодов: единица стоит на позициях сигналов эпизода.
    Выразите косинусную меру через эти векторы и объясните, почему её так назвали.

  #colbreak()
  + Сравните индекс Жаккара и косинусную меру.

    + Для любых непустых конечных $X$ и $Y$ докажите $Jaccard(X, Y) <= Cosine(X, Y)$.
      #Hint[Запишите обе меры через $a = card(X inter Y)$, $b = card(X without Y)$, $c = card(Y without X)$.]
    + Найдите необходимое и достаточное условие равенства $Jaccard(X, Y)$ и $Cosine(X, Y)$ и проверьте его на парах эпизодов из таблицы.
    + Пусть множества $X$ и $Y$ непусты и имеют равные мощности.
      Выразите $Cosine(X, Y)$ через $Jaccard(X, Y)$ и приведите пару множеств с разными мощностями, для которых $Jaccard(X, Y) neq Cosine(X, Y)$.

  + Проверьте метрические свойства расстояний $JaccardDist$ и $CosineDist$.

    + Выведите из определения формулу для расстояния Жаккара:
      $
        JaccardDist (X, Y) = card(X symdiff Y) / card(X union Y).
      $

    + Проверьте для обоих расстояний аксиомы неотрицательности и симметрии, а также тождество неразличимых:
      $
        d (X, Y) = 0 quad iff quad X = Y.
      $

    + Куратор "доказывает" неравенство треугольника для $JaccardDist$:

      #Block[
        По неравенству треугольника для симметрической разности
        $
          card(A symdiff C) <= card(A symdiff B) + card(B symdiff C),
        $
        а так как $card(A union C) >= card(A union B)$ и $card(A union C) >= card(B union C)$, получаем требуемое.
      ]

      Найдите ошибку.

    + Докажите неравенство треугольника: для любых непустых конечных $A$, $B$ и $C$
      $
        JaccardDist (A, C) <= JaccardDist (A, B) + JaccardDist (B, C).
      $
      #Hint[Разбейте элементы $A union B union C$ по областям Венна и проследите вклад каждой области в числители и знаменатели всех трёх дробей.]

    + Выясните, является ли $CosineDist$ метрикой, и обоснуйте ответ.
]

== Задача 3: Самореферентные конфигурации #h(1fr)#TagChallenge

В техническом журнале конфигурации заданы необычно.
Мощность одного множества участвует в определении другого, а другого --- в определении первого.

#tasklist("t3")[
  + Найдите все пары $(X, Y)$, удовлетворяющие условию:
    $
      X & = {1, 2, card(Y)}, \
      Y & = {card(X), 3, 4}.
    $
    Если решений нет, приведите доказательство отсутствия решения.
    #Hint[Мощности --- малые целые: достаточно перебрать возможные значения $card(X)$ и $card(Y)$.]

  #colbreak()
  + Найдите все тройки $(A, B, C)$, удовлетворяющие условию:
    $
      A & = {1, card(B), card(C)}, \
      B & = {2, card(A), card(C)}, \
      C & = {1, 2, card(A), card(B)}.
    $
    Если решений нет, приведите доказательство отсутствия решения.

  + Постройте собственную нетривиальную систему из 2--4 конечных множеств, имеющую ровно одно решение.
    Нетривиальной считайте систему, в которой каждое множество определяется через мощность другого множества.
    В единственном решении все множества содержат не менее двух элементов.
    Приведите систему, решение и доказательство единственности.
]


== Задача 4: Нечёткие множества и операции #h(1fr)#TagChallenge

Датчик №7 разучился говорить "да" и "нет" и вместо ответа пишет "похоже".
Теперь каждая его запись показывает, насколько сигнал похож на ожидаемый.
Инженеры списали его как неисправность, хотя именно у него самые подробные записи во всём архиве.
В журнале два сеанса наблюдений, $F$ и $R$, и их нужно свести в одну картину, а обычные операции над множествами к таким записям неприменимы.

#Given[
  Для каждого сигнала из $U = {a, b, c, d, e}$ в журнале записана степень принадлежности --- число от 0 до 1:
  $
    mu_F & = {a:0.4, b:0.8, c:0.2, d:0.9, e:0.7} \
    mu_R & = {a:0.6, b:0.9, c:0.4, d:0.1, e:0.5}
  $
]

#tasklist("t4")[
  + Определите операции над нечёткими множествами по образцу обычных --- через условия принадлежности вроде $x in A union B iff x in A or x in B$.
    Правила над числами из $[0; 1]$ должны на нулях и единицах совпадать с обычными $or$ и $and$ и сохранять тождества вроде $A union A = A$ и $A inter A = A$.

    + Вычислите $overline(F)$, $overline(R)$, $F union R$, $F inter R$, $F without R$, $R without F$.
    + Объясните, почему выбранный перенос естественен.

  + Проверьте, верно ли, что $F union overline(F) = U$ и $F inter overline(F) = emptyset$ (у множества $U$ степень всюду равна 1, у $emptyset$ --- нулю).
    Если равенства нарушаются, укажите, в каких точках это происходит, и объясните причину.

  + Обобщите индекс Жаккара на степени принадлежности, определите соответствующее расстояние и вычислите оба для $F$ и $R$.
    Критерий удачного обобщения: на чётких данных (степени только 0 и 1) мера должна совпадать с обычным Жаккаром.

  + Постройте пороговые срезы нечёткого множества: порог $theta in [0; 1]$ отбирает сигналы со степенью не ниже $theta$ --- получается обычное множество.

    + Выпишите пороговые множества для $F union R$ при $theta = 0.25, 0.75, 0.90$.
    + Объясните, почему с ростом порога множество может только сужаться.
    + Сколько различных множеств даёт вся шкала $theta in [0; 1]$?
]


== Задача 5: Геометрические множества на плоскости #h(1fr)#TagChallenge

В архиве нашёлся ровно один документ без слов, план сектора.
Пояснений к нему нет, и читать чертёж приходится по определениям множеств.

#Given[
  На действительной плоскости заданы множества:

  - $G = [0; 8] times [0; 7]$ --- исследуемый сектор
  - $S = (1; 4) times (5; 7]$ --- рабочий проход
  - $W = {pair(x, 4) mid(|) 0 <= x <= 6}$ --- перегородка
  - $D = {pair(x, y) in G mid(|) y < x or y < 4}$ --- нестабильная область
  - $T = {pair(x, y) mid(|) x in {1, 2, 3}, y in {1, 2}}$ --- отмеченные точки
  - $B = {pair(x, y) in G mid(|) 16 (x - 9)^2 + 25 y^2 <= 400}$ --- область устойчивого поля
]

#tasklist("t5")[
  + Изобразите $S$, $D$, $B$ и перегородку $W$ на фоне сектора $G$.
    Принадлежащие множеству участки края рисуйте сплошной линией, непринадлежащие --- пунктиром.

  + Подсчитайте целые точки $(x, y)$, принадлежащие $D$, но не лежащие ни на краю сектора~$G$ (стороны $x = 0$, $x = 8$, $y = 0$, $y = 7$), ни на перегородке $W$.

  + Проследите за экспедиционным дроном: он стартует в $P_0 = pair(2, 6)$ и последовательно смещается по векторам $v_1 = pair(4, 0)$, $v_2 = pair(1, -2)$, $v_3 = pair(-4, -3)$.

    + Отметьте все четыре позиции дрона на чертеже.
    + Для каждой позиции укажите, в какие из множеств $G, S, W, D, T, B$ она входит. \
      Отдельно отметьте позиции на краю сектора $G$.
    + Существует ли порядок, в котором дрон применяет векторы $v_1, v_2, v_3$, чтобы из $P_0$ попасть в $B$?

  + Какие точки перегородки $W$ лежат в $D$?
    Объясните, почему остальные точки перегородки в $D$ не попадают.

  + Завершите разбор устойчивого поля:
    + Найдите точку касания прямой $y = 4$ с границей эллипса и докажите, что $B inter W = emptyset$.
    + Докажите, что $B subset.eq D$: устойчивое поле целиком лежит в нестабильной области.
]


== Задача 6: Булеан и его свойства #h(1fr)#TagCore

Конфигурационный журнал перечисляет настройки станции, и каждая настройка задаётся подмножеством одного и того же набора переключателей.
Вопрос о полноте журнала упирается в более общий: сколько вообще бывает подмножеств?

#Given[
  Для любого множества $X$ через $cal(P)(X)$ обозначается множество всех его подмножеств --- булеан.
  Считайте, что множества $A$ и $B$ конечны, если не указано иначе.
]

#tasklist("t6")[
  + Сколько элементов в $cal(P)(cal(P)(emptyset))$?
    А в $cal(P)(cal(P)(cal(P)(emptyset)))$?

  + Докажите два структурных свойства булеана.

    + Докажите, что из $A subset.eq B$ следует $cal(P)(A) subset.eq cal(P)(B)$.
    + Докажите равенство $cal(P)(A inter B) = cal(P)(A) inter cal(P)(B)$.

  #colbreak()
  + Куратор "доказывает" равенство $cal(P)(A union B) = cal(P)(A) union cal(P)(B)$:

    #Block[
      Если $X subset.eq A union B$, то $X subset.eq A$ или $X subset.eq B$, значит $X in cal(P)(A) union cal(P)(B)$. \
      Обратное включение очевидно.
    ]

    Найдите ошибку, установите верное включение и приведите контрпример к этому шагу с наименьшим возможным $card(A union B)$.

  + Докажите $card(cal(P)(A)) = 2^n$, где $card(A) = n$, для конечных множеств.

  + Зафиксируйте универсум $U = {u_1, ..., u_n}$ и сопоставьте каждому множеству $A subset.eq U$ его _характеристический вектор_.
    Это строка из $n$ нулей и единиц, где на позиции $i$ стоит единица тогда и только тогда, когда $u_i in A$.
    Такие векторы уже строились в задаче 2 для эпизодов.
    Докажите, что характеристический вектор $A symdiff B$ получается из векторов $A$~и~$B$ побитовой операцией $xor$.
]


== Задача 7: Логические формулы и таблицы истинности #h(1fr)#TagCore

Из всей техники станции по-настоящему бодрствует один терминал.
Он не принимает объяснений и работает только с истинностными значениями.
Формальной логике, в отличие от людей, не нужен собеседник, и эти двадцать лет прошли для неё незамеченными.

#Given[
  Правило $phi$ зависит от четырёх бинарных сигналов:
  - $p$ --- "в секторе есть движение",
  - $q$ --- "датчик подтверждён",
  - $r$ --- "внутренний канал свободен",
  - $s$ --- "резервный контур доступен".
]

#tasklist("t7")[
  + Какие из следующих фраз являются высказываниями, а какие --- нет?
    Для каждой кратко укажите причину.
    + "Открой шлюз."
    + "Датчик подтверждён?"
    + "$p + q$"
    + "Внутренний канал свободен достаточно долго."
    + "В секторе нет движения."
    + "Резервный контур доступен или недоступен."
    + "Модуль $x$ запущен."
    + "Надпись на терминале: эта надпись ложна."

  + Не выписывая полной таблицы, найдите все наборы значений $p, q, r, s$, на которых $phi = (p imply q) and (not r or s)$ ложна, и сосчитайте наборы, на которых она истинна.
    #Hint[Конъюнкция ложна ровно тогда, когда ложен хотя бы один из сомножителей.]

  + Классифицируйте каждую формулу: тавтология, противоречие или контингентная (выполнимая, но не тавтология).
    #tasklist("t7.3", cols: 2, format: "letters", full: false)[
      + $p and not p$
      + $p or not p$
      #colbreak()
      + $p or q$
      + $(p imply q) or (q imply p)$
    ]

  + Преобразуйте условие истинности $phi$ в формулу без связки $imply$ и проверьте, что она ложна ровно на наборах из предыдущего пункта.

  + Восстановите условие открытия шлюза: у второго терминала --- три сигнала $p$, $q$, $r$.
    В~протоколе перечислены все наборы значений, при которых шлюз открывался, и только они: $(1,1,0)$, $(0,1,1)$, $(1,0,1)$.
    + Запишите его формулой в дизъюнктивной нормальной форме --- по конъюнкции на каждый открывающий набор --- и проверьте, что на остальных пяти наборах шлюз закрыт.
    + Опишите условие одной фразой на естественном языке.
]


== Задача 8: Импликация и выразимость связок #h(1fr)#TagCore

В документации главного терминала описано только несколько связок.
Остальные туда не попали, их остаётся выражать через описанные, и хорошо бы заранее знать, что выражается, а что нет.

#tasklist("t8")[
  + Выразите $p imply q$ через $not$ и $and$, подтвердив эквивалентность таблицей истинности.
    Отсюда выразите $not (p imply q)$ через $not$ и $and$ уже без новой таблицы --- объясните, почему она не нужна.

  + Сравните таблицы истинности формул $not p imply q$ и $not (p imply q)$.
    + Эквивалентны ли они?
    + Какое правило приоритета связок объясняет результат?

  + Докажите законы де Моргана:

    $
      not (p and q) equiv (not p or not q), quad not (p or q) equiv (not p and not q).
    $

  + Куратор "доказывает" закон де Моргана проверкой одной строки:

    #Block[
      При $p = 1$, $q = 1$ обе части $not (p and q) equiv not p or not q$ равны 0, значит закон верен.
    ]

    Найдите ошибку.
    Сколько строк таблицы нужно проверить, чтобы доказать эквивалентность формул от $n$ переменных?
]


== Задача 9: Логика предикатов #h(1fr)#TagCore

Модули станции описаны не схемами, а свойствами.
Схем не сохранилось, и каждое утверждение о модуле имеет вес лишь тогда, когда записано формулой.

#Given[
  Пусть предметная область состоит из модулей станции.
  Для каждого модуля $x$ заданы предикаты:
  - $S(x)$ --- "модуль $x$ запущен",
  - $P(x)$ --- "модуль $x$ проверен",
  - $R(x)$ --- "модуль $x$ соединён с резервным контуром",
  - $E(x)$ --- "модуль $x$ потребляет энергию".
]

#tasklist("t9")[
  + Переведите на язык логики предикатов:
    + "Все запущенные модули проверены."
    + "Некоторые запущенные модули не проверены."
    + "Всякий проверенный запущенный модуль соединён с резервным контуром."
    + "Существует запущенный модуль, который не проверен и не соединён с резервным контуром."

  + Покажите, что вторая фраза пункта 1 выражает логическое отрицание первой.

  + Журнальная запись за один такт сохранилась целиком: в ней три модуля $m_1, m_2, m_3$ и значения всех четырёх предикатов.

    #table(
      columns: 5,
      align: center,
      stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
      table.header([*Модуль*], [*S*], [*P*], [*R*], [*E*]),
      [$m_1$], [1], [1], [0], [1],
      [$m_2$], [0], [1], [1], [1],
      [$m_3$], [1], [0], [0], [0],
    )

    + Какие из четырёх утверждений пункта 1 истинны по этой записи?
    + В том же такте зафиксирована зависимость модулей: $x D y$ читается "модуль $x$ зависит от модуля $y$".
      Выполнены пары $(m_1, m_1)$, $(m_2, m_1)$, $(m_3, m_2)$.
      Формализуйте две фразы и выясните, какая из них истинна:
      - "каждый модуль зависит от какого-то модуля"
      - "существует модуль, от которого зависят все модули"

  + Формализуйте и проверьте рассуждение:

    #Block[
      Все запущенные модули потребляют энергию.
      Модуль $m$ запущен.
      Следовательно, $m$ потребляет энергию.
    ]

    Назовите правило вывода, используемое при переходе от общей посылки к заключению.

  + Объясните, почему из $forall x (S(x) imply P(x))$ нельзя заключить $forall x (P(x) imply S(x))$, и приведите журнальную запись с двумя модулями, в которой первая формула истинна, а вторая ложна.
]

== Задача 10: Декартово произведение #h(1fr)#TagCore

Нумерация узлов станции двухступенчатая --- уровень, затем отсек, --- и полная карта узлов совпадает с произведением множеств уровней и отсеков.

#Given[
  $A = {1, 2, 3}$ --- уровни, $B = {p, q}$ --- отсеки.
  Декартово произведение $X times Y$ --- множество всех пар $pair(x, y)$, где $x in X$ и $y in Y$.
]

#tasklist("t10")[
  + Изобразите узлы $A times B$ сеткой уровней и отсеков.
    Сколько подмножеств у этой сетки?

  + Некоторые подмножества сетки задают _правило_ "каждому уровню --- ровно один отсек", остальные --- нет.
    Запишите условие на $R subset.eq A times B$, отделяющее правила от не-правил, и проверьте его на паре примеров.

  + Сколько правил даёт сетка?
    Сверьте с числом функций из $m$-элементного множества в $n$-элементное --- правило и есть функция, а её график --- подмножество произведения.

  + А сколько соответствий действует в обратную сторону --- "каждому отсеку --- ровно один уровень"?
    Почему числа в двух направлениях разные, хотя сетка одна?
]


#pagebreak()

#align(center)[
  #text(1.2em, weight: "bold", fill: accent)[Необязательные бонусные задачи]
]

#Block[
  Бонусные задачи не входят в обязательный минимум.
  Они заметно сложнее основных и требуют либо программирования, либо самостоятельного обобщения материала.
  Оценивается в них прежде всего объяснение модели, её корректности и границ применимости.
]


== Задача A: Движок тождеств #h(1fr)#TagBonus

Журналы станции пестрят равенствами над множествами.
Куратор подозревает, что половина этих равенств не законы, а многолетние опечатки, от которых станция и деградировала.
Проверять на глаз нельзя, нужен движок, который для любого равенства честно отвечает "закон" или "опечатка" и во втором случае предъявляет контрпример.

#tasklist("tA")[
  + Выберите представление множеств и реализуйте $union$, $inter$, $without$, $symdiff$, $cal(P)$, проверку включения и мощность.
    Объясните выбор: что должно лежать в основе, чтобы операции были однотипными и быстрыми.

  + Напишите минимальный парсер выражений над множествами: переменные, бинарные операции, дополнение, скобки.
    На некорректной записи --- например, лишней закрывающей скобке --- парсер должен отвечать внятной ошибкой.

  + Реализуйте проверку равенства двух выражений перебором всех присваиваний переменным подмножествами универсума мощности $n$.
    Выдайте не только вердикт, но и свидетельство: для "опечатки" --- контрпример с конкретными множествами, для "закона" --- число проверенных присваиваний.

  + Прогоните движок по этому списку и предскажите каждый вердикт до запуска:
    законы де Моргана, дистрибутивность $A inter (B union C) = (A inter B) union (A inter C)$, двойное дополнение, #box[$A without B = A$], $A symdiff B = B symdiff A$, $(A without B) without C = A without (B without C)$.
    Если предсказание и вердикт разошлись --- разберитесь, кто ошибся: человек или движок.

  + Оцените сложность перебора: сколько присваиваний нужно проверить для $k$ переменных на универсуме мощности $n$?

  + Найдите способ радикально сократить перебор при проверке тождеств, докажите его корректность и переделайте движок на этот способ.
    Во сколько раз ускорилась проверка при $k = 4$, $n = 6$?

  + Замерьте время работы старого и нового движка при $k = 4$ и $n = 1, ..., 8$ и сформулируйте, где проходит граница применимости каждого.
]


== Задача B: Нечёткий регулятор вентиляции #h(1fr)#TagBonus

Датчик №7 писал степень соответствия вместо нуля и единицы, и именно на таких датчиках строят настоящие регуляторы.
Соберите нечёткий контроллер вентиляции сектора, которому на вход подаётся уровень задымлённости, число от 0 до 1, а на выходе получается интенсивность проветривания в процентах.

#Given[
  Калибровка датчика по пяти опорным уровням задымлённости $x = 0.1, 0.3, 0.5, 0.7, 0.9$ даёт степени соответствия понятию "дым":
  $
    mu_F = {0.1:0.4, 0.3:0.8, 0.5:0.2, 0.7:0.9, 0.9:0.7}.
  $
]

#tasklist("tB")[
  + Восстановите функцию принадлежности "дым" по опорным точкам --- кусочно-линейную --- и выпишите параметры явно.
    Объясните, почему выбранная форма не противоречит данным.

  + Разбейте вход на лингвистические термы --- пересекающиеся нечёткие множества вроде "чисто", "дым", "гарь" --- и задайте их функции принадлежности.
    Выход тоже разбейте на термы: "тихий ход", "номинал", "аварийный продув".

  + Напишите базу из 3--5 правил вида "если ..., то ...".
    Прогоните через контроллер два сценария --- штатный и аварийный --- и покажите весь путь каждого: фаззификация входа, срабатывание правил, агрегация выходов.
    Фаззификация даёт степени принадлежности термам, агрегация --- поточечный максимум выходных термов, усечённых до степени срабатывания.

  + Дефаззифицируйте оба сценария двумя методами.
    Метод центра тяжести возвращает абсциссу центра масс площади под итоговой кривой, метод максимума --- середину участка наибольшего значения.
    Постройте случай, где методы заметно расходятся, и решите, какой из них разумнее для вентиляции и почему.

  + Пороговым срезом уровня $theta$ нечёткого множества $M$ называется обычное множество $M_theta = {x : mu_M (x) >= theta}$.
    Для множества с конечным числом различных степеней принадлежности докажите формулу $mu_M (x) = max {theta : x in M_theta}$.
    Проверьте её на термах "чисто", "дым" и "гарь".
]

== Задача C: Выгоревшие сводки #h(1fr)#TagBonus

Сводный журнал вёл учёт по трём признакам, и бумага в нём, как и вся станция, оказалась палимпсестом.
Строка с числом событий, обладающих всеми тремя признаками сразу, затёрлась.
Восстановить её точно нельзя, остались только частичные итоги, и по ним нужно выяснить, что о потерянном числе можно сказать наверняка.

#Given[
  За сезон зафиксировано 60 событий и три признака: $A$, $B$, $C$.
  Сохранились сводки: #box[$card(A) = 20$], $card(B) = 24$, $card(C) = 22$, $card(A inter B) = 13$, $card(A inter C) = 12$, $card(B inter C) = 14$.
]

#tasklist("tC")[
  + Разбейте события на восемь элементарных групп по наборам принадлежности к $A$, $B$ и $C$ --- как в задаче 1, но теперь с численностями --- и обозначьте их переменными.
    Запишите каждую сохранившуюся сводку уравнением или неравенством на них.

  + Найдите наименьшее и наибольшее возможное значение $card(A inter B inter C)$.
    Для каждой границы докажите, что выйти за неё нельзя, и предъявите разложение всех областей, достигающее её.

  + Докажите, что каждое промежуточное значение тоже возможно.
    Меняется ли хоть одна граница, если общее число событий неизвестно?

  + Сформулируйте в общем виде --- для трёх множеств с известными мощностями и парными пересечениями --- двойное неравенство на мощность тройного пересечения.
    Сверьте его с найденными границами.
    Нижняя оценка --- частный случай неравенств Бонферрони.
]
