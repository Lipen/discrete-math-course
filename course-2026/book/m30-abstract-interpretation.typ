// M30 --- Абстрактная интерпретация: автоматическое доказательство свойств программ.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m25.typ": sign-lattice, widening

= Абстрактная интерпретация <chap:abstract-interp>

Программы ошибаются, и последствия бывают дорогими: деление на ноль останавливает программу, выход за границы массива портит данные, разыменование нулевого указателя роняет сервис.
Хочется знать заранее, что программа не упадёт и не сломается, --- до того, как она попадёт в продакшен.

Наивный способ проверить --- запустить программу и посмотреть, что будет.
Рассмотрим программу, которая делит на введённое число:
``` read x
y := 10 / x print y
```
Пусть она прошла тесты на трёх входах: $x = 5, x = -2, x = 7$.
Все три запуска успешны --- и всё равно мы ничего не доказали.
На четвёртом входе, $x = 0$, программа упадёт с ошибкой деления на ноль.
Тестирование показывает наличие ошибок, но никогда не доказывает их отсутствия: ошибка может прятаться на любом следующем входе.
Грузоподъёмность моста вычисляют по чертежу, из статики и механики материалов, а не гоняя по мосту всё более тяжёлые грузовики, пока он не рухнет.
Программная инженерия долго была устроена по второму способу: каждый тест --- грузовик, и предел программы узнают по моменту её падения.

Перебрать все входы невозможно.
Если вход --- 64-битное целое, возможных значений $2^64$ --- больше, чем секунд в возрасте Вселенной.
А для программы, читающей список чисел, входов бесконечно много.
Значит, доказывать свойство нужно для _всех_ запусков сразу, не выполняя программу ни на одном из них.

В предыдущей главе (@chap:complexity) мы измеряли сложность задач: как быстро вычисляется то, что вычислимо.
Ответы были асимптотическими --- классы сложности, NP-полнота.

Здесь вопрос другой --- можно ли доказать, что конкретная программа ведёт себя правильно, не выполняя её ни на одном входе.
Точного автоматического ответа не существует: свойства произвольной программы неразрешимы (глава @chap:decidability).

Остаётся приближённый ответ --- и его оказывается достаточно.
Точный ответ "да" или "нет" гарантировать нельзя --- теорема о неразрешимости запрещает.
Достаточно разрешить третий ответ, "не знаю", и задача перестаёт быть безнадёжной.
Простейший корректный анализ отвечает "не знаю" на любой вопрос: он не ошибается, но и не сообщает ничего полезного.
Вся дальнейшая работа --- ужимать этот "не знаю" до точного ответа там, где это возможно.

Аппарат для приближённого ответа мы уже наполовину построили.
В главе @chap:orders знаковая решётка появилась как предвестник: супремум сливал информацию из ветвей условного оператора, неподвижная точка (_fixed point_) --- результат анализа цикла.
Тогда это был взгляд в будущее. Теперь он становится главным.

Идея проста: заменить точные значения их грубыми обобщениями.
Чтобы доказать "знаменатель не равен нулю", не нужно знать, какое именно значение принимает $x$, --- достаточно знать, что $x$ не бывает нулём.
Вместо числа возьмём его _знак_: положительное, отрицательное или ноль.
Добавим два особых значения --- "неизвестно" и "недостижимо".
Таких обобщений конечное число, и программа "выполняется" на них за конечное число шагов.

Абстрактное выполнение _корректно_ аппроксимирует настоящее: всё, что может произойти в реальной программе, учтено в абстрактной.
Если абстрактный анализ показал, что деление на ноль невозможно, --- это доказано для всех запусков.
Цена --- точность: абстрактное выполнение может сообщить о возможной ошибке там, где на деле всё в порядке.
Знак не различает $5$ и $-3$, поэтому про сумму $5 + (-3)$ домен знаков скажет "неизвестно".
Это размен, на котором построена вся область: за конечный автоматический анализ мы платим неточностью, но не теряем ни одной реальной ошибки.

Чтобы размен стал техникой, нужен аппарат.
Решётки и информационный порядок --- из главы @chap:orders --- дают язык для описания точности.
Связка Галуа связывает абстрактные значения с множествами конкретных состояний.
_Переносящие функции_ переносят операторы программы в абстрактный мир, а теорема Кнастера--Тарского гарантирует существование неподвижных точек.
Для бесконечных доменов --- интервалов --- итерация не сходится, и в дело вступают widening и narrowing.
Как ужать ответ "не знаю" до точного, не потеряв ни одной реальной ошибки?

#history-note[Рождение идеи: от Флойда до Кузо][
  Первый шаг к автоматическому доказательству свойств программ --- метод индуктивных утверждений Роберта Флойда (1967) и логика Хоара (1969).
  Идея: разметить программу утверждениями, доказать, что каждое утверждение сохраняется переходом, и вывести, что итоговое свойство выполняется для всех запусков.
  Метод красив, но требует, чтобы инварианты циклов придумывал человек.

  В 1977 году Патрик Кузо и Радия Кузо (Patrick Cousot и Radhia Cousot) опубликовали статью
  "Abstract Interpretation: A Unified Lattice Model for Static Analysis of Programs by Construction or Approximation of Fixpoints".
  Работа показала, что статические анализаторы можно строить единообразно: как вычисление неподвижных точек операторов на решётках.
  Инвариант цикла больше не нужно изобретать --- он вычисляется автоматически.
  Если точная неподвижная точка недостижима, Кузо предложили приближать её операторами расширения (widening) и сужения (narrowing).
  Так абстрактная интерпретация стала общим каркасом, объединившим разрозненные анализы потоков данных:
  распространение констант, анализ интервалов, анализ указателей --- всё это оказалось частными случаями одной схемы.
]

Почти полвека каркас Кузо остаётся основой индустриального анализа программ.
Из знаков и интервалов домены выросли в полиэдры (Cousot и Halbwachs, 1978) и октагоны (Miné, 2001) --- каждый под свою задачу.
На этой теории работают оптимизирующие компиляторы, статические анализаторы и верификация систем, где ошибка стоит дороже любой неточности.

Наивная интуиция подсказывает: чтобы узнать, упадёт ли программа, её надо запустить.
Запуск показывает один путь из многих.
Рассуждение на абстракции покрывает все пути сразу --- и не требует ни одного запуска.
Точность при этом теряется, корректность --- никогда.

Тот же принцип --- рассуждать о неточном --- в главе @chap:fuzzy приведёт нас к нечётким множествам.

#chapter-overview[
  В этой главе мы займёмся знаковой абстракцией: заменим числа знаками и посмотрим, как на них "выполняется" программа.
  Связка Галуа свяжет абстрактные значения с множествами конкретных состояний, а переносящие функции --- с условием корректности.
  Познакомимся с доменами, которые решают реальные задачи: интервалами, константами, сравнениями и их произведениями.
  Разберём циклы: теорема Кнастера--Тарского объяснит, почему неподвижная точка существует, а widening заставит расходящуюся итерацию сойтись.
  Вернёмся к цене аппроксимации: ложные срабатывания неизбежны, пропуски ошибок --- нет.
  Закончим там, где абстрактная интерпретация работает в производстве: компиляторы, статические анализаторы, верификация полётного софта.

  После этой главы вы научитесь:
  + объяснять, почему тестирование не доказывает корректность;
  + строить абстрактные домены --- знаки, интервалы, константы --- и выполнять программу на них;
  + применять связки Галуа и переносящие функции;
  + находить неподвижные точки и пользоваться widening и narrowing;
  + объяснять, чем абстрактная интерпретация отличается от верификации Хоара.
]

== Знаки вместо чисел <sec:abstract-interp>

Пусть значения переменных --- целые числа.
Заменим каждое число его _знаком_: положительное, отрицательное или ноль.
Вместо "переменная равна $5$" скажем "переменная положительна".

#definition[Абстрактное значение знака][
  *Домен знаков* состоит из пяти элементов:
  - $top$ --- "знак неизвестен" (значение может быть любым),
  - $plus$ --- "значение положительно",
  - $0$ --- "значение равно нулю",
  - $minus$ --- "значение отрицательно",
  - $bot$ --- "значение невозможно" (переменная здесь не определена).
]

Эти элементы упорядочены по точности: $bot$ --- самое точное (значения нет).
$top$ --- самое грубое (значение любое).
Нагляднее всего порядок виден на рис. @fig:m25-sign-lattice.

#figure(
  sign-lattice,
  caption: [Решётка домена знаков: $bot prec.eq {minus, 0, plus} prec.eq top$.],
) <fig:m25-sign-lattice>

#definition[Информационный порядок][
  Говорим, что $a prec.eq b$ ("$a$ не грубее $b$"), если любой конкретный смысл $a$ содержится в конкретном смысле $b$.
  Например, $plus prec.eq top$: "положительное" точнее, чем "любое".
  И $bot prec.eq plus$: "недостижимо" точнее, чем "положительное".
]

#note[Почему порядок "наоборот"][
  В обычной математике $<=$ означает "меньше или равно".
  Здесь $prec.eq$ означает "не грубее": элемент ниже по порядку несёт больше информации.
  Это _информационный_ порядок, стандартная условность теории решёток: чем ниже спускаемся, тем точнее знаем состояние программы.
]

Каждое _абстрактное значение_ описывает множество конкретных значений.
Значение $plus$ описывает все положительные числа.
Значение $0$ --- только ноль.
Значение $top$ --- все целые числа.

#definition[Конкретизация и абстракция][
  Пусть $cal(C)$ --- множество конкретных состояний программы (например, все пары значений переменных).
  А $D$ --- абстрактный домен.
  - *Конкретизация* $gamma: D -> cal(P)(cal(C))$ отображает абстрактное значение в множество конкретных состояний, которые оно представляет.
  - *Абстракция* $alpha: cal(P)(cal(C)) -> D$ отображает множество конкретных состояний в наиболее точное абстрактное значение, его покрывающее:
  $
    alpha(X) = sqcap {d in D mid(|) X subset.eq gamma(d)}.
  $
  Для домена знаков: $alpha({5, 7}) = plus$.
  Конкретизация: $gamma(plus) = {x in ZZ mid(|) x > 0}$.
]

#remark[Связка Галуа][
  Пара $(alpha, gamma)$ образует _связку Галуа_:
  для любых $X subset.eq cal(C)$
  и $d in D$
  $
    alpha(X) prec.eq d arrow.l.r.double X subset.eq gamma(d).
  $
  Обе стороны утверждают одно: абстрактное значение $d$ описывает всё множество $X$.
  Абстракция даёт _наиболее точное_ описание, конкретизация --- _все_ значения, которые $d$ представляет.
  Это согласование делает анализ корректным по построению: абстрактный результат никогда не теряет реальные поведения.
]

#definition[Абстрактный домен][
  *Абстрактный домен* --- это решётка $(D, prec.eq, bot, top, sqcup, sqcap)$, где:
  - $prec.eq$ --- информационный порядок ("не грубее"),
  - $bot$ --- наименьший элемент ("недостижимо"),
  - $top$ --- наибольший элемент (все значения),
  - $sqcup$ --- объединение (_join_): $a sqcup b$ описывает всё, что описывают $a$ и $b$,
  - $sqcap$ --- пересечение (_meet_): $a sqcap b$ --- наиболее точное значение, совместимое с обоими.
  Объединение соответствует слиянию веток программы, пересечение --- усилению требования.
]

#example[Объединение и пересечение на домене знаков][
  Слияние двух веток: в одной $x = plus$, в другой $x = minus$.
  После слияния $x = plus sqcup minus = top$: какая ветка выполнилась, зависит от условия, и знак неизвестен.
  Ветка, помеченная как недостижимая, не добавляет информации: $bot sqcup plus = plus$.

  Пересечение уточняет знание: $plus sqcap top = plus$ --- из "любого знака" остаётся "положительное".
  Если требования несовместимы, пересечение пусто: $minus sqcap plus = bot$.
]

#note[Абстракция состояний, не только значений][
  Для программы с $n$ переменными абстрактное состояние --- вектор из $n$ абстрактных значений.
  Например, $(minus, plus, 0, dots)$.
  Порядок поэлементный: $a prec.eq b$, если каждый $a_i prec.eq b_i$.
  Так конечный текст программы описывается конечным множеством абстрактных состояний --- и вычисление над ним заведомо останавливается.
]

== Вычисления в абстрактном мире <sec:transfer>

Абстрактные значения описывают множества состояний, но анализ должен идти по программе: каждому оператору нужен двойник, работающий с абстрактными значениями.
Построим его от конкретной семантики оператора --- и перенесём эту механику в абстрактный мир.

Конкретная семантика оператора превращает множество состояний в множество состояний.
Например, оператор $x := x + 1$ переводит множество значений в сдвинутое множество.
То есть $X + 1 = {v + 1 mid(|) v in X}$.
Абстрактная семантика --- та же операция, но на абстрактных значениях.

#definition[Переносящая функция][
  *Переносящая функция* --- это отображение $llbracket s rrbracket^sharp : D -> D$, которое вычисляет абстрактное состояние после оператора $s$ по абстрактному состоянию до.
  Интуиция: "запустить оператор на абстрактных значениях".
]

Переносящая функция обязана быть _корректна_: она не имеет права терять ни одно реальное поведение.

#definition[Корректность переноса][
  Переносящая функция $llbracket s rrbracket^sharp$ _корректна_ (не пропускает реальных поведений), если для любого абстрактного состояния $d$
  $
    llbracket s rrbracket (gamma(d)) subset.eq gamma(llbracket s rrbracket^sharp (d)),
  $
  где $llbracket s rrbracket$ --- конкретная семантика оператора $s$ на множествах состояний.
  Словами: всё, что реально может произойти после $s$ в состоянии из $d$, покрывается абстрактным результатом.
  Перенос никогда не теряет настоящие поведения --- только добавляет невозможные.
  Дополнительно перенос предполагается _монотонным_: из $d_1 prec.eq d_2$ следует
  $llbracket s rrbracket^sharp (d_1) prec.eq llbracket s rrbracket^sharp (d_2)$.
  Без монотонности итерации по циклу могут не сходиться.
]

#example[Присваивание в домене знаков][
  Пусть $x := x + 1$.
  Абстрактный результат:
  - $plus -> plus$: положительное плюс единица --- положительное,
  - $minus -> top$: отрицательное плюс единица может стать нулём,
  - $0 -> plus$,
  - $top -> top, bot -> bot$.
  Правило $minus -> top$ --- честная цена абстракции: знак не различает $-1$ и $-5$, поэтому не видит, что $-1 + 1 = 0$.
  Перенос корректен: реальный результат покрывается абстрактным $top$.
]

#note[
  Домен знаков --- это тип с четырьмя значениями и таблицей операций:

  ```rust
  // Домен знаков: минус, ноль, плюс и "не знаю" (top).
  enum Sign { Neg, Zero, Pos, Top }

  // Сложение знаков: плюс ⊕ минус = Top, x ⊕ 0 = x.
  fn add(a: Sign, b: Sign) -> Sign {
      match (a, b) {
          (Sign::Zero, x) | (x, Sign::Zero) => x,
          (Sign::Neg, Sign::Neg) => Sign::Neg,
          (Sign::Pos, Sign::Pos) => Sign::Pos,
          _ => Sign::Top, // разные знаки могут дать всё что угодно
      }
  }
  ```

  Значения $plus$, $minus$, $0$, $top$ --- это варианты типа `Sign`, а таблица сложения --- функция `add`.
  Константы и интервалы --- соседние домены, построенные по той же схеме.
]

#example[Сложение в домене знаков][
  Операция $z := x + y$ требует абстрактной таблицы сложения знаков.
  Правила:
  - $plus sqplus plus = plus, minus sqplus minus = minus$,
  - $x sqplus 0 = x$ для любого знака $x$,
  - $plus sqplus minus = top$: положительное плюс отрицательное может быть и положительным, и нулём, и отрицательным,
  - $top sqplus x = top$ при $x != bot$.
  - $bot sqplus x = bot$.
    В частности, $top sqplus bot = bot$.
  Правило $plus sqplus minus = top$ --- источник неточности: знак не хранит величин.
  Он не видит, что $7 + (-4) = 3 > 0$.
  Корректность сохраняется: абстрактный результат $top$ покрывает реальный.
]

#example[Выражение $(x dot y) + z$ в домене знаков][
  Пусть $x = minus, y = plus, z = minus$.
  Сначала умножение: $minus times.square plus = minus$.
  Затем сложение: $minus sqplus minus = minus$.
  Абстрактный результат --- $minus$: выражение отрицательно.
  Конкретный пример: $(-3) dot 5 + (-2) = -17$, и знак совпал.
  Из таблицы умножения знаков понадобилась одна строка: минус на плюс --- минус.
  Точность не потеряна ни на одном шаге: знаки согласованы, и $sqplus$ не выродился в $top$.
]

#note[Как выводить переносящие функции][
  Переносящая функция выводится из конкретной семантики.
  Для оператора $z := x + y$ конкретный результат --- множество
  $X + Y = {u + v mid(|) u in gamma(x), v in gamma(y)}$.
  Абстрактный результат --- самая точная абстракция этого множества: $alpha(X + Y)$.
  Поэтому переносящая функция $#transfer("z := x + y") (d) = alpha({u + v mid(|) u in gamma(d_x), v in gamma(d_y)})$ корректна по построению.
  Именно так строятся таблицы абстрактных операций: перебрали все пары знаков, посчитали конкретное множество, абстрагировали.
]

== Домены на практике <sec:interval-domain>

Знаковая решётка --- простейший, но не единственный домен.
Разные вопросы о программе требуют разных доменов.

=== Интервальный домен

Программа суммирует элементы массива:
```
s := 0
for i in 0..n do
  s := s + a[i]
```
Индекс $i$ никогда не должен выйти за границы массива.
Чтобы доказать это, не нужно знать, какое именно значение принимает $i$ на каждом шаге:
достаточно знать, что $i$ лежит в отрезке $[0, n-1]$.
Отсюда и _интервальный домен_: значение переменной --- отрезок $[l, h]$, а не отдельное число.

#definition[Интервальный домен][
  Абстрактное значение --- интервал $[l, h]$ (либо особый элемент $bot$).
  Здесь $l, h in ZZ union {-oo, +oo}$.
  Конкретизация --- все целые числа из интервала $[l, h]$.
  $top = [-oo, +oo]$.
  $bot$ --- отдельный элемент "пусто".
  Порядок по включению: $[a, b] prec.eq [c, d]$ тогда и только тогда,
  когда $[a, b] subset.eq [c, d]$.
  Объединение $sqcup$ --- выпуклая оболочка (_convex hull_):
  $[a, b] sqcup [c, d] = ["min"(a, c), "max"(b, d)]$.
  Пересечение $sqcap$ --- наиболее точный интервал
  $["max"(a, c), "min"(b, d)]$ (пусто, если нижняя граница больше верхней).
]

Интервальный домен теряет связи между переменными.
Если в каждой точке программы $y = x - 1$, точное множество пар лежит на прямой.
Например, точки $(2, 1), (3, 2), (5, 4)$ --- три такие пары.
Интервальный анализ видит лишь прямоугольник $x in [2, 5], y in [1, 4]$ и не замечает, что $y$ определён значением $x$.
Связи между переменными возвращают более богатые домены --- октагоны и полиэдры, упомянутые в начале главы.

Интервалы хороши для доказательства границ: индексов массивов, счётчиков, буферов.
Сложение интервалов поэлементное: $[a, b] + [c, d] = [a + c, b + d]$.
Умножение (для конечных границ) --- $[a,b] dot [c,d] = [min(a c, a d, b c, b d), max(a c, a d, b c, b d)]$.
Если граница бесконечна, мы для простоты полагаем результат $top$ (стандартный интервальный домен разбирает бесконечные случаи точнее).

#example[Выход за границы массива][
  Пусть в программе $i in [0, 9]$, а массив имеет 10 элементов.
  Переносящая функция обращения к элементу $A[i]$ проверяет,
  что индекс в границах: $i in [0, 9]$.
  Если интервал $i$ выходит за границы --- например, $[0, 10]$ --- анализатор сообщает о возможном выходе за границы.
  Сообщение может оказаться ложным (переполнения нет), но реальный выход за границы он не пропустит: корректность гарантирует отсутствие ложноотрицательных.
]

=== Домен констант

#definition[Домен констант][
  Абстрактное значение --- либо конкретное целое $c$, либо особое значение $top$ ("не константа").
  Ещё одно особое значение --- $bot$ ("недостижимо").
  Порядок: $bot prec.eq c prec.eq top$ для всех целых $c$.
]

Домен констант доказывает равенства: после присваиваний $x := 3, y := x + 4$
выполняется равенство $y = 7$.
Компиляторы используют его для _распространения констант_: подставить в код вычисленное значение и упростить выражение.

Одно правило стоит особняком: умножение на ноль убивает неопределённость, $0 dot top = 0$.
Ноль, умноженный на любое конкретное значение, даёт ноль, поэтому результат известен точно даже при $top$-операнде.
Пример: $t := 0, v := top, z := t dot v$.
Анализ даёт $z = 0$, хотя $v$ неизвестно.

#example[Распространение констант][
  Программа:
  ```
  x := 3
  y := x + 4
  z := y * 2
  ```
  Анализ констант: $x = 3, y = 3 + 4 = 7, z = 14$.
  Компилятор выполнит замену $z := y * 2 -> z := 14$ и, возможно, удалит лишние присваивания.
  Это и есть применение абстрактной интерпретации в оптимизирующих компиляторах.
]

=== Домен сравнений

#definition[Домен сравнений (_congruence domain_)][
  Абстрактное значение --- класс вычетов $c mod m$: все числа, сравнимые с $c$ по модулю $m$.
  Например, $0 mod 2$ --- чётные числа.
  А $1 mod 2$ --- нечётные.
  Сложение: $(c_1 mod m_1) sqplus (c_2 mod m_2) = (c_1 + c_2) mod "gcd"(m_1, m_2)$.
  Умножение: $(c_1 mod m_1) times.square (c_2 mod m_2) = (c_1 c_2) mod "gcd"(m_1, m_2)$.
]

Домен сравнений отслеживает остатки по модулю и позволяет выводить свойства делимости: например, $x^2 + x$ чётно.
Он сочетается с интервальным доменом в произведениях.

#example[Домен сравнений выводит делимость][
  Пусть анализ знает, что $x equiv 3 mod 4$.
  Посчитаем $x^2 + x$ правилами домена.
  Умножение: $(3 mod 4) times.square (3 mod 4) = 9 mod "gcd"(4, 4) = 9 mod 4 = 1 mod 4$.
  Сложение: $(1 mod 4) sqplus (3 mod 4) = 4 mod 4 = 0 mod 4$.
  Вывод: $x^2 + x$ делится на 4, а значит, и на 2.
  Проверка на конкретном числе: $x = 7, space 7^2 + 7 = 56 = 14 dot 4$.
]

=== Произведение доменов

#definition[Произведение доменов][
  Чтобы получать сразу несколько свойств, домены объединяют в _произведение_.
  Абстрактное состояние произведения --- пара $(d_1, d_2)$.
  Здесь $d_i$ --- значение в соответствующем домене.
  Порядок поэлементный, объединение покомпонентное: $(a_1, a_2) sqcup (b_1, b_2) = (a_1 sqcup b_1, a_2 sqcup b_2)$.
]

Произведение знаков и констант даёт и знак, и точное значение (когда оно есть).
Произведение интервалов и сравнений --- и границы, и чётность.
Домены не обязаны быть независимыми: _редуцированное произведение_ переиспользует информацию одного домена для уточнения другого.

#example[Произведение знаков и констант][
  Программа:
  ```
  x := 5
  y := x - 10
  z := x + y
  ```
  В произведении знаков и констант состояние --- пара $(d_1, d_2)$: знак и константа.
  После первого присваивания: $x = (plus, 5)$.
  После второго: $y = (minus, -5)$.
  После третьего присваивания знак равен $plus sqplus minus = top$.
  Значение: $5 + (-5) = 0$.
  Произведение даёт $z = (top, 0)$: знак неизвестен, значение --- ноль в точности.
  Домен знаков в одиночку сказал бы $top$, домен констант --- $0$.
  Произведение хранит оба ответа сразу, и каждый из них пригодится своему анализу.
]

== Неподвижная точка и расходимость <sec:fixpoint-loop>

Простая последовательность операторов переносится естественно: идём по программе, применяя переносящие функции.
С условными операторами объединяем ветки через $sqcup$.
Сложность --- циклы: тело цикла может выполняться сколько угодно раз.

#definition[Перенос условий][
  Условие переносится как _фильтр_: абстрактное состояние пересекается с интервалом, заданным условием.
  Например, для условия `i < n` интервал $i$ в ветке "да" становится $[l, h] -> [l, "min"(h, n-1)]$ (при известной константе $n$).
  Если $l > n-1$ --- $bot$: ветка "да" недостижима.
  Ветка "нет" получает пересечение с дополнением: $[l, h] sqcap [n, +oo]$.
  Если $n$ неизвестна (её интервал --- $top$), фильтр ничего не отсекает.
  Верхняя граница не меняется: $"min"(h, +oo) = h$.
]

Рассмотрим цикл с телом $B$.
Пусть $E$ --- состояние в голове цикла при входе.
Определим $F(d) = E sqcup llbracket B rrbracket^sharp (d)$: объединение состояния на входе и результата одного прохода тела из $d$.
Состояние после любого числа проходов --- наименьшая неподвижная точка $F$:
$
  "lfp"(F) = sqcup_(k >= 0) F^k (bot).
$
Смысл: последовательность $F^k (bot)$ монотонно накапливает состояния, достижимые в голове цикла после всё большего числа проходов тела.
Объединив все $F^k (bot)$, получаем состояние после любого числа проходов.
В главе @chap:orders мы доказали (теорема Кнастера--Тарского), что такая неподвижная точка существует и что монотонная функция на конечной решётке достигает её за конечное число шагов.

#theorem[Итерация Клини сходится на конечных доменах][
  Если $F$ монотонна и домен конечен, последовательность итераций
  $d_0 = bot, d_(k+1) = F(d_k)$
  достигает $"lfp"(F)$ за конечное число шагов.
]

#proof[
  Монотонность даёт $d_0 prec.eq d_1 prec.eq d_2 prec.eq dots$: последовательность не убывает по информационному порядку.
  В конечной решётке любая неубывающая последовательность стабилизируется: с какого-то момента $d_(k+1) = d_k$.
  Тогда $F(d_k) = d_k$ --- неподвижная точка.
  Она наименьшая: любая неподвижная точка $F$ содержит $bot = d_0$.
  Значит, она содержит и все последующие $d_k$.
]

Домен знаков конечен --- циклы в нём анализируются перебором.
Интервальный домен бесконечен: интервалы вида $[0, n]$ с ростом $n$ никогда не стабилизируются.
Именно здесь наивная итерация ломается.

#example[Расходящаяся итерация][
  Рассмотрим цикл
  ```
  i := 0
  while i < n do
    i := i + 1
  ```
  Интервальный анализ состояния $i$ в голове цикла даёт последовательность $[0, 0], [0, 1], [0, 2], [0, 3], dots$
  Каждая итерация расширяет интервал на единицу.
  Процесс никогда не остановится: наивной итерацией неподвижную точку не получить.

  Условие `i < n` здесь не помогает: значение $n$ неизвестно (его интервал --- $top$), поэтому фильтр условия ничего не отсекает (см. определение переноса условий выше).
]

#example[Сходящаяся итерация: цикл с известной границей][
  Тот же цикл с известной границей:
  ```
  i := 0
  while i < 10 do
    i := i + 1
  ```
  Условие `i < 10` фильтрует состояние в голове цикла: перед телом интервал пересекается с $[0, 9]$.
  Итерация $d_(k+1) = F(d_k)$ от $bot$:
  #table(
    columns: (auto, auto),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    table.header([$k$], [$d_k$]),
    [$0$], [$bot$],
    [$1$], [$[0, 0]$],
    [$2$], [$[0, 1]$],
    [$3$], [$[0, 2]$],
    [$4$], [$[0, 3]$],
    [$dots$], [$dots.c$],
    [$10$], [$[0, 9]$],
    [$11$], [$[0, 9]$],
  )
  На десятом шаге интервал перестал расти: $d_10 = d_11 = [0, 9]$ --- неподвижная точка.
  Верхняя граница восстановлена фильтром условия: она равна $9$.
  Итерация сошлась, потому что граница $10$ известна заранее.
]

Ответ Кузо --- оператор _расширения_ (widening), который заставляет итерацию сойтись.

== Как заставить итерацию сойтись (widening) <sec:widening>

#definition[Оператор расширения][
  Бинарная операция $widen : D times D -> D$, для которой:
  - $a sqcup b prec.eq a widen b$ (результат не менее груб, чем объединение),
  - для любой возрастающей последовательности $d_0 prec.eq d_1 prec.eq dots$ последовательность
    $w_0 = d_0, w_(k+1) = w_k widen d_(k+1)$ стабилизируется за конечное число шагов.
  Словами: оператор $widen$ "ускоряет" рост, сбрасывая нестабильные компоненты в $top$.
]

#example[Widening для интервалов][
  Для интервалов классический widening:
  $
    [l_1, h_1] widen [l_2, h_2] = [l, h],
    l = cases(-oo "если" l_2 < l_1, l_1 "иначе"),
    h = cases(+oo "если" h_2 > h_1, h_1 "иначе").
  $
  В примере со счётчиком: $[0, 0] widen [0, 1] = [0, +oo]$: верхняя граница выросла --- сбрасываем её в бесконечность.
  Следующая итерация: $[0, +oo] widen [0, +oo] = [0, +oo]$ --- стабилизация.
  Неподвижная точка достигнута за два шага (рис. @fig:widening).
]

#figure(
  widening,
  caption: [Widening для цикла со счётчиком: наивная итерация $[0, 0], [0, 1], [0, 2], [0, 3], dots$ не сходится.
    Оператор $widen$ сбрасывает растущую границу и за два шага даёт $[0, +oo]$.],
) <fig:widening>

#algorithm[Вычисление неподвижной точки с widening][
  На каждой итерации в голове цикла применяем $F$ с widening:
  + $w_0 = bot$.
  + $w_(k+1) = w_k widen F(w_k)$.
  + Повторяем, пока $w_(k+1) != w_k$.
  По свойству widening последовательность стабилизируется за конечное число шагов.
  Результат --- корректная (но, возможно, грубая) неподвижная точка.
]

Widening гарантирует сходимость, но платит точностью: интервал $[0, +oo]$ в примере со счётчиком говорит лишь "$i$ неотрицательно", теряя верхнюю границу.
Чтобы вернуть точность, после widening применяют оператор _сужения_ (narrowing).

#definition[Оператор сужения][
  Бинарная операция $narrow$,
  для которой $a narrow b prec.eq a$: результат не грубее первого операнда.
  Повторяя сужение из уже достигнутой неподвижной точки, получаем более точную неподвижную точку.
  В примере со счётчиком сужение идёт по условию цикла.
  При известной константе $n$ фильтр `i < n` (перенос условий выше) уточняет интервал $[0, +oo] -> [0, n-1]$: верхняя граница восстановлена из условия.
]

#example[Widening и narrowing: цикл со счётчиком][
  Цикл $i := 0; "while" i < 100 "do" i := i + 1$ анализируем в два приёма.
  Фаза widening: $w_0 = bot$, затем $w_(k+1) = w_k widen F(w_k)$.
  $ w_1 = [0, 0], w_2 = [0, +oo], w_3 = [0, +oo]. $
  Уже на втором шаге верхняя граница сброшена в бесконечность, и итерация стабилизировалась.
  Фаза narrowing: из неподвижной точки $[0, +oo]$ уточняем результат по условию цикла.
  Фильтр `i < 100` даёт $[0, 99]$.
  Повторное сужение ничего не меняет: $[0, 99]$ --- неподвижная точка.
  Две фазы вместе дали точный ответ там, где ни widening, ни наивная итерация по отдельности не справились.
]

Схема "widening для сходимости, narrowing для точности" --- стандартный рецепт анализа циклов в интервальном домене.

#remark[Почему widening вообще работает][
  В бесконечной решётке интервалов нет никакой гарантии сходимости наивной итерации.
  Widening вводит _искусственную_ сходимость: как только граница начала расти, он объявляет её бесконечной.
  Это грубо, но корректно: результат всё ещё покрывает все реальные поведения.
  Точность теряется контролируемо --- ровно в той мере, в какой этого требует остановка.
]

== Цена аппроксимации <sec:correctness>

Абстрактный анализ корректен, но не точен.
Слово "корректен" здесь означает: анализатор не пропускает реальные ошибки.
Слово "не точен" означает: он может сообщить об ошибке, которой на деле нет.

#definition[Ложные срабатывания][
  Если анализатор сообщает о возможной ошибке в точке, где ошибки не бывает, --- это _ложное срабатывание_ (false positive).
  Корректный анализатор допускает ложные срабатывания, но не допускает пропусков: если ошибка реально возможна, анализатор обязательно её сообщит.
]

Почему анализатор не различает реальную ошибку и ложную тревогу?
Абстракция рисует вокруг исполнений оболочку, и если оболочка не задевает "запретную" область, ошибка доказанно невозможна.
Если оболочка область задевает, перед анализатором два неразличимых случая: либо внутрь области действительно попадает исполнение, либо не попадает ни одного.
Абстрактная картина в обоих случаях одинакова, и сообщить об ошибке нужно в обоих: замалчивание во втором было бы пропущенной реальной ошибкой.

#trap[Корректный не значит точный][
  "Анализатор не нашёл деление на ноль" означает "деление на ноль доказано невозможным" --- но только если анализатор корректен.
  Несложный (но некорректный) анализатор, который ничего не анализирует, тоже никогда не сообщит об ошибке.
  Ценность анализа --- именно в гарантии отсутствия пропусков.

  Плата --- ложные срабатывания: инженер вручную разбирает сообщения и выясняет, какие из них реальны.
  Искусство анализа --- уметь отличать реальные срабатывания от ложных, не пожертвовав корректностью.
  Масштаб платы оценивают цифрами: на полётном софте Airbus в сотни тысяч строк C коммерческие анализаторы выдают тысячи предупреждений, которые инженеры разбирают вручную.
  Astrée спроектировали так, чтобы довести ложные срабатывания до нуля, --- за счёт доменов, повторяющих структуру полётного софта.
]

#example[Точность и домен][
  Рассмотрим программу $x := 6, y := -7, z := x + y$.
  Домен знаков даёт для неё $z = top$: знак суммы не вычисляется.
  В действительности $z = -1$ --- определённое число.
  Домен констант посчитал бы $z = -1$ точно.
  Домен интервалов --- $[-1, -1]$.

  Выбор домена --- компромисс между точностью, стоимостью и тем, какое свойство нужно доказать.
  Корректность при этом не зависит от выбора: любой корректный домен не пропустит ошибку.
]

== Применения абстрактной интерпретации

Абстрактная интерпретация --- не абстрактная теория: на ней держится индустрия статического анализа.

=== Компиляторы

Оптимизирующие компиляторы (GCC, LLVM) внутри --- анализаторы потоков данных.
Распространение констант, анализ диапазонов значений, анализ указателей, удаление мёртвого кода --- всё это частные случаи абстрактной интерпретации.
Компилятор доказывает свойства программы, чтобы безопасно выполнить оптимизацию:
- заменить выражение константой,
- убрать неисполняемую ветку,
- развернуть цикл.

=== Верификация полётного софта

Статический анализатор Astrée, развитый в ENS в 2000-х, доказал отсутствие ошибок времени выполнения (деление на ноль, переполнение, выход за границы) в полётном софте самолётов Airbus.
Эти системы летают с результатами Astrée, потому что анализ корректен: доказательство отсутствия ошибок переживает сертификацию.
Домен октагонов, разработанный Антуаном Мине для Astrée, --- классический пример домена, спроектированного под конкретную задачу.

=== Промышленные анализаторы

Polyspace (MathWorks) использует абстрактную интерпретацию для верификации встраиваемого софта.
Frama-C --- открытая платформа анализа кода на C, ядро которой --- интервальный анализ на абстрактной интерпретации.
Абстрактная интерпретация --- стандартный инструмент индустрии.

=== Генерация инвариантов для верификации

Доказательство в логике Хоара требует инвариантов циклов (тройки Хоара --- в главе @chap:verification).
Абстрактная интерпретация вычисляет инварианты автоматически: неподвижная точка переносящей функции цикла и есть инвариант.
Вот как абстрактная интерпретация усиливает ручную верификацию: человек доказывает общую структуру, а инварианты циклов машина находит сама.

#history-note[Astrée: доказательство для самолёта][
  В 2003 году команда под руководством Патрика Кузо и его соавторов начала разработку Astrée --- анализатора, нацеленного на полётный софт Airbus.
  Позднее Astrée доказала отсутствие ошибок времени выполнения в программе управления полётом Airbus A340 и A380 --- системе из десятков тысяч строк C.
  Корректность анализа стала юридическим аргументом: сертификационные органы приняли машинное доказательство отсутствия ошибок.
  История Astrée --- пример того, как теория решёток и неподвижных точек из научной статьи превратилась в стандарт авиационной безопасности.
]

== Символьное исполнение <sec:symbolic>

Абстрактная интерпретация жертвует точностью ради корректности: анализатор может сообщить о ложной ошибке.
Символьное исполнение --- противоположный полюс: оно перебирает пути точно, ценой стоимости.

Вместо конкретного значения переменная получает _символьное выражение_ --- формулу от входных параметров.
Состояние вычисления --- это стек, память и _условие пути_: накопленные ограничения на входы.
При ветвлении состояние _разветвляется_: условие добавляется к условию пути одной ветки, его отрицание --- другой.
Решатель SAT/SMT (SAT с теориями, например с арифметикой) проверяет, выполнимо ли условие пути. Невыполнимое условие делает путь недостижимым, и состояние отбрасывается.
Для выполнимого условия решатель возвращает модель --- конкретные значения входных переменных, на которых условие истинно.
Так по каждому достижимому пути собирается конкретный набор входов, который этот путь выполняет.

#example[Одна программа, два пути][
  Программа: $x > 5 -> y := x + 1$,
  иначе $y := 2x$.

  После ветвления символьное исполнение ведёт два состояния.
  Первое: $x > 5, y = x + 1$.
  Второе: $x <= 5, y = 2x$.

  Из первого условия решатель выдаёт конкретный вход, например $x = 6$.
  Из второго --- например $x = 0$.
  Так символьное исполнение перечисляет все пути и для каждого находит конкретный пример.
]

Противоречие в условии пути иногда видно сразу: путь с условиями $x > 5, x <= 5$ недостижим при любых входах.
Каждый путь в дереве исполнения --- класс эквивалентности входов: все входы, которые ведут программу по одной и той же последовательности операторов.

Символьное исполнение лежит в основе генерации тестов, поиска уязвимостей и подтверждения отчётов статических анализаторов.
Оно точно там, где абстрактная интерпретация аппроксимирует, но платит за это:
число путей растёт экспоненциально, а завершение программы и достижимость конкретной ветки неразрешимы в общем случае (глава @chap:decidability).
Поэтому на практике два подхода работают вместе: консервативный анализ находит подозрительные места, символьное исполнение подтверждает или опровергает их по конкретным путям.

== Связь с курсом

Абстрактная интерпретация собирает воедино несколько нитей курса.
- Решётки и информационный порядок --- продолжение теории отношений порядка из главы @chap:orders, где мы впервые встретили знаковую решётку.
- Анализ потока данных IFDS из той же главы (@chap:orders) --- дистрибутивный частный случай абстрактной интерпретации:
  решётка фактов, переносящие функции должны быть дистрибутивными, а вызовы методов обрабатываются сводками (summaries).
- Неразрешимость (глава @chap:decidability) объясняет, почему точный анализ программ в общем случае невозможен: без аппроксимации не обойтись.
- Model checking из главы @chap:graphs проверяет конечные системы точно.
  Абстрактная интерпретация работает там, где пространство состояний бесконечно, --- жертвуя точностью ради автоматизма.
- Конечные автоматы (глава @chap:automata) находят в анализе свою роль: автомат --- удобный абстрактный домен для описания последовательностей событий в программе.

#note[Общая картина][
  Вопрос "можно ли автоматически доказать свойства программ" решается точно.
  Точно --- нет: проблема неразрешима (глава @chap:decidability).
  Приближённо --- да: абстрактная интерпретация вычисляет корректную аппроксимацию автоматически.
  Граница между "точно нельзя" и "приближённо можно" --- практическая граница: именно на ней строятся компиляторы, статические анализаторы и верификация индустриального софта.
]

== Итоги главы

Вопрос о том, можно ли доказать свойство программы для всех запусков сразу, соединил две знакомые темы --- неразрешимость (глава @chap:decidability) и приближение.
Вместо множества конкретных состояний анализ работает с маленькой решёткой --- знаки, интервалы, --- решётки с информационным порядком приходят из главы @chap:orders, а связь абстрактного с конкретным задаёт пара $alpha, gamma$ (связка Галуа).
Переносящие функции выводятся из конкретной семантики по единому рецепту --- посчитать конкретное множество и абстрагировать его, --- поэтому корректность анализа гарантирована по построению, а не доказывается отдельно.

Дальше из этой схемы вырастает практика.
Циклы анализируются неподвижными точками: итерация от $bot$ сходится к наименьшей неподвижной точке, а widening и narrowing ускоряют сходимость ценой контролируемой грубости.
Корректность при этом сохраняется: анализатор может сообщить о ложной ошибке, но пропустить реальную он права не имеет --- и именно это свойство сделало Astrée юридическим аргументом при сертификации полётного софта Airbus.
Символьное исполнение стоит на другом полюсе того же спектра: оно перебирает пути точно и платит за это стоимостью.

Аппроксимация с гарантиями работает там, где точное доказательство недостижимо, и это делает абстрактную интерпретацию повседневным инструментом компиляторов и верификаторов.
Дальше курс снимает последнее ограничение этого языка: нечёткие множества (глава @chap:fuzzy) допускают частичную принадлежность.

#check[Проверьте себя][
  + Чем элемент решётки отличается от множества конкретных состояний, и зачем нужны оба?
  + Почему корректность переноса --- это не то же самое, что точность?
  + Как связаны абстракция $alpha$ и конкретизация $gamma$?
  + Почему произведение доменов повышает точность, а не просто склеивает ответы?
  + В каком смысле widening заведомо неточен, и почему это приемлемо?
  + Почему ложные срабатывания неизбежны, а пропуски ошибок --- нет?
]

#whats-next
Абстрактная интерпретация всё время аппроксимирует: вместо точного значения --- знак, вместо множества --- интервал.
Сам язык аппроксимации построен на чёткой бинарной принадлежности: значение либо в интервале, либо нет.
Следующая и последняя глава снимает это ограничение:
нечёткие множества (глава @chap:fuzzy) допускают частичную принадлежность, и мы пересматриваем понятие множества, с которого начали курс, ---
теперь с позиции, вооружённой опытом логики, алгебры и вычислений.

== Упражнения

#tasklist("m30")[
  _Абстракция и переносящие функции._
  + Что такое переносящая функция? Почему она должна быть корректной?

  + Сформулируйте условие корректности переноса словами и формулой.

  + Постройте абстрактную таблицу умножения для домена знаков.
    Проверьте корректность каждой строки.

  + \* Объясните, почему $plus sqplus minus = top$ в домене знаков. Почему это корректно, но неточно? Приведите конкретный пример двух чисел, для которых домен знаков теряет точность при сложении.

  _Домены на практике._
  + Чем интервальный домен отличается от знакового? Какое свойство программы доказывает каждый из них?

  + Для программы
    ```
    x := -2
    y := x * x
    if y > 0 then z := 1 else z := 0
    ```
    проведите анализ в домене знаков.
    Что скажет домен констант?

  + Для цикла `i := 0. While i < 100 do i := i + 1` проведите интервальный анализ вручную.
    Считайте, что условие цикла фильтрует состояние (перенос условий: пересечение с $[0, 99]$).
    Сколько шагов нужны наивной итерации без widening?
    Что даёт widening?

  + \* Домен знаков с операцией $plus sqplus minus = top$ теряет точность.
    Предложите домен, который различает "строго положительное" и "неотрицательное", и постройте таблицу сложения.

  _Widening и неподвижные точки._
  + Пусть $F$ --- монотонная функция на конечной решётке.
    Докажите, что наивная итерация из $bot$ достигает неподвижной точки.

  + Покажите, что интервальный widening корректен: $[l_1, h_1] sqcup [l_2, h_2] prec.eq [l_1, h_1] widen [l_2, h_2]$.

  + \* Спроектируйте абстрактный домен для анализа чётности ($c mod 2$).
    Постройте переносящие функции для присваивания, сложения, умножения и условного оператора.
    Проверьте корректность.

  + \*\* В главе @chap:orders неподвижная точка цикла вычислялась на конечной знаковой решётке перебором.
    Объясните, почему тот же подход не работает для интервалов, и как widening это исправляет.
    Программа вычисляет факториал: что докажет интервальный анализ с widening и почему $top$ для результата при больших входных --- честный ответ, а не дефект анализа?

  + \* Пусть конкретный домен --- множество целых чисел $ZZ$ (одна переменная), а конкретизация знаков задана так:
    $gamma(bot) = emptyset$, $gamma(minus) = {x in ZZ mid(|) x < 0}$, $gamma(0) = {0}$, $gamma(plus) = {x in ZZ mid(|) x > 0}$, $gamma(top) = ZZ$.
    Вычислите $alpha({-7, 3, 0})$, $alpha({2, 4})$, $alpha(emptyset)$ и $alpha({5, -5})$.
    Проверьте условие связки Галуа $alpha(X) prec.eq d <=> X subset.eq gamma(d)$ для $X = {-1, 4}$ при $d = top$ и $d = plus$.
    Докажите, что пара $(alpha, gamma)$ всегда образует связку Галуа.
]


#project[Каркас абстрактных доменов][
  Соберите работающий каркас абстрактной интерпретации: решётку с информационным порядком, монотонный трансформер и итерацию к наименьшей неподвижной точке.
  Подключите к нему два домена --- интервалы и знаки, --- мини-язык программ с `assert` и конкретный интерпретатор, чтобы поверять анализ реальными запусками.
  Соберите его из связки Галуа, переносящих функций, теоремы Кнастера--Тарского и операторов расширения и сужения; новый домен подключается к _одному и тому же_ анализу.

  #project-stage[Каркас решётки и итерация][
    Реализуйте абстрактный домен как структуру с порядком "не грубее", элементами $bot$ и $top$, объединением $sqcup$ и пересечением $sqcap$.
    Задайте абстрактное состояние как отображение из переменных в абстрактные значения и продумайте, что означает состояние $bot$ и как отличить недостижимую ветку от живой.
    Реализуйте итерацию к наименьшей неподвижной точке монотонного трансформера с фазами widening и narrowing и проверьте на каждом домене аксиомы решётки и монотонность трансформера.
  ]
  #project-stage[Интервальный домен][
    Реализуйте отрезок $[l, h]$ с бесконечными границами: сложение и вычитание поэлементные, умножение по произведениям границ, деление по угловым частным.
    Продумайте, что возвращает деление на интервал, содержащий ноль: обрушение в $bot$ делает трансформер немонотонным, и итерация к неподвижной точке начинает осциллировать.
    Верните $top$ с отдельным предупреждением и объясните, почему корректность при этом сохраняется.

    Реализуйте перенос условий и widening по Кузо: растущая граница сбрасывается в бесконечность, и итерация сходится там, где наивная расходится.
    Продумайте, когда widening теряет точность безвозвратно и как narrowing по условию цикла возвращает потерянную границу.
    Что даёт перенос условия цикла в голову цикла, и почему без него widening останавливается на грубой неподвижной точке?
  ]
  #project-stage[Домен знаков][
    Реализуйте знак как битовую маску классов "отрицательное", "ноль", "положительное" и постройте таблицы арифметики по представителям классов: переберите все пары представителей, посчитайте конкретное множество, абстрагируйте.
    Продумайте, почему представителей классов достаточно для точной таблицы, и проверьте таблицы перебором конкретных пар, включая усечение целочисленного деления.
    Реализуйте перенос условий и проверку "definitely" для ассертов перебором пар знаков.
  ]
  #project-stage[Мини-язык и анализ][
    Соберите мини-язык --- присваивания, `if`, `while`, `assert`, чтение входа, --- построчный парсер и конкретный интерпретатор, который собирает наблюдения и реальные ошибки.
    Напишите общий анализатор, параметризованный доменом: слияние веток объединением, цикл через неподвижную точку, перенос условий, предупреждения о делении на возможно-ноль, чтении возможно неинициализированной переменной и возможно нарушенном ассерте.
    Продумайте, почему отсутствующая переменная в состоянии означает $top$, а не $bot$, и почему состояния и предупреждения тел циклов стоит записывать только от финального инварианта.
  ]
  #project-stage[Рандомизированная проверка][
    Напишите детерминированный генератор случайных программ: циклы с гарантированным завершением, случайные входы для интерпретатора.
    Сверьте анализ с прогонами: каждое реально достижимое значение лежит в конкретизации $gamma$ абстрактного состояния той же точки, каждая реальная ошибка предупреждена на том же операторе.
    Продумайте, что будет, если нарушится одно из двух условий, и почему случайные программы не доказывают корректность, но надёжно ловят её потери.
  ]
  #project-stage[Сравнение доменов][
    Соберите корпус программ: циклы со счётчиком, деление на вход, безопасное деление в цикле, дизъюнктивные значения, чтение неинициализированной переменной, реальные и ложные нарушения ассертов.
    Для каждой программы сведите в таблицу предупреждения и ложные срабатывания обоих доменов с эталоном из конкретного интерпретатора.
    Объясните, какие программы доказывает каждый домен и где они дополняют друг друга: интервалы видят границы, знаки --- точные множества знаков.
  ]

  #project-outcome[
    работающий каркас абстрактной интерпретации с двумя доменами: цикл со счётчиком и известной границей получает точный инвариант и точный выход, widening сходится там, где наивная итерация расходится, narrowing возвращает точность, а рандомизированная проверка и таблица сравнения доменов на корпусе подтверждают корректность и показывают разницу в точности.
  ]
]

