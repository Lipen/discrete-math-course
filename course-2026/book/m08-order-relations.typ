// M08 --- Отношения порядка: ранжирование, сравнение и структурирование дискретных объектов.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m08.typ": hasse-chain-3, hasse-divisors-12, hasse-powerset-2, hasse-powerset-3, lattice-m3, lattice-n5

= Отношения порядка <chap:orders>

Измерять множества мы научились в предыдущей главе: мощность отличает конечное от бесконечного.
Но между элементами есть и отношения порядка --- что чему предшествует, что от чего зависит.

Сборка проекта ждёт компиляции зависимостей, новая версия библиотеки --- совместимости со старой, документ в системе контроля версий --- своего предка.
Задача А выполняется раньше задачи Б, число $3$ меньше числа $7$, множество ${1}$ содержится в ${1, 2}$.
Одни вещи предшествуют другим, и в этой главе у предшествования появится точное определение.

Во всех этих случаях работает отношение порядка --- но, в отличие от функций, оно не обязано сопоставлять каждому элементу единственный образ.
Два элемента могут быть несравнимы, и именно это отличает частичный порядок от линейного --- и делает его полезным для моделирования зависимостей.

Решётки --- это алгебраические структуры, в которых у любой пары элементов есть супремум и инфимум.
Булевы алгебры --- их частный случай.
Решётки нашли применение в логике, топологии и информатике.

В абстрактной интерпретации программ свойства аппроксимируются элементами решётки.
Знак переменной --- одно из абстрактных значений: "положительное", "отрицательное", "ноль".
Супремум объединяет информацию из разных ветвей условного оператора.

Порядки и эквивалентности --- два основных типа бинарных отношений.
Если эквивалентности отвечают на вопрос "одинаково ли?", то порядки --- на вопрос "что раньше?".

#history-note[Гаусс и рождение структурной теории чисел][
  Карл Фридрих Гаусс опубликовал "Disquisitiones Arithmeticae"#footnote[C. F. Gauß. "Disquisitiones Arithmeticae". 1801.] в возрасте 24 лет.
  Эта книга заложила фундамент современной теории чисел --- и, неявно, теории отношений порядка.

  Гаусс ввёл сравнения по модулю: $a equiv b (mod m) iff m mid(|) (a - b)$.
  Сегодня мы опознаём в этой конструкции отношение эквивалентности (глава @chap:relations) --- но сам Гаусс мыслил в терминах арифметики, а не теории отношений.
  Сами понятия "отношение эквивалентности" и "класс эквивалентности" появятся лишь столетие спустя.

  В той же работе Гаусс изучал делимость целых чисел --- структуру, которую мы сегодня называем частичным порядком: $a | b$ рефлексивно, антисимметрично (на натуральных числах) и транзитивно.
  Он заметил, что не все числа сравнимы по делимости: 2 и 3 не делят друг друга --- свойство, отличающее частичный порядок от линейного.
  Гаусс не использовал термина "частичный порядок".
  Он говорил о "решете Эратосфена", "квадратичных формах" и "теории сравнений".
  Но математическая структура, которую он исследовал, --- это в точности наш частичный порядок делимости, а множество делителей числа с операциями НОД и НОК --- прообраз решётки.

  Это характерная черта математики XIX века: структурные понятия вызревают внутри конкретных теорий --- чисел, уравнений, функций --- прежде чем быть опознанными как самостоятельные абстракции.
  Гаусс не знал, что изучает отношение порядка.
  Но без "Disquisitiones Arithmeticae" теория порядков лишилась бы одного из своих опорных примеров.
]

#chapter-overview[
  В этой главе мы разберём три аксиоматики --- частичный, строгий и линейный порядок --- и увидим, что несравнимость --- инструмент: она означает независимость.
  Диаграммы Хассе сделают структуру видимой, а на экстремальных элементах, гранях, супремуме и инфимуме мы научимся находить "первые", "последние" и точные границы.
  Там, где у каждой пары есть супремум и инфимум, порядок вырастает в решётку.
  Топологическая сортировка развернёт частичный порядок в линейную последовательность, а теорема Кнастера--Тарского в конце главы гарантирует неподвижные точки монотонных функций.
  Весь аппарат применим к анализу программ --- от диаграмм Хассе до абстрактной интерпретации.

  После этой главы вы научитесь:
  + определять частичный, строгий и линейный порядок и проверять их аксиомы;
  + строить диаграммы Хассе и находить экстремальные элементы, супремумы и инфимумы;
  + распознавать решётки и булевы алгебры среди частичных порядков;
  + применять топологическую сортировку для выстраивания зависимостей;
  + формулировать теорему Кнастера--Тарского и видеть её в абстрактной интерпретации программ.
]

== Частичный порядок <sec:partial-orders>

Отношение порядка --- это формализация идеи "меньше или равно".
В отличие от полного порядка (как на числах), _частичный порядок_ допускает, что некоторые элементы несравнимы --- ни один не "меньше" другого.
Это ровно то обобщение, которое позволяет моделировать зависимости, иерархии и предпочтения.

#definition[Частичный порядок (_partial order_)][
  Бинарное отношение $prec.eq$ на множестве $A$ называется *частичным порядком*, если оно:
  - *Рефлексивно*: для всех $a in A$
    $
      a prec.eq a.
    $
  - *Антисимметрично*: если $a prec.eq b, b prec.eq a$, то
    $
      a = b.
    $
  - *Транзитивно*: если $a prec.eq b, b prec.eq c$, то
    $
      a prec.eq c.
    $

  Пара $(A, prec.eq)$ называется *ЧУМ* (частично упорядоченное множество, _poset_).
]

#example[Проверка аксиом частичного порядка][
  Пусть $A = {1, 2, 3, 6}, a prec.eq b iff a | b$.
  Проверим аксиомы:
  - Рефлексивность: каждое число делит себя: $1|1, 2|2, 3|3, 6|6$.
  - Антисимметричность: $a|b and b|a -> a = b$ (для натуральных чисел это верно).
  - Транзитивность: $a|b and b|c -> a|c$ (свойство делимости).

  Элементы $2$ и $3$ несравнимы: $2$ не делит $3$ и $3$ не делит $2$.
  Поэтому порядок частичный, а не линейный.
]

Определённый выше частичный порядок является нестрогим: он допускает равенство элемента с самим собой, как $<=$ на числах.
Для многих конструкций удобнее строгая версия --- аналог $<$, исключающий равенство.
Переход между ними механический, и каждому нестрогому порядку соответствует ровно один строгий.

#definition[Строгий порядок][
  Бинарное отношение $prec$ на множестве $A$ называется *строгим порядком*, если оно:
  - *Иррефлексивно*:
    $
      not(a prec a).
    $
  - *Асимметрично*: если $a prec b$, то
    $
      not(b prec a).
    $
  - *Транзитивно*: если $a prec b, b prec c$, то
    $
      a prec c.
    $
]

#trap[Антисимметрия $!=$ асимметрия][
  Антисимметричность и асимметричность --- свойства разных порядков.
  Антисимметричность: $a prec.eq b, b prec.eq a -> a = b$.
  Асимметричность: $a prec b, b prec a$ несовместны.
  Нестрогий порядок рефлексивен, строгий --- иррефлексивен: различие ровно как между $<=, <$ на числах.
  Смешение двух свойств в доказательстве даёт неверный вывод.
]

#proposition[Переход между строгим и нестрогим порядком][
  Имея нестрогий порядок, определим строгий: $a prec b iff a prec.eq b and a != b$.
  Имея строгий порядок, определим нестрогий: $a prec.eq b iff a prec b or a = b$.

  Обе конструкции сохраняют аксиомы.
  Проверим каждую:
  - *Асимметричность* строгого из антисимметричности нестрогого: $a prec b, b prec a -> a prec.eq b, b prec.eq a, a != b$, что противоречит антисимметричности.
  - *Транзитивность* строгого: $a prec b, b prec c -> a prec.eq b, b prec.eq c -> a prec.eq c$.
    Осталось исключить $a = c$.
    Если бы $a = c, a prec b, b prec a$ --- противоречие с асимметричностью.
    Значит, $a prec c$.
  - *Рефлексивность* нестрогого: $a = a -> a prec.eq a$ по определению.
  - *Антисимметричность* нестрогого: $a prec.eq b, b prec.eq a, a != b -> a prec b, b prec a$ --- противоречие с асимметричностью строгого порядка.
    Значит, $a = b$.
  - *Транзитивность* нестрогого: разберём случаи.
    Если $a prec b, b prec c -> a prec c$ по транзитивности строгого.
    Если $a = b, b prec.eq c -> a prec.eq c$.
    Если $b = c, a prec.eq b -> a prec.eq c$.
    Случаи $a = b = c$ уже покрыты предыдущими пунктами.
]

Эта двойственность аналогична паре $<=, <$ на числах.

#example[Строгий и нестрогий порядок][
  На множестве ${1, 2, 3}$ определим нестрогий порядок как отношение делимости.
  Правило: $a prec.eq b iff a | b$.
  Тогда $prec.eq = {(1,1), (1,2), (1,3), (2,2), (3,3)}$.
  Соответствующий строгий порядок $prec$ получается исключением равенств.
  Правило: $a prec b iff a | b and a != b$.
  Тогда $prec = {(1,2), (1,3)}$ --- те же пары, но без рефлексивных.

  На числах: нестрогий порядок $a <= b$ содержит пары $(3,3), (3,5)$.
  Строгий $a < b$ --- только $(3,5)$.
]

#definition[Линейный порядок (_total order_)][
  Частичный порядок, в котором любая пара элементов сравнима, называется *линейным* (или *полным*) порядком:
  $
    forall a, b: (a prec.eq b) or (b prec.eq a).
  $
  Линейно упорядоченное подмножество частично упорядоченного множества называют _цепью_.
]

Линейный порядок из трёх элементов выстраивается в вертикальную цепочку, как на рис. @fig:hasse-chain-3: три элемента по возрастанию.

#figure(
  hasse-chain-3,
  caption: [Цепь из трёх элементов.],
) <fig:hasse-chain-3>

#example[Линейные и частичные порядки][
  - $(NN, <=), (RR, <=)$ --- линейные порядки: любые два числа сравнимы.
  - Отношение делимости на $NN^+$ --- частичный порядок: 2 и 3 несравнимы (ни одно не делит другое).
  - $(cal(P)({1, 2, 3}), subset.eq)$ --- частичный порядок: ${1}$ и ${2}$ несравнимы (ни одно не подмножество другого).
]

#remark[Почему именно антисимметричность?][
  Рефлексивность и транзитивность вместе образуют _предпорядок_ --- отношение, в котором два различных элемента могут быть эквивалентны в смысле порядка.
  Тогда $a prec.eq b, b prec.eq a, a != b$.
  Предпорядок задаёт направление, но не различает элементы, замкнутые в цикл длины два.

  Добавление симметричности превращает предпорядок в отношение эквивалентности --- структуру, которая классифицирует: разбивает множество на классы неразличимых.
  Добавление антисимметричности --- в частичный порядок: структуру, которая ранжирует, выстраивает элементы по уровням.
  Одно свойство определяет, будет ли отношение _группировать_ по сходству или _упорядочивать_ по старшинству.
  Рефлексивность и транзитивность --- общий фундамент обеих теорий.
  Выбор между симметрией и антисимметрией --- точка ветвления.

  На графе различие видно невооружённым глазом.
  Симметрия тянет за собой обратные стрелки, транзитивность --- новые связи: вся компонента связности схлопывается в один комок.
  Антисимметрия между разными элементами запрещает обратные стрелки, и картинка вытягивается в иерархию ярусов.
]

Не все линейные порядки устроены одинаково.

#note[Плотность рациональных чисел][
  Порядок $(QQ, <=)$ _плотен_: между любыми двумя различными рациональными числами найдётся третье.
  Формально: $forall a, b in QQ, a < b -> exists c in QQ: a < c < b$.
  Доказательство: $a < (a + b) / 2 < b$ по свойствам сложения и деления.
  В отличие от этого, порядки $(NN, <=)$ и $(ZZ, <=)$ _дискретны_: между $n, n+1$ нет других целых чисел.

  Например: $1/3 < (1/3 + 1/2) / 2 = 5/12 < 1/2$.
  Между любыми двумя рациональными числами можно вставить их среднее арифметическое --- и так до бесконечности.
]

#example[Порядок в Rust: PartialOrd и Ord][
  Частичный порядок --- это множество, элементы которого мы научили сравниваться: определили, когда один не превосходит другого, но не потребовали, чтобы любые два были сравнимы.
  В Rust частичный порядок выражается трейтом `PartialOrd`: его метод `partial_cmp` возвращает `Option<Ordering>`, и значение `None` как раз означает несравнимость.
  Трейт `Ord` добавляет тотальность --- метод `cmp` возвращает `Ordering` всегда, поэтому `Ord` задаёт линейный порядок.

  Упорядочим множество по включению: $a prec.eq b iff a subset.eq b$.
  Это частичный порядок: ${1}, {2}$ несравнимы, потому что ни одно не подмножество другого.
  Значит, включение выражается через `PartialOrd`, а не через `Ord`.

  ```rust
  use std::cmp::Ordering;

  /// Множество как отсортированный список без повторений.
  struct Set(Vec<u32>);

  impl PartialEq for Set {
      fn eq(&self, other: &Self) -> bool { self.0 == other.0 }
  }
  impl Eq for Set {}

  impl PartialOrd for Set {
      /// a <= b означает, что a подмножество b.
      fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
          let sub = |x: &Set, y: &Set|
              x.0.iter().all(|e| y.0.binary_search(e).is_ok());

          match (sub(self, other), sub(other, self)) {
              (true, true)   => Some(Ordering::Equal),   // a = b

              (true, false)  => Some(Ordering::Less),    // a входит в b
              (false, true)  => Some(Ordering::Greater), // b входит в a
              (false, false) => None,                    // несравнимы
          }
      }
  }
  ```

  `None` в `partial_cmp` --- это математическая несравнимость: $a prec.eq b, b prec.eq a$ оба ложны.
  `Ord` мы намеренно не реализуем: включение не тотально, поэтому произвольная досортировка несравнимых множеств задала бы уже другой порядок, не совместимый с включением.
]

== Диаграммы Хассе <sec:hasse>

Визуализация частичного порядка --- диаграмма Хассе --- изображает элементы как точки на плоскости, а отношение порядка --- как вертикальное расположение и рёбра.

#definition[Диаграмма Хассе][
  Чтобы построить диаграмму Хассе конечного ЧУ-множества $(A, prec.eq)$:
  + Убрать все петли (рефлексивность подразумевается неявно).
  + Убрать все рёбра, следующие из транзитивности (оставить только отношение покрытия: $b prec a$ без промежуточных элементов).
  + Расположить элементы по вертикали: при $b < a$ элемент $b$ ниже элемента $a$.
  + Нарисовать ненаправленные линии между элементами, связанными отношением покрытия --- направление задаётся вертикальным положением.
]

Диаграмма Хассе --- это "скелет" частичного порядка: она показывает в точности те пары, которые нельзя вывести из транзитивности.
Каждое сокращение безопасно: то, что убрано, восстанавливается по определению ЧУМ.
Петли опущены, но подразумеваются рефлексивностью.
Транзитивные рёбра опущены: если до элемента можно дойти по рёбрам, связь следует из транзитивности.
Стрелки не нужны: если бы они были, все они указывали бы в одну сторону.

#example[Диаграмма делителей][
  Рассмотрим ЧУМ делителей числа 12: $D_12 = {1, 2, 3, 4, 6, 12}$, упорядоченное отношением $a | b$ (делимости).

  Отношения покрытия в этом множестве:
  - 1 покрывается элементами 2 и 3 (простые делители).
  - 2 покрывается элементами 4 и 6.
  - 3 покрывается элементом 6.
  - 4 и 6 покрываются элементом 12.

  Ребро 1--4 не рисуется, хотя 1 $|$ 4 верно: оно следует из транзитивности через 2.
  Диаграмма Хассе этого ЧУ-множества --- на рис. @fig:hasse-divisors-12.
]

#figure(
  hasse-divisors-12,
  caption: [Диаграмма Хассе делителей числа 12.],
) <fig:hasse-divisors-12>
Семейства подмножеств двух- и трёхэлементного множества, упорядоченные по включению, дают булевы решётки $B_2$ и $B_3$ --- их диаграммы Хассе показаны на рис. @fig:hasse-powerset-2 и @fig:hasse-powerset-3; термин "решётка" определим в разделе @sec:lattice.

#figure(
  hasse-powerset-2,
  caption: [Булева решётка $B_2$.],
) <fig:hasse-powerset-2>

#figure(
  hasse-powerset-3,
  caption: [Булева решётка $B_3$.],
) <fig:hasse-powerset-3>

#example[Булева решётка размерности 3][
  Для $(cal(P)({1, 2, 3}), subset.eq)$ диаграмма Хассе представляет собой трёхмерный куб:
  8 элементов (все подмножества трёхэлементного множества), 12 рёбер покрытия (добавление ровно одного элемента).
  Это первый пример _решётки_ --- структуры, которую мы определим ниже.
  Семейство подмножеств с включением называют булевой решёткой.
  Пустое множество $emptyset$ находится внизу, ${1, 2, 3}$ --- вверху.
  Это простейший пример _булевой решётки_ $B_3$.
]

#remark[Пищевые сети как диаграммы Хассе][
  Экологи рисуют пищевые сети по тому же правилу.
  Стрелка $A -> B$ означает, что $A$ ест $B$.
  Но изображают только прямые пищевые связи: волк ест зайца, заяц ест траву.
  Связь "волк ест траву" --- через зайца --- не рисуют: она следует из транзитивности.
  Отношение покрытия --- это в точности "кто кого ест непосредственно".

  Трофический уровень организма --- его высота в такой диаграмме.
  Трава внизу, волк наверху.
  Заяц и сурок на среднем уровне несравнимы: ни один не ест другого, но оба едят траву.
  Это частичный порядок, не линейный.
  Несравнимость здесь означает _независимость_, а не отсутствие связи.
]

== Экстремальные элементы <sec:extremal>

В частичном порядке одни элементы занимают привилегированное положение: выше (или ниже) них ничего нет, либо они сравнимы со всеми.
Эти понятия обобщают "первый" и "последний" на случай, когда порядок не линеен.

#definition[Минимальный элемент][
  Пусть $(A, prec.eq)$ --- ЧУМ с нестрогим частичным порядком.

  Здесь $prec$ --- индуцированный _строгий_ порядок:
  $
    (a prec b) iff (a prec.eq b) and (a != b)
  $

  Элемент $m$ называется *минимальным*, если нет элемента строго меньше.
  $
    & exists.not a in A . space a prec m \
    & equiv \
    & forall a in A . space a prec.not m
  $
]

#definition[Максимальный элемент][
  Элемент $m$ называется *максимальным*, если нет элемента строго больше:
  $
    & exists.not a in A . space m prec a \
    & equiv \
    & forall a in A . space m prec.not a
  $
]

Первая пара свойств локальна: минимальный и максимальный элементы отличаются отсутствием строгого соседа и могут оставаться несравнимыми с остальными элементами.
Вторая пара глобальна: наименьший и наибольший элементы сравнимы с каждым элементом множества.

#definition[Наименьший элемент][
  Элемент $m$ называется *наименьшим*, если он меньше (нестрого) всех:
  $
    & forall a in A . space m prec.eq a \
    & equiv \
    & exists.not a in A . space m prec.eq.not a
  $
]

#definition[Наибольший элемент][
  Элемент $m$ называется *наибольшим*, если он больше (нестрого) всех:
  $
    & forall a in A . space a prec.eq m \
    & equiv \
    & exists.not a in A . space a prec.eq.not m
  $
]

Эти четыре понятия легко спутать.
Наименьший элемент --- всегда минимальный, но не наоборот: минимальных может быть несколько (если они несравнимы в нижней части ЧУМ), а наименьший --- элемент, сравнимый со всеми и меньший всех.
Аналогично связаны наибольший и максимальные элементы.

Минимальные и максимальные элементы лежат на самом нижнем и самом верхнем уровне диаграммы.
В диаграмме с несколькими связными компонентами у каждой из них есть свои минимальные и максимальные элементы.
В конечном ЧУ-множестве они всегда существуют: иначе из любого элемента можно было бы бесконечно спускаться к строго меньшему, а в конечном множестве бесконечная цепочка не помещается.
Наименьший и наибольший такой гарантии не имеют.
Если наименьший или наибольший существует --- он единственен.

#example[Экстремальные элементы в ЧУ-множестве делителей][
  Рассмотрим ЧУМ $({2, 3, 4, 6, 12}, |)$ с отношением делимости:
  - Минимальные элементы: 2 и 3 (оба являются простыми числами, ни одно не делит другое, то есть "несравнимы").
  - Максимальный элемент: 12 (делится на все остальные).
  - Наименьший элемент: не существует (2 и 3 несравнимы).
  - Наибольший элемент: 12 (делится на все остальные).
]

Следующая таблица обобщает различия между экстремальными элементами:

#figure(
  table(
    columns: 4,
    align: (left, center, center, left),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    table.header([*Понятие*], [*Условие*], [*Единственен?*], [*Существует в конечном ЧУМ?*]),
    [Минимальный], [$not(exists a: a prec m)$], [Нет --- может быть несколько], [Всегда],

    [Максимальный], [$not(exists a: m prec a)$], [Нет --- может быть несколько], [Всегда],

    [Наименьший], [$forall a in A . space m prec.eq a$], [Да], [Не гарантирован],

    [Наибольший], [$forall a in A . space a prec.eq m$], [Да], [Не гарантирован],
  ),
  caption: [Сравнение экстремальных элементов частичного порядка.],
)

#check[Экстремальные элементы][
  + Может ли у конечного ЧУ-множества быть несколько максимальных элементов, но при этом существовать наибольший?
  + Почему максимальность определяется через строгий порядок --- "нет элемента строго больше" --- а не через нестрогий?
]

== Грани, супремум, инфимум <sec:sup-inf>

Экстремальные элементы характеризуют всё ЧУМ.
Когда нас интересует подмножество $S$ ЧУ-множества, мы говорим о гранях --- элементах, ограничивающих его сверху или снизу.

#definition[Грани (_bounds_)][
  Пусть $(A, prec.eq), S subset.eq A$ --- ЧУМ и его подмножество.
  - Элемент $u in A$ --- *верхняя грань* множества $S$, если $forall s in S: s prec.eq u$.
  - Элемент $l in A$ --- *нижняя грань* множества $S$, если $forall s in S: l prec.eq s$.
]

Граней может быть много.
Интерес представляют "наилучшие" грани --- наименьшая среди верхних и наибольшая среди нижних.

#definition[Супремум и инфимум][
  - Наименьшая верхняя грань множества $S$ называется *супремумом* (или точной верхней гранью), обозначается $sup S$.
    Для пары элементов $a, b$ пишут $a join b$ (читается $"a join b"$).
  - Наибольшая нижняя грань множества $S$ называется *инфимумом* (или точной нижней гранью), обозначается $inf S$.
    Для пары элементов $a, b$ пишут $a meet b$ (читается $"a meet b"$).
]

#example[Наименьшего элемента нет, инфимум есть][
  У конечного множества действительных чисел наименьший элемент всегда существует: перебрали все элементы --- нашли самый малый.
  У бесконечного множества его может не быть.
  Множество чисел ${1/n : n >= 1}$ не имеет наименьшего элемента.
  Все элементы множества больше нуля и сколь угодно близко к нему подходят, но нуль в множество не входит.
  Однако инфимум у этого множества есть --- это нуль.
  Инфимум --- обобщение наименьшего элемента, которому не обязательно принадлежать множеству.
]

#example[Грани, супремум и инфимум в ЧУ-множестве делителей][
  Рассмотрим подмножество $S = {2, 3}$ делителей числа 12.
  - Верхние грани $S$: ${6, 12}$ (оба делятся и на 2, и на 3).
  - Супремум $S$: $6$ --- наименьшая из верхних граней (6 делит 12).
  - Нижние грани $S$: ${1}$ (1 делит и 2, и 3).
  - Инфимум $S$: $1$ --- наибольшая из нижних граней.

  У подмножества ${4, 6}$ верхняя грань --- ${12}$, супремум --- $12$.
  Нижняя грань --- ${1, 2}$, инфимум --- $2$ (наибольшая из нижних).
]

#trap[Супремум $!=$ максимум][
  Супремум --- наименьшая верхняя грань, он может не принадлежать множеству.
  В $RR sup {q in QQ mid(|) q^2 < 2} = sqrt(2) in.not QQ$.
  Максимум должен принадлежать множеству --- у этого множества максимума нет.
]

Супремум и инфимум единственны, когда существуют.
Но в произвольном ЧУ-множестве они могут отсутствовать.
Например, пусть $a < b, a < c$, а $b$ и $c$ несравнимы.
Тогда у пары ${b, c}$ нет общей верхней грани.
Поэтому $b join c$ не существует.

Частичный порядок даёт структуру сравнения: для любой пары элементов ровно одно из $a prec.eq b, b prec.eq a$ или несравнимость.
Для моделирования зависимостей этого достаточно --- несравнимость позволяет говорить о параллелизме в планировании задач и о конкурентности событий в распределённых системах.
Но для статического анализа программ и верификации этого уже недостаточно.

Когда две ветви условного оператора сходятся, необходимо объединить информацию из обеих ветвей.
Пара несравнимых элементов может не иметь ни "наименьшего общего потомка", ни "наибольшего общего предка" --- супремум и инфимум могут отсутствовать.
Без гарантированного супремума (join) алгоритм не может корректно слить частичные результаты --- он вынужден либо терять точность, либо полагаться на эвристики.
То же верно для абстрактной интерпретации, верификации, оптимизации запросов --- везде, где информация течёт по графу программы и сливается в точках соединения.

#remark[Ближайший общий предок][
  Корневое дерево становится ЧУ-множеством, если упорядочить вершины от корня вниз: потомок меньше предка.
  Корень --- наибольший элемент, листья --- минимальные.
  У любой пары вершин в таком порядке есть супремум --- их ближайший общий предок.
  Общие предки у пары всегда есть (хотя бы корень), и среди них всегда есть наименьший.

  Инфимум у пары вершин существует не всегда --- поэтому дерево не дотягивает до решётки.
  Супремум здесь --- рабочий инструмент алгоритмов.
  Расстояние между вершинами выражается через него: $"dist"(a, b) = "depth"(a) + "depth"(b) - 2 dot "depth"(a join b)$.
]

Решётка добавляет ровно то, чего не хватало для алгоритмических применений: существование бинарных супремума и инфимума для каждой пары элементов.
Это минимальное усиление структуры.
Не требуется линейность --- несравнимость по-прежнему допускается.
Не требуется полнота --- не нужно определять sup и inf для бесконечных подмножеств.
Достаточно двух бинарных операций, чтобы частичный порядок превратился в алгебраическую структуру, пригодную для вычислений.

Мы подошли к понятию решётки.
Решётки возникают везде, где есть иерархия и операции объединения/пересечения: булевы алгебры, типы в языках программирования, статический анализ, топология.

== Решётки <sec:lattice>

#definition[Решётка][
  ЧУМ называется *решёткой*, если у каждой пары элементов существуют $a join b, a meet b$.
]

#example[ЧУМ, не являющееся решёткой][
  ЧУМ $({2, 3, 4, 6, 12}, |)$ с отношением делимости не является решёткой.
  У пары ${2, 3}$ нет нижней грани в этом множестве: элемент 1 (который делит и 2, и 3) отсутствует.
  Поэтому инфимум не существует --- и структура не дотягивает до решётки.

  Добавление 1 в множество превращает его в $D_12$ --- решётку делителей числа 12, с которой мы уже работали.
]

#example[Примеры решёток][
  - $(cal(P)(A), subset.eq)$: супремум --- $X union Y$, инфимум --- $X inter Y$.
    Это булева решётка.
  - $(NN^+, |)$: супремум --- $"НОК"(a, b)$, инфимум --- $"НОД"(a, b)$.
  - Линейный порядок: супремум и инфимум пары --- $max(a, b), min(a, b)$.
]

#example[Решётка делителей числа 60][
  Рассмотрим множество делителей числа 60: $D_60 = {1, 2, 3, 4, 5, 6, 10, 12, 15, 20, 30, 60}$ с отношением $a | b$ (делимости).

  Это решётка.
  Возьмём пару ${6, 10}$: элементы несравнимы (6 не делит 10, 10 не делит 6).
  Их $"НОД"(6, 10) = 2$ --- инфимум (наибольшая нижняя грань).
  Их $"НОК"(6, 10) = 30$ --- супремум (наименьшая верхняя грань).

  Для пары ${4, 5}$: ни одно не делит другое.
  Их $"НОД"(4, 5) = 1, "НОК"(4, 5) = 20$, причём НОД --- наименьший элемент решётки.
  Для пары ${2, 4}$: 2 делит 4.
  Поэтому $"НОД"(2, 4) = 2, "НОК"(2, 4) = 4$ --- один из элементов пары является гранью другого.

  Элементы 2 и 3 несравнимы (2 не делит 3, 3 не делит 2), но НОД и НОК определены --- это 1 и 6 соответственно.
  Именно это делает структуру решёткой, а не линейным порядком.
  В линейном порядке несравнимых элементов не существует.
  В решётке они _допустимы_, и на каждой паре определены операции $join, meet$.
  Конкретный пример: на диаграмме Хассе делителей 12, показанной на рис. @fig:hasse-divisors-12, элементы 4 и 6 несравнимы, но их инфимум равен 2, а супремум --- 12.
]

#history-note[От делителей чисел до статического анализа программ][
  Рихард Дедекинд в работе "Über Zerlegungen von Zahlen durch ihre grössten gemeinsamen Teiler"#footnote[R. Dedekind. "Über Zerlegungen von Zahlen durch ihre grössten gemeinsamen Teiler". 1897.]
  изучал структуру делителей чисел и ввёл понятия, ставшие основой теории решёток: операции пересечения и объединения, дистрибутивные законы.
  Он показал, что множество натуральных чисел с операциями НОД и НОК образует структуру, аналогичную булевой алгебре, но без дополнений.

  Гаррет Биркгоф в монографии "Lattice Theory"#footnote[G. Birkhoff. "Lattice Theory". 1940.]
  систематизировал разрозненные результаты и превратил теорию решёток в самостоятельную математическую дисциплину.
  Книга выдержала три издания (1940, 1948, 1967) и стала стандартным справочником.
  Биркгоф показал, что решётки возникают в алгебре (подгруппы, подкольца), топологии (замкнутые множества), логике (булевы алгебры) и проективной геометрии.

  Патрик и Радия Кузо в 1977 году опубликовали "Abstract Interpretation: A Unified Lattice Model for Static Analysis of Programs",
  где предложили использовать решётки для статического анализа программ.
  Каждое свойство программы (знак переменной, диапазон значений) моделируется элементом решётки.
  Объединение ветвей условного оператора --- супремум в решётке.
  Абстрактная интерпретация стала фундаментом промышленных анализаторов: Astrée доказал отсутствие ошибок времени выполнения в ПО Airbus A380.
  Infer от Meta находит баги в миллионах строк кода ежедневно.
]

Свойства операций решётки напоминают свойства арифметических операций, но с важными отличиями.
Ассоциативность, коммутативность и идемпотентность выполняются во всех решётках, а дистрибутивность --- нет.

#definition[Дистрибутивная решётка][
  Решётка называется *дистрибутивной*, если
  $
    a meet (b join c) = (a meet b) join (a meet c).
  $
  (И двойственное тождество.)
]

#definition[Булева алгебра][
  Дистрибутивная решётка с наименьшим элементом (0), наибольшим (1) и операцией дополнения $overline(a)$ называется *булевой алгеброй*.
  Дополнение удовлетворяет $a meet overline(a) = 0, a join overline(a) = 1$.
]

#note[
  Логика высказываний работает с операциями $and, or, not$.
  Алгебра множеств --- с $inter, union, overline(X)$.
  Вместе с булевой алгеброй это проявления одной структуры: дистрибутивной решётки с дополнениями.
  Это объединение подробно изучается в главе @chap:boolean.
]

#example[Булева алгебра из четырёх элементов][
  Решётка $cal(P)({1, 2})$, показанная на рис. @fig:hasse-powerset-2, --- булева алгебра.
  Наименьший элемент --- $emptyset$, наибольший --- ${1, 2}$.
  Дополнение множества $X$ --- его разность с ${1, 2}$: $overline({1}) = {2}$, $overline({2}) = {1}$.

  Проверим аксиому дополнения на паре ${1}, {2}$: ${1} union overline({1}) = {1} union {2} = {1, 2}$, и ${1} inter overline({1}) = emptyset$.
  Те же операции --- пересечение, объединение и дополнение --- на подмножествах любого множества дают булеву алгебру.
]

#example[Дистрибутивные и недистрибутивные решётки][
  Рассмотрим две дистрибутивные решётки.
  Булева решётка $(cal(P)({1, 2, 3}), subset.eq)$ дистрибутивна.
  Для любых подмножеств $A inter (B union C) = (A inter B) union (A inter C)$.
  Решётка делителей $(D_12, |)$: на элементах $2, 3, 6$ обе части равенства дают $6$:
  $
    2 join (3 meet 6) = "НОК"(2, "НОД"(3, 6)) = "НОК"(2, 3) = 6, \
    (2 join 3) meet (2 join 6) = "НОД"("НОК"(2, 3), "НОК"(2, 6)) = "НОД"(6, 6) = 6.
  $

  Одна проверка --- не доказательство.
  Решётка делителей любого числа дистрибутивна по другой причине: НОД и НОК действуют на показателях простых множителей поточечно --- НОК берёт максимум показателя по каждому простому, НОД ---
  минимум, --- и дистрибутивность сводится к тождеству $"min"(x, "max"(y, z)) = "max"("min"(x, y), "min"(x, z))$.

  Недистрибутивный пример --- решётка $N_5$ (пентагон).
  Её элементы: ${0, a, b, c, 1}$.
  Порядок: $0 < a < b < 1, 0 < c < 1$, а $c$ несравним с $a$ и $b$.
  Проверим нарушение закона на тройке $a, b, c$, взяв двойственную форму.
  Для недистрибутивности достаточно нарушения любой из двух, а в решётках они равносильны.
  + $b meet c = 0$: общая нижняя грань $b$ и $c$ --- только $0$, так как $a$ несравним с $c$.
    Поэтому $a join (b meet c) = a join 0 = a$.
  + $a join c = 1$: верхняя грань $a$ и $c$ --- только $1$.
    Поэтому $(a join b) meet (a join c) = b meet 1 = b$.
  + Получили $a != b$ при одинаковой левой части, и дистрибутивный закон нарушен.
]

В главе об отношениях (@chap:relations) характеризация Биркгофа была лишь анонсирована.
Здесь разберём понятия, на которых она стоит.
Наименьший элемент решётки пишут как $0, bot$.
Наибольший --- как $1, top$.
Это лишь разные записи одного понятия.
Формулировка теоремы использует две малые решётки $M_3, N_5$, изображённые на рисунках @fig:lattice-m3 и @fig:lattice-n5.
$M_3$ и $N_5$ --- две наименьшие недистрибутивные решётки, причём $N_5$ --- ещё и наименьшая не-модулярная (модулярность рассмотрим ниже).
"Изоморфная" подрешётка означает "устроенная так же": те же соотношения между элементами, с точностью до переименования.

#figure(
  lattice-m3,
  caption: [Решётка $M_3$ (ромб).],
) <fig:lattice-m3>

#figure(
  lattice-n5,
  caption: [Решётка $N_5$ (пентагон).],
) <fig:lattice-n5>

#definition[Подрешётка][
  Подмножество элементов решётки называется *подрешёткой*, если оно замкнуто относительно операций $join, meet$: результаты обеих операций над элементами подмножества снова лежат в подмножестве.
]

#example[Подрешётки][
  В булевой решётке $cal(P)({1, 2, 3})$, показанной на рис. @fig:hasse-powerset-3, цепочка $S_1 = {emptyset, {1}, {1, 2}, {1, 2, 3}}$ --- подрешётка: супремум и инфимум любых двух её элементов снова лежат в цепочке, например ${1} join {1, 2} = {1, 2}$ и ${1} meet {1, 2} = {1}$.

  Множество $S_2 = {emptyset, {1}, {2}}$ подрешёткой не является: ${1} join {2} = {1, 2}$, а этого элемента в $S_2$ нет.
  Одна недостающая операция разрушает замкнутость.
]

#theorem[Характеризация дистрибутивных решёток][
  Решётка дистрибутивна тогда и только тогда, когда она не содержит подрешётки, изоморфной $M_3, N_5$.
]

Утверждение "тогда и только тогда" называют _характеризацией_: оно задаёт необходимое и достаточное условие --- направление "только тогда" даёт необходимость, обратное --- достаточность.
Направление "только тогда" простое: дистрибутивность наследуется подрешётками.
Если равенство $a meet (b join c) = (a meet b) join (a meet c)$ верно во всей решётке, оно верно и внутри любой подрешётки.
А в $M_3, N_5$ оно нарушается.
Проверку для $N_5$ мы уже видели в примере выше.
Проверка для $M_3$ --- в примере про модулярные решётки ниже.
Решётка с такой подрешёткой не может быть дистрибутивной.

Обратное направление --- что всякая недистрибутивная решётка содержит $M_3, N_5$ --- нетривиально.
Из тройки $a, b, c$, нарушающей дистрибутивный закон, нужно построить одну из двух фигур.
Доказательство приведено в классических монографиях по теории решёток.#footnote[
  G. Birkhoff, "Lattice Theory", 1940.
  G. Grätzer, "General Lattice Theory", 1978.
]

#example[Применение: IFDS в статическом анализе][
  Задачи анализа потока данных формулируются на решётках фактов о программе.
  Элемент решётки --- набор фактов, например "переменная $x$ --- константа" или "указатель $p$ может быть нулевым".
  Порядок --- включение.
  Супремум --- объединение наборов фактов, приходящих из разных ветвей.
  Функция перехода $f$ описывает эффект отдельного оператора: набор фактов на входе она переводит в набор фактов на выходе.

  Для произвольной монотонной функции $x prec.eq y -> f(x) prec.eq f(y)$.
  Монотонность даёт только одну сторону неравенства: $f(x) join f(y) prec.eq f(x join y)$.
  Слить входы до применения $f$ --- значит потерять точность.
  Может оказаться, что $f(x join y)$ грубее, чем $f(x) join f(y)$.
  Слияние стирает информацию о происхождении фактов.
  Поэтому в общем случае анализ нельзя разложить на независимые подзадачи по отдельным фактам.

  Если функция перехода _дистрибутивна_, то $f(x join y) = f(x) join f(y)$ для любой пары $x, y$.
  Оба порядка операций дают одинаковый результат.
  На булеане дистрибутивность означает $f(S) = union_(e in S) f({e})$.
  Каждый факт можно прогнать через $f$ по отдельности, а результаты объединить.
  Анализ распадается на независимые подзадачи.
  Дистрибутивность здесь --- требование к функциям, а не к решётке: решётка фактов (булеан) дистрибутивна и так, но этого недостаточно.

  На этом основан класс задач IFDS (Interprocedural, Finite, Distributive, Subset) --- межпроцедурный анализ потока данных с конечным множеством фактов и дистрибутивными функциями перехода.
  Буква Subset напоминает, что функции действуют на подмножествах этого конечного множества.
  Межпроцедурность --- что анализ должен переживать вызовы функций и возвраты из них.
  Задачи этого класса решаются за полиномиальное время: анализ сводится к достижимости в специально построенном графе --- той же технике, что и в главе о графах (@chap:graphs).

  Среди приложений --- достигающие определения (reaching definitions) и taint-анализ: проверка, что непроверенные данные не попадают в критичные места программы.#footnote[
    T. Reps, S. Horwitz, S. Sagiv, "Precise Interprocedural Dataflow Analysis via Graph Reachability", POPL 1995.
  ]
  Дистрибутивность нужна именно этому классу задач.
  Общая абстрактная интерпретация работает и на недистрибутивных решётках, и к ней мы вернёмся в главе @chap:abstract-interp.
]

Дистрибутивность --- сильное свойство.
Есть и естественные недистрибутивные примеры, например решётка подпространств векторного пространства.
Для плоскости это нуль, вся плоскость и прямые через начало.
Над полем из двух элементов прямых ровно три, и решётка совпадает с $M_3$ из примера ниже.
Она удовлетворяет более слабому закону --- модулярности.

#definition[Модулярная решётка][
  Решётка называется *модулярной*, если из $a prec.eq c$ следует
  $
    a join (b meet c) = (a join b) meet c.
  $
  Всякая дистрибутивная решётка модулярна, но обратное неверно.
]

#example[Модулярная, но не дистрибутивная решётка][
  Решётка $M_3$ --- ромб, показанный на рис. @fig:lattice-m3, состоит из пяти элементов.
  Три из них --- $a, b, c$ --- попарно несравнимы.
  Каждый лежит между $bot, top$.
  Решётка $M_3$ модулярна.
  Для любой тройки выполняется $x prec.eq z -> x join (y meet z) = (x join y) meet z$.
  Но $M_3$ не дистрибутивна.
  $a meet (b join c) = a meet top = a$.
  $(a meet b) join (a meet c) = bot join bot = bot$ --- дистрибутивный закон нарушен.
]

Та же идея --- запрещённая подрешётка --- характеризует и модулярные решётки.

#proposition[Характеризация модулярных решёток][
  Решётка модулярна тогда и только тогда, когда она не содержит подрешётки, изоморфной $N_5$.
]

$N_5$ не модулярна.
Имеем $a prec.eq b -> a join (c meet b) = a join 0 = a$.
У $c$ и $b$ общая нижняя грань --- только $0$.
С другой стороны, $(a join c) meet b = 1 meet b = b$.
Здесь $a join c = 1$ --- единственная общая верхняя грань.
Итак, $a != b$.
$M_3$ модулярна, поэтому в модулярной характеризации она не участвует.
Обратное направление --- всякая не-модулярная решётка содержит $N_5$ --- доказывается в тех же источниках, что и дистрибутивная характеризация.

Модулярные решётки возникают в линейной алгебре: решётка линейных подпространств модулярна, и это используется при анализе кодов (глава @chap:codes).

Частичный порядок задаёт ограничения: одно должно быть раньше другого.
Превратить эти ограничения в конкретную последовательность действий --- задача топологической сортировки.
Несравнимость означает независимость: между $a, b$ нет зависимостей, поэтому их можно выполнять в любом порядке или параллельно.
Результат --- линейный порядок, совместимый с исходным частичным.

== Топологическая сортировка <sec:topo-sort>

Частичный порядок моделирует зависимости: одно действие должно быть выполнено раньше другого.
Чтобы составить расписание, нужно "развернуть" частичный порядок в линейный --- топологическую сортировку.

#definition[Топологическая сортировка][
  Линейный порядок на конечном ЧУ-множестве $(A, prec.eq)$ называется *топологической сортировкой* (или линейным расширением).
  Требование: $a prec.eq b -> a <= b$.
]

#example[Топологическая сортировка учебного плана][
  Курсы учебного плана с зависимостями: "Основы программирования" --- обязательный пререквизит для "Алгоритмов" и "Структур данных". "Структуры данных" --- пререквизит для "Базы данных".
  Тогда $"ОП" prec.eq "Алг", "ОП" prec.eq "СД", "СД" prec.eq "БД"$.
  Топологическая сортировка даёт допустимый порядок прохождения:
  ОП, Алг, СД, БД (или ОП, СД, Алг, БД, или ОП, СД, БД, Алг --- "Алгоритмы" и "Базы данных" несравнимы, их можно проходить в любом порядке после пререквизитов).
]

Определение топологической сортировки ставит естественный вопрос:
всякий ли частичный порядок допускает линеаризацию,
или существуют ЧУ-множества, которые принципиально нельзя выстроить в линейную последовательность без нарушения зависимостей.
Следующая теорема отвечает на этот вопрос утвердительно --- топологическая сортировка существует всегда.

#theorem[Существование топологической сортировки][
  Любое конечное ЧУМ имеет хотя бы одну топологическую сортировку.
]

#proof-sketch[
  Докажем построением: предъявим алгоритм, который строит топологическую сортировку.

  В конечном ЧУ-множестве всегда существует хотя бы один минимальный элемент.
  Алгоритм: выбрать любой минимальный элемент, вывести его, удалить из ЧУ-множества и рекурсивно повторить для оставшейся части.
  Порядок вывода даёт топологическую сортировку.

  *Корректность:* когда мы выводим элемент, все элементы, которые должны предшествовать ему, уже удалены, ведь он был минимальным в оставшемся множестве.
]

#note[
  Каждая топологическая сортировка конечного ЧУМ даёт линейный порядок, совместимый с исходным: если $a prec.eq b$ в исходном порядке, то $a$ предшествует $b$ и в линейном.
  Несравнимые элементы получают произвольное упорядочение.
  Разные сортировки соответствуют разным способам разрешить эту свободу --- именно поэтому частичный порядок допускает параллелизм: несравнимые задачи можно выполнять одновременно.

  Системы сборки --- Make, Bazel и другие --- определяют порядок компиляции той же сортировкой:
  если модуль `A` зависит от `B`, то `B` компилируется раньше, а несравнимые модули собираются параллельно (флаг `-j` в Make).
  В планировании задач метод PERT (Program Evaluation and Review Technique) вычисляет ранние и поздние сроки начала и завершения каждого узла обходом в топологическом порядке.
  Критический путь определяется как максимальная суммарная длительность зависимых задач.
  Любая система, моделирующая зависимости --- CI/CD пайплайны, планировщики задач, электронные таблицы --- опирается на ту же идею.#footnote[
    A. B. Kahn, "Topological Sorting of Large Networks", Communications of the ACM, 1962.
  ]
]

#example[Топологическая сортировка в коде][
  Алгоритм Кана --- та же жадная схема, что и в наброске доказательства выше: очередь минимальных элементов, извлечение, обновление счётчиков входящих рёбер.
  Функция возвращает порядок сборки или `None`, если граф содержит цикл: у циклического графа топологической сортировки нет.
  ```rust
  /// Топологическая сортировка (алгоритм Кана):
  /// порядок сборки или None при цикле.
  fn topo_sort(
      n: usize,
      edges: &[(usize, usize)],
  ) -> Option<Vec<usize>> {
      let mut indeg = vec![0usize; n];
      let mut adj = vec![vec![]; n];
      for &(u, v) in edges { adj[u].push(v); indeg[v] += 1; }

      let mut q: VecDeque<usize> =
          (0..n).filter(|&v| indeg[v] == 0).collect();
      let mut order = vec![];
      while let Some(u) = q.pop_front() {
          order.push(u);
          for &v in &adj[u] {
              indeg[v] -= 1;

              if indeg[v] == 0 { q.push_back(v); }
          }
      }
      (order.len() == n).then_some(order) // None, если есть цикл
  }
  ```
]

Топологическая сортировка --- лишь одно из применений порядков в программировании.
Ниже --- ещё несколько: подтипирование в языках с типами, семантическое версионирование, векторные часы и лексикографический порядок.

== Лексикографический порядок <sec:lexicographic>

// Макросы строгих порядков: scripts(...) чтобы индексы были сбоку, а не снизу.
// Нестрогий порядок --- как обычно: $a preceq b iff a prec b or a = b$.
#let precLex = $scripts(prec)_"lex"$
#let precDict = $scripts(prec)_"dict"$
#let precSlex = $scripts(prec)_"slex"$
#let precColex = $scripts(prec)_"colex"$

Порядок можно не только изучать, но и строить.
Иногда нужен лишь новый способ упорядочить уже знакомые объекты --- например, пары элементов или строки символов.
Ответ даёт лексикографический порядок: когда каждый компонент пары взят из своего частичного порядка, сравниваем первые компоненты, а при равенстве --- вторые.
Ровно так работают словари: сначала по первой букве, потом по второй, и так далее.

=== На парах

Пусть $(A, prec.eq_A), (B, prec.eq_B)$ --- два ЧУМ.
Имея порядки на компонентах, мы строим порядок на декартовом произведении $A times B$.

#definition[Лексикографический порядок на парах][
  Строгий лексикографический порядок $precLex$ на $A times B$ определяется так:
  $
    (a_1, b_1) precLex (a_2, b_2) iff
    (a_1 prec_A a_2) or (a_1 = a_2 and b_1 prec_B b_2),
  $
  где $prec_A, prec_B, prec.eq_A, prec.eq_B$ --- строгие и нестрогие порядки компонент.

  Если исходные порядки частичные --- результирующий тоже частичный.
  Если исходные линейные --- результирующий тоже линейный.
]

#example[Лексикографический порядок на парах][
  Пусть $A = B = {0, 1}$ с обычным порядком $0 < 1$.
  Тогда $A times B = {(0,0), (0,1), (1,0), (1,1)}$.
  Лексикографический порядок выстраивает их так:
  $
    (0, 0) precLex (0, 1) precLex (1, 0) precLex (1, 1).
  $
  Сравним $(0, 1), (1, 0)$: первые компоненты различаются.
  Так как $0 < 1$, кортеж $(0, 1)$ меньше, хотя его вторая компонента больше.
  Первая компонента имеет приоритет.
]

=== Обобщение на кортежи одинаковой длины

Следующий шаг --- $k$ компонентов.
Кортеж длины $k$ (или $k$-кортеж) --- упорядоченный набор $(a_1, a_2, dots, a_k)$ элементов ЧУМ.
Множество всех $k$-кортежей обозначается $A^k$.

#definition[Лексикографический порядок на $k$-кортежах][
  Для двух $k$-кортежей из $A^k$ положим
  $
    (a_1, dots, a_k) precLex (b_1, dots, b_k)
  $
  если в первой позиции, где $a_i != b_i$, выполнено $a_i prec b_i$.

  Алгоритмически: сравниваем пару $a_1, b_1$.
  Если равны --- переходим к паре $a_2, b_2$.
  И так далее.
  Порядок определяет первая же позиция, в которой кортежи различаются.
]

#example[Лексикографический порядок на тройках][
  Пусть $A = {0, 1}$ с порядком $0 < 1$.
  Кортежи длины 3 (битовые строки из трёх битов) упорядочиваются так:
  $
    (0,0,0) precLex (0,0,1) precLex (0,1,0) precLex (0,1,1)
    precLex (1,0,0) precLex (1,0,1) precLex (1,1,0) precLex (1,1,1).
  $
  Первый бит --- старший: все кортежи с $0$ на первой позиции идут раньше всех кортежей с $1$.
]

=== Строки разной длины: два подхода

До сих пор мы сравнивали кортежи одинаковой длины.
Со строками разной длины --- например, $"a"$ и $"aa"$ --- правило "сравниваем первые символы" не даёт ответа: символы кончаются раньше, чем находится различие, а одна строка является префиксом другой.
Возможны два расширения.

Если одна строка --- префикс другой, более короткая считается меньшей.
Именно так устроены бумажные словари: "кот" идёт раньше "котик", потому что "кот" --- префикс "котика".

#definition[Словарный порядок][
  Пусть $Sigma$ --- алфавит с линейным порядком на символах.
  Для строк $s, t in Sigma^*$ словарный порядок $precDict$ определяется так:
  + Если одна строка --- собственный префикс другой, более короткая меньше: $s precDict t$.
  + Иначе --- на первой позиции, где строки различаются, порядок определяют их символы (как в лексикографическом порядке на кортежах одинаковой длины).
]

#example[Словарный порядок][
  Над алфавитом ${"a", "b"}$ с порядком $"a" < "b"$ словарный порядок даёт:
  $
    "a" precDict "aa" precDict "aaa" precDict "aab" precDict "ab" precDict "b" precDict "ba" precDict "bb".
  $
  $"b" precDict "ba"$, потому что $"b"$ --- префикс $"ba"$.
  $"aa" precDict "ab"$, потому что на второй позиции $"a" < "b"$.
]

Словарный порядок линеен над строками конечной длины, если порядок на алфавите линеен.
Но он не является _вполне упорядоченным_ (well-ordered).
Множество ${"b", "ab", "aab", "aaab", dots}$ не имеет наименьшего элемента: каждая строка с дополнительным $"a"$ в начале оказывается меньше.
$
  "b" succ "ab" succ "aab" succ "aaab" succ dots
$
Бесконечная убывающая цепь не имеет минимума: в словарном порядке такие цепи существуют.

Альтернативный подход --- сравнивать строки в первую очередь по длине.
Сначала все строки длины 0, затем длины 1, затем длины 2, и так далее.
Внутри каждой длины --- обычный лексикографический порядок.

#definition[Shortlex][
  Для строк $s, t in Sigma^*$ порядок *shortlex* (или length-lexicographic) определяется так:
  $
    s precSlex t iff
    (|s| < |t|) or (|s| = |t| and s precLex t),
  $
  где $|s|$ --- длина строки.
]

#example[Shortlex][
  Над алфавитом ${"a", "b"}$ с порядком $"a" < "b"$:
  $
    epsilon precSlex "a" precSlex "b"
    precSlex "aa" precSlex "ab" precSlex "ba" precSlex "bb"
    precSlex "aaa" precSlex dots
  $
  В отличие от словарного порядка, $"b"$ идёт раньше $"aa"$ --- потому что длина 1 меньше длины 2.
]

В отличие от словарного, shortlex вполне упорядочен на множестве конечных строк над конечным алфавитом:
любое непустое множество имеет наименьший элемент (сначала --- все кратчайшие строки, а среди них --- лексикографически первую).
Это свойство делает shortlex полезным в алгоритмах полного перебора: перебор в порядке shortlex гарантирует, что каждая строка конечной длины будет достигнута за конечное число шагов.

#note[Словарный или Shortlex?][
  Словарный порядок естественен для интерфейсов, где человек ожидает порядка бумажного словаря: файловые менеджеры, списки имён, глоссарии.
  Shortlex --- инструмент алгоритмический: генерация всех строк, перебор конфигураций, построение контрпримеров минимальной длины.
  Выбор порядка определяется задачей: работаете с человеком --- словарный.
  Пишете алгоритм --- shortlex.
]

=== Колексикографический порядок

Лексикографический порядок сравнивает компоненты слева направо --- первый компонент самый важный.
Колексикографический порядок (colex) переносит приоритет на последний компонент:
это лексикографический порядок на перевёрнутых кортежах, когда мы разворачиваем каждый кортеж справа налево и сравниваем лексикографически.

#definition[Колексикографический порядок][
  Для двух $k$-кортежей из $A^k$ положим
  $
    (a_1, dots, a_k) precColex (b_1, dots, b_k)
    iff
    (a_k, dots, a_1) precLex (b_k, dots, b_1).
  $
  Сравниваем последние компоненты.
  Если они равны --- предпоследние.
  И так далее справа налево.
  Порядок определяет первый же компонент (считая справа) в котором кортежи различаются.
]

Чтобы понять разницу между lex и colex, сравним две конкретные тройки пошагово.

#example[Lex vs Colex: пошаговое сравнение][
  Возьмём кортежи $(0, 0, 1), (0, 1, 0) in {0, 1}^3$ с порядком $0 < 1$.

  *Lex* (слева направо):
  - Позиция 1: $0 = 0$ --- равны, идём дальше.
  - Позиция 2: $0 < 1$ --- найдено различие.
    Результат: $(0, 0, 1) precLex (0, 1, 0)$.

  *Colex* (справа налево):
  - Позиция 3 (последняя): $1 > 0$ --- найдено различие, и $0 < 1$, поэтому меньшим считается кортеж с $0$ на этой позиции.
    Результат: $(0, 1, 0) precColex (0, 0, 1)$.

  Порядки противоположны: в lex раньше $(0, 0, 1)$, в colex --- $(0, 1, 0)$.
  Различие на второй позиции (lex) против различия на третьей (colex) --- и итоговый порядок развернулся.
]

Теперь посмотрим на полный порядок всех троек из ${0, 1}^3$ --- сначала в lex, затем в colex.

#example[Все тройки из ${0, 1}^3$ в lex и colex][
  Порядок lex (первые компоненты --- старшие):
  $
    (0,0,0) precLex (0,0,1) precLex (0,1,0) precLex (0,1,1)
    precLex (1,0,0) precLex (1,0,1) precLex (1,1,0) precLex (1,1,1).
  $

  Порядок colex (последние компоненты --- старшие):
  $
    (0,0,0) precColex (1,0,0) precColex (0,1,0) precColex (1,1,0)
    precColex (0,0,1) precColex (1,0,1) precColex (0,1,1) precColex (1,1,1).
  $

  Colex группирует кортежи по последней компоненте: первая половина --- все с $0$ на конце, вторая --- все с $1$.
  Внутри группы с последней $0$ кортежи упорядочены по средней компоненте (второй справа), а при равенстве средней --- по первой (третьей справа).
  Структура ровно та же, что у lex, но развёрнутая: старший разряд --- правый вместо левого.
]

Колексикографический порядок --- рабочий инструмент комбинаторики.
Подмножества множества ${1, dots, n}$ часто кодируют характеристическими векторами длины $n$.
Бит $i$ равен 1, если элемент $i$ входит в подмножество.
Colex-порядок на таких векторах группирует подмножества по _наибольшему_ элементу --- потому что последняя (самая правая) единица и есть максимум.
Это удобно для индукции по наибольшему элементу и для алгоритмов, перебирающих семейства множеств.

== Приложения отношений порядка

Отношения порядка встречаются в computer science во многих неочевидных контекстах.

=== Иерархии типов в ООП

Отношение "является подтипом" ($<:$) --- это частичный порядок на типах.
- Рефлексивно: $T <: T$.
- Транзитивно: если `A <: B` и `B <: C`, то `A <: C`.
- Антисимметрично: если `A <: B` и `B <: A`, то $A$ и $B$ --- один тип.
Если `Dog <: Animal` и `Cat <: Animal`, то `Dog` и `Cat` несравнимы --- ни один не является подтипом другого.

Множественное наследование вводит объединения типов: `Dog or Cat` --- тип, надтипом которого являются `Dog` и `Cat`.
Наименьший общий надтип `Dog or Cat` (если он существует) --- супремум в решётке типов.
Аналогично, пересечение типов `Dog & Cat` (если существует) --- инфимум.

Большинство объектно-ориентированных языков не гарантируют существования наименьшего общего надтипа для произвольной пары типов.
Решёточная модель --- идеализация, описывающая структурные свойства системы типов, а не детали реализации конкретного компилятора.#footnote[
  L. Cardelli, "A Semantics of Multiple Inheritance", Information and Computation, 1988.
  B. Pierce, "Types and Programming Languages", 2002, главы 15--16.
]

=== Векторные часы и причинность

В распределённой системе невозможно определить глобальное "сейчас" без дорогостоящей синхронизации часов.
Лесли Лэмпорт#footnote[L. Lamport. "Time, Clocks, and the Ordering of Events in a Distributed System". Communications of the ACM, 1978.]
предложил обойтись без глобального времени: достаточно знать не _когда_ произошло событие, а _произошло ли_ оно до другого.

Отношение "произошло-до" ($->$) определяется тремя правилами:
+ Если $a$ и $b$ --- события в одном процессе и $a$ произошло раньше $b$, то $a -> b$.
+ Если $a$ --- отправка сообщения, а $b$ --- его получение, то $a -> b$.
+ Транзитивность: $a -> b, b -> c -> a -> c$.

Это отношение --- строгий частичный порядок.
События с $a slash(->) b, b slash(->) a$ называются _конкурентными_: они происходят в разных процессах и не связаны причинно.

Векторные часы реализуют этот частичный порядок.
+ Каждый из $N$ процессов хранит массив $"VC"[1..N]$ целых чисел (изначально нули).
+ При локальном событии процесс $i$ инкрементирует свой счётчик: $"VC"[i] := "VC"[i] + 1$.
+ При отправке сообщения процесс вкладывает в него свой текущий вектор $"VC"$.
+ При получении сообщения процесс $i$ обновляет вектор поэлементным максимумом: $"VC"[j] := max("VC"[j], "VC"_"msg"[j])$ для всех $j$.
  Затем инкрементирует $"VC"[i]$ (получение --- локальное событие).

Сравнение векторных часов: $a -> b iff forall j: "VC"(a)[j] <= "VC"(b)[j]$, и хотя бы одно неравенство строгое.
Если векторы несравнимы (ни один не превосходит другой покомпонентно) --- события конкурентны.
#footnote[L. Lamport, "Time, Clocks, and the Ordering of Events in a Distributed System", Communications of the ACM, 1978.]

=== Версионирование и совместимость

Семантическое версионирование (SemVer) сочетает два разных порядка на одном множестве версий.
Лексикографический порядок $(1.2.0 < 1.2.1 < 1.3.0 < 2.0.0)$ --- линейный: любые две версии сравнимы по старшинству.
Отношение обратной совместимости --- предпорядок: после отождествления взаимно совместимых версий он становится частичным порядком.
Версии $1.2.0, 2.0.0$ несравнимы: мажорная версия изменилась, обратная совместимость не гарантирована.
А $1.2.0, 1.2.1$ сравнимы: вторая обратно совместима с первой.

Выбор совместимого набора зависимостей --- это поиск максимальной конфигурации в ЧУ-множестве версий с отношением совместимости:
для каждого пакета нужно выбрать наибольшую версию, совместимую с уже выбранными версиями других пакетов.
Именно эту задачу решают пакетные менеджеры (npm, Cargo, pip) при разрешении зависимостей:
строится граф зависимостей с ограничениями на версии, и SAT/ASP-решатель ищет удовлетворяющую конфигурацию.#footnote[
  T. Preston-Werner, "Semantic Versioning 2.0.0", https://semver.org, 2013.
]

=== Наследование и линеаризация

В языках с множественным наследованием (Python, C++, Scala) иерархия классов образует ЧУМ.
Класс $A$ предшествует классу $B$, если $A$ наследует от $B$ (прямо или транзитивно).
Наследник стоит раньше своего предка.
Несравнимые классы --- те, что находятся в разных ветках иерархии и не связаны наследованием.

Порядок разрешения методов (MRO --- Method Resolution Order) --- это линеаризация данного ЧУМ:
линейный порядок на классах, совместимый с отношением наследования (наследники предшествуют родителям: класс ищется раньше своих предков).
Когда метод не найден в текущем классе, поиск идёт по линейному порядку MRO.

Алгоритм C3 (используемый в Python)#footnote[
  Описание C3 в документации Python: https://www.python.org/download/releases/2.3/mro/.
] вычисляет топологическую сортировку, удовлетворяющую двум дополнительным ограничениям:
+ порядок родителей в объявлении класса (слева направо) должен быть сохранён в MRO.
+ наследники должны появляться в MRO раньше родителей.
Если эти ограничения невыполнимы (например, из-за конфликта при "ромбовидном" наследовании с несогласованным порядком), C3 отвергает определение класса --- в Python это вызывает `TypeError`.#footnote[
  K. Barrett, B. Cassels, P. Haahr, D. A. Moon, K. Playford, P. T. Withington, "A Monotonic Superclass Linearization for Dylan", OOPSLA 1996.
]

== Неподвижные точки в решётках <sec:fixpoints>

Вернёмся к решёткам и посмотрим на них с новой стороны.
Функция $f: L -> L$ называется _монотонной_.
Монотонность: $x prec.eq y -> f(x) prec.eq f(y)$.
_Неподвижная точка_ (fixed point) функции $f$ --- элемент $x$ с $f(x) = x$.

Монотонные функции на решётках отличаются от функций на числах одним свойством: при слабых дополнительных условиях неподвижная точка гарантированно существует.
Эти условия даёт понятие полной решётки.

#definition[Полная решётка][
  Решётка называется *полной*, если _любое_ подмножество (не только конечное) имеет супремум и инфимум.
  Булева решётка $cal(P)(A)$ полна: супремум произвольного семейства подмножеств --- их объединение, инфимум --- пересечение.
  Решётка $NN$ с обычным порядком не полна: у неё нет супремума (нет наибольшего натурального числа).
]

#theorem[Кнастера--Тарского о неподвижной точке#footnote[
    Knaster B. "Un théorème sur les fonctions d'ensembles", Annales de la Société Polonaise de Mathématique, 1928.
  ]#footnote[
    Tarski A. "A lattice-theoretical fixpoint theorem and its applications", Pacific Journal of Mathematics, 1955.]][
  Пусть $(L, prec.eq)$ --- полная решётка (решётка, в которой супремум и инфимум существуют для любого, не обязательно конечного, подмножества).
  Всякая монотонная функция $f: L -> L$ имеет наименьшую и наибольшую неподвижные точки.
  Их обозначают $"lfp"(f), "gfp"(f)$.
]

#proof-sketch[
  Докажем построением: предъявим наименьшую неподвижную точку как инфимум множества пост-неподвижных точек, а наибольшую получим двойственностью.

  Рассмотрим множество $P = {x in L mid(|) f(x) prec.eq x}$ ("пост-неподвижные точки" --- элементы, которые функция не увеличивает).
  Покажем сначала, что его инфимум $a = inf P$ --- наименьшая неподвижная точка.

  $forall x in P: a prec.eq x$, ведь инфимум меньше всех элементов.
  По монотонности $f(a) prec.eq f(x) prec.eq x$: последнее верно, потому что $x$ принадлежит $P$.
  Значит, $f(a) prec.eq a$, то есть $f(a)$ --- нижняя грань $P$, ведь инфимум --- наибольшая нижняя грань.

  С другой стороны, $f(a) prec.eq a -> f(f(a)) prec.eq f(a)$ по монотонности.
  Значит, $f(a) in P$.
  Тогда $a prec.eq f(a)$, ведь инфимум меньше всех элементов $P$.

  Из $f(a) prec.eq a, a prec.eq f(a)$ следует $f(a) = a$ по антисимметричности: $a$ --- неподвижная точка.
  Если $b$ --- неподвижная точка, то $f(b) = b$, значит $f(b) prec.eq b$ и $b in P$.
  Тогда $a prec.eq b$: $a$ действительно наименьшая.

  Для наибольшей неподвижной точки конструкция двойственна: инфимум заменяем на супремум, а множество $P$ --- на ${x mid(|) x prec.eq f(x)}$.
]

#example[Итерация к неподвижной точке][
  На решётке $cal(P)({1, 2, 3})$ рассмотрим монотонную функцию $f(X) = X union {1}$.
  Итерация от низа даёт цепочку:
  $
    emptyset -> f(emptyset) = {1} -> f({1}) = {1} -> dots
  $
  Уже на втором шаге множество перестало меняться: $f({1}) = {1}$, и ${1}$ --- неподвижная точка.
  Она наименьшая: любая неподвижная точка обязана содержать $1$, иначе $f$ добавила бы его, а ${1}$ --- наименьшее из таких множеств.

  Та же картина в анализе программ: решётка --- множества фактов, функция --- эффект цикла, и итерация от низа сходится к наименьшей неподвижной точке за конечное число шагов.
]

Анализ программы с циклами и рекурсией --- это в точности поиск неподвижной точки.
Состояние программы на каждом шаге --- элемент абстрактной решётки.
Тело цикла --- монотонная функция на этой решётке.
Результат анализа после цикла --- наименьшая неподвижная точка этой функции, к которой процесс итеративного анализа сходится за конечное число шагов в конечной решётке.

Без теоремы Кнастера--Тарского абстрактная интерпретация висела бы в воздухе: мы применяли бы итеративный алгоритм, не зная, почему он сходится и к чему.
С ней мы получаем гарантию: монотонная функция на полной решётке всегда имеет наименьшую неподвижную точку.
В конечной решётке итеративный процесс $bot prec.eq f(bot) prec.eq f(f(bot)) prec.eq dots$ достигает её за конечное число шагов.
В бесконечной полной решётке итерация может не остановиться за конечное число шагов.
Наименьшая неподвижная точка строится трансфинитной итерацией от $bot$:
$
  f^0 (bot) = bot, \
  f^(alpha+1) (bot) = f(f^alpha (bot)), \
  f^lambda (bot) = "sup"_(alpha < lambda) f^alpha (bot)
$
где $lambda$ --- предельный ординал.

Ординалы здесь не случайны: для достижения неподвижной точки может потребоваться больше чем $omega$ итераций.
Этот результат связывает теорию порядков с трансфинитной теорией.
Полное введение в ординалы, предельные ординалы и трансфинитную индукцию даётся в главе @chap:beyond-n.

#note[Неподвижные точки и языки программирования][
  Неподвижные точки возникают в computer science в нескольких обличьях:
  - _Абстрактная интерпретация_: состояние программы приближается элементом решётки.
    Тело цикла --- функция на решётке.
    Анализ --- вычисление $"lfp"$.
  - _Денотационная семантика_: значение рекурсивной функции --- наименьшая неподвижная точка функционала, построенного по её определению (Скотт, 1970-е).
  - _Теория типов_: рекурсивные типы (например, `data List a = Nil | Cons a (List a)`) определяются как неподвижные точки функторов на категории типов.
  - _Model checking_: вычисление достижимых состояний --- наименьшая неподвижная точка оператора перехода.
    Вычисление инвариантов --- наибольшая неподвижная точка.
]

== Первый взгляд на абстрактную интерпретацию

О программе можно рассуждать, не запуская её.
Рассмотрим программу с делением: $x := "read"(), y := 100 / x$.
Хочется гарантировать, что деление никогда не выполнится с $x = 0$.
Запуск этого не доказывает: программа читает вход, и перебрать все возможные значения нельзя.
Рассуждение о программе без запуска называется статическим анализом.

Идея в том, чтобы заменить множество возможных значений элементом решётки и пересчитывать его по операторам программы.
Самый простой домен --- знаки: "отрицательное", "ноль", "положительное", упорядоченные по включению представляемых множеств.
Слияние ветвей условного оператора --- это супремум.
Циклы --- неподвижные точки.
Если перед делением знак $x$ --- отрицательный или положительный, деление безопасно.
Если может быть ноль --- возможно падение.

Знаки --- лишь первый шаг.
Для других вопросов нужны другие домены: интервалы против выхода за границы массива, классы вычетов, произведения доменов.
Как устроены домены, что означает "корректный" анализ и как заставить циклы сходиться --- в главе об абстрактной интерпретации (@chap:abstract-interp).

== Итоги главы

Частичный порядок описывает предшествование, но его главный ресурс --- несравнимость: элементы, не связанные порядком, независимы, и именно эта независимость допускает параллелизм.
Три аксиоматики --- частичный, строгий и линейный порядок --- дают разную силу: линейный порядок запрещает несравнимость вовсе, и потому частичный порядок ценнее там, где есть параллельные ветви.

Диаграмма Хассе сжимает порядок до скелета из отношений покрытия, опуская транзитивные рёбра, и делает структуру видимой.
На ней мы научились находить экстремальные элементы и грани: минимальный и максимальный отличаются от наименьшего и наибольшего, а супремум и инфимум --- точные границы, которых может и не быть.

Там, где у каждой пары элементов есть супремум и инфимум, порядок вырастает в решётку --- алгебраическую структуру, пригодную для вычислений --- и булевы алгебры оказываются её частным случаем.
Топологическая сортировка разворачивает скелет обратно в линейную последовательность, а теорема Кнастера--Тарского гарантирует неподвижные точки монотонных функций.

Порядки связывают отношения (глава @chap:relations) с графами (глава @chap:graphs).
Решётки с монотонными функциями подводят к неподвижным точкам --- к абстрактной интерпретации (глава @chap:abstract-interp), где состояние программы описывается элементом решётки, а анализ сводится к поиску неподвижной точки.
Тот же аппарат работает в планировщиках, компиляторах и распределённых системах.

#check[Проверьте себя][
  + Почему частичный порядок допускает параллелизм, а линейный --- нет?
  + Чем супремум отличается от наибольшего элемента и может ли супремум не существовать?
  + Чем минимальный элемент отличается от наименьшего?
  + Что гарантирует теорема Кнастера--Тарского?
  + Почему диаграмма Хассе не рисует все рёбра отношения?
  + Чем словарный порядок отличается от shortlex и у какого из них всякое непустое множество строк имеет наименьший элемент?
]

#whats-next
От упорядоченных структур мы переходим к универсальному языку связей --- графам.
Диаграмма Хассе, которую мы только что рисовали, --- это уже граф: вершины и рёбра, только специального вида.
В главе @chap:graphs мы изучим общую теорию:
деревья, двудольные и планарные графы, эйлеровы и гамильтоновы циклы, алгоритмы BFS, DFS и Дейкстры, раскраску ---
инструментарий для моделирования сетей, зависимостей и маршрутов.

== Упражнения

#tasklist("m08")[
  _Частичные порядки и аксиомы._
  + Проверьте, что отношение делимости является частичным порядком на натуральных числах: выполните рефлексивность, антисимметричность и транзитивность.

  + Является ли отношение делимости частичным порядком на целых числах?
    Указание: проверьте антисимметричность на паре $2, -2$.

  + Приведите пример бинарного отношения, которое рефлексивно и транзитивно, но не антисимметрично.

  + \* Докажите, что отношение включения $subset.eq$ на множестве всех подмножеств является частичным порядком.
    Является ли оно линейным?

  _Диаграммы Хассе и экстремальные элементы._
  + Нарисуйте диаграмму Хассе для ЧУМ $({1, 2, 3, 4, 6, 12}, mid(|))$.
    Найдите минимальные, максимальные, наибольший и наименьший элементы.

  + Приведите пример частичного порядка, в котором есть ровно два минимальных элемента и нет наименьшего.

  + Докажите, что в частичном порядке наименьший элемент, если он существует, единственен.

  + \* Исследуйте: сколько существует частичных порядков на множестве из $n$ элементов?
    Для $n = 1, 2, 3, 4$ вычислите точные значения.

  _Грани, супремум, инфимум и решётки._
  + Найдите все верхние и нижние грани, супремум и инфимум для пары $4$ и $6$ в ЧУМ $({1, 2, 3, 4, 6, 12}, mid(|))$.

  + Является ли множество всех подмножеств ${1, 2, 3}$ с отношением включения дистрибутивной решёткой?
    Проверьте аксиомы.

  + Докажите, что $(NN^+, mid(|))$ является решёткой.
    Что является супремумом и инфимумом двух чисел?

  + \* Докажите законы поглощения в любой решётке: $a join (a meet b) = a, a meet (a join b) = a$.

  + \*\* Докажите теорему Кнастера--Тарского для конечных решёток: любое монотонное отображение $f: L -> L$ имеет неподвижную точку.

  _Лексикографический порядок._
  + Сравните строки $"apple"$ и $"apricot"$ в словарном порядке и в shortlex.

  + \* Приведите пример бесконечного множества строк без наименьшего элемента в словарном порядке.

  _Топологическая сортировка._
  + Предложите топологическую сортировку для следующего частичного порядка (зависимости курсов): \
    Алгебра $->$ Дискретная математика. \
    Алгебра $->$ Линейная алгебра. \
    Программирование $->$ Алгоритмы. \
    Дискретная математика $->$ Алгоритмы. \
    Дискретная математика $->$ Логика.

  + Докажите, что в конечном ЧУ-множестве топологическая сортировка существует всегда (подсказка: индукция или жадный алгоритм).

  + \* Придумайте частичный порядок, для которого существует ровно одна топологическая сортировка, и докажите это.

  + \* Найдите ошибку в "доказательстве" того, что в любом непустом частичном порядке есть максимальный элемент.
    Построим цепь: на каждом шаге, если есть элемент строго больше текущего, добавим его.
    Если процесс не завершается, цепь бесконечна, и её супремум $a^*$ является максимальным элементом.
    Почему супремум бесконечной цепи может не существовать в $P$, и почему даже существующий супремум не обязан быть максимальным?
]


#project[Статический анализ знаков для поиска деления на ноль][
  Постройте статический анализатор, который отвечает на вопрос, может ли переменная $x$ быть нулём перед делением, не выполняя программу.
  Соберите его из решётки знаков, монотонных функций и теоремы Кнастера--Тарского о наименьшей неподвижной точке.

  #project-stage[Решётка знаков][
    Реализуйте множество знаков ("отрицательное", "ноль", "положительное"), упорядоченное по включению представляемых множеств, с операциями $join$ и $meet$.
    Продумайте, почему получается решётка и что является её низом и верхом.
  ]
  #project-stage[Абстрактная семантика][
    Задайте знак суммы, произведения и деления.
    Продумайте, какой знак даёт деление на "может быть ноль".
    Обратите внимание: отображение такого деления в низ решётки делает операцию немонотонной --- итерация к наименьшей неподвижной точке перестаёт сходиться.
    Продумайте альтернативу: верх решётки с отдельным предупреждением об ошибке.
  ]
  #project-stage[Программа как функция][
    Представьте программу (присваивания, if, while) монотонной функцией на решётке: слияние ветвей условного оператора --- супремум, тело цикла --- итерация функции.
    Объясните, почему построенная функция монотонна.
    Пометьте каждую точку программы флагом достижимости: состояние с низом решётки означает недостижимую ветку, и её тело при анализе пропускается.
  ]
  #project-stage[Неподвижная точка][
    Вычислите наименьшую неподвижную точку итерацией от низа: $bot <= f(bot) <= f(f(bot)) <= dots.c$.
    Покажите, что на конечной решётке итерация останавливается, и выведите знак $x$ в каждой точке программы.
  ]
  #project-stage[Проверка программ][
    Прогоните набор программ и ответьте, может ли $x$ быть нулём перед делением.
    Продумайте, какие ложные срабатывания даёт абстракция знаков и как их устранить, уточнив решётку.
  ]

  #project-outcome[
    статический анализатор, который находит или исключает деление на ноль в небольших программах, используя решётку знаков и теорему о неподвижной точке.
  ]
]



