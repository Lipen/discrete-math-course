// M14 --- SAT: задача булевой выполнимости и её роль в теории вычислений.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m14.typ": (
  cdcl-conflict-graph, cook-levin-table, dpll-tree,
  implication-graph-2sat-simple,
)
#import "diagrams/m01.typ": resolution-dag

= SAT <chap:sat>

Две схемы должны делать одно и то же.
Инженер, заменяющий проверенную схему оптимизированной, обязан ответить на этот вопрос уверенно --- ни один вход не должен развести их выходы.
Ответ не найти перебором, ведь у схемы с сотней входов $2^(100)$ комбинаций, и просмотреть их все не успеет ни одна машина.
Нужен способ рассуждать о схеме целиком, не подставляя входы по одному.

В прошлой главе (@chap:codes) данные защищались от ошибок в пути: шум портит биты, код замечает повреждение и чинит его.
Но код сторожит передачу, а производство остаётся вне его ведения: он предполагает, что вычисление уже сделано верно.
Здесь вопрос о самом вычислении --- верно ли оно, прежде чем данные отправлены.
Защита от ошибок и проверка правильности --- разные службы, и вторая не проще первой.
Тот же вопрос о существовании стоит за множеством инженерных задач.

Можно ли составить расписание экзаменов так, чтобы ни один студент не сдавал два экзамена в один день?
Есть ли решение у судоку?
Найдётся ли вход, на котором программа ошибается?
Всё это --- вопросы вида: существует ли объект с нужными свойствами?
Общее у них одно --- во всех случаях объект есть набор битов, а свойство --- логическое условие.

Булева алгебра (глава @chap:boolean) даёт язык, на котором такие вопросы записываются точно.
Каждое ограничение становится дизъюнктом, ограничения соединяются конъюнкцией, и вся задача --- одна формула в _конъюнктивной нормальной форме_.
Вопрос "есть ли решение" превращается в вопрос: существует ли набор значений переменных, при котором формула истинна?
Это и есть задача булевой выполнимости, SAT.
Вопрос об эквивалентности схем (глава @chap:circuits) --- тоже SAT: схемы расходятся ровно тогда, когда формула, описывающая их различие, выполнима.

Формулировка обманчиво проста.
За ней стоит граница, которая делит задачи на два мира.
Формулы с дизъюнктами из двух литералов решаются быстро: 2-SAT сводится к _графу импликаций_ и разбирается за линейное время.
Стоит добавить третий литерал --- и задача переходит в число самых трудных: 3-SAT NP-полна.
Резкая смена сложности при добавлении одного литерала объясняется структурой дизъюнкта.
Но формулу надо ещё записать: от выбора переменных зависит, решит ли решатель задачу за секунды или не решит вовсе.

В 1971 году Стивен Кук доказал, что SAT NP-полна: всякая задача из класса $"NP"$ сводится к выполнимости.
Два года спустя Леонид Левин, не зная о работе Кука, пришёл к тому же результату.
В 1972 году Ричард Карп свёл SAT к двум десяткам известных комбинаторных задач --- раскраске графа, вершинному покрытию, задаче коммивояжёра --- и показал, что все они столь же трудны.
С этих работ у теории сложности появилась точка отсчёта: SAT стала эталоном, к которому сводятся остальные трудные задачи.
Список таких задач давно перерос комбинаторику. В нём:
- судоку
- сапёр
- тетрис
- FreeCell
- составление кроссвордов
- сворачивание белков
Все они NP-полны, то есть это одна и та же трудность: быстрый способ решать судоку был бы быстрым способом сворачивать белки.

Из NP-полноты сразу следует, что полиномиальный алгоритм для SAT означал бы $"P" = "NP"$.
Быстрым стало бы решение любой задачи из класса $"NP"$.
Вопрос остаётся открытым.
Систематически классы сложности мы разберём в главе @chap:complexity.
Но NP-полнота --- не приговор для практики.
Современные решатели на алгоритме CDCL справляются с формулами из миллионов переменных --- когда у формул есть структура.
Верификация микросхем, ограниченная проверка моделей, разрешение зависимостей пакетов --- индустрия применяет SAT каждый день.

Перед нами противоречие --- NP-полная задача, которая регулярно решается на индустриальных входах.
NP-полнота описывает худший случай --- намеренно построенные формулы, которые в инженерии не встречаются.
Инженерная формула наследует структуру породившей её задачи, и решатель эту структуру находит.
Что отличает формулы, которые решаются за секунды, от тех, на которых решатель застревает навсегда?

#chapter-overview[
  В этой главе мы уточним, что такое КНФ-формула, дизъюнкт и выполнимость.
  Затем увидим, как длина дизъюнкта решает судьбу задачи: 2-SAT сводится к графу импликаций и решается за линейное время, а 3-SAT NP-полна.
  Теорема Кука--Левина сделает это утверждение точным, а сведения --- раскраска графа, расписание экзаменов, судоку --- покажут, как прикладная задача становится КНФ-формулой.
  Разбор решателя CDCL объяснит, откуда берётся его эффективность на индустриальных формулах с миллионами переменных.
  SMT --- обобщение SAT на арифметику и другие теории --- вынесено в отдельную главу @chap:smt.
  Здесь мы вернёмся к вопросу $"P" = "NP"$, который SAT сделал точным.

  После этой главы вы научитесь:
  + приводить задачу к КНФ и проверять выполнимость формулы;
  + решать 2-SAT через граф импликаций за линейное время;
  + объяснять NP-полноту 3-SAT и строить простые сведения к ней;
  + объяснять работу CDCL: распространение, обучение на конфликтах, возврат;
  + понимать, почему индустриальные формулы с миллионами переменных решаются, несмотря на NP-полноту.
]

== Задача SAT <sec:sat-problem>

#history-note[NP-полная задача, которую научились решать][
  Мартин Дэвис и Хилари Патнэм#footnote[M. Davis, H. Putnam. "A Computing Procedure for Quantification Theory". Journal of the ACM, 1960.] предложили первую систематическую процедуру для проверки выполнимости булевых формул --- алгоритм Davis-Putnam, основанный на резолюции и элиминации переменных.
  Алгоритм страдал от экспоненциального роста памяти: каждое применение правила резолюции порождало новые дизъюнкты.

  Дэвис, Логеман и Лавленд#footnote[M. Davis, G. Logemann, D. Loveland. "A Machine Program for Theorem-Proving". Communications of the ACM, 1962.] модифицировали его в DPLL: замена резолюции на поиск с возвратом (backtracking) и распространение единичного дизъюнкта (unit propagation).
  DPLL стал стандартным подходом на три десятилетия, но на трудных формулах всё равно требовал экспоненциального времени.

  Стивен Кук#footnote[S. A. Cook. "The Complexity of Theorem-Proving Procedures". 1971.] и Леонид Левин#footnote[L. A. Levin. "Universal Search Problems". 1973.] независимо доказали NP-полноту SAT.
  Теорема Кука--Левина утверждает: если SAT решается за полиномиальное время, то $"P" = "NP"$.
  SAT стал эталонной трудной задачей --- и одновременно главной целью алгоритмических атак.

  В 1996 году Жуан Маркес Силва и Карем Сакаллах предложили Conflict-Driven Clause Learning (CDCL) --- механизм обучения на конфликтах с нехронологическим возвратом.
  Отличие CDCL от DPLL --- в смене парадигмы: главную роль играет обучение на конфликтах.
  DPLL забывает конфликты сразу после возврата.
  CDCL --- запоминает.

  Анализируя граф импликаций, решатель извлекает _причину_ конфликта в виде нового дизъюнкта --- и добавляет его в формулу.
  Этот дизъюнкт логически следует из исходной формулы (он не сужает пространство решений), но запрещает ту конкретную комбинацию присваиваний, которая привела к конфликту.

  CDCL-решатели (GRASP, Chaff, MiniSat, Glucose, CaDiCaL) научились решать формулы с миллионами переменных.
  NP-полная задача оказалась практически решаемой для структурированных индустриальных формул.
  CDCL-решатели стали стандартным инструментом формальной верификации и комбинаторной оптимизации.
]

=== Определения SAT

Задача выполнимости булевой формулы --- одна из старейших алгоритмических проблем.
От верификации аппаратуры до решения головоломок --- всё сводится к SAT.

Формулы обычно приводятся к конъюнктивной нормальной форме (КНФ), с которой и работают алгоритмы.

#definition[КНФ-формула][
  *КНФ-формула* --- формула вида $phi = C_1 and ... and C_m$, где каждый дизъюнкт $C_i = (l_(i,1) or ... or l_(i,k_i))$.
]

#definition[Литерал][
  *Литерал* --- переменная $x$ или её отрицание: $overline(x)$.
]

#example[КНФ-формула][
  $phi = (x or overline(y)) and (overline(x) or y or z) and (overline(z))$.
  Здесь три дизъюнкта:
  - $C_1 = (x or overline(y))$ (два литерала)
  - $C_2 = (overline(x) or y or z)$ (три литерала)
  - $C_3 = (overline(z))$ (один литерал)
  Единичный дизъюнкт (_unit clause_) $(overline(z))$ немедленно принуждает $z = 0$ в любом выполняющем наборе.
]

Теперь, когда определение КНФ готово, сформулируем саму задачу SAT.

#definition[SAT][
  *SAT* (задача булевой выполнимости) --- задача определения, существует ли модель для булевой формулы $phi$.
]

#definition[Модель][
  *Модель* формулы $phi$ --- выполняющий набор значений переменных, при котором формула истинна.
]

#definition[Выполнимая формула][
  Формула *выполнима*, если модель существует.
  В противном случае формула *невыполнима*.
]

Резолюция --- система вывода для КНФ, исторически связанная с SAT: алгоритм Дэвиса--Патнэма основан на ней.
Обзор систем вывода --- в главе @chap:deduction.
Здесь резолюция служит рабочим инструментом.

== Резолюция <sec:sat-resolution>


Таблицы истинности для формулы от $n$ переменных требуют $2^n$ строк.
С ростом $n$ этот метод перестаёт работать.
Нужен метод, проверяющий логические следствия без перебора всех интерпретаций.

_Принцип резолюции_ (_resolution_) --- это система вывода, где для проверки противоречивости набора дизъюнктов достаточно одного правила вывода --- резолюции.
Он работает на КНФ и лежит в основе автоматических доказателей теорем (SAT-решателей) и логического программирования (Prolog).

Здесь и далее черта над переменной означает отрицание: $overline(x) = not x$.
Надчёркивание удобнее в дизъюнктах: отрицание пишется над переменной, и отдельный знак перед ней не нужен.

#definition[Правило резолюции][
  Из двух дизъюнктов, содержащих контрарную пару литералов (одну и ту же переменную с противоположными знаками), выводится новый дизъюнкт --- дизъюнкция всех литералов обоих дизъюнктов, кроме контрарной пары.

  Формально:
  $
    C_1 or x, space C_2 or overline(x) proves C_1 or C_2.
  $
]

#definition[Резольвента][
  *Резольвента* --- дизъюнкт, выводимый из двух исходных правилом резолюции.
]

Интуиция: если переменная истинна, то дизъюнкт $C_2 or overline(x)$ истинен только тогда, когда истинен $C_2$.
Если переменная ложна, то дизъюнкт $C_1 or x$ истинен только тогда, когда истинен $C_1$.
В любом случае истинна дизъюнкция $C_1 or C_2$.
Резолюция --- это формализация разбора случаев "переменная либо истинна, либо ложна, и в обоих случаях что-то следует".

#example[Применение правила резолюции][
  Из дизъюнктов $(x or y or overline(z))$ и $(overline(x) or overline(y) or w)$ с контрарной парой $x / overline(x)$ получаем резольвенту $(y or overline(z) or overline(y) or w)$.

  Эта резольвента --- тавтология: она содержит контрарную пару $y, overline(y)$.
  Тавтологическую резольвенту отбрасывают: она всегда истинна, не добавляет ограничений и не может привести к пустому дизъюнкту.
]

Процедура доказательства методом резолюции:

#algorithm[Метод резолюции][
  + Привести посылки и отрицание заключения к КНФ.
  + Многократно применять правило резолюции к парам дизъюнктов, добавляя резольвенты в множество.
  + Если на каком-то шаге выводится _пустой дизъюнкт_ (_empty clause_) ($square$), множество противоречиво --- исходные посылки влекут заключение.
  + Если пустой дизъюнкт не выводится, а новых резольвент нет --- множество выполнимо, заключение не следует.
]

Пустой дизъюнкт возникает как резольвента пары $(x)$ и $(overline(x))$ --- двух единичных дизъюнктов с контрарными литералами.
Это противоречие в чистом виде.

#example[Доказательство через резолюцию][
  Посылки: $p -> q equiv overline(p) or q$, $q -> r equiv overline(q) or r$.
  Заключение: $p -> r equiv overline(p) or r$.

  Приводим отрицание заключения к КНФ: $p and overline(r)$ --- два единичных дизъюнкта: $(p), (overline(r))$.

  Исходное множество дизъюнктов: ${(overline(p) or q), (overline(q) or r), (p), (overline(r))}$.

  Резолюция:
  + Из пары $(p), (overline(p) or q)$ получаем резольвенту $(q)$.
  + Из пары $(q), (overline(q) or r)$ получаем резольвенту $(r)$.
  + Из пары $(r), (overline(r))$ получаем пустой дизъюнкт $square$.
]

Проведённое опровержение удобно читать по графу: узлы --- дизъюнкты, рёбра --- шаги резолюции, отсекаемая переменная подписана на ребре.
Резольвенты извлекаются из входных дизъюнктов, пока не получен пустой дизъюнкт $square$ --- как на рис. @fig:resolution-dag.

#figure(
  resolution-dag,
  caption: [Резолюционное опровержение.],
) <fig:resolution-dag>

Метод резолюции корректен и полон: противоречивость набора дизъюнктов доказывается резолюцией тогда и только тогда, когда набор действительно противоречив.
На практике наивная резолюция порождает экспоненциально много дизъюнктов.
Современные SAT-решатели используют эвристики (упорядочение литералов, стратегии предпочтения), и полный перебор резольвент им не нужен.

Логическое программирование (Prolog) основано на SLD-резолюции --- ограниченной форме резолюции.
Ему посвящена отдельная глава (глава @chap:logic-programming).
В SLD-резолюции один из дизъюнктов всегда является правилом Хорна, а второй --- фактом или целью.
Правило Хорна --- это дизъюнкт с ровно одним положительным литералом (хорновский дизъюнкт допускает и ноль положительных литералов --- такие дизъюнкты называются целями):
$
  overline(p_1) or ... or overline(p_k) or q,
$
что соответствует импликации $p_1 and ... and p_k -> q$.
Это ограничение делает резолюцию эффективно реализуемой, а для хорновских формул полноту сохраняет направленная стратегия SLD-резолюции (глава @chap:logic-programming).

#raven[
  Одно правило вывода для всей логики высказываний --- это факт: резолюция полна.
  Резолюция заменяет таблицы истинности: вместо перебора $2^n$ строк --- синтаксическая операция над дизъюнктами.
  Из пары $C_1 or x, C_2 or overline(x)$ вывести $C_1 or C_2$ --- и больше ничего не нужно.
]

#check[Логика высказываний: нормальные формы и резолюция][
  + Почему ДНФ собирается из строк таблицы истинности, где формула истинна, а КНФ --- из строк, где она ложна?
  + Почему резолюция работает с КНФ, и почему ДНФ для неё не подходит, хотя обе формы эквивалентны исходной формуле?
]

#trap[Выполнимость --- не тавтология][
  Выполнимость и общезначимость (_validity_) --- разные вопросы.
  Формула $x$ выполнима: при $x = 1$ она истинна.
  Тавтологией она не является: при $x = 0$ формула ложна.
  Тавтология $x or overline(x)$ истинна при _всех_ присваиваниях.
  Отрицание выполнимой формулы не обязано быть невыполнимым: $overline(x)$ выполнима при $x = 0$.
]

Выбор КНФ как основной формы для SAT --- это _инженерный компромисс_.
Есть альтернативы: DNF (дизъюнктивная нормальная форма) и схемы (circuit-SAT).
DNF-SAT тривиальна: формула выполнима, если хотя бы один терм выполним.
Circuit-SAT, наоборот, слишком неструктурирован для систематических алгоритмов.

КНФ занимает промежуточное положение:
- достаточно регулярна для эффективного распространения единичного дизъюнкта (движок CDCL),
- достаточно выразительна для кодирования любой NP-задачи: преобразование Цейтина переводит любую схему в КНФ с линейным ростом (подробнее --- ниже).

#example[Выполнимая формула][
  Формула $phi$ из примера "КНФ-формула" выше выполнима: присваивание $x = 1, y = 1, z = 0$ выполняет все три дизъюнкта, первый --- через $x$, второй --- через $y$, третий --- через $overline(z)$.
]

Вот как выглядит простейшая невыполнимая формула.

#example[Невыполнимая формула][
  $psi = (x) and (overline(x))$ невыполнима: переменная $x$ не может быть одновременно истинной и ложной.
]

=== k-SAT: ограничение длины дизъюнктов

Ограничение длины дизъюнктов порождает семейство задач k-SAT.
Сложность задачи резко меняется при переходе от $k = 2$ к $k = 3$.

#definition[k-SAT][
  *$k$-SAT* --- задача SAT, в которой каждый дизъюнкт содержит ровно $k$ литералов.
]

Для $k = 2$ задача разрешима за полиномиальное время (граф импликаций + компоненты сильной связности).
Для $k = 3$ задача NP-полна.

#example[2-SAT и 3-SAT][
  Формула $(x or y) and (overline(x) or z) and (overline(y) or overline(z))$ --- это 2-SAT: каждый дизъюнкт содержит ровно два литерала.

  Формула $(x or y or z) and (overline(x) or overline(y) or z)$ --- это 3-SAT.
  Добавление третьего литерала в каждый дизъюнкт меняет класс сложности задачи.
]

=== Алгоритм для 2-SAT

Полиномиальный алгоритм для 2-SAT использует связь с теорией графов.
Каждый дизъюнкт из двух литералов можно рассматривать как импликацию: $(l_1 or l_2) equiv (overline(l_1) -> l_2)$.
Построив граф всех таких импликаций, мы сводим вопрос выполнимости к поиску компонент сильной связности (КСС).

Вот как это работает на конкретной формуле.

#example[Граф импликаций 2-SAT][
  Рассмотрим формулу $phi = (x or y) and (overline(x) or z) and (overline(y) or overline(z))$.
  Переводим дизъюнкты в импликации:
  - $(x or y): overline(x) -> y, overline(y) -> x$,
  - $(overline(x) or z): x -> z, overline(z) -> overline(x)$,
  - $(overline(y) or overline(z)): y -> overline(z), z -> overline(y)$.

  Граф содержит 6 вершин $x, overline(x), y, overline(y), z, overline(z)$ и 6 рёбер.
  Переменная и её отрицание не лежат в одной КСС --- формула выполнима.
  Обратный топологический порядок даёт: $x = 1, y = 0, z = 1$.
]

#trap[Дизъюнкт порождает две импликации][
  Дизъюнкт $(l_1 or l_2)$ даёт _две_ импликации:
  $overline(l_1) -> l_2, overline(l_2) -> l_1$.
  Вторая --- контрапозиция первой: если $l_2$ ложно, то $l_1$ обязано быть истинным.
  Построив в графе только одну из двух, мы потеряем половину рёбер, и компоненты сильной связности изменятся --- а вместе с ними и ответ о выполнимости.
]

Построим ориентированный граф: вершины --- все литералы $x_i$
и их отрицания $overline(x_i)$.
Для каждого дизъюнкта $(l_1 or l_2)$ добавим два ребра:
$overline(l_1) -> l_2, overline(l_2) -> l_1$ (контрапозиции).

#proposition[Критерий выполнимости 2-SAT][
  Формула 2-SAT невыполнима тогда и только тогда, когда
  в графе импликаций некоторая пара $x_i, overline(x_i)$
  принадлежит одной компоненте сильной связности.
]

Если формула выполнима, выполняющий набор строится так:
+ Находим компоненты сильной связности (алгоритмом Косарайю или Тарьяна) и строим конденсацию графа (DAG компонент).
+ Обрабатываем КСС в *обратном* топологическом порядке --- от стоков к истокам.
+ Для каждой ещё не обработанной переменной $x_i$:
  если КСС литерала $x_i$ идёт позже в топологическом порядке, чем КСС $overline(x_i)$, присваиваем $x_i =$ истина.
  Иначе $x_i =$ ложь.
(Альтернативная формулировка: присваиваем истину всем литералам компоненты и ложь их отрицаниям.)

#proof-sketch[
  Докажем корректность построения.
  Если пара $x_i, overline(x_i)$ лежит в одной КСС, то $x_i iff overline(x_i)$ --- противоречие.

  Если же переменная и её отрицание лежат в разных КСС, компоненты упорядочены топологически.
  Присваивание по обратному топологическому порядку гарантирует, что при наличии пути из одного литерала в другой первый получает значение не больше второго (ложь < истина), поэтому каждая импликация $overline(l_1) -> l_2$ истинна.

  Алгоритм работает за $O(n + m)$, где $n$ --- количество переменных, а $m$ --- количество дизъюнктов.
]

Оба дизъюнкта формулы $(x or y) and (overline(x) or y)$ ведут в $y$: $x or y$ даёт импликацию $overline(x) -> y$, а $overline(x) or y$ даёт импликацию $x -> y$.
Контрапозиционные рёбра опущены и на вывод не влияют.
Пара $y, overline(y)$ не лежит в одной КСС, поэтому формула выполнима, и $y$ истинна --- это видно на рис. @fig:implication-graph-2sat.

#figure(
  implication-graph-2sat-simple,
  caption: [Граф импликаций для формулы $(x or y) and (overline(x) or y)$.],
) <fig:implication-graph-2sat>

=== 2-SAT против 3-SAT: структурная причина

Контраст между 2-SAT (разрешима за линейное время алгоритмом на графе импликаций) и 3-SAT (NP-полна) --- структурный скачок сложности, ведь добавление третьего литерала к каждому дизъюнкту переводит задачу из класса P в класс NP-полных.

Структурная причина различия: каждый дизъюнкт 2-SAT $(l_1 or l_2)$ эквивалентен двум импликациям $overline(l_1) -> l_2, overline(l_2) -> l_1$.
Следовательно, вся 2-SAT-формула --- это ориентированный граф на $2n$ вершинах (литералах), и выполнимость сводится к проверке отсутствия цикла, содержащего переменную и её отрицание.
Эта задача решается алгоритмом Косарайю или Тарьяна за $O(n + m)$.

Для 3-SAT дизъюнкт $(l_1 or l_2 or l_3)$ не разлагается в пару импликаций --- в графе потребовались бы гиперрёбра.
Вспомогательные переменные (преобразование Цейтина) здесь не помогают: они лишь переписывают формулу с сохранением выполнимости --- результат всё равно содержит 3-дизъюнкты, то есть не становится экземпляром 2-SAT.
Сводись всякая 3-КНФ к 2-SAT, SAT решался бы за полиномиальное время --- вопреки NP-полноте 3-SAT.

Добавление одного литерала в дизъюнкт разрушает структуру, делающую полиномиальный алгоритм возможным.#footnote[
  B. Aspvall, M. F. Plass, R. E. Tarjan, "A Linear-Time Algorithm for Testing the Truth of Certain Quantified Boolean Formulas", Information Processing Letters, 1979;
  контраст с общим случаем: S. A. Cook, "The Complexity of Theorem-Proving Procedures", STOC 1971 --- доказательство NP-полноты SAT.
]

Разложение на импликации --- мотивировка контраста: оно показывает, почему 2-SAT лёгок, а 3-SAT труден.
Сам факт устанавливается сведением произвольной КНФ к 3-КНФ: длинный дизъюнкт разбивают на тройки с помощью вспомогательных переменных, и формула остаётся равновыполнимой исходной.

#example[Сведение длинного дизъюнкта к 3-КНФ][
  Дизъюнкт из четырёх литералов разбивается одной свежей переменной $y$:
  $
    (l_1 or l_2 or y) and (overline(y) or l_3 or l_4).
  $
  Равновыполнимость: если $l_1$ или $l_2$ истинен, берём $y = 0$.
  Если $l_3$ или $l_4$ --- берём $y = 1$.
  Если же все четыре литерала ложны, $y$ не спасёт: первая скобка требует $y$, вторая --- $overline(y)$.
  Дизъюнкт из пяти литералов разбивается так же, двумя свежими переменными:
  $
    (l_1 or l_2 or y_1) and (overline(y_1) or l_3 or y_2) and (overline(y_2) or l_4 or l_5).
  $
  Каждый шаг снимает по два литерала исходного дизъюнкта, поэтому формула растёт линейно от длины дизъюнкта, а не экспоненциально.
]

#raven[
  Граница между P и NP-полным проходит между $k = 2$ и $k = 3$.
  Структурная причина: дизъюнкт длины 2 разлагается в пару импликаций и сводится к достижимости в графе.
  Дизъюнкт длины 3 --- уже нет.
  Странно, что для большинства других пар задач в теории сложности граница не имеет столь же ясного объяснения...
]

#check[Выполнимость и граф импликаций 2-SAT][
  + Почему дизъюнкт $(l_1 or l_2)$ порождает в графе импликаций две стрелки, и почему одной не хватает?
  + Чем выполнимость отличается от общезначимости, и почему отрицание выполнимой формулы не обязано быть невыполнимым?
]

== Кодирование задач в SAT

Теорема Кука говорит нам, что _любая_ задача из NP сводится к SAT.
Но "сводится" в смысле существования полиномиального сведения, а не в смысле практического удобства.
Существование сведения и его качество --- разные вещи.
Плохое сведение порождает КНФ, которую не решит ни один современный решатель.
Хорошее --- использует структуру задачи и порождает формулу, на которой CDCL работает эффективно.
Эффективное кодирование в SAT требует выбирать переменные, отражающие структуру задачи.
Механическое переписывание её формального определения в дизъюнкты здесь вредит.

SAT-решатели --- мощные инструменты.
Но чтобы ими воспользоваться, нужно уметь _кодировать_ прикладную задачу в виде КНФ-формулы.
Выбор переменных и структуры дизъюнктов радикально влияет на производительность решателя.

Общая стратегия кодирования задачи в SAT:
+ Выбрать булевы переменные, описывающие возможное решение.
+ Записать ограничения задачи в виде дизъюнктов.
+ Запустить SAT-решатель на полученной КНФ.
+ Интерпретировать результат: SAT $=>$ декодируем найденную модель, UNSAT $=>$ решения не существует.

Рассмотрим несколько классических примеров кодирования.

#example[Преобразование Цейтина][
  Любую булеву схему можно перевести в равновыполнимую КНФ-формулу с линейным ростом размера.
  На каждый вентиль заводится одна вспомогательная переменная --- значение его выхода --- и дизъюнкты связывают эту переменную с входами вентиля.
  Например, для вентиля $y = overline(a) and b$ добавляем дизъюнкты
  $
    (overline(y) or overline(a)) and (overline(y) or b) and (y or a or overline(b)),
  $
  которые разрешают ровно те комбинации $(a, b, y)$, где $y = overline(a) and b$.
  Модель исходной схемы продолжается до модели формулы, и наоборот, поэтому выполнимость сохраняется.
]

#example[Раскраска графа в $k$ цветов][
  Дано: граф $G = (V, E)$ и число $k$.
  Вопрос: можно ли раскрасить вершины в $k$ цветов так, чтобы смежные вершины имели разные цвета?

  *Переменные:* $x_(v, c) = 1$, если вершина $v$ имеет цвет $c$ (для $v in V, c in {1, ..., k}$).

  *Ограничения:*
  - Каждая вершина имеет хотя бы один цвет: $or.big_(c=1)^k x_(v, c)$ (длинный дизъюнкт на вершину).
  - Ни одна вершина не имеет двух цветов: $overline(x_(v, c)) or overline(x_(v, d))$ для каждой вершины и каждой пары различных цветов $c < d$ (бинарные дизъюнкты).
  - Смежные вершины различаются:
    $overline(x_(u, c)) or overline(x_(v, c))$ для каждого ребра и каждого цвета.

  Всего получается $O(|V| dot k^2 + |E| dot k)$ дизъюнктов.
]

Раскраска графа --- классическая NP-полная задача.
Но SAT-кодирование применимо и к более "бытовым" головоломкам.

#example[Судоку][
  Стандартное судоку 9$times$9 кодируется в SAT с $9^3 = 729$ переменными: $x_(r, c, d)$ означает, что клетка $(r, c)$ содержит цифру $d$.

  *Каждая клетка содержит хотя бы одну цифру* --- дизъюнкт из девяти литералов:
  $
    x_(r,c,1) or x_(r,c,2) or ... or x_(r,c,9).
  $
  Остальные ограничения запрещают повторения и записываются бинарными дизъюнктами:

  #table(
    columns: 3,
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    [*Ограничение*], [*Формула*], [*Количество*],
    [клетка содержит не более одной цифры],
    [$overline(x_(r,c,d)) or overline(x_(r,c,d'))$ для $d < d'$],
    [$81 dot binom(9, 2) = 2916$],

    [в строке $r$ цифра $d$ не повторяется],
    [$overline(x_(r,c,d)) or overline(x_(r,c',d))$ для $c < c'$],
    [$9 dot 9 dot 36 = 2916$],

    [в столбце $c$ цифра $d$ не повторяется],
    [$overline(x_(r,c,d)) or overline(x_(r',c,d))$ для $r < r'$],
    [$2916$],

    [в блоке $3 times 3$ цифра $d$ не повторяется],
    [попарные запреты для пар клеток внутри блока],
    [$2916$],
  )

  *Заданные цифры*: если в клетке $(r, c)$ стоит цифра $d$, добавляем единичный дизъюнкт $x_(r,c,d)$.

  Суммарно около 12 тысяч дизъюнктов --- небольшой размер по меркам современных SAT-решателей.
  Они решают судоку любой сложности за миллисекунды.
  SAT-решатель не имеет никакой информации о правилах судоку --- ему дана бесструктурная КНФ из 12 тысяч дизъюнктов, и он находит модель, не имея понятия о строках, столбцах и блоках.
]

Следующий пример добавляет количественное ограничение: требование "покрыть все рёбра" усиливается до "покрыть не более чем $k$ вершинами".
Такие ограничения мощности --- отдельный вызов при кодировании в SAT.

#example[Вершинное покрытие][
  Дано: граф $G = (V, E)$ и число $k$.
  Вопрос: существует ли вершинное покрытие размера $<= k$?

  *Переменные:* $x_v = 1$, если вершина $v$ входит в покрытие.

  *Ограничения:*
  - Каждое ребро покрыто: $x_u or x_v$ для каждого ${u, v} in E$.
  - Ограничение размера: сумма $x_v$ не превышает $k$.
    В чистом SAT это кодируется через ограничение мощности (cardinality constraint).
    Для малых значений, например $k = 2$, можно использовать запреты на тройки: $overline(x_u) or overline(x_v) or overline(x_w)$ для каждой тройки вершин --- "не более двух из трёх".
    Для произвольного $k$ применяют sequential counter encoding за $O(n k)$ дизъюнктов или encoding через двоичный сумматор: сумма значений $x_v$ сравнивается с $k$ поразрядным переносом.

  Ограничение мощности --- единственная часть этого кодирования, требующая нелокальных дизъюнктов.
  Рёберные ограничения локальны (по два литерала на ребро).
]

От абстрактных графов перейдём к задаче, с которой сталкивается любой университет: составить расписание экзаменов без конфликтов.

#example[Планирование экзаменов][
  Дано: $n$ экзаменов, $m$ студентов, каждый записан на подмножество экзаменов, и $k$ временных слотов.
  Вопрос: можно ли составить расписание без конфликтов (студент не сдаёт два экзамена в одном слоте)?

  *Переменные:* $x_(e, t) = 1$, если экзамен $e$ назначен на слот $t$ (для $e in {1, ..., n}, t in {1, ..., k}$).

  *Ограничения:*
  - Каждый экзамен назначен хотя бы в один слот: $or.big_(t=1)^k x_(e, t)$ для каждого экзамена.
  - Экзамен не назначен в два слота одновременно: $overline(x_(e, t_1)) or overline(x_(e, t_2))$ для каждой пары слотов $t_1 < t_2$.
  - Для каждого студента и каждой пары экзаменов $(e_1, e_2)$, на которые он записан, и каждого слота $t$: $overline(x_(e_1, t)) or overline(x_(e_2, t))$ (студент не может быть в двух местах одновременно).

  Эта задача --- классический пример того, как NP-полная задача (раскраска графа) возникает в реальной логистике.
]

Геометрические ограничения тоже переводятся в логические.
Задача об $N$ ферзях --- пример такого перевода.

#example[N-ферзей как SAT][
  Задача: расставить $N$ ферзей на шахматной доске $N times N$ так, чтобы никакие два не атаковали друг друга.

  *Переменные:* $x_(i, j) = 1$, если ферзь стоит в клетке $(i, j)$.

  *Ограничения:*
  - Ровно один ферзь в каждой строке: для каждой строки $i$ ровно одна из переменных $x_(i, 1), ..., x_(i, N)$ истинна.
    Кодируется одним "хотя бы один"-дизъюнктом и $binom(N, 2)$ бинарными "не более одного"-дизъюнктами.
  - Не более одного ферзя в каждом столбце: для каждого столбца $j$ и каждой пары строк $i_1 < i_2$: $overline(x_(i_1, j)) or overline(x_(i_2, j))$.
  - Не более одного ферзя на каждой диагонали: для каждой пары клеток $(i_1, j_1), (i_2, j_2)$, лежащих на одной диагонали ($|i_1 - i_2| = |j_1 - j_2|$), запрещаем одновременное присутствие: $overline(x_(i_1, j_1)) or overline(x_(i_2, j_2))$.

  При $N = 8$ число дизъюнктов подсчитывается точно.
  Восемь дизъюнктов "хотя бы один ферзь в строке", по $8 dot binom(8, 2) = 224$ попарных запретов в строках и в столбцах и $2 dot (2 dot (1 + 3 + 6 + 10 + 15 + 21) + 28) = 280$ запретов на диагоналях обоих направлений --- всего $8 + 224 + 224 + 280 = 736$ дизъюнктов.
  Решатель справляется мгновенно.
  Так геометрия шахматной доски превращается в набор дизъюнктов.
]

Искусство кодирования в SAT --- в выборе правильных переменных и записи компактных дизъюнктов.
Плохие кодировки раздувают число дизъюнктов экспоненциально.
Хорошие кодировки наследуют структуру задачи --- и на таких формулах CDCL работает особенно быстро.

=== Принципы эффективного кодирования

У эффективного кодирования четыре принципа.
- *Экономия переменных*: каждая переменная должна нести смысловую нагрузку.
  Избыточные переменные раздувают пространство поиска экспоненциально.
- *Компактность ограничений*: бинарные дизъюнкты обрабатываются быстрее длинных.
  Глобальные ограничения вида "не более одного из $k$" можно заменить группой бинарных дизъюнктов (попарные запреты, при необходимости с вспомогательными переменными) --- это часто ускоряет решатель.
  Сам по себе $k$-арный дизъюнкт в чистые бинарные не превращается --- иначе 3-SAT сводилась бы к 2-SAT (см. выше).
- *Структурная информация*: если задача имеет симметрию или иерархию --- это стоит отразить в переменных.
  Решатели с CDCL извлекают структурные закономерности из повторяющихся паттернов в формуле.
- *Кардинальность*: ограничения вида "не более $k$ из $n$" кодируются специальными схемами: sequential counter за $O(n k)$ дизъюнктов, cardinality network за $O(n log^2 n)$ --- экспоненциально лучше наивного перебора подмножеств.

#example[Цена наивного кодирования мощности][
  Ограничение "не более двух из десяти" наивно запрещает каждую тройку переменных: $binom(10, 3) = 120$ дизъюнктов.
  Sequential counter обходится $O(10 dot 2)$ дизъюнктов --- десятки вместо сотен.
  С ростом числа переменных разрыв становится катастрофическим: "не более двух из ста" наивно требует $binom(100, 3) = 161700$ дизъюнктов.
]

#raven[
  При кодировании задачи в КНФ её внутренняя структура теряется: решатель получает плоский набор дизъюнктов.
  Он не знает, что это была раскраска графа или судоку.
  CDCL затем восстанавливает структурные закономерности через выученные дизъюнкты --- как если бы форма, утраченная при переводе, проступала снова.
]

== Современные SAT-решатели: CDCL <sec:cdcl>

CDCL --- это DPLL плюс умение учиться на конфликтах.
Когда поиск заходит в тупик, решатель отступает на шаг назад, как делал DPLL, но с одним отличием.
Он анализирует, _почему_ произошёл конфликт, извлекает выученный дизъюнкт и добавляет его в формулу.
Этот дизъюнкт предотвращает повторение той же конфигурации присваиваний в будущем, отсекая целые подпространства перебора.

Современные SAT-решатели, способные находить модель или доказывать невыполнимость для формул с миллионами переменных, основаны на алгоритме CDCL (Conflict-Driven Clause Learning).
Он эволюционировал из алгоритма DPLL (Дэвис--Патнэм--Логеман--Лавленд, 1962).

Базовый DPLL --- это поиск с возвратом (backtracking) с двумя оптимизациями:
- _Распространение единичного дизъюнкта_ (unit propagation): если в дизъюнкте все литералы, кроме одного, ложны, оставшийся обязан быть истинным.
- _Устранение чистых литералов_ (pure literal elimination): если переменная встречается только с одной полярностью, ей можно присвоить выполняющее значение.

#example[Единичное распространение каскадом][
  Формула $(x) and (overline(x) or y) and (overline(y) or z) and (overline(z))$.
  Единичный дизъюнкт $(x)$ принуждает $x = 1$.
  Теперь $(overline(x) or y)$ стал единичным: литерал $overline(x)$ ложен, и $y$ обязан быть истинным.
  Из $(overline(y) or z)$ следует $z = 1$, после чего единичный $(overline(z))$ входит в конфликт.
  Каскад принуждений дошёл до противоречия без единого решения --- формула невыполнима.
]

#example[Устранение чистых литералов][
  В формуле $(x or y) and (overline(x) or z)$ переменная $y$ встречается только положительно, и $z$ --- тоже только положительно.
  Присваивание $y = 1, z = 1$ выполняет оба дизъюнкта при любом значении $x$: чистая переменная не может навредить ни одному дизъюнкту.
  Формула выполнима, и перебор по $x$ не нужен.
]

CDCL добавляет к этому механизм _обучения на конфликтах_, который радикально ускоряет поиск.

#remark[Сигнальный каскад и граф импликаций][
  В сигнальном каскаде клетки каждое "почему" имеет адрес: ген экспрессировался, потому что активировался фактор транскрипции.
  Фактор активировался, потому что его фосфорилировала киназа.
  Киназа активировалась, потому что сработал рецептор.
  Это ориентированный граф причин, где рёбра направлены от причины к следствию.

  Граф импликаций в CDCL --- та же структура: ребро $A -> B$ означает, что $B$ получил значение, потому что дизъюнкт $overline(A) or B$ стал единичным.
  Конфликт --- точка, где две цепочки таких "почему" сходятся на одной переменной с противоположными требованиями.
  Решатель отступает, запоминая "форму рельефа", о который споткнулся.
]

Поиск с возвратом в DPLL виден целиком на дереве: каждая ветвь --- присваивание переменной, unit propagation распространяет вынужденные значения, и когда обе ветви ведут к конфликту, формула невыполнима.
Такое дерево показано на рис. @fig:dpll-tree.

#figure(
  dpll-tree,
  caption: [DPLL-дерево поиска с возвратом.],
) <fig:dpll-tree>

#algorithm[CDCL][
  CDCL расширяет DPLL механизмом обучения на конфликтах.
  Основные компоненты алгоритма:

  - *Распространение единичного дизъюнкта* (Boolean Constraint Propagation, BCP):
    если в дизъюнкте все литералы, кроме одного, ложны, оставшийся должен быть истинным.
    Распространяем принудительные присваивания до фиксированной точки или до конфликта.
    На практике BCP занимает подавляющую часть времени работы решателя#footnote[По данным профилирования MiniSat и Glucose, BCP доминирует в общем времени выполнения --- типичные значения 80--90%.],
    поэтому его реализация критична для производительности.

  - *Принятие решения* (decision): когда распространение исчерпано и конфликта нет, выбираем неприсвоенную переменную и присваиваем ей значение.
    Эвристика VSIDS (Variable State Independent Decaying Sum) отдаёт приоритет переменным, часто появляющимся в недавних конфликтах.

  - *Анализ конфликта* (conflict analysis): когда дизъюнкт становится ложным, анализируем граф импликаций --- историю того, какие присваивания привели к конфликту.
    Из анализа извлекается *выученный дизъюнкт* (learned clause) --- новое логическое следствие формулы, запрещающее конфликтующую комбинацию присваиваний.
    Стандартной техникой является схема первого UIP (Unique Implication Point): UIP --- вершина графа импликаций текущего уровня, через которую проходят все пути от последнего решения к конфликту.
    Первый UIP --- ближайшая к конфликту такая вершина.

  - *Нехронологический возврат* (non-chronological backtracking): отменяем решения до точки, в которой выученный дизъюнкт становится единичным --- это может быть значительно раньше, чем последнее решение.

  - *Обучение и перезапуск*: добавляем выученный дизъюнкт в формулу.
  Периодически перезапускаем поиск с нуля (сохраняя выученные дизъюнкты), чтобы выйти из бесперспективных ветвей.
  Частота перезапусков --- важный параметр: слишком часто --- не успеваем исследовать ветвь.
  Слишком редко --- застреваем в бесперспективной области.
]

#example[Анализ конфликта: первый UIP][
  Пусть поиск дошёл до решений $x_1 = 1$ (уровень 1) и $x_2 = 0$ (уровень 2), и unit propagation вывел:
  $x_3 = 1$ из $(overline(x_1) or x_3)$, $x_4 = 1$ из $(overline(x_3) or x_4)$, $x_5 = 1$ из $(x_2 or x_5)$.
  Дизъюнкт $(overline(x_4) or overline(x_5) or x_6)$ стал единичным и дал $x_6 = 1$, после чего $(overline(x_4) or overline(x_5) or overline(x_6))$ оказался ложным --- конфликт.

  Граф импликаций: $x_1 -> x_3 -> x_4 -> x_6 -> square$, $x_2 -> x_5 -> x_6 -> square$ и ребро $x_5 -> square$.
  Обе цепочки от решения уровня 2 ($x_2 = 0$) к конфликту проходят через $x_5$, а через $x_6$ --- только одна, поэтому первый UIP --- $x_5$.

  Разрез сразу за UIP отделяет причину от следствия.
  Выученный дизъюнкт --- отрицание литералов на стороне причины: $overline(x_4) or overline(x_5)$.
  Он запрещает комбинацию $x_4 = 1, x_5 = 1$, породившую конфликт.

  Нехронологический возврат: $x_4$ присвоен на уровне 1, $x_5$ --- на уровне 2, поэтому откатываемся к уровню 1, минуя решение уровня 2.
  Там выученный дизъюнкт единичен и даёт $x_5 = 0$, дизъюнкт $(x_2 or x_5)$ принуждает $x_2 = 1$, и все дизъюнкты выполнены: модель найдена.
]

Разрез и выученный дизъюнкт из этого примера показаны на рис. @fig:cdcl-conflict-graph: две цепочки причин сходятся в $bot$, а пунктирный разрез сразу за первым UIP разделяет причину и следствие.

#figure(
  cdcl-conflict-graph,
  caption: [Граф импликаций при конфликте.],
) <fig:cdcl-conflict-graph>

== Применения SAT-решателей

Современные CDCL-решатели (MiniSat, Glucose, CaDiCaL, Kissat) регулярно решают индустриальные примеры с миллионами переменных и десятками миллионов дизъюнктов, несмотря на NP-полноту SAT в худшем случае.
Практические индустриальные формулы обладают структурой (сообщества переменных, иерархия ограничений, малый treewidth графа дизъюнктов), которую CDCL эксплуатирует через выученные дизъюнкты и эвристику VSIDS.

У случайных формул структуры нет, и картина другая --- у случайной $k$-SAT есть _фазовый переход_ по отношению дизъюнктов к переменным (для 3-SAT --- около $4.3$).
Ниже порога формула почти наверняка выполнима, выше --- почти наверняка нет, а на самом пороге лежит самая трудная для решателя область.
Именно такие бесструктурные формулы перебираются экспоненциально долго --- эффективность CDCL объясняется структурой формул.

Основные применения SAT:
- *Верификация аппаратуры*: проверка эквивалентности оптимизированной схемы спецификации через митер (см. главу @chap:circuits).
  SAT-решатель либо доказывает невыполнимость митера (схемы эквивалентны), либо находит контрпример.
- *Ограниченная проверка моделей* (bounded model checking, BMC):
  программа разворачивается на $k$ шагов выполнения в КНФ-формулу.
  Выполнимость означает существование ошибки (нарушения утверждения) в пределах $k$ шагов.
  Используется в верификации драйверов устройств и критического ПО.
- *Криптоанализ*: шифр с уменьшенным числом раундов переводится в КНФ.
  SAT-решатель ищет ключ, переводящий известный открытый текст в известный шифротекст.
  Так были впервые взломаны некоторые варианты SHA-1 и AES с пониженным числом раундов.
- *Управление конфигурациями*: зависимости пакетов Linux-дистрибутива кодируются в КНФ.
  Пакет $p$ версии $v$ установлен $->$ все его зависимости удовлетворены.
  Решатель находит совместимый набор версий.

Этот феномен --- NP-полная задача, систематически решаемая на практике для структурированных входов --- превратил SAT из теоретической диковинки в индустриальный инструмент.#footnote[
  S. Malik, L. Zhang, "Boolean Satisfiability: From Theoretical Hardness to Practical Success", Communications of the ACM, 2009;
  A. Biere et al. (ред.), "Handbook of Satisfiability", 2-е изд., IOS Press, 2021.
]

Можно взглянуть на CDCL с более абстрактной позиции --- и увидеть знакомую картину.

== Граф импликаций: от 2-SAT до CDCL

Вся конструкция алгоритмического SAT держится на одной идее: логические ограничения можно рассматривать как направленные импликации в графе.

Для 2-SAT этот граф явный: каждая пара литералов дизъюнкта связана двумя рёбрами --- прямым и контрапозиционным.
Контрапозиция импликации $a -> b$ --- это импликация $overline(b) -> overline(a)$.
Логически они эквивалентны.
Алгоритм находит компоненты сильной связности.
Формула невыполнима, если переменная и её отрицание попали в один цикл.

CDCL обобщает эту идею на КНФ произвольной длины: граф импликаций строится неявно по ходу присваиваний.
Когда дизъюнкт $(a or b or c)$ становится единичным (два литерала ложны), он порождает принудительное присваивание оставшегося литерала --- и ребро в графе импликаций от отрицаний двух ложных литералов к вынужденному литералу.

Конфликт возникает, когда дизъюнкт становится ложным: все его литералы получили значение "ложь".
В графе импликаций это видно как сходимость двух цепочек присваиваний, приписавших одному литералу противоположные значения.

Анализ конфликта выделяет *разрез* графа импликаций, разделяющий причину конфликта и его следствие.
Выученный дизъюнкт --- это отрицание литералов на стороне причины: он запрещает комбинацию присваиваний, приведшую к конфликту, и тем самым "перерезает" все пути, ведущие к данному конфликту в будущем.#footnote[
  J. P. Marques-Silva, K. A. Sakallah, "GRASP: A Search Algorithm for Propositional Satisfiability", IEEE Transactions on Computers, 1999.
]

#remark[Два графа под одним именем][
  Граф импликаций 2-SAT и граф импликаций CDCL --- разные объекты, и общее у них только имя.
  Граф 2-SAT статичен и полон: вершины --- все $2n$ литералов, рёбра заданы формулой заранее.
  Граф CDCL динамичен и частичен: вершины --- лишь литералы, уже получившие значения, и рёбра появляются по ходу распространения.
  Объединяет их одна идея: ребро $A -> B$ читается как "причина $A$ влечёт следствие $B$".
]

#check[Кодирование задач в SAT][
  + Почему "не более одного" в кодировании раскраски требует $binom(k, 2)$ попарных дизъюнктов, тогда как "хотя бы один" --- лишь одного?
  + Чем хорошее сведение отличается от плохого, если оба формально корректно сводят задачу к SAT?
]

== SAT и NP <sec:sat-np>

SAT --- точка отсчёта в теории сложности.
Заполненную сетку судоку проверить легко: ошибка, если она есть, видна за минуту.
Найти решение с нуля --- труднее: наивный перебор пробует $9^81$ заполнений.
Что "найти" действительно труднее "проверить", пока не доказано, --- и этот разрыв и есть вопрос $"P" = "NP"$.
Чтобы говорить о NP-полноте, нужны два понятия (формально --- глава @chap:complexity. Здесь достаточно интуитивных версий).
+ _Класс NP_ --- задачи, чей ответ "да" можно проверить за полиномиальное время: предъявленному сертификату (например, набору значений переменных) верификатор отвечает быстро.
+ _Полиномиальная сводимость_ --- преобразование входов одной задачи во входы другой, сохраняющее ответ и выполнимое за полиномиальное время.
+ Задача NP-полна, если она лежит в NP и к ней сводится любая задача из NP.
Как предъявить задачу из NP, которая не лежит в P, если не знаешь даже, существует ли она?
Самый разумный ход --- искать "самую трудную" задачу класса: если решается она, решается всё.
Теорема Кука (1971) утверждает, что SAT --- NP-полная задача, "самая трудная" в классе NP.

#theorem[Теорема Кука --- формулировка#footnote[
    Cook S. A. "The Complexity of Theorem-Proving Procedures", Proceedings of the 3rd ACM Symposium on Theory of Computing (STOC), 1971.
    Первая доказанная NP-полнота.
    Конструкция сводит произвольную NP-машину к булевой формуле через таблицу вычисления.
  ]][
  SAT NP-полна: SAT $in$ NP, и всякая задача из NP полиномиально сводится к SAT.
]

#proof-sketch[
  Докажем в две стороны: сначала покажем, что SAT принадлежит NP, затем --- что SAT NP-трудна.

  *SAT принадлежит NP*: сертификат --- выполняющий набор переменных, а проверка состоит в подстановке значений в КНФ и проверке всех дизъюнктов, что делается за полиномиальное время.

  *SAT NP-трудна*: нужно показать, что любая задача из NP сводится к SAT.
  Недетерминированная машина Тьюринга (НДТМ) устроена как обычная, но в каждой конфигурации может выбрать одну из нескольких команд, и вычисление ветвится.
  Вход принимается, если принимает хотя бы одна ветвь, а угадывание сертификата --- это и есть выбор ветви.

  Класс NP можно описать и иначе: машина сначала угадывает сертификат, затем работает детерминированно.
  Эквивалентность этого описания определению через верификатор стандартна и здесь не доказывается.
  Угадывание --- сжатая запись перебора: миллион копий машины, по одной на каждый возможный сертификат, и принимает та копия, чей сертификат оказался верным.

  Пусть $M$ --- недетерминированная машина Тьюринга, распознающая язык $L in "NP"$ за время $p(n)$.
  Для входа $x$ строится булева формула $phi_x$ в КНФ, выполнимая в точности тогда, когда вход принимается машиной $M$.

  Конструкция использует *таблицу вычисления* размера $p(n) times p(n)$: ячейка $(i, t)$ кодирует содержимое $i$-й позиции ленты на шаге $t$.
  Переменные: $T_(i,t,a)$ истинна, если ячейка $(i, t)$ содержит символ $a$, а $H_(i,t,q)$ истинна, если головка в позиции $i$ на шаге $t$ в состоянии $q$.
]

Таблица вычисления локальна: содержимое ячейки на следующем шаге определяется тремя ячейками над ней.
Окно $2 times 3$ такого фрагмента показано на рис. @fig:cook-levin-table.

#figure(
  cook-levin-table,
  caption: [Таблица вычисления машины Тьюринга.],
) <fig:cook-levin-table>

#proof-sketch[
  Построим формулу $phi_x$ из четырёх групп дизъюнктов, кодирующих принимающее вычисление, и проверим её размер.

  Первая группа --- *начальная конфигурация*: $x$ записан на ленте, головка в начальной позиции, состояние --- начальное.
  Вторая группа --- *локальная корректность перехода*: для каждого окна $2 times 3$ в таблице вычисления содержимое ячейки на шаге $t+1$ определяется тремя ячейками над ней на предыдущем шаге --- в соответствии с функцией переходов $M$.
  Третья группа --- *единственность*: в каждой ячейке --- не более одного символа, головка --- не более чем в одной позиции, машина --- не более чем в одном состоянии.
  Наконец, четвёртая группа --- *принимающее условие*: на последнем шаге $M$ находится в принимающем состоянии.

  Каждый дизъюнкт содержит $O(1)$ литералов, общее число дизъюнктов --- $O(p(n)^2)$.
  Формула выполнима тогда и только тогда, когда существует принимающее вычисление $M$ на входе $x$, а сведение полиномиально --- как по времени построения формулы, так и по её размеру.
]

Из NP-полноты SAT напрямую следует ответ о цене перебора.

#corollary[Следствие для P vs NP][
  Если SAT $in$ P, то $"P" = "NP"$.
  Если SAT требует сверхполиномиального времени, то $"P" != "NP"$.
]

#proof[
  Докажем эквивалентность $"SAT" in "P"$ и $"P" = "NP"$ в обе стороны.

  Если SAT $in$ P, то существует полиномиальный алгоритм $A$ для SAT.
  По теореме Кука любая задача $L in "NP"$ полиномиально сводится к SAT функцией $f$, поэтому композиция $A compose f$ даёт полиномиальный алгоритм для $L$.
  Отсюда $L in "P"$ и $"P" = "NP"$.

  Обратно, если $"P" = "NP"$, то SAT, как NP-полная задача, принадлежит P, следовательно, утверждения $"P" = "NP"$ и $"SAT" in "P"$ эквивалентны.
  Поэтому если SAT требует сверхполиномиального времени, то $"P" != "NP"$.
]

"Сверхполиномиальное время" не обязательно означает экспоненту --- достаточно, например, $2^(sqrt(n))$.
Нижняя граница пока не доказана.#footnote[
  Если SAT принадлежит $"P"$,
  то $"P" = "NP"$.
  Обратное также верно: если $"P" != "NP"$, то SAT не решается за полиномиальное время.
  Какое из двух утверждений истинно --- открытый вопрос.
]

#remark[Универсальный представитель][
  Теорема Кука--Левина устанавливает больше, чем NP-полноту SAT.
  Конструкция в доказательстве --- таблица вычисления недетерминированной машины Тьюринга, закодированная в КНФ --- показывает, что задача выполнимости и задача "существует ли принимающее вычисление?" структурно идентичны.
  Любая NP-задача, от раскраски графа до планирования экзаменов, при сведении к SAT передаёт ему эту структуру: свидетель становится моделью формулы.

  Именно эта универсальность делает вопрос "SAT $in$ P?" эквивалентным вопросу "P = NP?".
  Если SAT решается полиномиально, то всякое полиномиально проверяемое утверждение --- корректность криптографического протокола, оптимальность расписания, верность математической теоремы с доказательством полиномиальной длины --- получает полиномиальный алгоритм распознавания.
  Если же SAT требует сверхполиномиального времени, то NP-полнота остаётся сертификатом неразрешимости в худшем случае.

  Распознать доказательство и найти его --- разные умения, и в мире, где $"P" = "NP"$, они совпали бы: каждый, кто способен проследить рассуждение шаг за шагом, был бы Гауссом.
]

Понимание SAT --- и алгоритмическое, и теоретическое --- до сих пор формирует облик computer science.
Статус SAT _двойствен_: NP-полная задача в худшем случае и систематически решаемая на индустриальных входах.

NP-полнота --- свойство задачи в худшем случае: среди _всех_ КНФ-формул есть намеренно сконструированные патологические, на которых решатели работают экспоненциально долго.
Пример --- КНФ-кодировки принципа Дирихле, на которых resolution требует экспоненциального времени.
Масштаб эффекта виден на судоку: сетку $9 times 9$ щёлкает телефон, а сетку $100 times 100$ в худшем случае не одолеет ни одна машина.
Индустриальные же формулы порождены конкретными инженерными задачами --- верификацией аппаратуры, планированием, BMC --- и наследуют их структуру.

Три структурных свойства отличают индустриальные формулы от случайных:
+ _Сообщества переменных_ (community structure): переменные группируются в кластеры, внутри которых ограничений много, а между кластерами --- мало.
  CDCL учит дизъюнкты, связывающие переменные внутри кластера.
+ _Малая древесная ширина_ (treewidth) графа инцидентности (переменные и дизъюнкты): формула близка к древовидной, что позволяет эффективно отсекать бесперспективные ветви.
+ _Наличие "чёрного хода"_ (backdoor): небольшое подмножество переменных, присваивание значений которым сводит формулу к полиномиально разрешимой.

CDCL решает SAT на индустриальных входах, а не в общем случае.
Алгоритм эксплуатирует структурные свойства формул через два механизма:
+ VSIDS --- отдаёт приоритет "активным" переменным.
+ Обучение дизъюнктам --- накопление "структурного знания" о формуле.#footnote[
    C. P. Gomes, H. Kautz, A. Sabharwal, B. Selman, "Satisfiability Solvers", глава 2 в "Handbook of Knowledge Representation", Elsevier, 2008;
    C. Ansótegui, M. L. Bonet, J. Levy, "On the Structure of Industrial SAT Instances", CP 2009.
  ]

== \* Современные SAT-решатели (DPLL и CDCL)

Прежде чем перейти к инженерным деталям, посмотрим, как DPLL работает на конкретной формуле.

#example[DPLL в действии: полный прогон][
  Рассмотрим формулу
  $
    F = (x_1 or x_2 or x_3) and (overline(x_1) or x_2) and (x_2 or overline(x_3)) and (overline(x_2) or x_3) and (overline(x_2) or overline(x_3)).
  $
  Прогон DPLL с единичным распространением и возвратом:

  #table(
    columns: 4,
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    [*Ветвь*], [*Решение*], [*Unit propagation*], [*Результат*],
    [$x_1 = 1$],
    [$x_1 = 1$],
    [$x_2 = 1$ из $overline(x_1) or x_2$, затем $x_3 = 0$ из $overline(x_2) or overline(x_3)$],
    [конфликт в $overline(x_2) or x_3$],

    [$x_1 = 0, x_2 = 0$],
    [$x_2 = 0$],
    [$x_3 = 0$ из $x_2 or overline(x_3)$],
    [конфликт в $x_1 or x_2 or x_3$],

    [$x_1 = 0, x_2 = 1$],
    [$x_2 = 1$],
    [$x_3 = 0$ из $overline(x_2) or overline(x_3)$],
    [конфликт в $overline(x_2) or x_3$],
  )

  Оба значения $x_2$ при $x_1 = 0$ приводят к конфликту.
  Ветвь $x_1 = 1$ тоже конфликтна.
  Значит, $F$ невыполнима: DPLL исчерпал все возможности и вернул UNSAT.
]

Прогон демонстрирует три элемента DPLL:
- _Решение_ --- эвристический выбор значения переменной.
- _Единичное распространение_ --- принудительное присваивание из дизъюнкта, где все литералы ложны, кроме одного.
- _Возврат_ --- откат последнего решения при конфликте.
В этом примере возврат хронологический: откатываемся к предыдущему решению.
Как CDCL делает возврат нехронологическим и откуда берётся выученный дизъюнкт, разобрано в разделе @sec:cdcl.

Остаются две инженерные детали, без которых обучение не было бы быстрым.

*Watched literals.* Вместо того чтобы проверять все литералы дизъюнкта при каждом присваивании, решатель следит ровно за двумя.
Пока оба не ложны --- дизъюнкт не единичный.
Это превращает распространение (BCP) из квадратичной операции в практически линейную по длине формулы.
Без watched literals распространение по миллионам дизъюнктов становится практически нереализуемым.

*Перезапуски.* Периодически решатель отбрасывает текущие присваивания и начинает поиск заново, сохраняя выученные дизъюнкты.
Первые решения часто принимаются наспех, без достаточной информации о структуре формулы.
Перезапуск с накопленными дизъюнктами даёт VSIDS шанс выбрать лучший порядок переменных.

Современные решатели --- MiniSat (2003), Glucose (2009), CaDiCaL (2018) --- умещаются в тысячи строк кода, но работают на формулах с миллионами переменных.
Секрет --- в плотности идей на строку.
NP-полнота говорит о худшем случае, инженерные формулы --- не худший случай, и CDCL --- инструмент, который эту разницу эксплуатирует.

#note[
  Ядро DPLL умещается в полсотни строк.
  Формула --- это число переменных и список дизъюнктов, где литерал --- номер переменной со знаком:

  ```rust
  // Формула (x1 ∨ x2) ∧ (¬x1): дизъюнкты списками литералов.
  let clauses = vec![vec![1, 2], vec![-1]];

  // Модель: x1 = false (литерал -1 выполнен), x2 = true.
  let model = solve(2, &clauses).unwrap();
  assert!(!model[0]); // x1 = false
  assert!(model[1]);  // x2 = true
  ```

  Внутри `solve` --- три процедуры из прогона выше: решение, единичное распространение, возврат.
]

== Итоги главы

SAT сводит вопрос о существовании объекта с нужными свойствами к одной формуле: существует ли набор значений переменных, при котором формула истинна.
Эквивалентность двух схем --- то же самое: они расходятся ровно тогда, когда формула об их различии выполнима, и проверка через митер сводится к задаче выполнимости.
Запись задач в КНФ дала единый формат: каждое ограничение становится дизъюнктом, ограничения соединяются конъюнкцией.

Длина дизъюнкта решает судьбу задачи.
Дизъюнкт из двух литералов --- это две импликации, и граф импликаций с его сильно связными компонентами даёт ответ за линейное время.
Стоит добавить третий литерал --- и 3-SAT NP-полна: дизъюнкт становится гиперребром, а теорема Кука--Левина превращает SAT в эталонную NP-полную задачу, к которой сводятся раскраска, судоку и десятки других.

Вторая половина --- про практику.
CDCL учится на конфликтах, и выученные дизъюнкты с нехронологическим возвратом направляют поиск.
Именно поэтому индустриальные формулы с миллионами переменных решаются за секунды, тогда как случайные застревают на пороге фазового перехода.
Эффективное кодирование задачи в КНФ --- отдельное ремесло: переменные должны отражать структуру задачи, а ограничения --- укладываться в форматы, которые решатель обрабатывает быстро.

SAT соединяет схемотехнику (глава @chap:circuits) с теорией сложности (глава @chap:complexity): полиномиальный алгоритм для неё означал бы $"P" = "NP"$.
Резолюция, изученная в этой главе, станет основой следующей: глава @chap:logic-programming превратит её в язык программирования.


#check[Проверьте себя][
  + Почему 2-SAT решается за линейное время, а 3-SAT NP-полна?
  + Что такое сведение, и почему из NP-полноты SAT следует: если SAT в $"P"$, то $"P" = "NP"$?
  + В чём идея CDCL: откуда берётся выученный дизъюнкт и почему возврат называется нехронологическим?
  + Как закодировать раскраску графа в КНФ, и какие группы дизъюнктов отвечают за "хотя бы один цвет" и "не более одного цвета"?
  + Чем индустриальные формулы отличаются от случайных, и почему NP-полная задача решается на первых на практике?
  + Как граф импликаций определяет, выполнима ли 2-SAT-формула?
]

#whats-next
Мы остаёмся в логике: следующая глава расширяет SAT теориями.
В главе @chap:smt к булевым переменным добавятся арифметика, равенство и массивы, а цикл DPLL(T) соединит булево рассуждение с theory-солверами, чтобы отвечать, выполнима ли формула над этими теориями.
SAT решал выполнимость булевых формул.
SMT спрашивает то же про формулы с арифметикой и другими теориями --- и это станет движком верификации программ в главе @chap:verification.

== Упражнения

#tasklist("m14")[
  _Базовые понятия SAT._
  + Какие из следующих КНФ-формул выполнимы? Для выполнимых укажите модель.
    + $(x or y) and (overline(x) or overline(y))$
    + $(x) and (overline(x))$
    + $(x or y or z) and (overline(x)) and (overline(y)) and (overline(z))$
    + $(x or y) and (overline(x) or y) and (overline(y))$

  + Приведите пример невыполнимой КНФ-формулы, содержащей ровно два дизъюнкта.
    Объясните, почему она невыполнима.

  _Резолюция._
  + Примените метод резолюции, чтобы доказать:
    из $p or q, overline(p) or r, overline(q) or r$
    логически следует $r$.
    Нарисуйте DAG резолюционного опровержения.
  + Почему резолюция работает с КНФ, и почему ДНФ для неё не подходит, хотя обе формы эквивалентны исходной формуле?

  + \* Чем выполнимость отличается от общезначимости?
    Приведите пример выполнимой, но не общезначимой формулы и пример невыполнимой, отрицание которой выполнимо.
    #metadata((ex-id: "m14:t5"))

  _2-SAT и граф импликаций._
  + Переведите каждый дизъюнкт 2-SAT-формулы
    $
      (x_1 or overline(x_2)) and (overline(x_1) or x_3) and (overline(x_2) or overline(x_3))
    $
    в пару импликаций и постройте граф импликаций.
    Сколько вершин и рёбер в графе?

  + Решите 2-SAT-формулу с помощью графа импликаций:
    $
      (x_1 or x_2) and (overline(x_1) or x_3) and (overline(x_2) or overline(x_3)) and (overline(x_2) or x_4) and (overline(x_4) or overline(x_3)).
    $
    Найдите все компоненты сильной связности, постройте конденсацию и присвойте значения переменным.

  + \* Объясните, почему 2-SAT решается за полиномиальное время, а 3-SAT NP-полна.
    Что именно добавление третьего литерала в дизъюнкт разрушает в графе импликаций?
    #metadata((ex-id: "m14:t8"))

  _Кодирование задач в SAT._
  + Закодируйте задачу 3-раскраски графа-треугольника $K_3$ в виде КНФ-формулы.
    Используйте переменные $x_(v, c)$ (вершина $v$ имеет цвет $c$).
    Выпишите все дизъюнкты.
    Выполнима ли формула?

  + Закодируйте условие "ровно две переменные из $x_1, x_2, x_3, x_4$ истинны" в КНФ.
    Указание: скомбинируйте ограничения "хотя бы две" и "не более двух".
    Подсчитайте число дизъюнктов.

  + \* Закодируйте задачу поиска гамильтонова цикла в графе с $n$ вершинами как SAT.
    Опишите переменные и основные группы дизъюнктов.
    Почему наивное кодирование через перечисление всех перестановок порождает экспоненциально много дизъюнктов и как этого избежать?
    #metadata((ex-id: "m14:t11"))

  _DPLL и CDCL._
  + Промоделируйте работу DPLL (с unit propagation) на формуле:
    $
      (x_1 or x_2) and (overline(x_1) or x_3) and (overline(x_2) or overline(x_3)) and (x_1 or overline(x_3)).
    $
    Используйте порядок решений: сначала $x_1 = 1$, затем $x_2 = 1$ (если потребуется).
    Для каждого узла дерева поиска укажите частичное присваивание и результат unit propagation.

  + \* Дан частичный вывод CDCL:
    решение $x_1 = 1$.
    Решение $x_2 = 0$.
    Unit propagation из $(overline(x_1) or x_3)$ даёт $x_3 = 1$.
    Unit propagation из $(x_2 or x_4)$ даёт $x_4 = 1$.
    Конфликт в дизъюнкте $(overline(x_3) or overline(x_4))$.

    Нарисуйте граф импликаций.
    Примените анализ конфликта: найдите разрез, соответствующий первому UIP, и выпишите выученный дизъюнкт.
    #metadata((ex-id: "m14:t13"))

  _SAT и NP._
  + Почему из NP-полноты SAT следует: если SAT решается за полиномиальное время, то $"P" = "NP"$?

  + \* Докажите, что задача о сумме подмножества (subset sum) NP-полна, сведя к ней 3-SAT.
    Постройте по 3-КНФ-формуле $phi$ с $n$ переменными и $m$ дизъюнктами множество чисел и целевую сумму $T$ так, что подмножество с суммой $T$ существует тогда и только тогда, когда $phi$ выполнима.
    Указание: числа записываются в десятичной системе, и разряды отвечают переменным и дизъюнктам.
    Каждой переменной $x_i$ отвечают два числа (для $x_i$ и $overline(x_i)$), каждому дизъюнкту --- два числа с цифрами $1$ и $2$ в его разряде.
    Целевая сумма $T$ имеет $1$ в каждом переменном разряде и $4$ в каждом дизъюнктном:
    сумма в дизъюнктном разряде складывается из единиц выбранных литералов и служебных чисел и равна $4$, только если хотя бы один литерал дизъюнкта истинен.
    #metadata((ex-id: "m14:t15"))

  + \* "Доказательство" того, что $"SAT" in P$.
    Перебираем переменные $x_1, ..., x_n$ по порядку.
    Для каждой пробуем оба значения и выбираем то, при котором выполнено больше дизъюнктов (при равенстве --- 0).
    После обработки всех переменных получаем полное присваивание.
    Если формула была выполнима, алгоритм якобы найдёт выполняющее присваивание.
    Алгоритм полиномиален: $n$ итераций по $m$ дизъюнктов.
    Найдите ошибку: почему жадный выбор значения не гарантирует выполнимости, даже если модель существует?
    #metadata((ex-id: "m14:t16"))
]


#project[Фазовый переход случайного 3-SAT][
  Случайные 3-SAT-формулы с $n$ переменными и $m$ дизъюнктами при малом отношении $m/n$ почти всегда выполнимы, при большом --- почти всегда нет, а переход происходит в узком окне вблизи $m/n approx 4.26$.
  Соберите генератор случайных формул и решатель и проследите на работающем коде, как доля выполнимых и время решения зависят от $m/n$.

  #project-stage[Решатель][
    Соберите DPLL с unit-распространением и чистыми литералами и добавьте статистику решений, пропагаций и конфликтов.
    Как расширение продумайте CDCL с анализом конфликтов и изучением дизъюнктов.
  ]
  #project-stage[Генератор][
    Постройте генератор случайных 3-SAT-формул с фиксированным $n$ и регулируемым отношением $m/n$, без дублирующих дизъюнктов, и добавьте вывод в формате DIMACS.
  ]
  #project-stage[Кривая выполнимости][
    Проследите долю выполнимых формул при росте $m/n$ и сверьте положение перехода с известным $m/n approx 4.26$.
  ]
  #project-stage[Кривая времени][
    Измерьте медианное время решения в каждой точке и продумайте, почему самые трудные формулы сосредоточены вблизи порога.
  ]
  #project-stage[Структурные формулы][
    Закодируйте в КНФ задачу со структурой, например судоку или раскраску графа, и сравните время решения таких формул со случайными той же плотности.
    Объясните, почему структура делает формулу лёгкой для решателя.
  ]

  #project-outcome[
    две кривые фазового перехода с проверкой перехода против известного $m/n approx 4.26$ и сравнение случайных и структурных формул.
  ]
]
