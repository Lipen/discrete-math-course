// M32 --- Модальная логика: модели Крипке, LTL/CTL, model checking.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m32.typ": kripke-traffic, modal-cube, mutex-states

= Модальная логика <chap:modal>

Глава @chap:verification доказала корректность программ тройками Хоара: что верно до и после оператора.
Но есть свойства, которые не видны ни на входе, ни на выходе: "запрос рано или поздно получит ответ", "никогда не наступит взаимная блокировка".
Они говорят о самом _процессе_: о том, как система ведёт себя во времени.
Прежде чем говорить о времени, стоит задать вопрос постарше, с которого начинается эта глава: что вообще значит "утверждение истинно"?

Вот два утверждения.
"Завтра в 14:00 пойдёт дождь" и "2 + 2 = 4".
Оба претендуют на истину, но истина у них разной природы: дождь мог бы и не пойти, а арифметика другой быть не может.
"Завтра пойдёт дождь" --- истина _случайная_, она держится на капризе обстоятельств.
"2 + 2 = 4" --- истина _необходимая_, не бывает мира, где это не так.
Как будто слово "истинно" прячет внутри два разных смысла --- и логика из первых глав слепо их смешивает.
Вот с этой загадки и начинается глава.

Различие древнее: Аристотель в трактате "De Interpretatione" на примере завтрашнего морского сражения первым заметил, что здесь есть логика.
Необходимое влечёт возможное, но обратное неверно: "возможно" и "необходимо" выражаются друг через друга --- "необходимо" значит "невозможно не", "возможно" --- "не необходимо не".
Для них понадобятся два оператора: $square$ читается "необходимо".
$diamond$ --- "возможно".

Но что значит "необходимо"?
Сказать "истинно всегда" --- лишь пересказ загадки другими словами.
Лейбниц предложил картину, определившую всю дальнейшую историю: необходимо истинно то, что верно "во всех возможных мирах".
Слово "мир" здесь --- метафора, которую стоит держать в кавычках: _способ, каким мог бы обстоять мир_.
Каждый "мир" --- одна из мыслимых картин реальности: в одном идёт дождь, в другом --- нет, но "2 + 2 = 4" истинно в каждой.
Утверждение необходимо, если оно истинно во всех таких картинах, и возможно, если истинно хотя бы в одной.
Двести лет эта картина оставалась метафорой: не было ответа, какие "миры" считать и как по ним проверять формулы.

Метафора умалчивает и главное: каких "миров" брать в расчёт?
Для физика --- те, где действуют наши законы природы: быстрее света не разогнаться никому.
Для логика --- все непротиворечивые картины, даже те, где физика другая.
"Невозможно" в устах физика и логика --- разные слова, и пока вопрос без ответа, формальной логики не построить.

Первую попытку формализации предпринял Карнап#footnote[R. Carnap. "Meaning and Necessity: A Study in Semantics and Modal Logic". 1947.]:
"мир" --- полное описание того, какие атомарные утверждения истинны, --- что-то вроде строки таблицы истинности.
"Необходимо" значило: истинно во всех таких описаниях.
Попытка споткнулась о вложенные модальности: формула "возможно необходимо $p$" ничем не отличалась от "необходимо $p$".
Причина --- в плоскости самой картины: все "миры" в ней равноправны, между ними нет никакой структуры, и вложенным модальностям не за что зацепиться.

В 1959 году вышла статья о полноте модальной логики, написанная Крипке ещё школьником
#footnote[S. A. Kripke. "A Completeness Theorem in Modal Logic". Journal of Symbolic Logic, 1959.], --- и структура появилась.
Из данного мира видны лишь те "миры", что _достижимы_ из него.
$square p$ истинно в мире, где $p$ верно во всех достижимых мирах.
$diamond p$ --- где $p$ верно хотя бы в одном.
Достижимость даёт вложенным модальностям за что зацепиться: из мира видно, какие миры достижимы, из них --- какие дальше, и так строится лестница вложений.

Так "миры" превращаются из метафоры в рабочий инструмент.
Формально "мир" --- просто точка с приписанными истинностными значениями атомов, а "достижимость" --- бинарное отношение между точками.
Отношение эквивалентности, частичный порядок, переходы программы --- всё это примеры.
Свойства самого отношения записываются как формулы: рефлексивность --- $square p -> p$.
Транзитивность --- $square p -> square square p$.
Это соответствие "свойство отношения --- аксиома" --- главный сюжет первой половины главы.

Гибкость чтения --- суть замысла.
Одна и та же пара операторов описывает знание, убеждение, обязанность и время, и каждое чтение диктует свои аксиомы.
Знание истинно: $square p -> p$.
Убеждение может быть ложным, и эта аксиома ему запрещена.
Для знания достижимы миры, неотличимые для агента, для обязанности --- миры, где долг исполнен.

Если "миры" --- моменты времени, а "достижимость" --- течение времени, модальная логика становится темпоральной.
$square$ --- "всегда".
$diamond$ --- "когда-нибудь".
И это ровно тот инструмент, которого не хватало инженеру в начале главы: свойства "запрос получит ответ" и "не будет взаимной блокировки" получают точный смысл.
Именно их автоматически проверяет model checking.
Загадка об истине, которая не могла бы быть иной, обернулась инструментом, который доказывает, что система никогда не свернёт в дурной мир.
Как из загадки о завтрашнем дожде вырастает инструмент, проверяющий поведение систем во времени?

#chapter-overview[
  Фреймы и операторы: что такое "мир", достижимость и оценка, как определяется истинность формул $square p, diamond p$.
  Затем мы установим прямое соответствие: свойства отношения достижимости записываются аксиомами и наоборот.
  Рефлексивность --- это $square p -> p$, транзитивность --- это $square p -> square square p$.
  Из аксиом соберутся именованные системы $D, T, B, S_4, S_5$, мы научимся их выбирать и сложим в единую картину --- куб модальностей.

  Потом мы перейдём к задачам модальной логики: выполнимости и общезначимости, алгоритмам и солверам, приложениям вне model checking.
  Дальше мы рассмотрим другие чтения той же пары операторов: знание, убеждение, обязанность.
  Затем время становится модальностью: темпоральные логики LTL и CTL.
  В конце главы мы обратимся к model checking на практике: как свойства систем проверяются автоматически.

  После этой главы вы научитесь:
  + строить фреймы Крипке и вычислять истинность модальных формул;
  + связывать свойства отношения достижимости с аксиомами;
  + различать именованные системы $D, T, B, S_4, S_5$ и выбирать нужную;
  + читать модальности как знание, убеждение, обязанность и время;
  + объяснять model checking и проверку свойств вида "никогда не наступит блокировка".
]

== Фреймы и операторы

Модальная логика --- логика _миров_ и их взаимной достижимости.
Формула может быть истинна в одном мире и ложна в другом, а операторы $square, diamond$ говорят о том, что верно во всех или в некотором достижимом мире.

Формулы строятся из _атомарных утверждений_ --- элементарных высказываний вроде "идёт дождь" или "процесс в критической секции".
Атом не разбирается на части: он просто истинен или ложен в каждом мире, а формулы собираются из атомов связками и операторами, как в главе @chap:logic.
Набор всех атомов обозначают $"AP"$ (от англ. _atomic propositions_).

#definition[Фрейм][
  _Фрейм_ --- пара $F = (W, R)$, где:
  - $W$ --- непустое множество _миров_ (точек, ситуаций).
  - $R subset.eq W times W$ --- отношение _достижимости_.
    Запись $R(u, v)$ читается "из мира $u$ виден мир $v$".

  Фрейм описывает только геометрию: какие миры из каких видны.
  Например, фрейм с $W = {w_1, w_2}, R = {(w_1, w_2)}$.
  Из $w_1$ виден $w_2$, а из $w_2$ --- ничего.
  Что в этих мирах истинно, фрейм не говорит --- за это отвечает оценка.
]

#definition[Модель и оценка][
  _Оценка_ приписывает каждому атому множество миров, где он истинен: $V : "AP" -> cal(P)(W)$.
  Запись $w in V(p)$ означает "атом $p$ истинен в мире $w$".
  _Модель_ --- тройка $M = (W, R, V)$: фрейм плюс оценка.
  Модель _построена на_ фрейме $F$, если она имеет вид $(W, R, V)$ для некоторой оценки $V$.

  В примере выше оценка может решить, что $p$ истинен в $w_2$ и ложен в $w_1$, а $q$ истинен в обоих.
  Одна и та же геометрия фрейма допускает много оценок, и каждая даёт свою модель.
]

#definition[Истинность модальной формулы][
  Отношение $M, w models A$ ("формула $A$ истинна в мире $w$ модели $M$") определяется индукцией по построению формулы.

  База и булевы связки:
  - атом: $M, w models p$ тогда и только тогда, когда
    $w in V(p)$.
  - отрицание: $M, w models not B$ тогда и только тогда, когда
    $M, w nmodels B$.
  - конъюнкция: $M, w models B and C$ тогда и только тогда, когда истинны обе формулы (дизъюнкция и импликация --- как обычно).

  Модальные случаи --- единственное, что отличает эту логику от пропозициональной:
  - необходимость: $M, w models square B$ тогда и только тогда, когда
    $M, w' models B$ для _всех_ $w'$
    с $R(w, w')$.
  - возможность: $M, w models diamond B$ тогда и только тогда, когда
    $M, w' models B$ для _некоторого_ $w'$
    с $R(w, w')$.

  Формула $A$ истинна в модели $M$, если $forall w in W: M, w models A$.
]

$square$ читается "необходимо".
$diamond$ --- "возможно".
Формула $square p$ означает "необходимо $p$".
Формула $diamond p$ означает "возможно $p$".

Операторы двойственны --- каждый выражается через другой с отрицанием:
$
   square A & = not diamond not A, \
  diamond A & = not square not A.
$
В самом деле, "необходимо" значит "невозможно не", а "возможно" --- "не необходимо не".

#example[Считаем истинность вручную][
  Пусть $W = {w_1, w_2}$.
  Из $w_1$ достижимы оба мира, из $w_2$ --- только $w_2$.
  Атом $p$ истинен только в $w_2$, атом $q$ --- в обоих.
  Тогда:
  - $M, w_1 models diamond p$: из $w_1$ виден $w_2$, где $p$ истинен.
  - $M, w_1 nmodels square p$: из $w_1$ виден и $w_1$, где $p$ ложен.
  - $M, w_1 models square diamond p$: в каждом достижимом из $w_1$ мире (в $w_1$ и в $w_2$)
    формула $diamond p$ истинна --- из обоих виден $w_2$.

  Последний пункт --- первый пример _вложенной_ модальности: оценка $diamond p$ внутри $square$ смотрит на достижимость "второго уровня".
]

#example[Три мира и $square (p -> q)$][
  Миры $w_0, w_1, w_2$; достижимость: $w_0 -> w_1, w_0 -> w_2, w_1 -> w_2, w_2 -> w_2$.
  Оценка: $V(p) = {w_2}, V(q) = {w_1, w_2}$.
  Проверим формулу $square (p -> q)$ в мире $w_0$.
  + Из $w_0$ видны $w_1$ и $w_2$.
  + В $w_1$ атом $p$ ложен, поэтому импликация $p -> q$ истинна.
  + В $w_2$ истинны оба атома, и $p -> q$ истинна.
  + Импликация истинна во всех достижимых мирах: $M, w_0 models square (p -> q)$.
  Для сравнения, $square p$ в $w_0$ ложна: в $w_1$ атом $p$ не выполнен.
  А $diamond (p and q)$ истинна: свидетель --- мир $w_2$.
]

#remark[Пустая достижимость][
  Если из мира $w$ не достижим ни один мир, то $M, w models square B$ выполнено _вакуумно_ --- нет ни одного мира, опровергающего $B$.
  А $M, w models diamond B$ ложно --- нет ни одного мира, подтверждающего $B$.

  Это тонкость с пустыми мирами, которую устраняет свойство серийности (ниже): требование, чтобы у каждого мира был хотя бы один достижимый.
]

Формула $A$ _валидна во фрейме_ $F$ ($F models A$), если она истинна в _каждой_ модели на $F$.
Валидность на уровне фреймов --- основное понятие: аксиома соответствует целому классу фреймов.

Чтобы из формул строить доказательства, нужна система вывода.
Минимальная _нормальная_ модальная логика $K$ задаётся одной аксиомой и одним правилом:
$
  square (p -> q) -> (square p -> square q) .
$
- аксиома $K$ читается так: "если необходимо $p -> q$, то из необходимости $p$ следует необходимость $q$".
- правило необходимости: из выводимости $A$ выводится $square A$ (доказанное утверждение необходимо).

Нормальными называют логики, замкнутые относительно правила необходимости и содержащие аксиому $K$.
Сама $K$ --- наименьшая из них, все остальные получаются добавлением аксиом.
Читается аксиома $K$ на примере: если необходимо, что дождь влечёт мокрую землю, и необходимо, что идёт дождь, то необходимо, что земля мокрая.

== Свойства отношения и аксиомы

Главное содержание модальной логики --- _соответствие_ между свойствами отношения достижимости и аксиомами.
Каждая аксиома ровно выражает одно свойство $R$, но на уровне фреймов.

#theorem[Соответствие свойства и аксиомы][
  Схема в правой колонке валидна во фрейме $F$ тогда и только тогда, когда отношение $R$ фрейма обладает свойством из левой колонки:

  - _Серийность_ ($forall u exists v: R(u, v)$) --- у каждого мира есть хотя бы один достижимый.
    Соответствует аксиоме $D$: $square p -> diamond p$.
  - _Рефлексивность_ ($forall w: R(w, w)$) --- каждый мир виден из самого себя.
    Соответствует аксиоме $T$: $square p -> p$.
  - _Симметричность_ ($R(u, v) -> R(v, u)$) --- достижимость взаимна.
    Соответствует аксиоме $B$: $p -> square diamond p$.
  - _Транзитивность_ ($R(u, v) and R(v, w) -> R(u, w)$) --- два шага склеиваются в один.
    Соответствует аксиоме $4$: $square p -> square square p$.
  - _Евклидовость_ ($R(w, u) and R(w, v) -> R(u, v)$) --- если из $w$ видны $u$ и $v$, то $u$ и $v$ видят друг друга.
    Соответствует аксиоме $5$: $diamond p -> square diamond p$.
]

#proof[Доказательство (на примере $T$)][
  Докажем соответствие в обе стороны на примере рефлексивности и аксиомы $T$.

  Покажем сначала, что из рефлексивности $R$ следует схема $T$.
  Пусть $R$ рефлексивно, и в модели на $F$ истинно $square p$ в мире $w$.
  По рефлексивности выполнено $R(w, w)$.
  Поэтому из истинности $square p$ в $w$ следует истинность $p$ в $w$.
  Схема $square p -> p$ истинна.

  Обратно, пусть $R$ не рефлексивно: есть мир $w$ с $not R(w, w)$.
  Возьмём оценку $V(p) = W setminus {w}$ (атом $p$ истинен всюду, кроме $w$).
  Тогда $M, w models square p$ (все достижимые миры удовлетворяют $p$ --- их может не быть вовсе).
  Но $M, w nmodels p$: схема $T$ ложна.

  Остальные аксиомы проверяются аналогичной конструкцией.
]

#example[Рефлексивность и аксиома $T$][
  Фрейм с мирами $w_0, w_1$ и рефлексивной достижимостью: $R = {(w_0, w_0), (w_1, w_1)}$.
  Проверим аксиому $T$ --- схему $square p -> p$ --- в мире $w_0$.

  При оценке $V(p) = {w_1}$ формула $square p$ в $w_0$ ложна: достижимый из $w_0$ мир $w_0$ сам опровергает $p$.
  Импликация $square p -> p$ истинна: посылка ложна.
  При оценке $V(p) = {w_0, w_1}$ формула $square p$ в $w_0$ истинна, и $p$ истинна там же --- импликация снова истинна.

  Рефлексивность работает в общем случае: мир $w$ достижим из себя, и посылка $square p$ влечёт $p$ в $w$.
  Без петли $(w_0, w_0)$ схема опровергается: при оценке $V(p) = {w_1}$ посылка $square p$ в $w_0$ истинна вакуумно, а заключение $p$ ложно.
]

#example[Транзитивность и аксиома 4][
  Фрейм с мирами $w_0, w_1, w_2$ и транзитивным отношением: $R = {(w_0, w_1), (w_1, w_2), (w_0, w_2)}$.
  Оценка $V(p) = {w_1, w_2}$: атом $p$ ложен только в $w_0$.
  В $w_0$ формула $square p$ истинна: из $w_0$ видны $w_1$ и $w_2$, в обоих $p$ выполнен.
  И $square square p$ истинна в $w_0$: из $w_1$ виден $w_2$ с $p$, из $w_2$ не видно ничего, и $square p$ там истинна вакуумно.
  Схема $square p -> square square p$ в $w_0$ выполнена.

  Уберём переход $w_0 -> w_2$: отношение $R = {(w_0, w_1), (w_1, w_2)}$ больше не транзитивно.
  Та же оценка $V(p) = {w_1, w_2}$.
  В $w_0$ формула $square p$ по-прежнему истинна: из $w_0$ виден только $w_1$, и в нём $p$ выполнен.
  Но $square square p$ в $w_0$ ложна: из $w_1$ виден $w_2$, где $p$ не выполнен, значит, $square p$ в $w_1$ ложна.
  Схема аксиомы $4$ опровергнута ровно там, где два шага не склеились в один.
]

#remark[Почему именно фреймы][
  На уровне отдельной модели соответствие ложно: $square p -> p$ может быть истинна в модели, где $R$ не рефлексивно.
  Пример: $W = {w}, R = emptyset, V(p) = {w}$.
  Тогда $M, w models square p$ вакуумно.
  И $M, w models p$.
  Схема $square p -> p$ истинна, хотя $R$ не рефлексивно.

  Соответствие выполняется, когда мы квантуем по всем оценкам, то есть по всем моделям на фрейме.
]

#remark[Почему "евклидовость"][
  Название --- по аналогии с аксиомой Евклида "равные третьему равны между собой": если из $w$ видны $u$ и $v$, то $u$ и $v$ видят друг друга.
  Для отношения эквивалентности (рефлексивного, симметричного и транзитивного) евклидовость выполняется автоматически.
]

Каждая строка теоремы --- это уже отдельная система.
$K$ плюс аксиома $D$ --- система $D$ (логика серийных фреймов).
$K$ плюс $T$ --- система $T$ (логика рефлексивных фреймов).
Добавив к $T$ аксиому $B$, получаем систему $B$.
Добавив $4$, получаем $S_4$.
Добавив $5$, получаем $S_5$.

Так системы собираются из аксиом как из кубиков.
Следующий раздел складывает все кубики в одну картину.

== Модальные системы и куб модальностей

_Модальная система_ --- это логика, заданная аксиомами: берём $K$ и добавляем схемы.
Чем больше аксиом, тем сильнее логика --- тем больше формул в ней выводимо.
Систем бесконечно много: аксиом можно добавлять самыми разными способами, и почти каждая добавка даёт новую логику.
Ниже --- избранные системы, которые заслужили собственные имена.
Другие существуют, и часть из них мы увидим на кубе.

#table(
  columns: (auto, auto, 1fr, 1fr),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header[Система][Добавка к $K$][Класс фреймов][Типичное чтение],
  [$K$], [---], [все фреймы], [минимальная логика],
  [$D$], [аксиома $D$], [серийные], [обязанность, убеждение],
  [$T$], [аксиома $T$], [рефлексивные], [необходимость, знание],
  [$B$], [$T + B$], [рефлексивные и симметричные], [редко сама по себе],
  [$S_4$], [$T + 4$], [рефлексивные и транзитивные (предпорядки)], [знание, время],
  [$S_5$], [$T + 5$], [отношения эквивалентности], [полное знание],
)

$S_5$ допускает несколько равносильных заданий.
Это $K + T + 5, K + T + B + 4, K + T + B + 4 + 5$ --- все они дают одну и ту же логику.

Разберём каждую систему отдельно: что она добавляет к $K$, каким фреймам соответствует и где её читают.

=== Система $K$: минимум

$K$ --- наименьшая нормальная логика: она есть в любой другой, и её аксиома --- это просто правило переноса необходимости через импликацию.
Аксиома $K$ называется _аксиомой распределения_: $square$ распределяется по импликации.
Без неё необходимость вела бы себя как произвольный одноместный оператор без внутренней логики.

=== Система $D$: серийность

Добавив к $K$ аксиому $D$ ($square p -> diamond p$), получаем логику серийных фреймов: у каждого мира есть хотя бы один достижимый.
Это устраняет вакуумную истину $square$: если что-то необходимо, то найдётся мир, где оно истинно.
Деонтическое чтение: $O p -> P p$ --- "если $p$ обязательно, то $p$ разрешено".
Обязанность без разрешения --- деспотия.

=== Система $T$: рефлексивность

$T$ --- это $K$ плюс аксиома $T$ ($square p -> p$): необходимое истинно.
Ей соответствуют рефлексивные фреймы: каждый мир виден из самого себя.
Алетическое чтение: если $p$ необходимо, то $p$ просто истинно --- "2 + 2 = 4" необходимо, и 2 + 2 = 4.
Эпистемическое чтение: если агент знает, что $p$, то $p$ истинно --- знание не бывает ложным.

=== Система $B$: симметричность

$B$ (в честь Брауэра) --- это $T$ плюс аксиома $B$ ($p -> square diamond p$): если $p$ истинно, то необходимо, что $p$ возможно.
Ей соответствуют рефлексивные симметричные фреймы.
Сама по себе система $B$ встречается редко, но это важная ступень: вместе с транзитивностью она даёт $S_5$.

=== Система $S_4$: транзитивность

$S_4$ --- это $T$ плюс аксиома $4$.
Аксиома $4$ --- это $square p -> square square p$.
Рефлексивные транзитивные фреймы, то есть _предпорядки_.
Эпистемическое чтение: $K p -> K K p$ --- _позитивная интроспекция_: "если знаю, то знаю, что знаю".
Временное чтение: "всегда всегда $p$" --- то же, что "всегда $p$": цепочка времени не уводит в сторону.

=== Система $S_5$: евклидовость

$S_5$ --- вершина куба: $T$ плюс аксиома $5$.
Аксиома $5$ --- это $diamond p -> square diamond p$.
Равносильное задание --- $T + B + 4$.
Ей соответствуют ровно отношения эквивалентности: мир раскладывается на классы эквивалентности, и $square$ с $diamond$ становятся кванторами по одному классу.
Эпистемическое чтение добавляет _негативную интроспекцию_: $not K p -> K not K p$ --- "если не знаю, то знаю, что не знаю".
$S_5$ --- стандартная логика знания: агент полностью прозрачен для самого себя.

Включения между системами образуют картину, которую называют _кубом модальностей_:

#figure(
  modal-cube,
caption: [Куб модальностей: системы, порождённые аксиомами $T, B, 4$.],
) <fig:modal-cube>

Читается куб так: каждая вершина --- система $K$ плюс своё подмножество аксиом $\{T, B, 4\}$.
Три оси --- три аксиомы, поэтому вершин $2^3 = 8$.
Ребро вверх --- добавление одной аксиомы, так что все включения на кубе --- настоящие и соседние системы отличаются ровно одной аксиомой.

Известные системы занимают четыре вершины: $T$, $B$ (это $T + B$).
$S_4$ --- это $T + 4$.
$S_5$ --- это $T + B + 4$ (вспомните равносильное задание выше).
Серые вершины $K B, K_4, K B_4$ --- такие же нормальные логики, просто без привычных имён.
Точка $D$ стоит на ребре $K -> T$.
Аксиома $D$ выводима из $T$ (рефлексивность влечёт серийность), поэтому $D subset T$.
Но сама $D$ --- не ось куба.

#remark[Четвёртая ось и полный куб][
  Аксиомы $T$, $B$, $4$ --- не единственные "переключатели": аксиома $5$ дала бы четвёртую ось и 4-мерный куб с $2^4 = 16$ вершинами.
  А систем вообще бесконечно много.
  Между $S_4, S_5$, например, лежит бесконечно много промежуточных логик.
  Это $S_4.2, S_4.3$ и их обобщения.
  Куб --- это карта известных систем, и исчерпывающим перечнем он не служит.
]

#remark[Всё ли описывается фреймами][
  Соответствие "аксиома --- свойство отношения" работает не для всякой аксиомы.
  Есть нормальные логики, которым не соответствует никакой класс фреймов (их называют _неканоническими_), --- но их построение требует аппарата далеко за пределами этого курса.
  Кроме того, бывают _не-нормальные_ логики --- без правила необходимости или без аксиомы $K$.
  Таковы, например, ранние системы Льюиса $S_1, S_2, S_3$.
  Большинство логик, которые встречаются на практике, --- нормальные и фреймовые.
]

=== Как выбрать систему

Выбор системы --- это выбор аксиом, а значит --- выбор свойств достижимости и чтения операторов.
Алгоритм выбора прост:

+ Решите, что означают $square, diamond$ в вашей задаче: необходимость, знание, убеждение, обязанность, время.
+ Сформулируйте принципы, которые для этого чтения обязаны выполняться.
+ Соберите систему из соответствующих аксиом.

Примеры готовых сборок:
- _Знание_: истинность ($T$) и позитивная интроспекция ($4$) дают $S_4$.
  Полное знание ($5$) --- $S_5$.
- _Убеждение_: не обязано быть истинным, но непротиворечиво ($D$), с интроспекциями ($4$, $5$) --- логика $K D 4 5$.
- _Обязанность_: $D$ (обязан --- значит разрешено), без $T$: обязанность не гарантирует факт.
- _Время_: транзитивность ($4$) как минимум.
  Линейность или ветвление будущего --- темпоральные логики ниже.

Чем больше аксиом, тем сильнее система: больше выводимых формул, но меньше моделей.
Выбор --- это компромисс между силой логики и адекватностью чтению.

== Задачи модальной логики

У каждой логики есть свои задачи, вокруг которых "всё крутится": в пропозициональной это SAT (глава @chap:sat), в логике первого порядка --- выполнимость и общезначимость (глава @chap:fol).
В модальной логике задач три:

- _Выполнимость в модели_: есть ли мир $w$, где $M, w models A$?
  В конечной модели это решается обходом за полиномиальное время --- этим занимается model checking (ниже).
- _Валидность во фрейме_: выполняется ли $A$ во всех моделях на фрейме $F$?
- _Общезначимость в системе_: валидна ли $A$ во всех фреймах класса, заданного системой (например, во всех рефлексивных)?

Сложность общезначимости и выполнимости --- посередине между пропозициональной и первопорядковой:
для $K, T, S_4, S_5$ задача выполнимости PSPACE-полна#footnote[R. E. Ladner. "The Computational Complexity of Provability in Systems of Modal Propositional Logic". SIAM Journal on Computing, 1977.].
Это сложнее NP-полного SAT, но всё ещё разрешимо, тогда как логика первого порядка неразрешима вообще.

Алгоритмы для модальной логики:

- _Табличный метод_ (tableau): пытаемся построить мир, где формула истинна, применяя правила к каждой подформуле.
  Модальные правила создают новые миры: $diamond A$ требует свежий достижимый мир с $A$.
  $square A$ требует $A$ во всех уже известных достижимых мирах.
  Для нормальных логик достаточно конечных моделей (свойство конечной модели), поэтому поиск всегда завершается.
- _Перевод в логику первого порядка_: семантика Крипке записывается формулами первого порядка (мир --- элемент, $R$ --- отношение, $square$ --- квантор по достижимым).
  Для $K$ перевод сохраняет выполнимость, и солвер может перепоручить задачу первопорядковому решателю.
- _Символьные методы_ (BDD) --- для темпорального model checking, о них ниже.

#example[Модальная формула как формула первого порядка][
  Формула $square diamond p$ в мире $w$ переводится как
  $ forall u: R(w, u) -> exists v: (R(u, v) and p(v)) . $
  Читается: из любого мира, достижимого из $w$, достижим мир, где $p$ истинно.
  Свободная переменная $w$ --- тот мир, в котором формулу проверяют.
  Символы $R$ и $p$ становятся обычными предикатами, и задача о модальной формуле становится задачей первого порядка.
]

Солверы и инструменты:

- Табличные и переводящие: LoTREC, Spartacus (перевод в первый порядок + SPASS), KSP, InKreSAT, MleanCoP.
- Model checkers: NuSMV, SPIN --- про них в конце главы.
- Доказательные ассистенты: модальные логики формализованы в Coq и Lean.

Приложения модальной логики выходят далеко за пределы model checking:

- _Эпистемическая логика_: знание и убеждение в мультиагентных системах.
  Логика BAN#footnote[M. Burrows, M. Abadi, R. Needham. "A Logic of Authentication". ACM Transactions on Computer Systems, 1990.] анализирует протоколы аутентификации:
  что именно знает каждый участник после обмена сообщениями.
- _Деонтическая логика_: нормы, обязанности и разрешения в праве и контрактах.
- _Логика доказуемости_ ($G L$): $square p$ читается "формула $p$ доказуема в арифметике".
  Аксиома Лёба формализует вторую теорему Гёделя о неполноте
  #footnote[M. H. Löb. "Solution of a Problem of Leon Henkin". Journal of Symbolic Logic, 1955; K. Gödel. "Über formal unentscheidbare Sätze der Principia Mathematica und verwandter Systeme". Monatshefte für Mathematik und Physik, 1931.].
- _Темпоральные базы данных_: стандарт SQL:2011
  #footnote[ISO/IEC 9075:2011. "Information technology --- Database languages --- SQL". 2011.] хранит периоды времени, а запросы "когда было истинно" --- это темпоральные операторы.

== Знание, обязанность, убеждение

Пара $square, diamond$ --- не только "необходимо" и "возможно": достаточно переопределить, что считать "мирами" и "достижимостью", и та же рамка получает новое содержательное чтение.
Схоластика различала необходимые и случайные истины, Кант --- аналитические и синтетические.
Модальная логика делает такие различения формальными.

Аксиомы предыдущего раздела --- точный логический отпечаток каждого чтения:

- *Алетические модальности* --- необходимость и возможность.
  Философский источник --- Лейбниц: необходимо истинно то, что верно во всех _возможных мирах_.
  Аксиома $T$ обязательна: необходимое истинно.
- *Эпистемические модальности* --- знание: $K p$ читается "агент знает, что $p$".
  Аксиома $T$ ($K p -> p$) --- знание истинно.
  Этим знание отличается от убеждения.
  Аксиома $4$ ($K K p -> K p$) --- _позитивная интроспекция_: "если знаю, то знаю, что знаю".
  Аксиома $5$ ($not K p -> K not K p$) --- _негативная_: "если не знаю, то знаю, что не знаю".
  Обе описывают рационального агента.
- *Деонтические модальности* --- обязанность и разрешение: $O p$ ("$p$ обязательно").
  $P p$ --- "$p$ разрешено".
  Операторы двойственны, как $square$ и $diamond$: $O p equiv not P not p$ --- "обязанность" значит "не разрешено обратное".
  Вместо $T$ действует $D$: $O p -> P p$ (если обязан, то разрешено).
  Но $O p -> p$ неверно: обязанность не гарантирует факт.
- *Доксастические модальности* --- убеждение: $B p$ ("агент полагает, что $p$").
  Выполняется $D$, но не $T$: убеждения могут быть ложными.

#example[Чтения в деле][
  Формула $square p -> p$ истинна при чтении "знание": если агент знает, что идёт дождь, то дождь идёт.
  При чтении "обязанность" та же формула абсурдна: из "водитель обязан остановиться" не следует, что машина стоит.
  При чтении "убеждение" она тоже запрещена: можно искренне верить в ложное.
]

*Темпоральная логика* --- ещё одно чтение той же пары: $square$ --- "всегда", $diamond$ --- "когда-нибудь", а "миры" --- моменты времени.
Именно это чтение доведено до инженерного инструмента --- model checking --- и разбирается в следующих разделах.

== Время как модальность

Для реактивных систем "мир" --- это _состояние_ программы или аппаратуры, а "достижимость" --- один шаг выполнения.
Модель Крипке --- это модальная модель, в которой вместо оценки $V$ на мирах используется разметка состояний.
Это одно и то же, записанное по-разному: $V(p) = {s mid(|) p in L(s)}$.

#definition[Модель Крипке][
  Модель Крипке --- тройка $M = (S, R, L)$, где:
  - $S$ --- конечное множество _состояний_.
  - $R subset.eq S times S$ --- отношение _переходов_ (обычно серийное: у каждого состояния есть хотя бы один следующий шаг).
  - $L : S -> cal(P)("AP")$ --- _разметка_: каждому состоянию приписано множество атомарных утверждений (тот самый набор $"AP"$), истинных в нём.
]

- Состояние --- снимок системы (значения всех переменных).
- Переход --- один шаг выполнения.
- Разметка --- что истинно в этом состоянии.
_Путь_ (_path_) --- бесконечная последовательность состояний $pi = s_0, s_1, s_2, dots$.
Каждый шаг подчинён условию $(s_i, s_(i+1)) in R$ для всех $i$.
Движение по переходам без остановки.

#example[Модель Крипке: светофор][
  Состояния: $G$ (зелёный), $Y$ (жёлтый), $R$ (красный).
  Переходы: $G -> Y -> R -> G$ (цикл длины 3).

  #align(center, kripke-traffic)
  Атомарные утверждения: $"green"$, $"yellow"$, $"red"$.
  Разметка: $L(G) = {"green"}, L(Y) = {"yellow"}, L(R) = {"red"}$.
  Свойство "после зелёного рано или поздно красный" записывается формулой $square ("green" -> diamond "red")$ --- "всегда, если зелёный, то когда-нибудь красный".
]

=== LTL: линейная темпоральная логика

_LTL_ (Linear Temporal Logic) рассматривает _один путь_ за раз: формула истинна или ложна на бесконечной последовательности состояний.

#definition[Синтаксис LTL][
  Формулы LTL:
  - Атомарное утверждение $p in "AP"$ --- формула.
  - Если $phi, psi$ --- формулы, то $not phi, phi and psi, phi or psi$ --- формулы.
  - Если $phi, psi$ --- формулы, то $circle phi$ --- формула ("$phi$ в следующем состоянии").
  - Если $phi, psi$ --- формулы, то $phi U psi$ --- формула ("$phi$ до тех пор, пока $psi$").
]

Оператор $U$ (until, "до тех пор, пока") --- основной темпоральный оператор: $phi U psi$ --- "сначала $phi$, и в какой-то момент наступает $psi$".
Остальные --- производные:
$
  diamond phi & = "true" U phi, \
   square phi & = not diamond not phi.
$
$diamond phi$ --- "когда-нибудь $phi$" (в будущем, включая сейчас).
$square phi$ --- "всегда $phi$" (во всех состояниях пути).

#definition[Семантика LTL][
  Пусть $pi = s_0, s_1, s_2, dots$ --- путь.
  $pi^i$ --- хвост пути с $s_i$.
  Запись $pi models phi$ означает "формула $phi$ истинна на пути $pi$".
  $
              pi models p & iff p in L(s_0), \
        pi models not phi & iff pi nmodels phi, \
    pi models phi and psi & iff pi models phi and pi models psi, \
     pi models phi or psi & iff pi models phi or pi models psi, \
     pi models circle phi & iff pi^1 models phi.
  $
  Для оператора $U$: $pi models phi U psi$ тогда и только тогда, когда
  существует $j$ такое, что $pi^j models psi$,
  и $forall i < j: pi^i models phi$.
  По-русски: на пути сначала (сколько нужно) идёт $phi$, и в момент $j$ наступает $psi$.
  Пример: $"yellow" U "red"$ --- "горит жёлтый, и в какой-то момент загорается красный".
]

#example[Свойства в LTL][
  Светофор: $square ("green" -> diamond "red")$ --- "после зелёного когда-нибудь красный".
  Последовательность фаз: $square ("green" -> circle "yellow")$ --- "после зелёного сразу жёлтый" (на каждом пути светофора это верно).
  Взаимная блокировка: $square not ("deadlock"_1 and "deadlock"_2)$ --- "никогда не будет блокировки".
  Ответ на запрос: $square ("request" -> diamond "response")$ --- "каждый запрос получает ответ".
]

#example[Проверка LTL-формулы на пути][
  Путь светофора: $pi = G, Y, R, G, Y, R, dots$ --- бесконечный цикл фаз.
  Проверим формулу $square ("green" -> circle "yellow")$.
  В состоянии $G$ следующий шаг --- $Y$, импликация истинна.
  В состояниях $Y$ и $R$ посылка ложна, импликация истинна вакуумно.
  Формула истинна на всём пути.

  Проверим формулу $square ("green" -> diamond "red")$.
  Из $G$ через два шага наступает $R$, импликация истинна.
  В состояниях $Y$ и $R$ посылка ложна.
  Формула тоже истинна на пути.

  А формула $square circle "red"$ ложна: в состоянии $G$ следующий шаг --- $Y$, а не $R$.
]

Полезны две эквивалентности-развёртки (они же --- неподвижные точки):
$
   square phi & = phi and circle square phi, \
  diamond phi & = phi or circle diamond phi.
$
"Всегда" значит "сейчас и всегда в следующем состоянии".
"Когда-нибудь" --- "сейчас или когда-нибудь в следующем".

Для model checking модель снабжается начальным состоянием $s_0$.
LTL-формула выполняется на $M$, если она выполняется на _всех_ путях из $s_0$:
$M models phi$.

=== CTL: темпоральная логика ветвящегося времени

_CTL_ (Computation Tree Logic) рассматривает _все_ пути сразу: кванторы пути $A$ ("на всех путях") и $E$ ("существует путь") предшествуют темпоральному оператору.

#definition[Синтаксис CTL][
  Формулы CTL:
  - Атомарное утверждение --- формула.
  - Булевы комбинации формул --- формулы.
  - Если $phi, psi$ --- формулы, то $A circle phi, E circle phi, A (phi U psi), E (phi U psi)$ --- формулы.
  Производные: $A diamond phi equiv A ("true" U phi)$ ("на всех путях когда-нибудь").
  $E diamond phi equiv E ("true" U phi)$ ("существует путь, где когда-нибудь").
  $A square phi equiv not E diamond not phi$.
  $E square phi equiv not A diamond not phi$.
]

#definition[Семантика CTL][
  Формула CTL оценивается в _состоянии_ модели, и пути входят в оценку только через кванторы $A$ и $E$.
  Отношение $M, s models phi$ определяется индукцией по формуле.
  Клаузы для $circle$:
  $
    M, s models A circle phi & iff forall s' in R(s): M, s' models phi, \
    M, s models E circle phi & iff exists s' in R(s): M, s' models phi .
  $
  Для $U$:
  - $M, s models A (phi U psi)$ --- на _всех_ путях, начинающихся в $s$,
    выполняется $phi U psi$ (в смысле LTL: сначала $phi$, затем $psi$).
  - $M, s models E (phi U psi)$ --- существует путь из $s$,
    на котором выполняется $phi U psi$.
]

$A$ читается "во всех возможных будущих", $E$ --- "в некотором возможном будущем".
Например, $A circle "resp"$ --- "в любом следующем состоянии --- ответ".
А $E circle "resp"$ --- "есть хотя бы один следующий шаг с ответом".

Различие между $A$ и $E$: $A diamond "resp"$ --- "ответ гарантирован при любом ходе системы".
$E diamond "resp"$ --- "существует ход, приводящий к ответу".
Первую формулу проверяет model checking как свойство системы, вторую --- как возможность.

#example[LTL против CTL][
  Свойство "всегда возможно перезапуск" --- $A square E diamond "restart"$: в каждом состоянии любого пути существует продолжение, ведущее к перезапуску.
  Это типично CTL-свойство: LTL его не выражает, потому что LTL не умеет говорить "существует путь" внутри пути.
  Обратно, LTL выражает $square diamond p -> square diamond q$ --- справедливость (fairness).
  "Если $p$ встречается бесконечно часто, то и $q$ встречается бесконечно часто" --- это CTL не выражает.
  Выразительные силы LTL и CTL несравнимы: есть свойства, выразимые только в одной из логик.
]

#remark[Истинность CTL в состоянии][
  CTL-формула выполняется в _состоянии_ модели.
  $M, s models E square phi$ --- "существует путь из $s$, на котором $phi$ всегда истинна".
  $M, s models A square phi$ --- "на всех путях из $s$ формула $phi$ всегда истинна".
]

== Алгоритмы model checking

Проверка, выполняется ли CTL-формула в модели, --- это разметка состояний подформулами: от атомарных к составным, снизу вверх.
Ключ к операторам --- неподвижные точки.

_Неподвижная точка_ --- множество $X$, которое не меняется при некотором преобразовании.
Для $E (phi U psi)$ преобразование таково:
$X -> psi or (phi and E circle X)$.
Наименьшее множество, остающееся на месте, --- это в точности $E (phi U psi)$.
Для $E square phi$ --- наоборот, наибольшая неподвижная точка.

#definition[Неподвижные точки CTL][
  Для $E (phi U psi)$ выполняется наименьшая неподвижная точка:
  $ E (phi U psi) equiv psi or (phi and E circle E (phi U psi)) . $
  Для $E square phi$ --- наибольшая:
  $ E square phi equiv phi and E circle E square phi . $
  Для $A (phi U psi)$:
  $ A (phi U psi) equiv psi or (phi and A circle A (phi U psi)) . $
]

#example[Labeling-алгоритм для CTL][
  Проверим $E (phi U psi)$ в модели
  $M = (S, R, L)$.
  Обозначим $"pre"_exists (X) = {s in S : exists s' in R(s), s' in X}$ --- состояния, из которых одним шагом можно попасть в $X$ ("пре-образ по существованию").
  Алгоритм:
  + $Y := {s : s models psi}$.
  + Повторять, пока не стабилизируется: $Y := Y union ({s : s models phi} inter "pre"_exists (Y))$.
  Полученное $Y$ --- в точности состояния, где истинно $E (phi U psi)$.
  Для $E square phi$ --- двойственно:
  $Y := {s : s models phi}$.
  Затем $Y := Y inter "pre"_exists (Y)$ до стабилизации.
]

Для $A square phi$ используется пре-образ "по всем преемникам":
$"pre"_forall (X) = {s : forall s' in R(s), s' in X}$.

Свойства живости ($diamond phi, phi U psi$) --- наименьшие неподвижные точки.
Свойства безопасности ($square phi$) --- наибольшие.
Различие отражает природу: безопасность опровергается конечным префиксом ("вот здесь оба процесса в критической секции"), живость --- только бесконечным путём ("запрос висит вечно, ответа нет").

#example[Labeling на светофоре][
  Проверим в модели светофора формулу $A square E diamond "red"$: "на любом пути всегда есть возможность дойти до красного".
  Начнём с labeling для $E diamond "red" = E ("true" U "red")$ --- наименьшей неподвижной точки.
  Начальное множество --- состояния с $"red"$.
  Это $\{R\}$.
  #table(
    columns: (auto, auto),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    table.header([Шаг], [Множество $Y$]),
    [$0$], [$\{R\}$],
    [$1$], [$\{Y, R\}$],
    [$2$], [$\{G, Y, R\}$],
    [$3$], [$\{G, Y, R\}$],
  )
  На первом шаге добавляется $Y$: из него за один шаг достигается $R$.
  На втором --- $G$: из него за один шаг достигается $Y$.
  Дальше множество не растёт: $E diamond "red"$ истинна во всех трёх состояниях.
  Тогда $A square E diamond "red"$ истинна.
  В каждом состоянии каждого пути выполняется $E diamond "red"$.
  Формула подтверждает свойство светофора: из любого состояния красный достижим.
]

#remark[LTL через автоматы Бюхи][
  Model checking LTL сложнее, чем CTL: формула говорит обо всех путях, и прямой labeling не работает.
  Стандартный путь --- три шага:
  + Построить автомат Бюхи $A_(not phi)$ для отрицания свойства: он принимает ровно те пути, где $not phi$ истинна.
  + Построить синхронное произведение $M times A_(not phi)$ модели и автомата:
    состояния --- пары (состояние модели, состояние автомата), согласованные по разметке, переходы --- одновременный шаг обоих.
  + Искать в произведении путь, бесконечно часто попадающий в принимающие состояния.
  Если такого пути нет --- свойство выполняется.
  Если есть --- это контрпример: конкретное ошибочное поведение системы.
]

== Model checking на практике

Model checking --- метод автоматической верификации конечных систем.
Программа или аппаратный модуль моделируется как размеченная система переходов.
Это граф, где вершины --- состояния, рёбра --- переходы, а вершины размечены утверждениями о значениях переменных.

Практическая трудность --- взрыв пространства состояний: $n$ булевых переменных дают $2^n$ состояний.
40 переменных --- уже около $10^12$ состояний, а реальные системы имеют тысячи переменных.
Современные model checker'ы (NuSMV, SPIN) справляются с $10^(20)$ и более состояниями.

Способы борьбы с взрывом делятся на два лагеря:

- *Символьные* (NuSMV): множества состояний представляются булевыми формулами в виде BDD (Binary Decision Diagrams) --- компактных ациклических графов, кодирующих булевы функции.
  Операции над множествами (объединение, пересечение, проверка пустоты) выполняются как операции над BDD, а общие подструктуры формул разделяются, поэтому графы остаются маленькими.
  #footnote[E. M. Clarke, O. Grumberg, D. Peled. "Model Checking". 1999.]
- *Явные* (SPIN): состояния перебираются по одному, а взрыв обходится двумя приёмами --- редукцией частичного порядка
  (независимые переходы разных процессов можно переставлять, проверяется один представитель каждого класса эквивалентных путей)
  и битовым хэшированием (пройденные состояния кладутся в битовую таблицу по хэшу, память крошечная, но коллизии могут заставить пропустить состояние).
  #footnote[C. Baier, J.-P. Katoen. "Principles of Model Checking". 2008.]

#example[Взаимное исключение][
  Два процесса конкурируют за критическую секцию.
  Состояние --- пара положений процессов: $O$ (вне секции) или $C$ (в критической секции).
  Атомарные утверждения: $"crit"_1, "crit"_2$ ("процесс $i$ в критической секции").

  #align(center, mutex-states)

  Требование взаимного исключения --- $A square not ("crit"_1 and "crit"_2)$: на всех путях никогда не бывает, что оба процесса в критической секции одновременно.
  Model checker строит пространство состояний из всех комбинаций переменных процессов и проверяет формулу.
  Состояние $(C, C)$ --- то самое, где
  $"crit"_1 and "crit"_2$ истинно.
  В корректной модели оно недостижимо.
  Если же проверка находит путь в него, контрпример предъявляется как диагностика гонки.
]

#example[Безопасность: labeling для $A square not ("crit"_1 and "crit"_2)$][
  Состояния модели взаимного исключения: $"OO"$, $"OC"$, $"CO"$, $"CC"$.
  Первый символ --- положение первого процесса, второй --- второго: $O$ вне секции, $C$ в критической секции.
  Переходы: $"OO" -> "OC"$, $"OO" -> "CO"$, $"OC" -> "OO"$, $"CO" -> "OO"$.
  Labeling для наибольшей неподвижной точки $A square phi$.
  Здесь $phi = not ("crit"_1 and "crit"_2)$.
  + Начало: $Y = {"OO", "OC", "CO"}$ --- состояния, где $phi$ истинна.
    Состояние $CC$ в $Y$ не входит: в нём оба процесса в критической секции.
  + Шаг: $Y := Y inter "pre"_forall (Y)$.
    Из $OO$ оба перехода ведут в $Y$.
    Из $"OC"$ и $"CO"$ --- по одному переходу, тоже в $Y$.
    Состояние $CC$ остаётся вне $Y$ независимо от пре-образа.
  + Повторный шаг ничего не меняет: получена наибольшая неподвижная точка.
  Итог: формула $A square not ("crit"_1 and "crit"_2)$ истинна.
  Она истинна в состояниях $"OO"$, $"OC"$, $"CO"$.
  В начальном состоянии $OO$ взаимное исключение доказано.
  Состояние $CC$ свойство нарушало бы, но оно недостижимо из $OO$: контрпримера нет.
]

#remark[Хоар против Крипке][
  Тройки Хоара --- логика _состояний_: предусловие, постусловие, инвариант.
  Модели Крипке --- логика _поведения_: последовательности состояний, темпоральные операторы.
  Первые удобны для функций и циклов, вторые --- для реактивных систем: протоколы, аппаратура, распределённые алгоритмы.
  Абстрактная интерпретация (глава @chap:abstract-interp) соединяет оба мира: она вычисляет множества состояний, в которых свойство гарантированно выполняется.
]

== Итоги главы

Слово "истинно" прячет два разных смысла --- случайный и необходимый.
Ответ Крипке --- миры и достижимость: $square p$ истинно, когда $p$ верно во всех достижимых мирах, а $diamond p$ --- когда хотя бы в одном.
Фрейм задаёт геометрию достижимости, оценка --- что истинно в каждом мире, и на этой паре строится вся истинность модальных формул.

Свойства отношения достижимости записываются аксиомами: серийность, рефлексивность, симметричность, транзитивность и евклидовость порождают логики $D, T, B, S_4, S_5$.
Их включения складываются в куб модальностей с вершиной $S_5$ --- логикой отношений эквивалентности.
Выбор системы --- это выбор чтения операторов: знание, убеждение, обязанность или время диктуют разные аксиомы.

Вторая половина превратила метафору в инженерный инструмент: миры стали состояниями системы, достижимость --- шагом выполнения, а модальности --- словами "всегда" и "когда-нибудь".
Model checking проверяет такие свойства автоматически: CTL --- разметкой через неподвижные точки, LTL --- автоматами Бюхи, а взрыв пространства состояний сдерживается символьными представлениями вроде BDD.

Плата за выразительность --- сложность: выполнимость модальной логики PSPACE-полна, посередине между SAT и логикой первого порядка.
Всё это время истина оставалась классической --- два значения, привязанные к миру, --- и именно это допущение следующая глава поставит под вопрос.

#check[Проверьте себя][
  + Что такое мир Крипке и отношение достижимости, и как они задают истинность $square p$ и $diamond p$?
  + Какие свойства отношения достижимости записываются аксиомами $T$, $4$ и $B$?
  + Почему $S_5$ --- логика отношений эквивалентности, и что это значит для чтения модальностей?
  + Чем CTL отличается от LTL в model checking, и какими автоматами проверяется каждая?
  + Почему выполнимость модальной логики PSPACE-полна, а не NP-полна, как классический SAT?
]

#whats-next
Модальная логика описывает миры и достижимость.
Темпоральная логика --- её частный случай, где "миры" --- моменты времени.
В главе @chap:intuitionism мы сменим классическую семантику на конструктивную, где истина --- предъявленное доказательство.
== Упражнения

#tasklist("m32")[
  _Фреймы и системы аксиом._
  + Постройте фрейм из трёх миров, на котором $square p -> p$ (аксиома $T$) не валидна.
    Какое свойство отношения $R$ нарушено?
  + Какое свойство отношения соответствует аксиоме $D$: $square p -> diamond p$?
    Почему серийность устраняет вакуумную истину $square$?
  + Постройте модель Крипке для светофора с тремя состояниями.
    Запишите в LTL: "после зелёного когда-нибудь красный".
  + Запишите в LTL свойство "запрос всегда рано или поздно получает ответ".
    В чём отличие $square ("request" -> diamond "response")$ от
    $square diamond "response"$?
  + Проверьте labeling-алгоритмом, где истинно $E ("yellow" U "red")$ в светофоре.
  + Выпишите все семь включений между системами $K, D, T, B, S_4, S_5$.
    Подсказка: две цепочки сходятся в $S_5$.
  + Какая из систем $D, B, S_4, S_5$ --- логика отношений эквивалентности, а какая --- логика частичных предпорядков?
  + \* Назовите все восемь вершин куба с осями $T, B, 4$.
    Какие из них вам уже знакомы, а какие --- нет?
  + \* Почему выполнимость в модальной логике сложнее пропозициональной ($"NP"$-полной), но проще первопорядковой (неразрешимой)?
  + \* Аксиома $B$ ($p -> square diamond p$) валидна на симметричных фреймах.
    Постройте фрейм, где она не валидна, и объясните, какое свойство отношения нарушено.
  + \* Чем CTL-формула $A square E diamond "restart"$ отличается от любой LTL-формулы?
    Почему LTL не может выразить "существует путь"?
  + \* Почему свойства живости --- наименьшие неподвижные точки, а безопасности --- наибольшие?
    Свяжите с тем, чем опровергается каждое из них.
  + \* Опишите три шага model checking LTL через автоматы Бюхи.
    Что означает путь в синхронном произведении?

  + \* Проверьте табличным методом (tableau) для системы $K$:
    формула $square (p and q) -> (square p and square q)$ общезначима, а множество формул $diamond p$ и $square not p$ невыполнимо.
    Для первого постройте таблицу для отрицания формулы и покажите, что все ветви замкнуты.
    Для второго постройте таблицу и покажите, что единственная ветвь замкнута.
    Для каждого шага указывайте применяемое правило: пропозициональные правила, двойственность $not square A equiv diamond not A$, правило для $diamond$ (новый мир) и правило для $square$ (наследование в существующие миры).
]


#project[Проверка свойств безопасности и живости][
  Постройте CTL-чекер и проверьте на нём свойства безопасности и живости трёх систем.
  Model checking отвечает на вопрос, существует ли путь, где система входит в нежелательное состояние: оба процесса в критической секции, запрос без ответа, вечное ожидание.
  Свойства безопасности опровергаются конечным путём (оба процесса внутри), свойства живости --- только бесконечным (запрос висит вечно), и эта разница не деталь, а суть: перебор конечных путей вечный голод никогда не обнаружит, а проверка пустоты произведения --- обнаружит.
  Соберите инструмент из разметки через неподвижные точки и _проверки пустоты произведения_.

  #project-stage[Неподвижные точки][
    Реализуйте разметку состояний через неподвижные точки для CTL.
    Для $E (phi U psi)$ возьмите наименьшую неподвижную точку.
    Для $E square phi$ --- наибольшую.
    Добавьте пре-образы $"pre"_exists, "pre"_forall$.
  ]
  #project-stage[Системы][
    Соберите три системы: светофор, взаимное исключение двух процессов и "запрос/ответ" с вечно голодным запросом.
  ]
  #project-stage[Ключевой эксперимент][
    Для свойства живости постройте автомат Бюхи для отрицания вручную.
    Разберите случаи $square p, diamond p$.
    Покажите, что контрпример --- бесконечное лассо в произведении.
    Конечный перебор его не видит, а проверка пустоты --- видит.
  ]
  #project-stage[Два случая][
    Разделите два случая на одной системе: свойство безопасности, где контрпример --- конечный путь, и свойство живости, где он бесконечен.
    Предъявите оба контрпримера как трассы.
  ]
  #project-stage[Ловушка записи][
    Покажите, как легко обмануться в записи свойства.
    Сравните $square (p -> diamond q), square diamond q$ на одной системе.
    Объясните на контрпримере, где ваша формула соврала о вашем намерении.
  ]

  #project-outcome[
    работающий CTL-чекер с журналом "система $times$ свойство -> выполняется или контрпример (конечный путь или бесконечное лассо)", где видно, почему безопасность обнаруживается конечным перебором, а живость --- нет.
  ]
]



