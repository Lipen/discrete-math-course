// M14 --- SMT: Satisfiability Modulo Theories.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m15.typ": (
  congruence-closure-merge, dl-negative-cycle, dpll-t-architecture,
)

= SMT <chap:smt>

Инженер проверяет условие `x + y <= 10 and x > 5`: найдётся ли пара целых чисел, которая его выполняет?
Ответ есть --- например, $x = 6, y = 0$.
Но как получить этот ответ механически, не перебирая все пары?
Глава @chap:sat научила отвечать на такие вопросы для чисто булевых формул, но здесь переменные $x, y$ --- целые, а атом `x + y <= 10` --- _утверждение об арифметике_.

Закодировать условие в булеву формулу можно: представить числа наборами битов, перевести сложение в булевую схему, а неравенства --- в дизъюнкты.
Цена велика: булева переменная несёт один бит, и целое число занимает столько же переменных, сколько в нём битов: каждое действие над числами приходится собирать как схему из вентилей.
Умножение 64-битных чисел с переносами разрядов --- это тысячи вентилей, а перевод схемы в дизъюнкты растягивает её ещё в несколько раз.
Один оператор задачи оборачивается десятками тысяч дизъюнктов, и решатель тонет в _битовой пыли_, хотя вопрос был всего о двух простых неравенствах.
Дело в способе записи: арифметика переводится в булевы переменные поразрядно, и в такой записи её структура не видна.

Другой путь --- дать решателю понимать арифметику напрямую.
Тогда структура задачи сохраняется: условие `x + y <= 10` остаётся одним атомом, а решатель рассуждает, не перебирая значения.
Над целыми числами он выводит:
$ x > 5, x + y <= 10 arrow.r.double y <= 4 $
Логика высказываний не видит внутренней структуры атома: для неё `x + y <= 10` --- просто переменная $p$, которой надо как-то задать значение.
Причина --- в словаре языка: в нём нет ни сложения, ни сравнения, ни целых чисел, и атому не на что опереться, поэтому его значение назначается целиком, как у любой булевой переменной.

Логика предикатов из главы @chap:fol умеет говорить об объектах, но допускает любые интерпретации символов.
В чистой логике $+$ может означать любую бинарную операцию: в одной интерпретации --- "выбрать большее", в другой --- "перемножить".
Вопрос "найдётся ли пара целых чисел" теряет смысл, потому что ничто не заставляет переменную пробегать именно целые числа.
Нужен язык, где смысл символов закреплён: терм имеет тип, сложение интерпретируется как настоящее сложение целых, а массив читается и пишется по индексу, как память.

Многосортная логика даёт для этого точный язык: сорт переменной указывает её предметную область.
Запись $x : "Int"$ означает, что переменная обозначает целое число.
Запись $a : "Array"$ --- что переменная обозначает массив.
Типизированная формула отклоняется ещё до семантики, если сорта не сходятся.
Но язык сам по себе не даёт алгоритма: полная логика первого порядка неразрешима (глава @chap:decidability), и бесконечность арифметики нельзя обойти наивным перебором.

Выход --- разделение труда: булева логика и теория работают в одном цикле.
Булев решатель отвечает за связки и управляет поиском, а атомы теории --- такие, как `x + y <= 10`, --- передаёт процедуре, которая понимает арифметику.
Процедура проверяет предложенный набор атомов по своим правилам и, если находит противоречие, возвращает возражение.
Булев решатель учитывает возражение и продолжает поиск --- раунд за раундом --- пока формула не будет решена.

Такой способ рассуждения --- булев поиск плюс отдельные процедуры для арифметики и памяти --- называется _SMT_ (от англ. Satisfiability Modulo Theories, _выполнимость по модулю теорий_).
Как устроен такой солвер и почему это работает на практике, хотя полная логика первого порядка неразрешима?

#history-note[От разрозненных процедур к общему циклу][
  К началу 1970-х годов для отдельных теорий существовали собственные процедуры разрешения: для арифметики Пресбургера, для равенства, для линейных неравенств.
  Каждая процедура работала в своей области и не умела говорить с соседями: формула, смешивающая арифметику с равенством, не поддавалась ни одной из них.
  В 1979 году Грег Нельсон и Дерек Оппен показали, как соединить процедуры разных теорий в одну согласованную --- при условии, что теории делят только равенство.#footnote[G. Nelson, D. Oppen. "Simplification by Cooperating Decision Procedures". ACM Transactions on Programming Languages and Systems, 1979.]
  С этой работы начинается комбинирование теорий --- основа современных SMT-солверов.

  Второй толчок пришёл со стороны SAT.
  CDCL --- алгоритм обучения на конфликтах из главы @chap:sat --- позволил булевым решателям обрабатывать формулы с миллионами переменных.
  Возникла идея: пусть булев поиск руководит формулой, а процедуры теорий отвечают на его вопросы о наборах атомов.
  В 2006 году Роберт Ньивенхёйс, Альберт Оливерас и Чезаре Тинелли описали этот цикл в абстрактной форме и назвали его DPLL(T).
  #footnote[R. Nieuwenhuis, A. Oliveras, C. Tinelli. "Solving SAT and SAT Modulo Theories: From an Abstract Davis--Putnam--Logemann--Loveland Procedure to DPLL(T)". Journal of the ACM, 2006.]
  Название закрепилось, и архитектура стала стандартом: почти все SMT-солверы работают в этом цикле.

  Третий шаг --- стандартизация.
  Язык SMT-LIB дал общий формат входных данных, а соревнование SMT-COMP --- единый полигон для сравнения солверов.#footnote[S. Ranise, C. Tinelli. "The SMT-LIB Standard". 2004.]
  В 2008 году Леонардо де Моура и Николай Бьёрнер представили солвер Z3#footnote[L. de Moura, N. Bjørner. "Z3: An Efficient SMT Solver". TACAS, 2008.], который превратил SMT в индустриальный инструмент: верификация, символьное исполнение и синтез программ опираются на него каждый день.
]

#chapter-overview[
  Язык SMT объединяет многосортную логику первого порядка и теории: сорт переменной задаёт её предметную область, а теория сужает класс допустимых интерпретаций.
  После языка мы перейдём к теориям, важным для практики SMT: разностная логика, равенство с неинтерпретируемыми функциями, линейная арифметика, битовые векторы, теория массивов.
  У каждой теории своя процедура решения, и разбор этих процедур по очереди объясняет, почему теория разрешима:
  отрицательный цикл в графе разностей означает невыполнимость, а congruence closure сливает классы равных термов.
  Затем цикл DPLL(T) покажет, как один булев поиск оркеструет готовые процедуры теорий:
  они обмениваются кандидатами и возражениями, и один раунд конфликта прослеживается от присваивания до выученного дизъюнкта.
  Процедура Нельсона--Оппена объяснит, как соединить решатели разных теорий в одну согласованную процедуру, когда теории делят только равенство.
  В конце главы мы рассмотрим формат SMT-LIB и устройство реальных солверов: запрос читается на входном языке, а ответ солвера предсказывается по правилам теорий.

  После этой главы вы научитесь:
  + записывать задачи в многосортной логике с теориями арифметики, равенства и массивов;
  + объяснять архитектуру DPLL(T) и роль процедур теории в булевом поиске;
  + применять процедуры для разностной логики и замыкания по равенству;
  + объяснять, почему SMT работает на кванторно-свободных фрагментах;
  + читать запросы в формате SMT-LIB и применять SMT к верификации и символьному исполнению.
]

== Многосортная логика первого порядка

Глава о логике первого порядка обошлась одним сортом: все термы говорили об одном универсуме, и этого хватало.
Формула из вступления таким языком не записывается: она требует, чтобы одна переменная была числом, а другая --- массивом, и односортная логика этого различия не видит.
В ней терм $"read"(x, 0)$ собирается без возражений: подставить массив на место числа --- обычная операция, и семантика честно припишет терму значение.
Формула получает смысл, которого автор не вкладывал: число читается как массив, а смешение сортов не наказывается.
Тип --- это запрет, наложенный до семантики: сорта не дают терму собраться, если его части не согласованы, и бессмысленное выражение не существует в языке.

SMT работает с _многосортной_ логикой (от англ. many-sorted): у каждого терма есть сорт, и функция принимает аргументы строго определённых сортов.
_Сорт_ переменной задаёт её предметную область.
Запись $x : "Int"$ означает, что переменная обозначает целое число.
Запись $a : "Array"$ означает, что переменная обозначает массив.

Если функция применяется к аргументу не того сорта, формула отклоняется ещё до рассмотрения семантики.
Так и компилятор останавливается на ошибке типов, не запуская программу: вычислять нечего, потому что программа не собрана.

#definition[Многосортная сигнатура][
  *Многосортная сигнатура* $Sigma = (S, F)$ состоит из множества *сортов* $S$ и множества *функциональных символов* $F$.

  Сорта --- это типы объектов: булевы значения, целые числа, вещественные числа, массивы.
  Функциональные символы --- это операции и отношения: равенство, сложение, сравнение, чтение и запись.

  Каждый функциональный символ $f$ имеет *ранг* --- кортеж сортов
  $ "rank"(f) = (sigma_1, dots, sigma_n, sigma_(n+1)) $,
  показывающий, какие сорта аргументов он принимает и какой сорт возвращает.

  Символ без аргументов называется *константой*.
  Символ, возвращающий булево значение, называется *предикатом*.

  Всякая сигнатура содержит булев сорт с константами истины и лжи и предикат равенства для каждого сорта.
]

#example[Сигнатуры][
  - Арифметика натуральных чисел.
    Сорта: $S = {"Nat"}$.
    Символы: $F = {0, S, <, +, times}$.
  - Массивы.
    Сорта: $S = {"Array", "I", "E"}$.
    Символы: $F = {"read", "write"}$.

  Ранги символов:
  #table(
    columns: (auto, auto),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    table.header[Символ][Ранг],
    [$0$], [$"rank"(0) = ("Nat")$],
    [$S$], [$"rank"(S) = ("Nat", "Nat")$],
    [$<$], [$"rank"(<) = ("Nat", "Nat", "Bool")$],
    [$+$], [$"rank"(+) = ("Nat", "Nat", "Nat")$],
    [$times$], [$"rank"(times) = ("Nat", "Nat", "Nat")$],
    [$"read"$], [$"rank"("read") = ("Array", "I", "E")$],
    [$"write"$], [$"rank"("write") = ("Array", "I", "E", "Array")$],
  )

  Ранги и состав символов отсекают бессмысленные сочетания ещё до семантики.
  - Терм с неверным сортом аргумента: $"read"(x, i)$.
    Первый аргумент обязан быть массивом, а здесь стоит переменная индексного сорта.
  - Терм с неверным сортом операнда: $"true" + x$.
    Сложение принимает два аргумента натурального сорта, а булевская константа в него не входит.
  - Терм с необъявленным символом: $"read"(x, 0)$.
    Константа $0$ в сигнатуре массивов не объявлена вовсе, и сорта у неё нет.
  Формула с таким термом не получает истинностного значения: её нет в языке.
]

Сигнатура задаёт словарь: из её символов по рангам собираются выражения, и правила сборки --- грамматика языка.
Терм --- выражение, обозначающее объект некоторого сорта.

#definition[Терм][
  *Корректно-сортированный терм* сорта $sigma$ строится по правилам.

  - Переменная любого сорта --- терм этого сорта.
  - Константа любого сорта --- терм этого сорта.
  - Правило применения: символ с рангом
    $ (sigma_1, dots, sigma_n, sigma) $
    принимает термы сортов $sigma_1, dots, sigma_n$ и даёт терм сорта $sigma$.

  Терм, который нельзя собрать этими правилами, называется *некорректно-сортированным*.
  В языке такого терма нет.
]

#example[Сборка терма][
  В сигнатуре массивов терм
  $ "read"("write"(a, i, v), i) $
  имеет сорт $"E"$.
  Внутренний $"write"$ собирает массив из переменных подходящих сортов.
  Внешний $"read"$ извлекает из него элемент.
  Терм описывает чтение только что записанного элемента --- ту операцию, ради которой массивы и входят в язык.
]

Теперь закрепим, что термы означают: интерпретация приписывает сортам и символам их значения.

#definition[Интерпретация][
  *Интерпретация* сигнатуры приписывает сортам, символам и переменным их значения.

  - Каждому сорту --- непустое множество элементов, *носитель* сорта.
  - Каждому символу $f$ --- функцию между носителями:
    $ f^cal(I) : sigma_1^cal(I) times dots times sigma_n^cal(I) -> sigma^cal(I) $.
  - Каждой переменной $x$ --- значение из носителя её сорта.
]

#example[Интерпретация целых чисел][
  В интерпретации для целочисленной арифметики:
  - сорт $"Int"$ --- целые числа,
  - символ $+$ --- сложение,
  - символ $<=$ --- нестрогий порядок.
  Переменная может получить значение $6$, и тогда удвоенный терм обозначит 12.
  Условие из введения --- `x + y <= 10 and x > 5` --- обретает смысл именно здесь: символы интерпретированы, и вопрос "найдётся ли пара" относится к целым числам.
]

Сорт --- город, а носитель --- его население.
Сорт $"Int"$ населён целыми числами.
Сорт $"Bool"$ --- двумя истинностными значениями.
Жители одного города в другом не живут.
Функция --- дорога между городами: она везёт аргументы из своих сортов и привозит результат в свой.
Если дороги между городами нет, перевозка не состоится --- формула не собрана.

Носитель сорта обязан быть непустым: пустой сорт лишил бы значения всякий терм этого сорта, а константа такого сорта не имела бы денотата вовсе.

Семантика связок и кванторов в многосортной логике та же, что в односортной из главы @chap:fol.
Формула собирается из атомов связками: конъюнкция, дизъюнкция, отрицание, импликация.
Квантор $forall x : sigma$ пробегает по носителю своего сорта.
В этой картине формула --- терм булева сорта: предикат применяется к аргументам и возвращает истинностное значение, а связки работают с такими термами, как функции.
Разница с односортной логикой --- только в сортах: значения переменных живут в разных носителях, и смешать сорта в одном терме нельзя.

Одна сигнатура допускает много интерпретаций.
Сорт натуральных чисел можно прочитать иначе: как вычеты по модулю $7$.
Тогда терм $S(6)$ обозначит ноль.
Выполнимость формулы зависит от выбора: в чистой логике законна любая интерпретация, в арифметике --- только правильные.
Какие интерпретации допустимы --- решает теория, и к ней переходит следующий раздел.

== Теории первого порядка

Формула $x + x = 2x$ истинна в арифметике.
В чистой логике она лишь выполнима: достаточно выбрать операцию $+$, не обладающую нужными свойствами, и равенство нарушится.
Одна и та же запись получает разные ответы --- изменился класс допустимых интерпретаций, а формула осталась прежней.
Вопрос о выполнимости без этого класса не имеет ответа: он зависит от того, кому разрешено свидетельствовать.
Теория --- это и есть решение о допуске: она фиксирует класс интерпретаций, и только их голоса учитываются.
Сужение класса --- цена, которую платят за закреплённый смысл: чем уже класс, тем больше истин, но тем сильнее ограничены модели.

#definition[Теория][
  Теория первого порядка --- пара $cal(T) = (Sigma, cal(M))$.
  Здесь $Sigma$ --- сигнатура.
  Класс $cal(M)$ --- допустимые интерпретации.
  Класс $cal(M)$ замкнут относительно переназначения переменных: вместе с каждой интерпретацией он содержит любую интерпретацию, отличающуюся от неё только значениями переменных.

  Замкнутость означает, что класс не зависит от конкретных имён переменных: переименование переменных формулы не выводит интерпретацию из класса.
  Интерпретации класса называют $cal(T)$-интерпретациями.
]

Без замкнутости теория вела бы себя по-разному для одинаковых по сути формул: переименование переменных не должно менять ответ о выполнимости.
Определение теории через класс интерпретаций, а не через множество аксиом --- выбор, продиктованный вопросом SMT.
Вопрос главы --- существует ли модель, а не выводится ли предложение: процедура решения ищет интерпретацию, и класс интерпретаций отвечает на этот вопрос напрямую.
Определение через аксиомы потребовало бы перевода: сначала зафиксировать аксиомы, потом доказывать, что модели аксиом --- в точности намеченный класс.
Класс интерпретаций задаёт и неаксиоматизируемые теории вроде $"Th"(NN)$: истина в стандартной модели --- тоже теория, хотя аксиом у неё нет.
Выбор класса как первоосновы делает определение теории достаточно широким, чтобы охватить и такие случаи.

Сужение класса интерпретаций закрепляет смысл символов.
В чистой логике символ $+$ может означать любую бинарную операцию: сложение, умножение, выбор минимума.
В теории вещественной арифметики символ $+$ обязан быть сложением вещественных чисел.
Символ $<=$ --- обычным порядком.
Рассуждение ведётся в этой закреплённой математике, и ответы перестают зависеть от произвола интерпретаций.

#example[Теория вещественной арифметики][
  Теория $cal(T)_"RA"$ закрепляет смысл символов:
  - сорт $"Real"$ --- вещественные числа,
  - символ $+$ --- сложение,
  - символ $<=$ --- нестрогий порядок.
  Формула $cal(T)_"RA"$-общезначима:
  $ x + y = y + x $.
  Сложение вещественных чисел коммутативно.
  В чистой логике та же формула общезначимой не была бы: ничто не мешает интерпретировать $+$ некоммутативной операцией.
]

Класс интерпретаций можно задавать и без перечисления --- условиями.
Теория частичного порядка: сигнатура с одним предикатом $<=$, аксиомы --- рефлексивность, антисимметричность, транзитивность:

$
  & forall x. x <= x, \
  & forall x, y. (x <= y and y <= x) -> x = y, \
  & forall x, y, z. (x <= y and y <= z) -> x <= z.
$

Модели этих аксиом --- в точности частичные порядки: любые множества с отношением, удовлетворяющим трём условиям.
Такой способ задания класса интерпретаций называется _аксиоматизацией_, и ниже ему посвящено отдельное определение.

#definition[$cal(T)$-выполнимость][
  Формула $alpha$ называется $cal(T)$-выполнимой, если её выполняет некоторая интерпретация теории.
]

#definition[$cal(T)$-общезначимость][
  Формула $alpha$ называется $cal(T)$-общезначимой, если её выполняют все интерпретации теории.
]

Теория сужает класс допустимых интерпретаций, поэтому $cal(T)$-общезначимость --- это общезначимость по моделям теории.

Совместность конъюнкции литералов --- то же, что $cal(T)$-выполнимость: набор ограничений _совместен_, если существует интерпретация теории, выполняющая все его литералы.
Термины "совместный" и "выполнимый" --- синонимы.
Первый удобен, когда речь о наборах ограничений.

#example[Выполнимость и общезначимость в вещественной арифметике][
  - $cal(T)_"RA"$-выполнима формула:
    $ x + y <= 1 $.
    Модель: $x = 0, y = 0$.
  - Та же формула не $cal(T)_"RA"$-общезначима.
    Контрмодель: $x = 5, y = 5$.
  - $cal(T)_"RA"$-невыполнима формула:
    $ forall x, y. x + y <= 1 $.
    Неравенство нарушается при $x = y = 1$.
  - $cal(T)_"RA"$-общезначима формула:
    $ forall x. (x + y <= 1.7) -> (y <= 1.7 - x) $.
    Над вещественными числами эти два неравенства равносильны.
]

Вопрос о $cal(T)$-выполнимости --- алгоритмический, и алгоритм, отвечающий на него, называется _процедурой решения_#footnote[D. Kroening, O. Strichman. "Decision Procedures: An Algorithmic Point of View". 2016; A. Bradley, Z. Manna. "The Calculus of Computation: Decision Procedures with Applications to Verification". 2007.].

#definition[Процедура решения][
  Процедура решения (decision procedure) для класса формул --- алгоритм, который для любой формулы из класса завершается и отвечает "да" или "нет".

  В этой главе процедуры решения отвечают на вопрос о $cal(T)$-выполнимости кванторно-свободных формул данной теории.
]

Правильность процедуры решения складывается из трёх свойств.
+ _Корректность_: ответ "да" процедура даёт только на выполнимые формулы.
+ _Полнота_: каждая выполнимая формула получает ответ "да".
+ _Завершимость_: алгоритм останавливается на любом входе.
Корректность и полнота вместе означают, что ответ всегда верен.
Завершимость означает, что ответа можно дождаться.

Ответ на вопрос о выполнимости в каждой теории даёт своя процедура, и сложность каждой сведена в таблицу следующего раздела.

#example[Процедура решения в действии][
  - Формула $x + y <= 10 and x > 5$ выполнима в целых числах.
    Модель: $x = 6, y = 0$.
  - Формула $x + y <= 10 and x > 5 and y > 6$ невыполнима.
    Целочисленность усиливает неравенства:
    $ x > 5 arrow.r.double x >= 6 $
    $ y > 6 arrow.r.double y >= 7 $
    Тогда сумма не меньше $13$, и ограничение нарушено.
  Процедура решения для целочисленной линейной арифметики обязана выдать "да" в первом случае и "нет" во втором.
]

=== Аксиоматизация

Класс $cal(M)$ в определении теории можно задавать двумя способами.
Первый --- перечислить все интерпретации класса: такой перечень бесконечен и в конечный текст не помещается.
Второй --- выписать аксиомы: предложения, которым обязана удовлетворять каждая интерпретация из класса.

#definition[Аксиоматизируемая теория][
  Теория задана аксиомами $cal(A)$, если её интерпретации --- в точности модели аксиом.
  Класс моделей обозначают $"Mod"(cal(A))$.
  Модели --- интерпретации, выполняющие каждое предложение из $cal(A)$.

  Теория аксиоматизируема, если существует множество аксиом, задающее её класс интерпретаций.
  Произвольным множеством аксиом аксиоматизируема всякая теория: достаточно взять в качестве аксиом все её истинные предложения.
]

Такой способ годится для любой теории: аксиомами служат все её истинные предложения --- поэтому о произвольной аксиоматизируемости говорят редко.
Содержательное понятие --- _рекурсивная_ аксиоматизируемость: существует алгоритм, перечисляющий аксиомы.
Ниже термин "аксиоматизируемая" понимается в рекурсивном смысле.

Проверим введённое понятие на примере, где оно даёт нетривиальный ответ: теория $"Th"(NN)$ --- множество всех предложений, истинных в стандартной модели натуральных чисел.
Набор аксиом у $"Th"(NN)$ известен: все её истинные предложения, только перечислить их алгоритмом нельзя.
Вопрос в том, можно ли задать $"Th"(NN)$ рекурсивно, и ответ --- нет, как показывает теорема Гёделя.

_Полная теория_ --- это теория, которая для каждого предложения языка решает его судьбу: для любого предложения $phi$ выводится либо оно само, либо его отрицание.
Полная и рекурсивно аксиоматизируемая теория разрешима: ответ о предложении $phi$ сводится к ожиданию вывода --- либо самого предложения, либо его отрицания.
Один из двух выводов гарантирован полнотой, и перебор доказательств рано или поздно его найдёт.

Первая теорема Гёделя о неполноте утверждает: непротиворечивая рекурсивно аксиоматизируемая теория, в которой выразима арифметика, неполна.
Отсюда следует, что $"Th"(NN)$ не рекурсивно аксиоматизируема.
Рассуждаем от противного: $"Th"(NN)$ по определению полна --- для каждого предложения она содержит либо его, либо его отрицание, ведь в стандартной модели истинно ровно одно.
Если бы $"Th"(NN)$ была ещё и рекурсивно аксиоматизируемой, она была бы полной рекурсивно аксиоматизируемой теорией с арифметикой --- а это противоречит первой теореме Гёделя.
Значит, такого набора аксиом нет.

Разрешимая теория всегда рекурсивно аксиоматизируема: процедура решения перечисляет её общезначимые предложения, и этот список можно принять за аксиомы.
Поэтому из отсутствия рекурсивной аксиоматизации следует неразрешимость $"Th"(NN)$.
Тот же вывод даёт теорема Тарского о неопределимости истины (глава @chap:decidability).

Положительные примеры аксиоматизации --- обычные алгебраические теории.
Теория частичного порядка из начала раздела аксиоматизируема тремя аксиомами: её модели --- в точности частичные порядки.
Теория групп задаётся тремя аксиомами:

$
  & forall x, y, z. (x dot y) dot z = x dot (y dot z), \
  & forall x. (x dot e = x and e dot x = x), \
  & forall x exists y. (x dot y = e and y dot x = e).
$

Модели этих аксиом --- в точности группы: аксиомы вырезают из всех интерпретаций сигнатуры ${dot, e}$ ровно намеченный класс.

Отрицательный пример --- та самая $"Th"(NN)$: её аксиомы не перечислимы алгоритмом, хотя теория задана чётко, как истина в стандартной модели.
Рекурсивная аксиоматизируемость --- рабочее понятие, потому что рекурсивные аксиомы дают алгоритмический доступ к теории:
следствия рекурсивно аксиоматизируемой теории полуразрешимы, перебор всех доказательств рано или поздно находит вывод любого следствия.
Арифметика Пеано --- рекурсивно аксиоматизируемая теория, и к её аксиоматике мы сейчас переходим.

=== Арифметика Пеано

#definition[Арифметика Пеано][
  Арифметика Пеано $cal(T)_"PA"$ --- теория натуральных чисел.
  Сигнатура --- ${0, S, +, times, =}$.
  Аксиомы:
  - Нуль не является образом наследника:
    $ forall x. S(x) != 0 $.
  - Наследник инъективен:
    $ forall x, y. S(x) = S(y) -> x = y $.
  - Сложение задаётся рекурсивно:
    $ x + 0 = x, x + S(y) = S(x + y) $.
  - Умножение задаётся рекурсивно:
    $ x dot 0 = 0, x dot S(y) = x dot y + x $.
  - Схема индукции: для каждой формулы $phi(x)$ языка аксиома
    $ (phi(0) and forall x. (phi(x) -> phi(S(x)))) -> forall x. phi(x) $.

  Скажем эти аксиомы обычными словами.
  Операция $S$ --- "прибавить единицу", а нуль --- начало отсчёта: из нуля единицей не вернуться, и разные числа имеют разных наследников.
  Сложение и умножение заданы рекурсивно: прибавить ноль --- ничего не менять, прибавить наследник --- сначала прибавить один шаг, и так далее.
  Умножение --- повторное сложение.
  Схема индукции --- это правило доказательства "для всех": если свойство верно для нуля и переходит с числа на его наследника, то оно верно для всех чисел.

  Схема индукции --- бесконечное семейство аксиом, по одной на каждую формулу.
  Схема --- чертёж, а не деталь.
  По одному чертежу штампуют сколько угодно деталей: для каждой формулы $phi$ схема выдаёт свою аксиому, а формул в языке бесконечно много.
  Одна аксиома не покрыла бы все свойства сразу --- каждому свойству нужен свой заготовочный штамп.
  Конечно аксиоматизируемой $cal(T)_"PA"$ не является, но рекурсивно аксиоматизируемой --- да: формулы языка перечислимы, и аксиомы перечисляются алгоритмом.
]

Умножение входит в язык и расширяет выразительность.
Формула
$ forall x, y, z. x dot (y + z) = x dot y + x dot z $
--- теорема $cal(T)_"PA"$: дистрибутивность доказывается индукцией.
Предложение
$ S(S(0)) dot S(S(0)) = S(S(S(S(0)))) $
--- то есть $2 dot 2 = 4$ --- доказывается вычислением.

Полноты аксиомы Пеано не дают: $cal(T)_"PA"$ неполна (первая теорема Гёделя о неполноте, глава @chap:decidability).
У теории есть модели, отличные от стандартной, --- _нестандартные_.

В нестандартной модели есть элементы, не достижимые из нуля конечным числом применений $S$.
Они не имеют вида $S(S(dots S(0) dots))$ ни с каким конечным числом шагов.

Формула "всякое число есть нуль или чей-то наследник"
$ forall x. (x = 0 or exists y. x = S(y)) $
доказуема в $cal(T)_"PA"$ индукцией и потому истинна во всех моделях: она не отличает стандартные элементы от нестандартных.

Формула не отличает стандартные элементы: в нестандартной модели всякий элемент, даже не достижимый из нуля, является наследником некоторого элемента --- достаточно взять его предшественника по $S$.
У нестандартного элемента предшественник тоже нестандартный, поэтому цепочка уходит в бесконечность, но каждый её шаг в модели существует.

Доказуемая формула истинна во всех моделях теории: что доказано из аксиом, то выполняется в любой модели (это корректность исчисления, входящая в теорему Гёделя о полноте, глава @chap:fol).
Различие невыразимо формулой первого порядка: свойство "быть достижимым из нуля конечным числом шагов" в языке теории не формализуемо.

Нестандартные модели разбираются в главе @chap:beyond-n.
Теория $cal(T)_"PA"$ неразрешима: нет алгоритма, отвечающего на вопрос, доказуемо ли предложение (глава @chap:decidability).

Сравнение арифметики Пеано с вариантом без умножения указывает на источник неразрешимости.

=== Арифметика Пресбургера

#definition[Арифметика Пресбургера][
  Арифметика Пресбургера $cal(T)_"Pres"$ --- теория натуральных чисел: арифметика Пеано без умножения.
  Сигнатура --- ${0, S, +, =}$.
  Аксиомы --- те же, что у $cal(T)_"PA"$, кроме аксиом умножения.
  Схема индукции сохраняется.
]

Вместе с умножением уходит выразительность.
Нельзя записать $x dot y$: произведение двух переменных --- не терм языка.
Нельзя закодировать пары и последовательности чисел: нумерация пар обязана расти квадратично.

На квадрате со стороной $n$:
$ (n+1)^2 $ пар, все должны получить разные коды.

А все функции, определимые в арифметике Пресбургера, растут не быстрее линейной --- классический результат, который приводится здесь без доказательства.
Квадратичный код в такой арифметике не выразим.

Цена выразительности --- выигрыш в разрешимости: $cal(T)_"Pres"$ разрешима, и это результат Пресбургера#footnote[M. Presburger. "Über die Vollständigkeit eines gewissen Systems der Arithmetik". 1929.].
Любая процедура решения полной теории требует в худшем случае времени, растущего как двойная экспонента от длины формулы: нижняя оценка Фишера и Рабина#footnote[M. J. Fischer, M. O. Rabin. "Super-Exponential Complexity of Presburger Arithmetic". 1974.].
_Двойная экспонента_ --- это рост вида $2^(2^n)$: степень двойки, показатель которой сам есть степень двойки от длины формулы.

Такой рост несравнимо быстрее обычной экспоненты.
При $n = 4$:
$ 2^n = 2^4 = 16 $
$ 2^(2^n) = 2^(2^4) = 2^(16) = 65536 $.

Нижняя оценка --- ограничение скорости, которого не обойти никаким решателем.
Как дорога с замером трассы: сколько ни старайся водитель, быстрее разрешённого на каких-то участках не проехать.
Оценка Фишера и Рабина говорит: на некоторых входах любой алгоритм обязан потратить время $2^(2^n)$ --- такой работы требует сам ответ, а качество алгоритма здесь ни при чём.

Контраст виден на кванторно-свободном фрагменте: он NP-полон --- трудный, но не дважды экспоненциальный.
Полная теория Пресбургера на порядок дороже.
Кванторно-свободный фрагмент $cal(T)_"Pres"$ NP-полон.
Над целыми числами тот же фрагмент называется LIA (линейная целочисленная арифметика, раздел о линейной арифметике ниже) и остаётся NP-полным (глава @chap:complexity).

Умножение --- источник неразрешимости арифметики.
Источник не в одних кванторах: кванторно-свободный фрагмент арифметики Пеано, с умножением, тоже неразрешим.
Выполнимость кванторно-свободной формулы --- это вопрос о значениях её свободных переменных, при которых она истинна.
Навешивая на все свободные переменные квантор существования, получаем экзистенциальное предложение арифметики: $exists x_1, dots, x_n. phi$.
А вопрос об истинности экзистенциальных предложений арифметики --- это десятая проблема Гильберта, решённая отрицательно Матиясевичем: алгоритма для него нет.
Умножение само по себе, без кванторов, уничтожает разрешимость.

=== Разрешимость и фрагменты

Теории различаются по разрешимости, и это свойство стоит определить отдельно.

#definition[Разрешимость теории][
  Теория $cal(T)$ разрешима, если множество её общезначимых предложений разрешимо: существует завершающаяся процедура, отвечающая "да" или "нет" для любого предложения.
]

То есть разрешимость теории --- это алгоритмический вопрос про её общезначимые предложения: умеет ли какой-то алгоритм за конечное время сказать, общезначимо предложение или нет.

Теория $cal(T)_"RA"$ разрешима: её полная теория допускает _элиминацию кванторов_ (результат Тарского, глава @chap:decidability).
Теория $cal(T)_"PA"$ неразрешима (глава @chap:decidability).
Между ними --- $cal(T)_"Pres"$: разрешима, но с двойной экспоненциальной нижней оценкой.
Для кванторно-свободных формул процедуры решения проверяют выполнимость.
Связь с общезначимостью: формула общезначима ровно тогда, когда её отрицание невыполнимо.

Процедуры решения для полных теорий либо отсутствуют, либо дороги.
SMT-солверы работают с подмножествами формул теорий --- _фрагментами_.

#definition[Фрагмент теории][
  Фрагмент теории --- синтаксически ограниченное подмножество её формул.
  _Кванторно-свободный фрагмент_ --- формулы без кванторов: атомы, связки, отрицание.
]

Кванторы --- единственное, что уносит с собой бесконечность: формула без кванторов говорит о конечном числе термов, и её истинность определяется конечными данными, которые можно перебрать.
С квантором вопрос становится утверждением обо всех элементах области, а все элементы бесконечной области не перечислить.
Запрет кванторов --- это цена, и платит её выразительность:
свойства вида "для всех индексов" (экстенсиональность массивов), индукция и любые универсальные законы в кванторно-свободный фрагмент не входят.
SMT покупает разрешимость именно этой ценой: фрагмент сохраняет формулы, которые возникают как условия в точке программы, и отбрасывает те, что говорят обо всей математике сразу.

#example[Кванторно-свободные формулы][
  - Формула $x + y <= 10 and x > 5$ кванторно-свободна: одни атомы и связки.
  - Формула $forall x exists y. y > x$ кванторно-свободной не является: в ней два квантора.
  - Разностная логика --- фрагмент линейной арифметики: её формулы --- конъюнкции литералов вида $x - y <= c$.
  - Формула $not (x + y <= 10)$ кванторно-свободна: отрицание висит на атоме, а не на формуле с квантором.
]

Экскурс в разрешимость подводит к выбору SMT: работать с _фрагментами_, а не с полными теориями.
Среди полных теорий есть и неразрешимые (арифметика Пеано), и разрешимые, но очень дорогие (арифметика Пресбургера --- двойная экспонента).
Ни те, ни другие для практики SMT не годятся.
Кванторно-свободные фрагменты разрешимы, и их процедуры имеют практический смысл.
Что описывает каждая теория и почём обходится ответ, сведено в таблицу следующего раздела.
Когда формула смешивает теории, решатели комбинируются в одну процедуру (раздел о комбинации теорий).

Таков язык SMT: теория, выполнимость по модулю теории, процедура решения и фрагмент, --- и теперь посмотрим, какие теории действительно решают SMT-солверы и зачем каждая из них нужна.

== Какие теории решает SMT

Проверка простого фрагмента программы требует сразу трёх умений: сравнивать термы, рассуждать об индексах и помнить о массивах.
Нельзя собрать одну универсальную процедуру, которая понимала бы всё сразу: каждая теория навязывает свой способ записи ограничений, и противоречие в каждой ищется в своей структуре.
Разность двух переменных --- это ребро графа, равенство термов --- класс эквивалентности, линейное неравенство --- полупространство.
SMT-солвер --- библиотека процедур, а не один алгоритм, и вопрос главы --- какая структура стоит за каждой теорией и где в ней прячется противоречие.

Почти все эти процедуры устроены по одному образцу: ограничения переводятся в структуру --- граф, классы эквивалентности, многогранник --- и противоречие ищется в ней.
Структура --- _верстак_, на который раскладывают ограничения.
Пока они лежат кучей в формуле, противоречие не видно.
Разложив их на граф или многогранник, глаз находит его сразу --- как мастер видит трещину в балке, когда деталь положена на стол.
У каждой теории свой верстак: у разностной логики граф, у EUF классы эквивалентности, у арифметики многогранник.
Дальше мы идём по этим верстакам по очереди.

Разностная логика (DL) --- теория ограничений вида $x - y <= c$: разность двух переменных не превосходит константы.
Это простейшая из теорий тура, и структура у неё самая наглядная --- граф.
Такие ограничения возникают в планировании и системах реального времени: сроки, временные метки, привязка событий.
Анализ сроков сводит проверку расписания к совместности набора разностных ограничений.

Теория равенства с неинтерпретируемыми функциями ($cal(T)_"EUF"$) --- это равенство и произвольные функции, о которых известно только одно: равные аргументы дают равные значения.
Структура здесь --- классы эквивалентности термов: противоречие возникает, когда равенства склеивают два терма, объявленных разными.
Так доказывают эквивалентность программ: если две функции возвращают равные результаты на равных входах, их можно считать равными, не заглядывая в тела.
Компилятор пользуется этим в оптимизации общих подвыражений: доказав равенство двух вхождений подвыражения, он вычисляет его один раз.

Линейная вещественная арифметика (LRA) --- ограничения вида $2x + 3y <= 5$ над вещественными переменными.
Она описывает непрерывные величины: время, расстояние, скорость, координаты.
Проверка совместности линейных ограничений --- задача линейного программирования, одна из самых изученных в вычислительной математике:
к ней сводятся геометрические задачи и анализ систем реального времени.
Структура --- выпуклый многогранник: ограничения вырезают из пространства область допустимых точек, и противоречие означает пустоту этой области.

Линейная целочисленная арифметика (LIA) --- те же ограничения над целыми числами.
Условия верификации программ населены целыми: счётчики циклов, индексы массивов, размеры буферов.
Формула из начала главы, $x + y <= 10 and x > 5$, --- условие в LIA.
Разница с вещественным случаем --- какие точки области допустимы: целые, а не любые.

Битовые векторы (BV) --- арифметика фиксированной разрядности: сложение 32-битных чисел, сдвиги, побитовые операции.
Это в точности семантика процессора (глава @chap:circuits), поэтому BV описывает аппаратуру и низкоуровневый код.
Проверка процессорных блоков, протоколов и драйверов переводится в выполнимость формул над битовыми векторами.
Структура здесь булева: каждый бит --- отдельная переменная, и арифметика разворачивается в схему над ними.

Теория массивов ($cal(T)_"AX"$) моделирует память: массив читается и пишется по индексу.
Чтение по индексу после записи по тому же индексу возвращает записанное значение.
Оперативная память программы --- это массив, поэтому символьное исполнение и верификация программ описывают память именно так.
Утверждение "после записи по адресу чтение по тому же адресу вернёт записанное" --- типичный литерал теории массивов.
Противоречие здесь ищется среди аксиом доступа: для каждого чтения из записи инстанцируется правило read-over-write.

Три строки таблицы помечены как NP-полные, и стоит сказать, что за этим стоит.
NP-полнота --- это статус самых трудных задач класса NP: решение такой задачи можно проверить быстро, а к ней сводится любая задача из NP.
NP-полная задача не решается за полиномиальное время, пока $P != "NP"$.
На практике её решают перебором, и в худшем случае он экспоненциальный.
Здесь достаточно помнить из главы о SAT: NP-полнота описывает худший случай, а реальные формулы часто решаются быстро.

#table(
  columns: (auto, auto, auto),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header[Теория][Что описывает][Кванторно-свободный фрагмент],
  [$cal(T)_"EUF"$],
  [равенство и неинтерпретируемые функции],
  [разрешим, $O(n log n)$],

  [$"LRA"$], [линейная арифметика над $RR$], [разрешим, полиномиально],
  [$"LIA"$],
  [линейная арифметика над $ZZ$ (Пресбургер)],
  [NP-полон (глава @chap:complexity)],

  [$"DL"$], [разности $x - y <= c$], [разрешим, полиномиально],
  [$"BV"$],
  [битовые векторы фиксированной длины],
  [NP-полон (глава @chap:complexity)],

  [$"AX"$], [массивы (read/write)], [NP-полон (глава @chap:complexity)],
  [$cal(T)_"PA"$],
  [арифметика Пеано (с умножением)],
  [неразрешим (десятая проблема Гильберта)],
)

Почти каждая теория из таблицы получит собственную процедуру решения.
Исключение --- арифметика Пеано, отмеченная как неразрешимая.
Сложности в таблице --- анонс: каждая разрешимая строка будет разобрана в своём разделе (LRA и LIA --- в одном), и там станет видно, откуда берётся оценка.
Сейчас достаточно понять, что кванторно-свободные фрагменты разрешимы, а их сложность колеблется от полиномиальной до NP-полной.

Осталось договориться, в каком виде процедура теории отвечает на вопрос о выполнимости.
Достаточно уметь решать конъюнкции литералов: ветка DPLL-поиска (глава @chap:sat) --- это частичное присваивание, то есть конъюнкция литералов, уже принятых как истинные.
Решатель не строит ветки заранее --- каждая ветка порождается поиском, и в каждой ветке theory-солвер проверяет только текущую конъюнкцию.
Поэтому вопрос к теории звучит так: совместна ли текущая конъюнкция литералов.

#definition[Theory-солвер][
  *Theory-солвер* ($cal(T)$-солвер) --- процедура, решающая выполнимость *конъюнкций литералов* в теории.
  На входе --- набор литералов, например:
  $ x + y <= 10, f(a) = b $.
  На выходе --- совместен набор или нет.

  Конъюнкция литералов *совместна*, если она $cal(T)$-выполнима: существует интерпретация теории, выполняющая все литералы.
]

#theorem[Сведение к конъюнкциям литералов][
  Выполнимость кванторно-свободной формулы в теории разрешима тогда и только тогда, когда разрешима выполнимость конъюнкций литералов в той же теории.
]

#proof[
  Докажем эквивалентность в обе стороны.

  Покажем сначала, что из разрешимости выполнимости конъюнкций литералов следует разрешимость выполнимости кванторно-свободных формул.
  Кванторно-свободную формулу можно привести к ДНФ (дизъюнктивной нормальной форме) --- дизъюнкции конъюнкций литералов, например $(a and b) or (c and d)$.
  Дизъюнкция выполнима тогда и только тогда, когда выполнима хотя бы одна её составляющая: достаточно взять модель одной конъюнкции.
  Поэтому выполнимость формулы сводится к перебору её конъюнкций и проверке каждой из них.

  Обратно: конъюнкция литералов --- частный случай кванторно-свободной формулы, поэтому из разрешимости последних следует и разрешимость конъюнкций.
]

Для каждой из этих теорий есть своя процедура --- тот самый theory-солвер из определения выше.
Разберём их по очереди, начиная с простейшей --- разностной логики.

== Разностная логика

Моменты начала двух событий обозначим переменными $x, y$.
Требование "первое событие не более чем на $c$ минут позже второго" записывается так:
$ x - y <= c $.

Ограничение связывает только разность двух переменных, и такие условия называют _разностными ограничениями_.
На числовой оси каждое такое условие --- утверждение о расстоянии: одна точка не дальше заданного порога вправо от другой.

Отрицательное $c$ разворачивает смысл.
Неравенство
$ x - y <= -2 $
равносильно $y - x >= 2$: первое событие на две минуты раньше второго.

Расписания --- самый наглядный источник разностных ограничений: сроки, временные метки, привязка событий.
Те же условия возникают в планировании, в системах реального времени и в верификации, когда нужно убедиться, что все временные требования выполнимы одновременно.
Вопрос всегда один: существует ли набор моментов времени, при котором каждое ограничение выполнено.

Разностная логика --- простейшая теория с содержательной процедурой решения.

#definition[Разностная логика][
  Разностная логика (DL, от англ. difference logic) --- фрагмент линейной арифметики, обозначаемый $cal(T)_"DL"$.

  Её формулы --- конъюнкции литералов вида $x - y op c$: разность двух переменных сравнивается с константой.

  Ограничения читаются как над целыми, так и над вещественными числами: DL --- фрагмент и целочисленной, и вещественной линейной арифметики.
]

Присваивание значений переменным --- это набор _координат_ на оси: у каждой переменной своя точка.
Ограничение $x - y <= c$ превращается в условие на расстояние между точками.
Конъюнкция выполнима, когда все точки удаётся расставить по оси так, чтобы каждое условие расстояния выполнилось.

#example[Разностная логика: координаты][
  Конъюнкция $ (x - y <= 2) and (y - z <= 3) and (x - z <= 5) $ выполнима.
  Расставим точки: $z = 0, y = 3, x = 5$.
  Проверяем каждое ограничение:
  - $x - y = 5 - 3 = 2 <= 2$ --- выполняется.
  - $y - z = 3 - 0 = 3 <= 3$ --- выполняется.
  - $x - z = 5 - 0 = 5 <= 5$ --- выполняется.
  На оси точки расставлены по возрастанию координат.
  Третье ограничение следует из первых двух: $x - z = (x - y) + (y - z) <= 2 + 3 = 5$.
]

Выполнимость любой разностной конъюнкции решается одинаково.
Процедура решения для DL --- три шага:
+ Нормализация: каждый литерал приводится к виду $u - v <= c$.
  Равенство $u - v = c$ распадается на два неравенства:
  $ (u - v <= c) and (v - u <= -c) $.
  Строгое неравенство над целыми числами заменяется на нестрогое с $c - 1$.
+ Построение графа ограничений: вершины --- переменные.
  Для каждого ограничения
  $ u - v <= c $
  рисуется ребро с весом $c$ от вычитаемого к уменьшаемому.
  Ориентация --- ход стрелки: из вычитаемого можно попасть в уменьшаемое, сдвинувшись вправо не больше чем на $c$.
  Путь по рёбрам превращается в цепочку смещений: каждый шаг добавляет свой сдвиг.
  Замкнутый путь обязан вернуться в ту же точку, а сумма смещений по кругу равна нулю, --- и если она отрицательна, вернуться невозможно.
  Отрицательный цикл --- дорога, которая уводит всё дальше, и конца ей нет.
+ Поиск _отрицательного цикла_: алгоритм Bellman--Ford
  #footnote[R. Bellman. "On a Routing Problem". Quarterly of Applied Mathematics, 1958; L. R. Ford Jr. "Network Flow Theory". RAND Corporation, 1956.]
  находит цикл с отрицательной суммой весов.

Разберём, почему нормализация устроена именно так.
Равенство $u - v = c$ означает сразу два условия:
$ (u - v <= c) and (u - v >= c) $.
Второе переписывается как $v - u <= -c$: умножение на минус единицу меняет знак неравенства.
Так равенство распадается на пару нестрогих неравенств того же вида $u - v <= c$.

Над целыми числами
$ u - v < c arrow.l.r.double u - v <= c - 1 $.
Причина в том, что разность целых чисел --- целое, и ближайшее меньшее число --- это $c - 1$.

Над вещественными числами такой замены нет: между константой и любой меньшей константой бесконечно много чисел.
Над вещественными числами строгие неравенства обрабатывают иначе: с символьной поправкой $epsilon$.

Путь в графе накапливает ограничения.
Проследим это по шагам.
Выпишем неравенства пути $v_0 -> v_1 -> dots -> v_k$:
$ v_1 - v_0 <= c_1, v_2 - v_1 <= c_2, dots, v_k - v_(k-1) <= c_k $.

Сложим левые и правые части отдельно: $(v_1 - v_0) + (v_2 - v_1) + dots + (v_k - v_(k-1)) <= c_1 + dots + c_k$.
Промежуточные переменные входят в сумму с плюсом и с минусом и сокращаются попарно.
Остаётся $v_k - v_0 <= c_1 + dots + c_k$.

Если путь замыкается в цикл, то $v_k = v_0$, левая часть обращается в ноль, и отрицательная сумма весов становится противоречием.

#theorem[Критерий выполнимости разностной конъюнкции][
  Конъюнкция разностных ограничений выполнима тогда и только тогда, когда в её графе ограничений нет отрицательного цикла.
  Если отрицательных циклов нет, кратчайшие расстояния от источника дают выполняющее присваивание.
]

Обе стороны критерия обоснованы в тексте вокруг: необходимость --- сложением неравенств вдоль цикла, достаточность --- построением модели кратчайшими расстояниями.
Когда отрицательных циклов нет, координаты строит сама процедура.
Добавим источник с нулевыми рёбрами во все переменные и вычислим кратчайшие расстояния $d(v)$.
Источник нужен, чтобы у каждой переменной было конечное расстояние: его рёбра достигают всех переменных, и кратчайший путь до любой из них существует.
Нулевые рёбра источника не создают новых отрицательных циклов, потому что в источник не входит ни одно ребро: цикл через него невозможен.

Что расстояния удовлетворяют ограничениям, объясняет неравенство треугольника.
Расстояние $d(u)$ --- длина кратчайшего пути из источника в переменную.
Если идти до переменной кратчайшим путём, а затем по ребру с весом, получится путь длиннее на вес ребра.

Кратчайший путь не длиннее пути через соседнюю вершину, поэтому
$ d(u) <= d(v) + c $
и, значит, $d(u) - d(v) <= c$.

А это ровно ограничение, закодированное ребром $v -> u$.
Раз выполнено каждое ребро, выполнены и все ограничения конъюнкции.
Расстояния $d(v)$ --- готовое выполняющее присваивание, и процедура решения возвращает его как модель.

Проверим критерий на конъюнкции, которая невыполнима.

#example[Разностная логика: отрицательный цикл][
  Рассмотрим конъюнкцию $ (x - y = 5) and (z - y >= 2) and (z - x > 2) and (w - x = 2) and (z - w < 0) $.
  Нормализуем каждый литерал к виду $u - v <= c$:
  - $x - y = 5$ распадается на два неравенства:
    $ (x - y <= 5) and (y - x <= -5) $.
  - $z - y >= 2$ переписывается:
    $ y - z <= -2 $.
  - $z - x > 2$ переписывается:
    $ x - z <= -3 $.
    Причина --- целочисленность: $z - x >= 3$.
  - $w - x = 2$ распадается на два неравенства:
    $ (w - x <= 2) and (x - w <= -2) $.
  - $z - w < 0$ переписывается:
    $ z - w <= -1 $.
  По правилу построения графа ограничению соответствует ребро с весом $c$: от вычитаемого к уменьшаемому.
  Три ребра образуют цикл:
  - ребро $x -> w$ с весом два.
    Оно построено из $w - x <= 2$.
  - ребро $w -> z$ с весом минус один.
    Оно построено из $z - w <= -1$.
  - ребро $z -> x$ с весом минус три.
    Оно построено из $x - z <= -3$.
  Сумма весов цикла отрицательна:
  $ +2 - 1 - 3 = -2 < 0 $.
  Ограничения вдоль цикла складываются.
  Сумма левых частей обращается в ноль, а сумма правых отрицательна: получается $0 <= -2$ --- противоречие.
  Конъюнкция невыполнима.
]

Координатное прочтение того же примера --- на рис. @fig:dl-negative-cycle. Зафиксируем сдвиг начала отсчёта, положив $x = 0$. Из ограничений следует $z >= 3, w >= z + 1, w >= 4$, а красная граница допускает лишь $w <= 2$: противоречие. На рисунке взята пробная позиция $z = 3.5$, при которой требуемое $w >= 4.5$ выходит за красный предел.

#figure(
  dl-negative-cycle,
  caption: [Координатное прочтение примера с отрицательным циклом.],
) <fig:dl-negative-cycle>

Алгоритм Bellman--Ford находит отрицательный цикл за $O(V dot E)$ операций.
Здесь $V$ --- число переменных.
Число $E$ --- рёбра после нормализации.

Оценка складывается из раундов расслабления: их ровно $V$, и в каждом проходят по всем рёбрам.
Один раунд распространяет знания о кратчайших расстояниях на один шаг пути: после $V - 1$ раундов до любой переменной доходят все пути, а ещё один раунд нужен как контрольный звонок.
Если и после него расстояние уменьшилось, значит, есть цикл с отрицательной суммой --- и Bellman--Ford его находит.

Для теории сложности (глава @chap:complexity) это означает: выполнимость разностной конъюнкции --- полиномиальная задача, в отличие от NP-полного кванторно-свободного фрагмента $"LIA"$.

Над вещественными числами процедура остаётся полиномиальной.
Строгое неравенство не заменяется нестрогим с конкретной константой: между константой и любым меньшим числом бесконечно много значений.
Процедура работает с символьной бесконечно малой поправкой $epsilon$.

Неравенство
$ u - v < c $
представляется как
$ u - v <= c - epsilon $:
поправка --- формальный символ, меньший любого положительного числа, и алгоритм оперирует им, не выбирая значения.

Модель, выданная такой процедурой, удовлетворяет исходным строгим неравенствам, а оценка сложности не меняется.
С поправкой отрицательным считается и цикл с нулевой суммой констант, в котором есть строгое ребро.

Полная арифметика неразрешима (глава @chap:decidability), а разностный фрагмент решается за полиномиальное время: ограничения превращаются в рёбра графа, и задачу решает алгоритм Bellman--Ford.

Разностная логика --- ядро многих SMT-приложений: планирование, системы реального времени, проверка временных свойств.
Как процедура всякой теории, DL-процедура работает как theory-солвер.
Как SAT-решатель оркеструет такие солверы в цикле DPLL(T), покажет один из следующих разделов.

Следующая теория главы --- равенство с неинтерпретируемыми функциями.
Там стратегия та же: ограничения переводятся в структуру, и противоречие ищется в ней.
Роль графа ограничений играют классы эквивалентности термов.

== Равенство с неинтерпретируемыми функциями

Компилятор постоянно доказывает равенство термов.
Примеры: совпадение значений функции на равных аргументах, замена повторного вычисления уже посчитанным значением.
Тела функций при этом неизвестны, а проверить равенство нужно.

Исключение общих подвыражений (common subexpression elimination) удаляет повторяющийся фрагмент вычисления, полагаясь на то, что равные термы действительно равны.
Распространение констант (constant propagation) заменяет переменную её известным значением --- тоже по равенству.
Дедупликация повторных вычислений сводится к тому же вопросу о совпадении значений термов.
Во всех случаях детали вычисления не важны: важно только, равны ли термы.

Решение --- отвлечься от тел функций.
Функция становится чёрным ящиком, о котором известно единственное правило: равные аргументы дают равные значения.
Это правило и есть _конгруэнтность_, а теория, в которой функции ничего больше не умеют, называется теорией равенства с неинтерпретируемыми функциями.

#definition[Теория EUF][
  Теория равенства с неинтерпретируемыми функциями $cal(T)_"EUF"$ имеет сигнатуру с равенством и произвольными функциональными символами.

  Её аксиомы --- рефлексивность, симметричность и транзитивность равенства, а также *конгруэнтность*: если аргументы равны, то и значения функции равны.
  $ x_1 = y_1, dots, x_n = y_n arrow.r.double f(x_1, dots, x_n) = f(y_1, dots, y_n) $.

  Функции здесь неинтерпретируемые: про них известно только правило конгруэнтности и ничего больше.
]

Слово "неинтерпретируемые" означает, что за символом $f$ не закреплено никакой конкретной функции.
Одна и та же формула верна в теории $cal(T)_"EUF"$ независимо от того, какая функция скрывается за символом.
Это и делает теорию _корректной абстракцией_: всё, что выведено в ней, остаётся верным для любого конкретного определения $f$.

#example[Congruence closure][
  Проверим выполнимость $ (a = b) and (f(a) = b) and not (g(a) = g(f(a))) $.
  Начнём с двух классов: ${a, b}$.
  Второй класс --- ${f(a), b}$.
  Оба порождены первыми двумя равенствами.

  Классы делят общий элемент $b$, а равенство транзитивно: равные термы склеиваются в один класс.
  Поэтому все три терма равны и образуют один класс ${a, b, f(a)}$.
  Теперь конгруэнтность даёт $g(a) = g(f(a))$: равные аргументы дают равные значения.
  Это прямо противоречит третьему литералу $not (g(a) = g(f(a)))$, значит формула невыполнима.
]

Процесс слияния классов показан на рис. @fig:congruence-closure-merge: два класса делят общий элемент $b$ и сливаются в один, а конгруэнтность даёт $g(a) = g(f(a))$ --- прямое противоречие третьему литералу.

#figure(
  congruence-closure-merge,
  caption: [Слияние классов в congruence closure.],
) <fig:congruence-closure-merge>

Третий литерал $not (g(a) = g(f(a)))$ и создаёт конфликт: без него противоречия не возникает.
Формула $ (a = b) and (f(a) = b) $ выполнима.
Модель простая: $a = b = f(a) = 0$.
Функция $g$ в этой точке принимает любое значение.
Оба равенства сводятся к $0 = 0$, и никакой отрицательный литерал не заставляет их нарушиться.
Так выглядит разница между невыполнимой формулой и её выполнимым подмножеством: конфликт появляется ровно тогда, когда конгруэнтность сталкивается с неравенством.

#algorithm[Congruence closure][
  + Взять все термы формулы как отдельные классы эквивалентности.
  + Для каждого явного равенства $s = t$ слить классы обоих термов.
  + Повторять: если аргументы функции попарно равны, а оба вхождения функции присутствуют в формуле, слить их классы.
  + Если неравенство $s != t$ получило оба терма в одном классе --- конфликт, формула невыполнима.
]

Каждое слияние склеивает два разных класса в один, а значит уменьшает число классов на единицу.
Число классов ограничено числом термов формулы, поэтому процесс завершается.
Число слияний ограничено тем же числом термов.
Каждое слияние и каждый поиск конгруэнтной пары проходят по термам формулы один раз, так что число шагов полиномиально.

Если по исчерпании слияний ни одно неравенство не оказалось внутри класса, набор равенств совместен.
Каждый класс можно наделить собственным значением.
Функции интерпретируются по классам: значение функции на наборе классов --- класс составного терма.
Конгруэнтность гарантирует, что результат не зависит от выбора представителей.

На практике слияние классов реализуют структурой данных union-find (система непересекающихся множеств): она быстро отвечает, в каком классе лежит терм, и склеивает два класса в один.

У каждого класса есть старший --- представитель, как бригадир у артели.
Вопрос "в каком классе терм" --- это вопрос "чей бригадир у терма": запрос находит его, идя по цепочке подчинения.
Слияние двух классов --- сведение двух артелей в одну: бригадир одной становится подчинённым бригадира другой.
_Сжатие путей_ --- разовое переподчинение: после слияния каждый рабочий запоминает бригадира напрямую, и следующий запрос идёт в один шаг.
Так запросы и слияния становятся почти мгновенными, и вся работа укладывается в почти линейное время.

Полный алгоритм, который ещё и ищет конгруэнтные пары термов, работает за $O(n log n)$ (Дауни--Сети--Тарьян) --- уточнённая оценка всего процесса.

Кванторно-свободная выполнимость в $cal(T)_"EUF"$ разрешима за полиномиальное время.
Полная теория чистого равенства (без функциональных символов) тоже разрешима, а с неинтерпретируемыми функциями и кванторами она уже неразрешима.
Механизм тот же, что у массивов: функция кодирует пары, а кванторы по парам дают достаточно выразительности для арифметики.
Теория наследует её неразрешимость (теорема Чёрча, глава @chap:decidability).
SMT-солверы поэтому работают именно с кванторно-свободным фрагментом EUF: он возникает из верификационных условий и решается быстро.

Есть и другой взгляд на тот же процесс --- _сведение Аккермана_.
Каждое вхождение функции заменяется свежей переменной, а равенство с исходным термом хранит связь.
Конгруэнтностное ограничение --- договор между двумя вхождениями: если аргументы равны, то и результаты обязаны быть равны.
После такой замены остаётся чистое равенство, которое решает union-find.

#example[Сведение Аккермана в действии][
  Формула $(f(a) = b) and (a = b) and not (f(b) = b)$.
  Вхождения $f(a), f(b)$ заменяем свежими переменными $u, v$.
  Связи $u = f(a), v = f(b)$ хранят исходные термы.
  Конгруэнтность становится условием $(a = b) -> (u = v)$.
  Из $a = b$ и $u = b$ по условию следует $u = v$, а значит $v = b$ --- прямое противоречие с $not (f(b) = b)$.
  После замены осталось чистое равенство, и конфликт виден без функций.
]

Практические солверы работают с общим _DAG_ термов, где совпадающие подтермы представлены один раз, --- по сути то же слияние классов.

Теория $cal(T)_"EUF"$ рассуждает только о равенстве термов.
Арифметические утверждения вида $x + y <= 10$ она не видит.
Следующая теория --- линейная арифметика --- добавляет к равенству сложение и порядок.

== Линейная арифметика

Условие $x + y <= 10 and x > 5$ из вступления --- типичное _верификационное условие_: проверка границ массива, счётчика цикла или свойства целочисленной программы.
Такие условия говорят о сумме переменных с числовыми коэффициентами.
Это и есть предмет линейной арифметики.

Слово "линейная" означает: терм --- сумма переменных, умноженных на константы.
Терм $2x$ допустим.
Терм $x times y$ --- нет: умножение переменной на переменную запрещено.
Умножение переменной на переменную запрещено: оно выводит терм из класса линейных и радикально меняет сложность задачи.

#definition[LRA][
  Линейная вещественная арифметика $"LRA"$ --- теория над вещественными числами.

  Символы --- сложение, вычитание, сравнение и умножение на константы (для каждой константы своя операция).
  Термы --- суммы вида $c_1 x_1 + dots + c_n x_n + d$: линейные комбинации переменных с константами.

  Умножение переменных друг на друга не входит в сигнатуру.
]

#definition[LIA][
  Линейная целочисленная арифметика $"LIA"$ --- та же линейная арифметика, но над целыми числами.
  Термы --- те же суммы $c_1 x_1 + dots + c_n x_n + d$.
  Сорт $"Int"$ интерпретируется как целые числа.
  Переменные принимают только целые значения.
]

#example[Выполнимость в LRA][
  - Формула над $"LRA"$ выполнима:
    $ x + y <= 10 and x > 5 $.
    Модель: $x = 6, y = 0$.
  - Формула $x times y = 1$ линейной не является: в ней переменные перемножаются, и её выполнимость --- задача другого, более трудного класса.
]

Проверка выполнимости системы линейных неравенств --- это вопрос о совместности системы над $RR$, то есть задача линейного программирования о существовании допустимой точки.
Сам вопрос о совместности решается за полиномиальное время:
метод эллипсоидов Хачияна#footnote[L. G. Khachiyan. "A Polynomial Algorithm in Linear Programming". Soviet Mathematics Doklady, 1979.]
и методы внутренней точки Кармаркара#footnote[N. Karmarkar. "A New Polynomial-Time Algorithm for Linear Programming". Combinatorica, 1984.] дают полиномиальные алгоритмы.

Геометрия объясняет, почему совместность решается быстро.
Система неравенств задаёт выпуклый многогранник, и вопрос один: пуст он или нет.
Метод эллипсоидов держит вокруг многогранника эллипсоид и на каждом шаге отсекает от него половину плоскостью нарушенного неравенства.
Объём нового эллипсоида убывает в геометрической прогрессии: он умножается на постоянный для данной размерности множитель, меньший единицы.
За полиномиальное число шагов либо находится точка, либо доказывается пустота.
Методы внутренней точки прокладывают путь внутри многогранника, минуя вершины: направление на каждом шаге выбирается гладко, без прыжков по рёбрам.

При этом классический симплекс-метод в худшем случае экспоненциален:
пример Клее--Минти#footnote[V. Klee, G. J. Minty. "How Good is the Simplex Algorithm?". 1972.] строит многогранник, на котором симплекс перебирает экспоненциально много вершин.
Пример Клее--Минти --- слегка скошенный гиперкуб: рёбра чуть наклонены, и симплекс, шагая по рёбрам, блуждает зигзагом по всем $2^n$ вершинам.
В обычном гиперкубе путь от старта к оптимуму короткий, а наклон рёбер превращает его в длинный серпантин.
На практике симплекс работает быстро, и SMT-солверы (в том числе инкрементальный симплекс Z3) используют именно симплекс-подобные процедуры.
Полиномиальность задачи не противоречит экспоненциальности конкретного алгоритма: для одних входов симплекс мгновенен, для специально построенных --- катастрофически медленен.

Целочисленная арифметика $"LIA"$ труднее вещественной.
Задача та же: найти допустимое решение, но теперь координаты обязаны быть целыми.

#example[Граница между LRA и LIA][
  Над $"LRA"$ система
  $ 2x <= 1, 2x >= 1 $
  выполнима: возьмём $x = 0.5$.

  Над $"LIA"$ она невыполнима.
  Выражение $2x$ при целочисленном значении переменной всегда чётно, а система требует равенства единице: решения нет.
  Вещественное решение существует, но целочисленного двойника у него нет: дробная часть области решений исчезает, когда допустимы только целые точки.
]

Кванторно-свободный фрагмент $"LIA"$ NP-полон (глава @chap:complexity).
Фрагмент $"LRA"$ решается за полиномиальное время.

Целые труднее вещественных по одной причине: вещественная задача --- поиск точки в выпуклом многограннике, и её решает непрерывный алгоритм.
Целочисленная задача --- поиск точки с целыми координатами, то есть дискретный перебор кандидатов, среди которых приходится выбирать.
Непрерывная геометрия здесь мало помогает: решение лежит в решётке целых точек, а не в континууме.

Механизм NP-трудности --- булев выбор.
Переменная, ограниченная парой неравенств $0 <= x <= 1$, принимает только два значения.
Каждая булева формула из главы о SAT выражается целочисленными ограничениями.
Кодирование клаузы: сумма литералов не меньше $1$ (литерал --- переменная, отрицание --- дополнение до единицы).

#example[Булева формула как целочисленные ограничения][
  Переменные $x_1, x_2, x_3 in {0, 1}$ кодируют булевы значения, отрицание --- дополнение: $overline(x_i) = 1 - x_i$.
  Дизъюнкт $(x_1 or overline(x_2) or x_3)$ становится неравенством
  $
    x_1 + (1 - x_2) + x_3 >= 1.
  $
  При $x_1 = 1, x_2 = 0, x_3 = 0$ сумма равна $1 + 1 + 0 = 2 >= 1$ --- дизъюнкт выполнен.
  При $x_1 = 0, x_2 = 1, x_3 = 0$ сумма равна $0 + 0 + 0 = 0$ --- дизъюнкт нарушен.
  Каждая скобка КНФ превращается в одно неравенство, и выполнимость булевой формулы --- это выполнимость системы целочисленных неравенств.
]

Выполнимость булевых формул NP-полна, а вместе с ней NP-полна и выполнимость в $"LIA"$.

Для $"LIA"$ применяют два семейства методов.
_Ветвление и границы_ (branch-and-bound) находят вещественное решение, а затем разбивают область ветвями, пока не получат целую точку.
_Отсечения_ (cutting planes) добавляют к системе неравенств новые ограничения, отсекающие нецелые решения, пока не останется целая вершина.

Отсечение --- новый барьер, который режет многогранник, отбрасывая нецелую вершину и не задевая ни одной целой точки.
После конечного числа таких резов остаётся многогранник, у которого каждая вершина целая, --- и целочисленное решение найдено.

Классическое семейство таких отсечений --- отсечения Гомори
#footnote[R. E. Gomory. "Outline of an Algorithm for Integer Solutions to Linear Programs". Bulletin of the American Mathematical Society, 1958.]:
они строятся из строк симплекс-таблицы и отрезают дробную часть решения.

#example[Отсечение в действии][
  Система $x + y <= 3.5, x, y >= 0$ над целыми числами.
  Вещественный решатель выдаст, например, точку $(3.5, 0)$ с дробной координатой.
  Неравенство $x + y <= 3$ --- отсечение: всякая целая точка области ему удовлетворяет (сумма целых координат не превосходит 3.5, значит, не превосходит 3), а вершина $(3.5, 0)$ отрезана.
  После добавления отсечения целое решение видно сразу: например, $x = 3, y = 0$.
]

Завершимость обоих методов --- нетривиальная теорема: ветвление исчерпывает конечное число целых точек в ограниченной области, а отсечения Гомори при целочисленной правой части гарантированно сходятся.

Полная теория вещественных чисел с умножением и порядком разрешима (теорема Тарского, глава @chap:decidability).
Для SMT это отвлечённый факт: солверы работают с кванторно-свободной линейной арифметикой, где методы проще и быстрее.

Линейная арифметика предполагает переменные неограниченной точности.
Процессор же оперирует числами фиксированной разрядности, и эта арифметика живёт по своим законам.
К ней мы и переходим.

== Битовые векторы

Процессор складывает числа фиксированной ширины, и сумма может не поместиться в отведённые биты.
Четырёхбитное число $15$ плюс единица: результат _переполнился_ и обнулился.
Пятибитная запись результата не помещается в четыре бита, и в них остаются одни нули.
Аппаратура и низкоуровневое программное обеспечение живут именно в этой семантике (глава @chap:circuits): целые здесь свёрнуты в кольцо по модулю $2^n$, а не неограниченные.

#definition[Битовые векторы][
  *Битовый вектор* --- последовательность битов фиксированной длины $n$.

  Теория $"BV"$ --- арифметика по модулю $2^n$: сложение, вычитание и умножение с переполнением, побитовые операции и знаковые или беззнаковые сравнения, как в процессоре.
]

Разницу с целой арифметикой показывает пример.

#example[Переполнение и выполнимость][
  Формула $x + 1 = 0$ над 4-битными векторами выполнима.
  Возьмём $x = 15$: двоичная запись --- четыре единицы.
  Сложение даёт пятибитную запись, а усечённый до четырёх битов результат --- ноль.

  Над натуральными числами формула $x + 1 = 0$ невыполнима.
  Никакое натуральное число её не удовлетворяет.

  Над целыми числами единственное решение $x = -1$ лежит вне диапазона четырёхбитного вектора.
  Дело в ширине, а не в конкретном значении: битовый вектор --- величина фиксированной разрядности, и именно это делает $"BV"$ отдельной теорией.
]

Формулы над битовыми векторами решают приёмом _bit-blasting_ (раздувание в биты): каждый бит вектора становится булевой переменной, а арифметическая операция превращается в булеву схему (глава @chap:circuits).
Полученную схему решает SAT-солвер из главы @chap:sat, а кодирование Тсейтина держит размер формулы почти линейным по числу вентилей: каждый вентиль получает свежую переменную и несколько клауз.

#algorithm[Bit-blasting][
  + Разложить каждый битовый вектор на отдельные булевы переменные по битам.
  + Перевести каждую арифметическую операцию в булеву схему (глава @chap:circuits).
  + Свести схему к КНФ кодированием Тсейтина (глава @chap:sat).
  + Решить полученную SAT-формулу.
]

Кванторно-свободный фрагмент $"BV"$ NP-полон (глава @chap:complexity).
NP-полнота относится к формулам, где ширина входит во вход.
Bit-blasting разворачивает формулу в SAT-задачу размера, полиномиального от суммарной ширины всех векторов, --- отсюда и NP-полнота.
Фиксированная ширина задачу не упрощает: полный перебор стоит $2^(n dot m)$ шагов, экспоненциально по числу переменных.

Удивляться нечему: решатель ищет значения битов, а такая задача и есть SAT.
Для фиксированной ширины область значений битового вектора конечна: $2^n$ значений, и кодирование в булеву логику ничего не теряет.
Масштаб разницы виден по числам.
При ширине $4$ значений всего 16.
При ширине 64 их около $1.8 times 10^(19)$.
Полный перебор потому остаётся практичным только для крошечных ширин, а для ширины настоящего регистра он бессмыслен.

#remark[NP-полнота и практика][
  Кванторно-свободные фрагменты LIA, AX и BV NP-полны, как и сам SAT.
  Худший случай снова не отражает практику.
  Инженерная формула наследует структуру породившей её задачи, и эта структура даёт решателю зацепки: выученные дизъюнкты, направления симплекса, эвристики выбора.
  Например, переменные задачи распадаются на почти независимые сообщества, и поиск работает внутри них (глава @chap:sat).
]

#note[Bit-blasting против символических процедур][
  Для битового вектора битовая семантика и есть настоящая семантика: операция по модулю $2^n$ в точности совпадает с поведением схемы, и перевод в булевы переменные не теряет информации.
  Для целой или вещественной арифметики тот же приём взрывает формулу: одна переменная несёт $2^n$ значений, и одно умножение 64-битных чисел раздувается в тысячи вентилей и десятки тысяч дизъюнктов.
  theory-солверы линейной арифметики рассуждают о неравенствах символически, не раскладывая переменные на биты, и потому работают с неравенствами напрямую, а не перебирают значения.
]

Битовый вектор моделирует регистр процессора.
Память же устроена как набор таких регистров, адресуемых индексом.
Следующая --- теория массивов --- описывает именно эту картину: память как массив битовых векторов.

== Теория массивов

Память компьютера --- один большой массив: адресуемые ячейки, в каждой --- значение.
Операция чтения по адресу возвращает содержимое ячейки, операция записи кладёт в ячейку новое значение.
Всякая работа с данными --- чтения и записи по адресам, одна за другой.

Инструменты проверки программ строят на этой картине целые рассуждения: _символьное исполнение_ в стиле KLEE представляет каждый фрагмент памяти как массив, а кучу --- как массив массивов битовых векторов.
Верификатор Dafny описывает изменение состояния программы последовательностью записей в такой массив.
Чтобы доказать, что две копии данных равны, верификатор сводит вопрос к равенству массивов: содержимое совпадает по каждому адресу, значит, массивы равны.

Логические законы массивов --- это законы памяти: правила, по которым чтения и записи складываются в согласованное описание вычисления.
Чтобы сделать эти законы точными, нужна теория первого порядка с двумя сортами --- индексов и элементов --- и двумя функциями чтения и записи.

#definition[Теория массивов][
  Теория массивов $cal(T)_"AX"$ имеет сигнатуру с тремя сортами --- массивы, индексы и элементы --- и двумя функциональными символами.

  - $"read"$ --- чтение элемента по индексу, с рангом $"rank"("read") = ("Array", "I", "E")$.
  - $"write"$ --- запись значения по индексу: новый массив, совпадающий со старым всюду, кроме одного индекса, с рангом $"rank"("write") = ("Array", "I", "E", "Array")$.
]

Обе функции тотальны: чтение определено для всякой пары (массив, индекс), запись --- для всякой тройки (массив, индекс, элемент).
Тотальность избавляет рассуждение от особых случаев: у всякого чтения есть значение, и всякая запись возможна.
Массив --- это функция из индексов в элементы, а картинка с ячейками --- лишь иллюстрация.

Запись не изменяет массив: она возвращает новый массив, согласованный со старым.
Старый массив продолжает существовать, и к нему по-прежнему можно обращаться.
В формуле $a = "write"(a, i, v)$ правая часть --- отдельный терм, обозначающий новую версию массива.
Старая версия остаётся доступной: две версии совпадают, только если запись не изменила содержимого.

Массив устроен как _записная книжка_, в которой каждая правка делается на новой странице: прежние страницы не зачёркиваются, и к ним можно вернуться в любой момент.
Это чисто функциональный взгляд, и у него есть имя --- _персистентные структуры данных_: изменение порождает новую версию, а прежние версии остаются доступны.
SMT-солвер мыслит именно так: записи добавляют новые версии массива, а прошлое не стирают.

#example[Запись не стирает старый массив][
  Пусть $b = "write"(a, i, 7)$.
  Новый массив совпадает со старым всюду, кроме одного индекса, где теперь лежит $7$.
  Старый массив продолжает существовать: чтение из него осмысленно и возвращает прежнее значение.
  Обе переменные стоят в формуле одновременно, и SMT-солвер рассуждает о них обеих.
]

Поведение чтения и записи задают три аксиомы.

#definition[Аксиомы теории массивов][
  *Аксиома чтения после записи.* Запись по индексу $i$ сразу же читается обратно.
  $
    "read"("write"(a, i, v), i) = v.
  $

  *Аксиома чтения по чужому индексу.* Запись по индексу $i$ не меняет значения по другим индексам.
  $
    i != j -> "read"("write"(a, i, v), j) = "read"(a, j).
  $

  *Аксиома экстенсиональности.* Массивы равны, когда равны по каждому индексу.
  $
    (forall i. "read"(a, i) = "read"(b, i)) -> a = b.
  $
]

Массивам нужны именно эти аксиомы: без них чтение и запись были бы неинтерпретируемыми функциями, и чтение из свежезаписанного массива могло бы вернуть что угодно.
Рассуждение о памяти рассыпалось бы: верификатор не вывел бы $"read"("write"(a, i, 7), i) = 7$, потому что аксиомы --- единственный источник этого факта.
Первые две аксиомы --- точная формализация двух свойств памяти: запись видна своему адресу и невидима чужим.
Третья аксиома --- про равенство, а не про память: она превращает массив в функцию, заданную содержимым, и без неё равенство массивов не следовало бы из равенства содержимого.

Первые две аксиомы называются _read-over-write_.
Запись по индексу $i$ читается обратно по тому же индексу, а по остальным индексам чтение возвращает прежние значения.
Вместе они описывают согласованность памяти: свежая запись видна своему адресу и не трогает соседние.

Третья аксиома --- _экстенсиональность_ --- задаёт равенство массивов как поточечное равенство функций.
Два массива равны тогда и только тогда, когда они совпадают по всем индексам.
Без этой аксиомы два массива с одинаковым содержимым могли бы оставаться различными, и верификатор не смог бы вывести $a = b$ из равенства содержимого.
Экстенсиональность --- то, что делает массив функцией из индексов в элементы, а не поименованной ячейкой: массив задаётся содержимым, а не именем.

#example[Массивы: аксиомы в действии][
  Первая аксиома сразу даёт $"read"("write"(a, i, 7), i) = 7$: формула общезначима.
  Вторая покрывает запись по чужому индексу: $i != j -> "read"("write"(a, i, 7), j) = "read"(a, j)$.
  Контраст двух записей по одному индексу и по разным индексам:
  - $"read"("write"("write"(a, i, 5), i, 9), i) = 9$: побеждает последняя запись.
  - $i != j -> "read"("write"("write"(a, i, 5), j, 9), i) = 5$: записи по разным индексам не мешают друг другу.
  Отрицание первой аксиомы невыполнимо: $"read"("write"(a, i, 7), i) != 7$.
  Экстенсиональность в действии: формула $(forall k. "read"(a, k) = "read"(b, k)) and a != b$ невыполнима.
  Она содержит квантор по индексу и потому лежит вне кванторно-свободного фрагмента: запись "для всех $k$" в нём невозможна, и именно поэтому экстенсиональность обрабатывают отдельно.
  Контраст: формула $"read"(a, k) = "read"(b, k) and a != b$ выполнима: массивы могут расходиться по другим индексам.
]

Кванторно-свободный фрагмент $cal(T)_"AX"$ --- булевы комбинации равенств и неравенств термов из чтения и записи, без кванторов.
Он NP-полон (глава @chap:complexity).
Полная теория массивов (с экстенсиональностью) неразрешима.
Разница --- в кванторах: формула без кванторов говорит о конечном числе адресов, формула с кванторами --- сразу обо всех.

Кванторы по индексам дают достаточно выразительности, чтобы закодировать арифметику натуральных чисел.
Число $n$ представляется массивом: единицы на первых позициях, нули на остальных.
Сравнение выражается квантором по индексу: $n < m$ тогда и только тогда, когда найдётся позиция, где в первом массиве стоит ноль, а во втором --- единица.
Прибавление единицы --- тоже кванторная формула: новый массив отличается от старого ровно в одной позиции.

Это интуитивный набросок: выразимость сложения требует арифметики на индексах, а строгое доказательство неразрешимости кодирует двухсчётчиковую машину либо определяет умножение через последовательности.
Как только теория выражает достаточно богатую арифметику (с умножением), она наследует её неразрешимость: арифметическая формула переводится в формулу о массивах с сохранением ответа, а арифметика неразрешима (глава @chap:decidability).
Кванторно-свободный фрагмент таких возможностей лишён: он сохраняет лишь конечные паттерны чтений и записей, и для них решение существует.

Верхняя граница NP следует из сведения к равенству с неинтерпретируемыми функциями.
После конечного числа инстанциаций формула превращается в булеву комбинацию литералов равенства: инстанцированные аксиомы добавляют дизъюнкты вида $j = i$ или равенство.
Дальше вступает congruence closure, а каждый дизъюнкт требует угадать сторону --- это и даёт верхнюю границу NP.

Выполнимость в кванторно-свободном фрагменте решают _ленивой инстанциацией аксиом_: аксиомы подставляют лишь те, что нужны для текущего конфликта, а не все сразу.
Процедура сводит задачу к теории равенства с неинтерпретируемыми функциями, для которой алгоритм congruence closure уже разобран выше.

#algorithm[Ленивая инстанциация аксиом][
  + Рассмотреть чтение и запись как неинтерпретируемые функциональные символы и применить к конъюнкции литералов congruence closure.
  + Когда замыкание встречает чтение из $"write"$, инстанцировать аксиому read-over-write для пары индексов этого чтения и добавить полученное равенство или дизъюнкт.
  + Повторять, пока не возникнет противоречие (формула невыполнима) или не перестанут выводиться новые равенства (построена выполняющая модель).
  Экстенсиональность обрабатывают отдельно: разбором случаев по свежему индексу или ленивым добавлением инстанцированных экстенсиональных равенств.
]

Лень здесь необходимость: инстанцировать все аксиомы для всех индексов невозможно, ведь индексов бесконечно много.
Инстанцируют ровно то, чего требует текущий конфликт, и ровно в том месте, где он возник.
Инстанцированная аксиома для чтения из записи принимает вид дизъюнкта: $j = i$ или равенство результатов чтения.

Дизъюнкт появляется так.
Импликация с неравенством в посылке равносильна дизъюнкции:
$ (i != j -> B) arrow.l.r.double (i = j or B) $.
Импликация ложна в единственном случае: посылка истинна, а заключение ложно.
Посылка с неравенством истинна ровно тогда, когда равенство ложно, поэтому импликация ложна ровно тогда, когда ложны и равенство, и заключение.
Дизъюнкция ложна ровно в этом же случае: ложны оба её члена.
Значит, вторая аксиома массивов допускает дизъюнктивную запись: $j = i$ или равенство результатов чтения.

Это та же конфликт-управляемая идея, что и в CDCL из главы @chap:sat: решатель учится на противоречии и движется дальше, не трогая то, что ему не мешает.
Каждая инстанцированная аксиома --- новый литерал, добавленный в конъюнкцию, и цикл замыкания повторяется, пока не остановится.

Он останавливается, потому что инстанциация не порождает новых массивов и индексов.
Инстанцированная аксиома связывает лишь термы, построенные из массивов и индексов, уже встречавшихся в формуле, а число таких термов конечно.
Рано или поздно замыкание либо находит противоречие, либо перестаёт выводить новые равенства.

Вложенные записи сводятся к одной по первой аксиоме: чтение возвращает последнее записанное значение.
Инстанцированные аксиомы возвращаются в SAT-часть DPLL(T) как общезначимые в теории дизъюнкты --- такие дизъюнкты называются _T-леммами_, и раздел о DPLL(T) разбирает этот механизм.

Массивы --- рабочая модель памяти в символьном исполнении.
KLEE кодирует память исполняемой программы массивами, и каждая ветвь вычисления проверяется как SMT-формула над массивами.
Ограниченная проверка моделей программ с массивами --- та же техника: развернуть вычисление на конечное число шагов и спросить SMT-солвер, выполнима ли формула (глава @chap:verification).
Сама теория массивов проста, а литературы вокруг неё много, и это не случайность.

#remark[Слои вокруг ядра массивов][
  Кванторно-свободная теория массивов --- лишь ядро.
  В практических задачах индексы --- арифметика, элементы --- битовые векторы (глава @chap:circuits), а экстенсиональность приходится учитывать вместе с кванторами.
  Комбинация массивов с теорией индексов, кванторные фрагменты (array property fragment --- кванторы по индексам специальной формы)
  и оптимизация экстенсиональности --- каждый из этих слоёв породил десятки работ.
  Везде одна и та же задача: выразительность растёт, а разрешимость надо сохранить.
]

Массивы редко появляются в формуле в одиночку: индексы --- числа, элементы --- битовые векторы.
Одна формула смешивает несколько теорий, и для неё нужен решатель, понимающий их вместе.
Как процедура решения встраивается в общий цикл SAT-поиска, показывает раздел о DPLL(T), а как решатели разных теорий собираются в одну согласованную процедуру --- раздел о комбинации теорий (Nelson-Oppen).

== Theory-солверы и DPLL(T)

Теперь у каждой теории есть своя процедура решения.
Осталось показать, как один SAT-решатель руководит ими всеми.
Theory-солвер отвечает на один вопрос: совместна ли конъюнкция литералов.
Формула же --- дизъюнкция конъюнкций, и наивный путь --- привести её к ДНФ и перебрать все конъюнкции.

Размер ДНФ может расти экспоненциально: число конъюнкций растёт экспоненциально от числа атомов формулы.
Например, у формулы $(p_1 or q_1) and (p_2 or q_2)$ ДНФ состоит из четырёх конъюнкций:
$ (p_1 and p_2) or (p_1 and q_2) or (q_1 and p_2) or (q_1 and q_2) $.
Каждая скобка удваивает число конъюнкций, и двадцать скобок дают больше миллиона вариантов.

Выход --- перебирать конъюнкции неявно, по одной на ветку поиска, и спрашивать теорию только о текущей.
SAT-решатель управляет ветками, theory-солвер судит о каждой, и ни один из них не делает чужой работы.
Теорема о сведении к конъюнкциям литералов гарантирует полноту такого способа: если формула выполнима, найдётся ветка, которую теория примет.
ДНФ-преобразование остаётся лишь доказательством существования: оно показывает, что сведение к конъюнкциям возможно, но строить ДНФ явно невыгодно.

Архитектура DPLL(T) --- это SAT-решатель на _булевом скелете_ плюс theory-солвер в роли оракула совместности.
_Булев скелет_ формулы получается заменой каждого атома теории (например, `x + y <= 10`) свежей булевой переменной.

Дальше цикл повторяет CDCL (глава @chap:sat):
+ SAT-решатель ищет модель булева скелета --- присваивание булевых переменных.
+ Theory-солвер проверяет конъюнкцию атомов теории, помеченных истинными, --- ту ветку, которую нашёл SAT.
+ Если конъюнкция несовместна, theory-солвер возвращает _T-лемму_: выученный дизъюнкт, отрицающий конфликтную конъюнкцию.
+ SAT-решатель добавляет T-лемму в формулу и продолжает поиск, как с любым выученным дизъюнктом.

В реальных солверах theory-солвер вызывается и на частичных присваиваниях: он доопределяет атомы, вынужденные теорией (_теория-пропагация_), а T-лемма строится из объяснения конфликта.

Кандидата-модель и T-лемму стоит назвать точно.
_Кандидат-модель_ --- это присваивание булевых переменных скелета, которое SAT предлагает на проверку: гипотеза, которой ещё предстоит стать моделью.
_T-лемма_ --- клауза, общезначимая в теории $cal(T)$: она отрицает конкретную несовместную конъюнкцию атомов и потому истинна в любой интерпретации теории.

T-лемма --- _накладная на отказ_, которую теория возвращает поставщику-поиску.
Булев решатель прислал заказ: вот набор атомов, прими их все.
Теория проверила склад и отвечает: набор несовместим, вот перечень позиций, из-за которых --- любой из них можешь пометить ложным.
Список --- дизъюнкт отрицаний, и булев решатель учит его так же, как учил бы собственный конфликтный дизъюнкт.

Цикл "поиск, конфликт, обучение" --- тот же, что в CDCL, но с теорией в роли оракула.
Всё, что обсуждалось про CDCL --- watched literals, VSIDS, выученные дизъюнкты, --- работает и внутри SMT-солвера, потому что T-лемма приходит в том же формате, что и булева выученная клауза.
DPLL(T) --- это CDCL, расширенный на формулы с атомами теории.

Обобщение точнее, чем кажется: в CDCL конфликт --- пара взаимно отрицающих литералов, в DPLL(T) конфликтом становится любой набор атомов, который теория объявляет несовместным.
Выученный дизъюнкт в SAT отрицает булев конфликт, T-лемма отрицает конфликт теории, и формат у них один.
Отличие --- в источнике знания: булева клауза выведена из структуры формулы, T-лемма --- из аксиом теории, и потому она годится для любой формулы с теми же атомами.
Обучение работает одинаково: запомнить набор, который не должен повториться.

Цикл обмена между двумя решателями показан на рис. @fig:dpll-t-architecture: вниз уходит кандидата-модель булева скелета, вверх возвращается T-лемма.

#figure(
  dpll-t-architecture,
  caption: [Цикл обмена между SAT-решателем и theory-солвером.],
) <fig:dpll-t-architecture>

Кандидат-модель подтверждается только тогда, когда theory-солвер принимает конъюнкцию.
Тогда модель булева скелета вместе с моделью теории дают модель всей формулы.

#example[DPLL(T) в действии][
  Проверим формулу $(x + y <= 10) and (x > 5) and (y > 5)$ над целочисленной линейной арифметикой.
  Булев скелет: три атома становятся тремя булевыми переменными:
  $ p = (x + y <= 10), q = (x > 5), r = (y > 5) $.
  + SAT-решатель предлагает кандидата-модель: все три переменные истинны, $p = q = r = 1$.
  + Theory-солвер проверяет конъюнкцию $x + y <= 10 and x > 5 and y > 5$.
    Над целыми $x >= 6$: неравенство усиливается.
    Аналогично $y >= 6$.
    Отсюда сумма не меньше $12$.
    Это противоречит $x + y <= 10$.
    Конъюнкция несовместна.
  + Конфликтную конъюнкцию образуют все три атома: они помечены истинными, а вместе несовместны.
    T-лемма --- это отрицание этой конъюнкции, $not (p and q and r)$.
    По закону де Моргана отрицание конъюнкции --- дизъюнкция отрицаний: $not p or not q or not r$.
    Клауза общезначима в теории $cal(T)_"LIA"$: любая модель теории отвергает хотя бы один из трёх атомов.
  + SAT-решатель добавляет её к скелету: теперь формула $p and q and r and (not p or not q or not r)$ невыполнима уже на булевом уровне.
    Ответ --- `UNSAT`.

  Контраст даёт выполнимый вариант $(x + y <= 10) and (x > 5)$.
  Скелет --- $p and q$.
  SAT предлагает кандидата-модель $p = q = 1$.
  Theory-солвер проверяет конъюнкцию и находит модель: $x = 6, y = 0$.
  Конфликта нет, кандидат-модель подтверждена, ответ --- `SAT`.
]

#note[Энергичное и ленивое встраивание теорий][
  Есть два способа соединить SAT и теорию.
  _Энергичный_ (_eager_) переводит теорию в булеву формулу заранее: для битовых векторов это bit-blasting, когда каждый бит разворачивается в булеву переменную, а всю арифметику решает SAT-солвер.
  _Ленивый_ (_lazy_) проверяет теорию по требованию, внутри цикла DPLL(T).
  Именно ленивый подход --- стандарт SMT: он тратит работу только на те ветки, которые SAT действительно рассматривает.
]

== Комбинация теорий (Nelson-Oppen)

Одна формула часто смешивает теории: `f(x) = g(y) and x < y` --- равенство с неинтерпретируемыми функциями и арифметику сразу.
Один theory-солвер не решит такую формулу: ни EUF-решатель, ни арифметический не понимает половины литералов.
Нужно собрать решатели разных теорий в одну согласованную процедуру, и за это отвечает метод Nelson-Oppen.

Запустить оба решателя по очереди нельзя.
Даже если формула разбита на две части, каждая в своей сигнатуре, по отдельности они могут быть выполнимы, а общего решения не быть.
Метод Nelson-Oppen требует два условия, и каждое устраняет конкретную причину такого расхождения.

Первое условие --- _непересекающиеся сигнатуры_: теории делят только равенство $=$.
Если обе теории интерпретируют символ $+$, они могут приписывать ему разные смыслы, и соединять их модели нельзя.
Поэтому общие подтермы выносятся в переменные, и теории общаются только равенствами и неравенствами об этих переменных.
Обмен равенствами достаточен: общие переменные --- единственные точки контакта двух частей, и любое влияние одной теории на другую проходит через значения этих переменных.
Отношение между двумя значениями исчерпывается альтернативой: они равны или различны, --- и, передавая равенства и неравенства, теория сообщает соседке всё, что та вправе знать.
Равенство --- единственный символ, смысл которого зафиксирован раз и навсегда, поэтому он и становится каналом связи.

Второе условие --- _стабильная бесконечность_.
Теория $cal(T)$ _стабильно-бесконечна_, если всякая выполнимая кванторно-свободная формула имеет модель, в которой носитель каждого сорта бесконечен.
Название читается буквально: бесконечность устойчива в том смысле, что выполнимая формула не теряет модель при переходе к бесконечным носителям.
"Стабильно" --- потому что свойство сохраняется: если каждая выполнимая формула имеет бесконечную модель, то бесконечность --- закон класса, а не случайность отдельных моделей.

Условие нужно, чтобы модели частей можно было склеить в одну модель.
Возьмём теорию, все модели которой содержат ровно два элемента некоторого сорта, и вторую теорию, которая требует бесконечного носителя этого сорта.
Каждая часть выполнима, но никакая общая интерпретация не годится обеим сразу: одна требует два элемента, другая --- бесконечно много.
Стабильная бесконечность исключает такой разлад: если обе теории готовы принять бесконечную модель, то есть и модель, на которой их части совпадают по общим переменным.

Бесконечность спасает: при склейке моделей значения общих переменных расставляют попарно различными, и в бесконечном носителе для них всегда найдётся место.
Конечная модель такой свободы не даёт: если общих переменных больше, чем элементов носителя, какие-то две переменные получат одно значение, а вторая часть может требовать их различия.
Бесконечность --- это запас элементов, которого хватает на любой конечный набор общих переменных.

Процедура Nelson-Oppen:
+ _Пурификация_: разбить формулу на литералы по теориям, заменив общие подтермы свежими переменными.
  Например, $f(y)$ в арифметическом литерале заменяется свежей переменной с равенством.
+ Каждый theory-солвер решает свою чистую часть --- формулу в одной сигнатуре.
+ Солверы обмениваются выведенными равенствами и неравенствами об общих переменных.
+ Обмен повторяется, пока не перестанут появляться новые факты.
+ Если новые факты перестали появляться, а теории невыпуклы, перебрать варианты расположения значений общих переменных (_расщепление по случаям_).
  Противоречие во всех вариантах означает невыполнимость, а наличие совместного варианта позволяет склеить модели.

Свойство, о котором идёт речь, называется _выпуклостью_: теория выпукла, если из выполнимости набора литералов вместе с дизъюнкцией равенств следует выполнимость с одним из этих равенств.
EUF выпукла: конгруэнтность выводит равенства по одному.
Целочисленная арифметика не выпукла: из двойного неравенства следует альтернатива из двух равенств, но ни одно из них в отдельности не следует.
Для выпуклых теорий хватает поочерёдной передачи равенств, для невыпуклых --- расщепления по случаям, и имя свойства объясняет, где проходит граница.

#example[Nelson-Oppen: разбор примера][
  Проверим формулу $(x = f(y)) and (x = y + 1) and (f(y) != y + 1)$.
  Работают две теории: равенство с неинтерпретируемыми функциями и целочисленная арифметика.
  Их сигнатуры не пересекаются, обе стабильно-бесконечны: для EUF бесконечные модели существуют тривиально, а для арифметики моделью служат сами целые числа.
  + Пурификация: подтерм $f(y)$ принадлежит EUF, но встречается и в арифметическом литерале.
    Вводим свежую переменную $x_1$ с равенством.
    EUF-часть:
    $ x_1 = f(y), x = x_1 $.
    Арифметическая часть:
    $ x = y + 1, x_1 != y + 1 $.
    Общие переменные двух частей --- $x, y, x_1$.
  + Арифметический решатель обрабатывает свою часть.
    Если бы свежая переменная совпадала с $x$, из равенства следовало бы противоречие с литералом.
    Разберём шаг подробно.
    Предположим противное: $x_1 = x$.
    Тогда в равенстве можно сделать замену и получить $x_1 = y + 1$: прямое противоречие с литералом.
    Значит, предположение ложно, и $x_1 != x$ --- следствие арифметической части.
    EUF-части этот факт неизвестен: её множество равенств совместно.
    Конфликт вскрывает обмен: арифметическая часть выводит $x_1 != x$, а это прямо противоречит EUF-части, поэтому следствие передают EUF-решателю.
  + EUF-решатель получает неравенство, но его часть содержит равенство: прямое противоречие.
    Формула невыполнима.

  Решающий шаг --- передача неравенства $x_1 != x$: без неё EUF-часть выглядела бы совместной, и процедура не заметила бы конфликта.
]

Стабильная бесконечность --- граница применимости метода.
Без неё шаг склейки моделей ломается: каждая часть выполнима, но общего решения нет.
Теория, все модели которой имеют ровно два элемента некоторого сорта, не сочетается с теорией, требующей бесконечных доменов, даже когда каждая формула по отдельности выполнима.
Метод Nelson-Oppen такие пары не комбинирует.

== SMT-LIB и солверы

После главы о теориях у каждой процедуры свой входной формат, и решатели разных теорий говорят на разных языках.
Формула, смешивающая арифметику с массивами, требует двух процедур, а передать её каждой в её собственном формате нельзя.
Ответ подсказывает архитектура: интерфейс DPLL(T) единообразен --- кванторно-свободные формулы над фиксированными сигнатурами теорий, --- и этот интерфейс можно сделать языком.
Такой язык --- _SMT-LIB_: запись в виде Lisp-подобных S-выражений, то есть вложенных списков в круглых скобках.
Каждая команда --- это список, который начинается с имени команды, а её аргументы --- остальные элементы списка.
Общность языка --- следствие единой архитектуры, а не удобство.

Перед примерами разберём команды.
#table(
  columns: (auto, 1fr),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header[Команда][Что делает],
  [`declare-const`], [`(declare-const x Int)` заводит переменную сорта $"Int"$],
  [`assert`],
  [добавляет формулу в задачу: солвер обязан учитывать все добавленные формулы],

  [`check-sat`], [спрашивает, выполнима ли совокупность добавленных формул],
  [`get-model`], [просит выдать модель, если солвер ответил `sat`],
  [`declare-fun`],
  [объявляет неинтерпретируемую функцию: `(declare-fun f (Int) Int)` --- функция из целых в целые],
)

#example[SMT-LIB: линейная арифметика][
  Проверка выполнимости $x + y <= 10 and x > 5$:
  ```smt2
  (declare-const x Int)
  (declare-const y Int)
  (assert (<= (+ x y) 10))
  (assert (> x 5))
  (check-sat)
  (get-model)
  ```
  Солвер отвечает `sat` и выдаёт модель: $x = 6, y = 0$.
  Ответ `sat` означает только "решение существует", но какое именно --- солвер выбирает сам.
  К одной и той же задаче он может подойти разными путями и выдать разные модели.
  Формула `get-model` просит предъявить одну из них, и любая подойдёт, раз вопрос был о существовании.
]

#example[SMT-LIB: равенство с неинтерпретируемыми функциями][
  Проверка выполнимости $f(x) = g(y) and x = y and not (f(y) = g(x))$:
  ```smt2
  (declare-fun f (Int) Int)
  (declare-fun g (Int) Int)
  (declare-const x Int)
  (declare-const y Int)
  (assert (= (f x) (g y)))
  (assert (= x y))
  (assert (not (= (f y) (g x))))
  (check-sat)
  ```
  Солвер отвечает `unsat`.
  Из $x = y$ по конгруэнтности следует:
  $ f(x) = f(y), g(y) = g(x) $.
  Цепочка $f(y) = f(x) = g(y) = g(x)$ даёт равенство: оно противоречит отрицанию.
]

#example[SMT-LIB: невыполнимая система][
  Добавим к первому примеру ещё одно ограничение:
  ```smt2
  (declare-const x Int)
  (declare-const y Int)
  (assert (<= (+ x y) 10))
  (assert (> x 5))
  (assert (> y 5))
  (check-sat)
  ```
  Солвер отвечает `unsat`.
  Над целыми переменные получают усиленные оценки: $x >= 6$.
  Аналогично $y >= 6$.
  Тогда сумма не меньше $12$, и ограничение нарушено.
  Модели нет, ограничения исключают друг друга.
]

Солверы --- готовая реализация этой архитектуры, и все они устроены одинаково: под капотом у каждого SAT-поиск, theory-солверы и цикл DPLL(T).
Различаются происхождением и инженерными деталями:
- Z3 из Microsoft Research, написанный Леонардо де Моурой и Николаем Бьёрнером, --- индустриальный стандарт.
- CVC5 --- наследник CVC4 и CVC3, развиваемый сообществом.
- Yices --- работа SRI International.
- Alt-Ergo, солвер с открытым кодом, широко используют во французском сообществе верификации.
Общий язык SMT-LIB связывает их в одно: одна формула запускается в любом солвере без переписывания.

Эти солверы стали рабочим инструментом автоматического рассуждения.
- _Символьное исполнение_ выполняет программу с символьными входными данными вместо конкретных значений: каждый путь кодируется формулой, и SMT-солвер решает, достижим ли путь.
  KLEE по умолчанию работает с солвером STP, а Z3 подключается как альтернативный солвер.
- _Верификация программ_ (глава @chap:verification) сводит условия корректности к SMT-запросам: так работает Dafny.
- _Синтез программ_ ищет программу, удовлетворяющую спецификации, перебирая кандидатов и проверяя каждого запросом к солверу (Rosette).
- _Model checking_ проверяет свойства системы запросами к её символьной модели, как в главе @chap:modal.
Во всех этих задачах одна и та же схема: булево рассуждение из главы @chap:sat, теории из этой главы и цикл DPLL(T) под капотом.

== Итоги главы

SMT --- это выполнимость по модулю теорий: булево рассуждение из главы @chap:sat работает в одном цикле с языком и семантикой логики первого порядка из главы @chap:fol.
Многосортная сигнатура дала термам типы, а теория сузила класс допустимых интерпретаций: $cal(T)$-общезначимость --- это общезначимость по моделям теории.
Полная арифметика неразрешима (глава @chap:decidability), поэтому практика работает с кванторно-свободными фрагментами, и именно их разрешимость --- двигатель SMT.

У каждой теории свой решатель, и каждый устроен как поиск противоречия в структуре ограничений.
Разностная логика --- поиск отрицательного цикла в графе ограничений. EUF --- замыкание по конгруэнтности. Линейная арифметика --- симплекс и целочисленные методы (ветвление и границы, отсечения). Битовые векторы --- развёртка в схему. Массивы --- инстанцирование аксиом.
Структура каждой процедуры продиктована формой литералов. Разностное ограничение делает пару ребром, и сцена становится графом. Равенство --- отношением эквивалентности --- разбиением термов на классы. Линейное неравенство вырезает полупространство, и сцена --- выпуклый многогранник.
Противоречие в теории --- геометрическая невозможность на этой сцене: отрицательный цикл, два терма в одном классе при объявленном различии, пустой многогранник.

Цикл DPLL(T) встраивает любой такой решатель в поиск CDCL: конфликт теории порождает T-лемму, SAT-решатель учит её, и поиск продолжается на булевом скелете.
Когда формула смешивает теории, процедура Нельсона--Оппена собирает решатели в одну согласованную процедуру, обмениваясь равенствами и неравенствами об общих переменных.

Формат SMT-LIB дал всем солверам общий язык, а солверы --- Z3, CVC5, Yices, Alt-Ergo --- превратили архитектуру в рабочий инструмент верификации программ, символьного исполнения и синтеза.
Разрешимость кванторно-свободных фрагментов не противоречит NP-полноте многих из них (глава @chap:complexity).

#check[Проверьте себя][
  + Почему кванторно-свободные фрагменты теорий разрешимы, хотя полная логика первого порядка неразрешима?
  + Как цикл DPLL(T) связывает булев поиск с решателями теорий, и что такое T-лемма?
  + Почему отрицательный цикл в графе разностных ограничений означает невыполнимость, и как его находит процедура решения?
]

#whats-next
SMT автоматизирует проверку выполнимости по модулю теорий: решатель отвечает, существует ли модель.
В главе @chap:logic-programming логика станет языком программирования: программа --- множество клауз, а выполнение --- поиск доказательства.
Вопрос меняется с "существует ли модель" на "какие ответы следуют из теории".

== Упражнения

#tasklist("m15")[
  _Базовые понятия SMT._
  + Что такое ранг функционального символа в многосортной логике? Приведите ранг $"read"$ из теории массивов.
  + Чем теория отличается от чистой логики первого порядка? Что означает $cal(T)$-общезначимость?
  + Какой результат гарантирует разрешимость арифметики Пресбургера, и какой символ отвечает за неразрешимость арифметики Пеано?

  _Теории и их решатели._
  + Нормализуйте литералы к виду $u - v <= c$.
    Возьмите строгое неравенство $x - y < 3$.
    И обратное: $z - x >= 4$.
  + \* Постройте граф для конъюнкции $(x - y <= 2) and (y - z <= -1) and (z - x <= -1)$ и определите, выполнима ли она.
    #metadata((ex-id: "m15:t5"))
  + Примените congruence closure к $(f(a) = b) and (a = b) and not (f(b) = b)$.

  _Теория массивов._
  + Докажите, что $"read"("write"(a, i, 5), i) = 5$ общезначимо в теории массивов.
    Объясните, почему отрицание этой формулы невыполнимо.
  + Объясните, почему формула $(forall k. "read"(a, k) = "read"(b, k)) and a != b$ невыполнима в полной теории массивов,
    но лежит вне кванторно-свободного фрагмента, и как её обрабатывает процедура решения.

  _DPLL(T) и комбинация теорий._
  + В чём идея DPLL(T)? Какую роль играет theory-солвер, и что возвращает SAT-решателю при конфликте?
  + Проследите цикл DPLL(T) на формуле $(x >= 0) and (y >= 0) and (x + y < 0)$.
    Укажите кандидата-модель, ответ theory-солвера и полученную T-лемму.
    Почему ответ --- `UNSAT`?
  + Проведите пурификацию и обмен на формуле $(x = f(y)) and (x = y + 1)$.
    Почему процедура останавливается без противоречия, и как склеиваются модели двух теорий?
  + Почему для битовых векторов применяют bit-blasting, а для линейной арифметики --- нет?

  _SMT-LIB и солверы._
  + Запишите в SMT-LIB проверку выполнимости $f(x) = g(y) and not (x = y)$.
    Какой ответ даст солвер, и почему?

  + Дана система разностных ограничений:
    $ z >= x + 3, w >= z + 1, w <= x + 2 $.
    Выразите каждое ограничение в форме $u - v <= c$, постройте взвешенный граф и найдите отрицательный цикл.
    Какой вывод о выполнимости системы даёт цикл?
    Что изменится, если последнее ограничение заменить на $w <= x + 4$?

  + \* Почему кванторно-свободная выполнимость в $cal(T)_"EUF"$ разрешима за полиномиальное время, а общезначимость произвольных формул первого порядка --- нет?
    Свяжите с теоремой Чёрча (неразрешимость проблемы разрешения) из главы @chap:decidability.
    #metadata((ex-id: "m15:t15"))

  + \* Закодируйте ограничение $x + y = 3$ для беззнаковых 2-битных векторов методом bit-blasting.
    Пусть биты вектора $x$ --- $x_1 x_0$, биты вектора $y$ --- $y_1 y_0$ (в каждом старший бит --- первый).
    Введите битовые переменные, выпишите уравнения сумматора, переведите их в дизъюнкты и приведите выполняющее присваивание.
    #metadata((ex-id: "m15:t16"))
]
