// M36 --- Теория категорий: язык стрелок.
#import "common.typ": *
#import "notation.typ": *
#import "diagrams/m36.typ": (
  adjunction-bijection, assoc-triangle, cone-diagram, free-category,
  image-inverse-image, initial-algebra, monad-from-adjunction,
  monoid-one-object, nat-square, poset-category, product-universal,
  string-diagram,
)

= Теория категорий <chap:categories>

Произведение двух множеств --- это пары, из которых извлекаются первая и вторая компонента.
Прямое произведение двух автоматов синхронно читает слово двумя головками, и каждая головка --- проекция этого произведения на свой автомат.
Конъюнкция двух высказываний даёт два правила вывода: из $p and q$ следует $p$ и следует $q$.
Пересечение двух подмножеств лежит в каждом из них, наибольший общий делитель двух чисел делится на любой их общий делитель.
Материал разный --- множества, автоматы, формулы, числа, --- а рисунок один и тот же: объект с парой стрелок к двум данным объектам, через который пропускается любая другая такая пара.

Одно и то же доказательство, слово в слово, работает во всех этих случаях.
Проверка того, что два натуральных числа восстанавливаются по своей паре, и проверка того, что состояние пары автоматов определяется состояниями частей, --- это одно рассуждение о композиции отображений.
Каждая глава курса перестраивала его заново, потому что общего языка для него не было.

Такой язык появился в математике не ради удобства пересказа, а из конкретной задачи.
В 1940-е топологи заметили: одни конструкции с группами и пространствами ведут себя "естественно", другие --- зависят от случайного выбора, например базиса.
Чтобы отличить первое от второго, пришлось определить, что такое естественность, --- и определение оказалось сильнее ожидаемого.
Оно потребовало нового типа математического объекта, для которого сами построения и их соответствия стали предметом теории.

Что за язык делает повторяемость конструкций видимой до всяких вычислений, и почему этот же язык описывает типы в языках программирования?

#history-note[Эйленберг, Мак Лейн и естественность][
  Сэмюэл Эйленберг и Сондерс Мак Лейн познакомились в 1940 году в Мичигане: тополог и логик, оказавшиеся над одной задачей --- гомологиями пространств с операцией умножения.
  Статья 1945 года "General Theory of Natural Equivalences"#footnote[S. Eilenberg, S. Mac Lane. "General Theory of Natural Equivalences". Transactions of the American Mathematical Society, 1945.] начиналась с наблюдения, что слово "естественный" в математике использовали постоянно, не определяя.
  Изоморфизм конечномерного векторного пространства с его двойственным существует у каждого базиса, а с дважды двойственным --- без всякого базиса: второй изоморфизм "естественный", первый --- нет.
  Разница ощущалась всеми, но не была формализована.

  Формализация потребовала ввести сразу три понятия: категорию, функтор и естественное преобразование.
  Слова заняли у философов: "категория" --- у Аристотеля и Канта, "функтор" --- у Рудольфа Карнапа, из его книги "Логический синтаксис языка".
  Мак Лейн признавался, что брал их "с удовольствием воровства у философов"#footnote[S. Mac Lane. "Categories for the Working Mathematician". 2-е изд., Springer, 1998.].

  Дальше язык зажил собственной жизнью.
  Александр Гротендик в 1960-е перестроил на нём алгебраическую геометрию целиком.
  Уильям Ловер в 1970-е вместе с Майлом Тирни построил элементарные топосы --- категории, внутри которых можно вести математику, --- и связал их с интуиционистской логикой.
  С 2010-х идёт прикладная волна: категории нашли применение в базах данных, теории сетей, вероятностях и машинном обучении, а сама дисциплина получила имя прикладной теории категорий.
]

#chapter-overview[
  Глава строит язык, в котором объект определяется не элементами, а стрелками.
  Сначала --- само определение категории и его настойчивая проверка на знакомых структурах: множествах, моноидах из главы об алгебраических структурах, порядках, графах.
  Затем два способа сравнивать категории между собой: функторы, переводящие объекты вместе со стрелками, и естественные преобразования, сравнивающие функторы.
  На этой тройке держится всё остальное: универсальные свойства, сводящие произведение, сумму и наибольший общий делитель к одной диаграмме, лемма Йонеды, сопряжённые функторы и монады --- вплоть до категорной семантики типов.
  Факультативные разделы уводят в стороны, где язык стрелок работает в полный рост: диаграммы-проводки, свёртки списков, метрики и нечёткость.

  После этой главы вы сможете:
  + проверять, что множество с композицией образует категорию, и узнавать категории в моноидах, порядках и графах;
  + отличать изоморфизм, мономорфизм и эпиморфизм от их теоретико-множественных двойников и пользоваться двойственностью;
  + строить функторы и естественные преобразования и проверять их законы;
  + формулировать универсальные свойства и узнавать произведения, копроизведения и терминальные объекты в разных категориях;
  + объяснять лемму Йонеды и сопряжённые функторы на примерах, включая связки Галуа из анализа программ;
  + связывать монады с композицией эффектных вычислений и читать категорную семантику простых типов.
]

== Категория и её стрелки <sec:cat-def>

Композиция --- самая устойчивая операция во всей математике.
Функции компонуются: если $f$ переводит $A$ в $B$, а $g$ --- $B$ в $C$, то после $f$ можно выполнить $g$, и результат зависит только от порядка действий, а не от того, когда именно мы решили их сгруппировать.
Отношения делимости компонуются: если $a$ делит $b$, а $b$ делит $c$, то $a$ делит $c$.
Пути в графе компонуются: пройденные друг за другом, они дают путь.
В главе об алгебраических структурах (@chap:algebra) моноид был определён ровно как множество с ассоциативной операцией и нейтральным элементом --- то есть как множество, на котором композиция есть и нейтральна.

Категория получается из этого наблюдения обобщением одной черты.
В моноиде композиция определена для _любой_ пары элементов, а в функциях --- только для тех, у которых выход одного совпадает со входом другого.
Это ограничение согласования и есть вся разница.
Оставим от объектов лишь метки входов и выходов, а главным содержимым сделаем сами компонуемые стрелки.

#definition[Категория][
  *Категория* $cal(C)$ состоит из:
  + совокупности *объектов* $A, B, C, ...$;
  + совокупности *стрелок* (морфизмов) $f: A -> B$, где объект $A$ называется _началом_, а $B$ --- _концом_ стрелки $f$;
  + операции *композиции*, сопоставляющей паре стрелок $f: A -> B$ и $g: B -> C$ стрелку $g circle.stroked.tiny f: A -> C$,

  при условии, что для любых компонуемых стрелок выполнены два закона:
  - *ассоциативность*: $h circle.stroked.tiny (g circle.stroked.tiny f) = (h circle.stroked.tiny g) circle.stroked.tiny f$;
  - *тождество*: у каждого объекта $A$ есть стрелка $"id"_A: A -> A$ с $"id"_B circle.stroked.tiny f = f = f circle.stroked.tiny "id"_A$ для каждой $f: A -> B$.

  Стрелки с общими началом $A$ и концом $B$ образуют множество $cal(C)(A, B)$.
]

Композиция читается справа налево: $g circle.stroked.tiny f$ означает "сначала $f$, затем $g$" --- так же, как в записи $g(f(x))$ функция $f$ применяется первой.
Внутри фиксированной пары объектов стрелки ведут себя как элементы моноида: их можно перемножать, когда пути согласованы, и это умножение ассоциативно.

В этой конструкции объекты --- только бирки.
У них нет "внутренности", которая вскрывалась бы вопросом "из чего состоит": весь материал категории разложен по стрелкам.
Это самое непривычное решение определения --- и самое рабочее: как только содержимое объекта перестаёт иметь значение, любые рассуждения, опирающиеся на одну композицию, автоматически верны во всех категориях сразу.

Первый пример --- рабочая лошадка всей математики.

#example[Категория множеств][
  Объекты $"Set"$ --- все множества, стрелки $A -> B$ --- все функции из $A$ в $B$, композиция --- обычная композиция функций, $"id"_A$ --- тождественная функция.
  Ассоциативность композиции функций проверялась в главе о функциях (@chap:functions): три функции подряд можно группировать любым способом.
]

Три следующих примера показывают, насколько шире определение, чем привычная картина "объекты с внутренностью".

#example[Моноид --- категория с одним объектом][
  Возьмём моноид $(M, star, e)$ из главы об алгебраических структурах (@chap:algebra).
  Объявим единственный объект $tilde(M)$, а стрелкам $tilde(M) -> tilde(M)$ поставим в соответствие элементы $M$: элементу $a$ --- стрелку с тем же именем.
  Композиция определяется операцией: $b circle.stroked.tiny a = b star a$, тождество --- нейтральный элемент $"id"_tilde(M) = e$.
  Ассоциативность композиции --- это в точности ассоциативность операции, а закон тождества --- свойство нейтрального элемента.

  Моноид и категория с одним объектом --- одна и та же структура в двух одеждах: таблица Кэли моноида становится таблицей композиции стрелок.
]

#figure(
  monoid-one-object,
  caption: [Моноид как категория: один объект, стрелки --- элементы, композиция --- операция.],
) <fig:monoid-cat>

#example[Порядок --- категория без параллельных стрелок][
  Возьмём частично упорядоченное множество $(P, <=)$ из главы о порядках (@chap:orders).
  Объекты --- элементы $P$, и стрелку $a -> b$ проведём ровно в том случае, когда $a <= b$.
  Композиция --- транзитивность: из $a <= b$ и $b <= c$ следует $a <= c$, тождественная стрелка --- рефлексивность $a <= a$.

  Такие категории называют _тонкими_: между любыми двумя объектами есть не более одной стрелки, так что вся информация заключена в том, какие стрелки существуют.
  Решётка из той же главы --- тонкая категория с произведениями и копроизведениями пар: инфимум и супремум окажутся частным случаем общих конструкций этой главы.
]

#figure(
  poset-category,
  caption: [Цепочка $0 <= 1 <= 2$ как тонкая категория: стрелка означает отношение.],
) <fig:poset-cat>

#example[Граф порождает свободную категорию][
  Ориентированный граф из главы о графах (@chap:graphs) задаёт объекты и образующие стрелки, но не композицию.
  Добавим её принудительно: стрелками объявим все _пути_ --- конечные последовательности рёбер, в которых конец каждого ребра совпадает с началом следующего.
  Композиция --- приписывание одного пути к концу другого, тождество --- пустой путь в каждой вершине.
  Ассоциативность здесь --- соглашение о скобках при конкатенации последовательностей, уже встречавшееся для списков в (@chap:algebra).

  Полученная категория называется _свободной_ категорией графа.
]

#figure(
  free-category,
  caption: [Свободная категория графа: путь $g circle.stroked.tiny f$ становится самостоятельной стрелкой.],
) <fig:free-cat>

Свободная категория отвечает на вопрос, заданный ещё в главе о графах: что получится, если рёбра графа воспринимать как шаги, а не как связи?
Получится категория, в которой из $A$ в $C$ ведут две разные стрелки --- путь через $B$ и свой путь на каждое вхождение.
Регулярные выражения из теории автоматов (@chap:automata) --- способ перечислять такие пути, а эпсилон-переходы НКА --- его категорный след: добавление формальных стрелок, не меняющих проходимость.

#note[Как читать диаграммы][
  Рисунок @fig:free-cat --- это не иллюстрация, а утверждение.
  Узлы --- объекты, стрелки --- морфизмы, и диаграмма _коммутирует_: любые два пути по стрелкам из одного узла в другой дают равные композиции.

  Проверка коммутативности треугольника на рис. @fig:assoc-triangle --- это вычисление одного равенства $g circle.stroked.tiny f = h$, и вся сила диаграмм в том, что сложные цепочки равенств сворачиваются в один взгляд на картинку.
]

#figure(
  assoc-triangle,
  caption: [Коммутативный треугольник: две дороги из $A$ в $C$ дают одну стрелку.],
) <fig:assoc-triangle>

#check[Категория и её примеры][
  + Проверьте, что $(ZZ, +, 0)$ задаёт категорию с одним объектом.
    Какая стрелка будет тождественной, и что значит закон ассоциативности в терминах сложения?

  + Почему для тонкой категории из частичного порядка ассоциативность композиции --- это транзитивность?

  + Выпишите все стрелки свободной категории графа из двух рёбер $A -> B -> C$ вместе с тождественными.
    Сколько стрелок ведёт из $A$ в $C$?

  + Что неверно в "категории", где тождественная стрелка объекта $A$ действует как $f circle.stroked.tiny "id"_A = f$, но $"id"_B circle.stroked.tiny f = g$ для некоторой $g != f$?
]

== Стрелки вместо элементов <sec:cat-arith>

В теории множеств базовый вопрос к объекту --- "какие у него элементы".
В категории базовый вопрос --- "какие стрелки в него ведут и из него выходят".
Разница работающая: половина свойств, привычно формулируемых через элементы, переформулируется языком стрелок, и переформулировка действует там, где элементов нет вовсе.

Начнём с привычного.
В множествах функция называется инъективной, если разные элементы переходят в разные, и сюръективной, если каждый элемент накрыт.
Оба определения требуют заглянуть _внутрь_ множеств.
Следующие определения выглядят иначе и не требуют ничего, кроме композиции.

#definition[Изоморфизм][
  Стрелка $f: A -> B$ называется *изоморфизмом*, если существует $g: B -> A$ с $g circle.stroked.tiny f = "id"_A$ и $f circle.stroked.tiny g = "id"_B$.
  Стрелка $g$ называется _обратной_ к $f$.
]

Обратная стрелка восстанавливает всё, что разрушила $f$: туда-обратно --- тождество в обе стороны.
В $"Set"$ изоморфизмы --- это в точности биекции, и это первый сигнал, что язык стрелок не слабее привычного.

#definition[Мономорфизм][
  Стрелка $f: A -> B$ называется *мономорфизмом*, если она сокращается слева: из $f circle.stroked.tiny g_1 = f circle.stroked.tiny g_2$ следует $g_1 = g_2$.
]

#definition[Эпиморфизм][
  Стрелка $f: A -> B$ называется *эпиморфизмом*, если она сокращается справа: из $g_1 circle.stroked.tiny f = g_2 circle.stroked.tiny f$ следует $g_1 = g_2$.
]

Сокращение здесь --- буквальная аналогия сокращения множителя в равенстве произведений: если произведение на $f$ одинаково, то и сомножители одинаковы.
Инъективность и сюръективность требуют заглядывать внутрь множеств, а моно- и эпиморфизм проверяются по одной композиции --- и потому имеют смысл в любой категории.

#proposition[В $"Set"$ всё совпадает со школьным][
  В категории $"Set"$ мономорфизмы --- в точности инъективные функции, эпиморфизмы --- в точности сюръективные, изоморфизмы --- биекции.
]

#proof[
  Докажем утверждение для мономорфизмов.
  Для эпиморфизмов рассуждение аналогично: если элемент $y in B$ не покрыт $f$, две функции $g_1, g_2: B -> {0, 1}$, отличающиеся только на $y$, совпадают после $f$, вопреки сокращению справа.

  *Мономорфизм инъективен.*
  Пусть $f: A -> B$ --- мономорфизм, и $f(x_1) = f(x_2)$ для элементов $x_1, x_2 in A$.
  Рассмотрим функции $g_1, g_2: 1 -> A$ из одноэлементного множества $1 = {∗}$, переводящие $∗$ в $x_1$ и $x_2$ соответственно.
  Тогда $f circle.stroked.tiny g_1 = f circle.stroked.tiny g_2$ как функции из $1$ в $B$, и свойство сокращения даёт $g_1 = g_2$, то есть $x_1 = x_2$.

  *Инъективность даёт мономорфизм.*
  Пусть $f$ инъективна и $f circle.stroked.tiny g_1 = f circle.stroked.tiny g_2$ для функций $g_1, g_2: X -> A$.
  Тогда для каждого $x in X$ выполнено $f(g_1 (x)) = f(g_2 (x))$, откуда инъективность даёт $g_1 (x) = g_2 (x)$, а значит, $g_1 = g_2$.
]

Тонкая категория из порядка устроена иначе, и это отрезвляющий пример.

#example[Мономорфизмы порядка][
  В тонкой категории частичного порядка между любыми двумя объектами есть не более одной стрелки, поэтому любые два параллельных $g_1, g_2$ совпадают автоматически.
  Свойство сокращения выполняется для всякой стрелки: каждая стрелка --- и мономорфизм, и эпиморфизм, независимо от того, как выглядят элементы.
  Инъективность и сюръективность к стрелкам такой категории неприменимы в принципе: у стрелки нет элементов, а стрелочные свойства выполняются для всех стрелок сразу.
]

Порядок и множества дают две крайности: в $"Set"$ стрелочные определения совпадают с элементными, в тонкой категории элементы исчезают полностью.
Между ними живут все остальные категории, и стрелочный язык --- единственный, работающий сразу везде.

#definition[Противоположная категория][
  Для категории $cal(C)$ *противоположная категория* $cal(C)^op$ имеет те же объекты и те же стрелки, но начало и конец каждой стрелки меняются местами: $f: A -> B$ в $cal(C)$ становится $f: B -> A$ в $cal(C)^op$.
  Композиция определяется обращённым порядком: $g circle.stroked.tiny^op f = f circle.stroked.tiny g$.
]

Утверждение о категориях, доказанное для произвольной $cal(C)$, верно и для $cal(C)^op$.
Разворачивая его обратно, получаем новое верное утверждение: все стрелки перевёрнуты, начала и концы поменялись местами, а композиции читаются в обратном порядке.
Это *принцип двойственности*: у каждого понятия и теоремы есть двойник.
Мономорфизм двойственен эпиморфизму, терминальный объект --- начальному, произведение --- копроизведению, предел --- копределу.
Половина теории категорий --- умение доказывать одну теорему и получать вторую бесплатно.

#check[Стрелки и двойственность][
  + Почему биекция --- это в точности изоморфизм в $"Set"$?
    Проверьте обе композиции.

  + Стрелка $a -> b$ тонкой категории порядка антидискретного отношения ("все со всеми сравнимы в обе стороны") --- изоморфизм ли она?

  + Сформулируйте двойник утверждения "композиция двух мономорфизмов --- мономорфизм" и докажите его, не повторяя доказательства.

  + Что такое категория $cal(C)^("op")$ для тонкой категории порядка $(ZZ, <=)$?
    Какому порядку она соответствует?
]

== Функторы и перенос структуры <sec:functors>

Между моноидами главы об алгебраических структурах (@chap:algebra) разумными отображениями были не любые функции, а гомоморфизмы: они сохраняли операцию.
Между автоматами (@chap:automata) --- не любые пары функций, а те, что согласованы с переходами.
Общий рецепт уже виден: отображение структур заслуживает имени, когда оно уносит за собой не только элементы, но и устройство.
Категория --- это объекты плюс стрелки плюс композиция, значит, честное отображение категорий должно переводить всё трое.

#definition[Функтор][
  *Функтор* $F: cal(C) -> cal(D)$ сопоставляет каждому объекту $A$ категории $cal(C)$ объект $F (A)$ категории $cal(D)$, а каждой стрелке $f: A -> B$ --- стрелку $F (f): F (A) -> F (B)$ так, что
  $
    F (g circle.stroked.tiny f) = F (g) circle.stroked.tiny F (f), quad F ("id"_A) = "id"_(F (A)).
  $
  Функтор $cal(C)^op -> cal(D)$ называют _контравариантным_ функтором из $cal(C)$ в $cal(D)$: он переворачивает стрелки.
]

Законы функтора говорят: сначала компоновать, потом переводить --- то же самое, что сначала переводить, потом компоновать.
Диаграммы сохраняются: коммутирующий квадрат переходит в коммутирующий квадрат.
В программировании функтор --- это контейнер с операцией `map`: список с `map` над функцией, опциональное значение с `map`, и обе аксиомы читаются как "применять композицию к отображённому --- то же, что отображать композицию".

Программистский взгляд стоит зафиксировать кодом, потому что дальше он станет главным героем.

```rust
// Функтор "список": map уважает композицию и тождество.
fn map<T, U>(xs: &[T], f: impl Fn(&T) -> U) -> Vec<U> {
    xs.iter().map(f).collect()
}
// map(xs, |x| g(f(x))) == map(map(xs, f), g)   --- композиция
// map(xs, |x| x)             == xs             --- тождество
```

Первый классический функтор живёт в каждом параграфе о множествах: конструкция булеана.

#example[Булеан в обе вариантности][
  Ковариантный булеан переводит множество $A$ в $cal(P)(A)$, а функцию $f: A -> B$ --- в отображение образа $"img"_f : cal(P)(A) -> cal(P)(B)$.
  На подмножество $S subset.eq A$ оно действует правилом $S partialto f(S) = {f(x) mid(|) x in S}$.
  Тождественная функция переходит в тождественную, образ композиции --- композиция образов, так что это функтор $"Set" -> "Set"$.

  Контравариантная версия переводит $f: A -> B$ в _прообраз_ $f^(-1): cal(P)(B) -> cal(P)(A)$: подмножество $T subset.eq B$ возвращается в $f^(-1)(T) = {x in A mid(|) f(x) in T}$.
  Здесь направление стрелок меняется: чем больше информации о $B$, тем больше известно и об $A$.
]

#figure(
  image-inverse-image,
  caption: [Функция $f$ поднимается на подмножества дважды: образ идёт вперёд, прообраз --- назад.],
) <fig:powerset-functor>

#proposition[Прообраз контравариантен][
  Отображение $f partialto f^(-1)$ определяет контравариантный функтор на $"Set"$: $"id"^(-1) = "id"$ и $(g circle.stroked.tiny f)^(-1) = f^(-1) circle.stroked.tiny g^(-1)$.
]

#proof[
  Докажем второе равенство включением в обе стороны.
  Первое получается той же проверкой.

  *Включение $subset.eq$.*
  Пусть $x in (g circle.stroked.tiny f)^(-1)(T)$ для подмножества $T subset.eq C$.
  По определению прообраза $g(f(x)) in T$, то есть $f(x) in g^(-1)(T)$, а значит $x in f^(-1)(g^(-1)(T))$.

  *Включение $supset.eq$.*
  Пусть $x in f^(-1)(g^(-1)(T))$.
  Тогда $f(x) in g^(-1)(T)$, то есть $g(f(x)) in T$, что и означает $x in (g circle.stroked.tiny f)^(-1)(T)$.
]

Прообраз --- рабочая конструкция теории вероятностей: события выражаются через прообразы случайных величин (@chap:probability), и контравариантность объясняет, почему распределение вероятностей тянет события "в обратную сторону".
Тот же функтор незаметно работал в алгебре логики и в анализе программ: предусловие команды --- прообраз множества постсостояний.

Следующая пара функторов --- мост между двумя мирами, и она ещё вернётся в секции о сопряжениях.

#example[Свободный и забывающий][
  _Забывающий_ функтор $U: "Mon" -> "Set"$ переводит моноид в его носитель, выбрасывая операцию: стрелки-гомоморфизмы переходят в обычные функции.
  _Свободный_ функтор $L: "Set" -> "Mon"$ переводит множество $A$ в моноид строк $A^*$ с конкатенацией --- строковый моноид из главы об алгебраических структурах (@chap:algebra); его называют _свободным_.

  Функция $f: A -> B$ продолжается в гомоморфизм $L (f): A^* -> B^*$ применением $f$ к каждому элементу списка: пустой список остаётся пустым, конкатенация сохраняется.
  Программист узнает здесь `map` над списками: свободный функтор --- это контейнер, который ничего не помнит кроме элементов, и потому переносит любые функции.
]

#note[Осторожно со словом "функтор"][
  В главе о логическом программировании (@chap:logic-programming) функтором назывался конструктор терма: `parent(tom, mary)` --- функтор `parent` с двумя аргументами.
  Здесь функтор --- отображение между категориями.
  Две структуры носят одно имя по случайности истории: карнаповское слово прижилось и в Прологе, и в теории категорий.
  Родственна и другая коллизия: "категоричность" аксиом из теории моделей (@chap:constructions) --- про единственность модели, а не про категории этой главы.
]

Третий пример --- самый важный для этой главы, хотя выглядит скромнее всех.

#example[Функтор стрелок из объекта][
  Зафиксируем объект $A$ категории $cal(C)$.
  Сопоставление $B partialto cal(C)(A, B)$ переводит объект в множество стрелок в него из $A$.
  Стрелка $h: B -> C$ переводится в операцию посткомпозиции $cal(C)(A, B) -> cal(C)(A, C)$, действующую правилом $f partialto h circle.stroked.tiny f$.
  Ассоциативность композиции --- в точности функториальность: $(h_2 circle.stroked.tiny h_1) circle.stroked.tiny f = h_2 circle.stroked.tiny (h_1 circle.stroked.tiny f)$.

  Получен функтор $cal(C)(A, -): cal(C) -> "Set"$, _представимый_ объектом $A$; его контравариантный родственник $cal(C)(-, B)$ строится предкомпозицией.
]

Замкнём кружок с изоморфизмами: функтор не может испортить то, что сформулировано на языке стрелок.

#proposition[Функтор сохраняет изоморфизмы][
  Если $f: A -> B$ --- изоморфизм в $cal(C)$ и $F: cal(C) -> cal(D)$ --- функтор, то $F (f): F (A) -> F (B)$ --- изоморфизм в $cal(D)$, причём $(F (f))^(-1) = F (f^(-1))$.
]

#proof[
  Пусть $g: B -> A$ --- обратная стрелка: $g circle.stroked.tiny f = "id"_A$ и $f circle.stroked.tiny g = "id"_B$.
  Применим $F$ к обоим равенствам и воспользуемся законами функтора:
  $
    F (g) circle.stroked.tiny F (f) = F (g circle.stroked.tiny f) = F ("id"_A) = "id"_(F (A)),
  $
  и аналогично $F (f) circle.stroked.tiny F (g) = "id"_(F (B))$.
  Стрелка $F (g)$ --- искомый обратный к $F (f)$.
]

Следствие для практики: если две структуры изоморфны, то любые их функториальные инварианты совпадают.
Счётчики, построенные композициями, диаграммы, сохранённые функтором, --- всё это переносится без пересчёта.
Именно так работают доказательства типа "эти два графа неизоморфны: у одного цикл длины 5 есть, у другого нет" --- только здесь закон указан явно.

#check[Функторы][
  + Проверьте аксиомы функтора для ковариантного булеана: что нужно проверить для $"id"$ и для композиции?

  + Забывающий функтор $"Grp" -> "Set"$ переводит группу в её носитель.
    Что он делает со стрелками и почему гомоморфизм переходит в функцию?

  + Почему `Option` с операцией `map` --- функтор на программистской категории типов?
    Проверьте оба закона на примерах `None`.

  + Функтор $cal(C)(A, -)$ переводит объект $A$ в множество.
    Что он делает с изоморфизмом $h: B -> C$?
    Будет ли отображение множеств $cal(C)(A, B) -> cal(C)(A, C)$ биекцией?
]

== Естественные преобразования <sec:nat>

Два функтора $F, G: cal(C) -> cal(D)$ едут из одной категории в другую.
Сравнить их --- значит для каждого объекта $A$ дать стрелку $alpha_A: F (A) -> G (A)$, и сделать это _согласованно_.
Согласованность и есть та самая естественность, ради которой Эйленберг и Мак Лейн построили весь аппарат.

#definition[Естественное преобразование][
  Пусть $F, G: cal(C) -> cal(D)$ --- функторы.
  *Естественное преобразование* $alpha: F => G$ --- это семейство стрелок $alpha_A: F (A) -> G (A)$, по одной на каждый объект $A$ категории $cal(C)$, такое что для каждой стрелки $f: A -> B$ коммутирует квадрат
  $ G (f) circle.stroked.tiny alpha_A = alpha_B circle.stroked.tiny F (f). $
]

#figure(
  nat-square,
  caption: [Квадрат естественности: два пути из $F(A)$ в $G(B)$ по сторонам квадрата дают одно и то же.],
) <fig:nat-square>

Естественность читается как честность семейства: компоненты $alpha_A$ согласованы со всеми стрелками категории, поэтому "не зависят от случайного выбора".
Классическая иллюстрация --- двойное двойственное пространство.
Вложение $V -> V^(**)$ конечномерного векторного пространства в его дважды двойственное естественно: оно строится без базиса, одним правилом $v partialto (phi partialto phi(v))$.
А изоморфизма $V -> V^*$ с базисом построить нельзя: смена базиса меняет его, и квадрат естественности разваливается.
Именно этот провал математики 1930-х ощущали пальцами, не имея языка, --- пока язык не появился.

Программисты используют естественные преобразования ежедневно, называя их полиморфными функциями.

#example[Безопасный первый элемент][
  Зафиксируем два функтора на категории типов: список $"List"$ и опциональное значение $"Option"$.
  Семейство `head` переводит список любого типа $A$ в `Option`, возвращая первый элемент или отсутствие: пустой список переходит в `None`, непустой --- в `Some(x)`.

  Естественность --- это равенство `map g (head xs) = head (map g xs)` для любой функции $g$: достать первый элемент и преобразовать его --- то же, что преобразовать все и достать первый.
  Равенство очевидно для `None` и для `Some`, и именно эта очевидность и есть коммутативность квадрата @fig:nat-square.
]

Семейство `head` не зависит от типа элементов и от конкретной функции --- только от формы контейнеров.
Такое свойство называется параметричностью, и оно автоматически выдаёт квадратики естественности для всех полиморфных функций.
Свободные теоремы Вадлера: каждая полиморфная функция удовлетворяет теореме, которую не нужно доказывать --- она следует из типа#footnote[P. Wadler. "Theorems for free!". Conference on Functional Programming Languages and Computer Architecture, 1989.].

Функторы одной пары категорий сами складываются в категорию.

#definition[Категория функторов][
  Функторы $cal(C) -> cal(D)$ и естественные преобразования между ними образуют *категорию функторов* $[cal(C), cal(D)]$: композиция задаётся покомпонентно, $(beta circle.stroked.tiny alpha)_A = beta_A circle.stroked.tiny alpha_A$.
]

Категория функторов --- третий этаж той же башни: объекты первой стали стрелками второй, и ничего не мешает подняться выше.

#definition[Эквивалентность категорий][
  Функтор $F: cal(C) -> cal(D)$ задаёт *эквивалентность категорий*, если найдётся $G: cal(D) -> cal(C)$ с естественными изоморфизмами $"id"_cal(C) tilde.equiv G circle.stroked.tiny F$ и $F circle.stroked.tiny G tilde.equiv "id"_cal(D)$.
]

Эквивалентность слабее изоморфизма категорий: обратный функтор обязан возвращать каждый объект лишь с точностью до изоморфизма.
Двойственность Стоуна из главы о булевых алгебрах (@chap:boolean) --- эквивалентность категории стоуновых пространств и категории, противоположной категории булевых алгебр: объекты не совпадают буквально, но вся комбинаторика переезжает целиком.
В теории моделей близкое явление называют категоричностью (@chap:constructions): теория имеет единственную модель с точностью до изоморфизма.
Эквивалентность категорий поднимает этот идеал на уровень целых миров: две эквивалентные категории неразличимы категорными средствами.

#check[Естественные преобразования][
  + Выпишите квадрат естественности для `head` и проверьте его явно на списках $[]$ и $[1, 2]$ с функцией $g(x) = x + 1$.

  + Семейство $alpha_A (x) = {x}$, вкладывающее множество $A$ в булеан $cal(P)(A)$ одноэлементными подмножествами, естественно ли относительно функтора $cal(P)$?

  + Почему композиция естественных преобразований покомпонентно снова естественна?
    Достаточно одной строки для каждой компоненты.

  + Чем эквивалентность категорий отличается от изоморфизма категорий?
    Какое из двух требований слабее и почему этого достаточно для переноса теории?
]

== Универсальные свойства <sec:universal>

Вернёмся к совпадениям из введения --- теперь на языке стрелок.
Общий рисунок у произведения множеств, прямого произведения автоматов и конъюнкции такой: объект $P$ с парой стрелок $p_1: P -> A$, $p_2: P -> B$, через который пропускается любая другая пара.
"Пропускается" --- значит для любого $X$ с парой $f: X -> A$, $g: X -> B$ найдётся ровно одна стрелка $chevron.l f, g chevron.r : X -> P$ с $p_1 circle.stroked.tiny chevron.l f, g chevron.r = f$ и $p_2 circle.stroked.tiny chevron.l f, g chevron.r = g$.
Свойство относится не к внутреннему устройству $P$, а к его положению среди стрелок --- потому и называется _универсальным_.

#figure(
  product-universal,
  caption: [Универсальное свойство произведения: любая пара стрелок в $A$ и $B$ раскладывается через проекции ровно одним способом.],
) <fig:product-universal>

Самая экономная версия той же идеи --- объект, определяемый одним универсальным свойством без пар.

#definition[Терминальный объект][
  Объект $1$ называется *терминальным*, если из каждого объекта категории ведёт ровно одна стрелка в $1$.
]

В терминальный объект вся категория "сваливается" единственным способом: в $"Set"$ это одноэлементное множество $1 = {∗}$, и функция в него ровно одна --- она забывает про элемент всё.
В тонкой категории порядка терминальный объект --- наибольший элемент: в него ведёт стрелка-неравенство от каждого.

#definition[Начальный объект][
  Объект $0$ называется *начальным*, если из $0$ в каждый объект категории ведёт ровно одна стрелка.
]

Начальный объект --- его зеркальное отражение: из него в каждое множество ведёт единственная (пустая) функция, а в порядке он будет наименьшим элементом.
В категории с одним объектом, порождённой тривиальным моноидом, этот объект одновременно начальный и терминальный.
Уже у $(ZZ, +, 0)$ стрелок из объекта в себя бесконечно много, так что ни начального, ни терминального объекта нет: крайности существуют не всегда.

#definition[Произведение][
  *Произведением* пары объектов $A$ и $B$ называется объект $A times B$ с парой стрелок $pi_1: A times B -> A$ и $pi_2: A times B -> B$, через который пропускается любая пара стрелок в $A$ и $B$: для $f: X -> A$ и $g: X -> B$ существует ровно одна стрелка $chevron.l f, g chevron.r : X -> A times B$ с $pi_1 circle.stroked.tiny chevron.l f, g chevron.r = f$ и $pi_2 circle.stroked.tiny chevron.l f, g chevron.r = g$.
]

В $"Set"$ это декартово произведение с обычными проекциями, и слово "ровно одна" здесь --- теорема, до сих пор проверявшаяся руками: пара функций в $A$ и $B$ задаёт функцию в $A times B$, и только одна пара функций даёт каждую.

#definition[Копроизведение][
  *Копроизведением* пары объектов $A$ и $B$ называется двойственная конструкция: объект $A plus B$ со стрелками $iota_1: A -> A plus B$ и $iota_2: B -> A plus B$, через который пропускается любая пара стрелок _из_ $A$ и _из_ $B$.
  А именно, для $f: A -> X$ и $g: B -> X$ существует ровно одна стрелка $[f, g]: A plus B -> X$ с $[f, g] circle.stroked.tiny iota_1 = f$ и $[f, g] circle.stroked.tiny iota_2 = g$.
]

В $"Set"$ копроизведение --- дизъюнктное объединение с включениями $iota_1, iota_2$: чтобы задать функцию на нём, достаточно задать её на каждой из частей по отдельности.

#example[Пять глав одной диаграммы][
  - $"Set"$: произведение --- декартово произведение с проекциями (@chap:sets), копроизведение --- дизъюнктное объединение с включениями.
  - Логика высказываний (@chap:logic): произведение в категории истинностных значений --- конъюнкция $p and q$, копроизведение --- дизъюнкция $p or q$; тождества $p -> q or p$ читаются как стрелки.
  - Тонкая категория порядка (@chap:orders): произведение --- точная нижняя грань $a meet b$, копроизведение --- точная верхняя $a join b$; универсальность --- определение грани через импликации между неравенствами.
  - Делимость целых чисел: произведение --- наибольший общий делитель, копроизведение --- наименьшее общее кратное; универсальность НОД --- "любой общий делитель делит его".
  - Автоматы (@chap:automata): прямое произведение автоматов --- произведение в категории автоматов с сохранением переходов; проекции --- две головки чтения.
]

#{
  set par(justify: false)
  table(
    columns: (auto, auto, auto, auto),
    stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
    align: (center, left, left, left),
    table.header([Категория], [Произведение], [Копроизведение], [Терминальный]),
    [$"Set"$], [пары $A times B$], [дизъюнктное объединение], [одноэлементное],
    [логика], [$p and q$], [$p or q$], [$top$ ("истина")],
    [порядок],
    [$a meet b$ --- инфимум],
    [$a join b$ --- супремум],
    [наибольший],

    [делимость], [$"НОД"(a, b)$], [$"НОК"(a, b)$], [$0$ (на всё делится)],
    [автоматы],
    [прямое произведение],
    [нет аналога общего вида],
    [тривиальный автомат],
  )
}

Разберём одну строку таблицы руками, чтобы универсальность перестала быть заклинанием.
В делимости стрелка $a -> b$ означает "$a$ делит $b$", и произведение пары $(12, 18)$ --- объект с делящими их обоими стрелками, через который пропускается любой общий делитель.
НОД $6$ подходит: $6 -> 12$ и $6 -> 18$ есть, а для $X = 2$ со стрелками $2 -> 12$, $2 -> 18$ стрелка $2 -> 6$ существует и единственна (тонкая категория).
Никакой другой общий делитель так не умеет: у $3$ нет стрелки в $12$, у $2$ --- в $18$.
"Наибольший" оказался не про сравнение чисел, а про универсальное положение среди делителей.

Универсальные объекты определяются не однозначно, а "с точностью до изоморфизма" --- и вот первое полноценное доказательство этого принципа.

#theorem[Единственность универсальных объектов][
  Любые два произведения пары $A, B$ изоморфны, причём изоморфизм единственный и согласован с проекциями.
  То же верно для копроизведений, терминальных и начальных объектов.
]

#proof[
  Докажем для произведения.
  Для остальных конструкций рассуждение одинаково, а для копроизведения --- двойственно.

  Пусть $P$ с проекциями $p_1, p_2$ и $P'$ с проекциями $p'_1, p'_2$ --- два произведения $A$ и $B$.
  Применим универсальное свойство $P$ к паре $p'_1: P' -> A$, $p'_2: P' -> B$: существует единственная стрелка $h: P' -> P$ с $p_1 circle.stroked.tiny h = p'_1$ и $p_2 circle.stroked.tiny h = p'_2$.
  Симметрично, существует единственная $h': P -> P'$ с $p'_1 circle.stroked.tiny h' = p_1$ и $p'_2 circle.stroked.tiny h' = p_2$.

  Композиция $h circle.stroked.tiny h': P -> P$ удовлетворяет $p_1 circle.stroked.tiny h circle.stroked.tiny h' = p_1$ и $p_2 circle.stroked.tiny h circle.stroked.tiny h' = p_2$.
  Этим же свойством обладает тождественная стрелка $"id"_P$, а универсальность $P$ даёт _единственность_: $h circle.stroked.tiny h' = "id"_P$.
  Аналогично $h' circle.stroked.tiny h = "id"_(P')$.
  Стрелка $h$ --- искомый изоморфизм, и он единственный по построению.
]

Одна диаграмма, пять столбцов таблицы, одно доказательство единственности --- весь выигрыш языка налицо: доказано сразу про множества, формулы, порядки, числа и автоматы.

Обобщение произведения --- предел.
Возьмём произвольную диаграмму в категории: набор объектов и стрелок между ними, например, три объекта $A, B, C$ со стрелками $A -> C$ и $B -> C$.

#definition[Конус над диаграммой][
  *Конусом* над диаграммой $D$ называется объект $N$ со стрелкой в каждый объект диаграммы, согласованной с её стрелками: для каждой стрелки $u: A -> C$ диаграммы треугольник $N -> A -> C$ и $N -> C$ коммутирует.
]

#definition[Предел][
  *Пределом* $lim D$ называется терминальный объект среди конусов: из любого конуса $N$ в него ведёт ровно одна стрелка, согласованная с ножками.
  *Копредел* --- двойственное понятие, кокус с универсальным свойством: объект со стрелками _из_ диаграммы, через который пропускается любой другой такой.
]

#figure(
  cone-diagram,
  caption: [Конус $N$ и терминальный конус $lim D$: через $lim D$ пропускается любой конус, как через произведение --- любая пара.],
) <fig:cone>

Произведение --- предел диаграммы из двух объектов без стрелок, пересечение подмножеств --- предел диаграммы вложений.
Наименьшая общая верхняя грань цепочки --- копредел цепочки.
Предел обратной системы --- конструкция p-адических чисел из колец вычетов $ZZ\/p^n$: та же диаграмма, прочитанная в других категориях, даёт пополнения и компактные группы.
Единой теоремой о пределах закрываются сразу все эти построения: предел сохраняется функторами, сохраняющими пределы, --- и такие функторы распознаются по сопряжениям, к которым мы идём.

#check[Универсальные свойства][
  + Проверьте, что в тонкой категории порядка произведение пары --- точная нижняя грань: выпишите универсальное свойство на языке неравенств.

  + Почему копроизведение в $"Set"$ не может быть обычным объединением $A union B$?
    Какую роль играют включения $iota_1, iota_2$?

  + Найдите терминальный объект тонкой категории делимости на множестве ${2, 3, 6, 12}$.
    Существует ли в ней начальный объект?

  + Конус над диаграммой из одного объекта $A$ без стрелок --- это просто объект со стрелкой в $A$.
    Что такое предел такой диаграммы?
]

== Лемма Йонеды <sec:yoneda>

Функторы вида $cal(C)(A, -)$ из примера о представимых функторах устроены скромно: они ничего не знают об объектах, кроме стрелок из $A$.
Лемма Йонеды утверждает, что и знать больше не нужно: любой функтор $cal(C) -> "Set"$ можно понять через такие "стрелочные" функторы, и словарь перевода предельно прост.

#theorem[Лемма Йонеды][
  Пусть $cal(C)$ --- категория, $A$ --- её объект, $F: cal(C) -> "Set"$ --- функтор.
  Естественные преобразования $alpha: cal(C)(A, -) -> F$ находятся во взаимно однозначном соответствии с элементами множества $F (A)$, и соответствие естественно по $A$ и по $F$.
]

#proof[
  Докажем, что отображение $alpha partialto alpha_A ("id"_A)$ --- биекция, построив обратное в обе стороны.

  *От семейства к элементу.*
  Каждое естественное преобразование $alpha$ даёт элемент $alpha_A ("id"_A) in F (A)$: компонента $alpha_A$ --- функция из множества стрелок $cal(C)(A, A)$ в $F (A)$, применённая к тождественной.

  *От элемента к семейству.*
  Пусть $x in F (A)$.
  Для каждого объекта $B$ определим компоненту $alpha^x_B: cal(C)(A, B) -> F (B)$ правилом $f partialto F (f)(x)$: стрелка $f: A -> B$ переводится функтором $F$ в функцию $F (f): F (A) -> F (B)$, действующую на элементе $x$.

  *Взаимная обратность.*
  Семейство $alpha^x$ естественно: для $h: B -> C$ выполнено $F (h) circle.stroked.tiny alpha^x_B (f) = F (h)(F (f)(x)) = F (h circle.stroked.tiny f)(x) = alpha^x_C (h circle.stroked.tiny f)$, что и есть коммутативность квадрата естественности.
  В другую сторону, естественность $alpha$ на стрелке $f: A -> B$ даёт равенство $F (f) circle.stroked.tiny alpha_A = alpha_B circle.stroked.tiny cal(C)(A, f)$, применённое к $"id"_A$.
  Подставив элемент, получаем $F (f)(alpha_A ("id"_A)) = alpha_B (f circle.stroked.tiny "id"_A) = alpha_B (f)$, то есть $alpha^(alpha_A ("id"_A))_B = alpha_B$.
  Семейства совпадают покомпонентно.
]

Читается лемма так: _функтор узнаётся по его значениям на стрелках из одного объекта_.
Вся информация о $F$, доступная из $A$, упакована в одно множество $F (A)$.
Естественность --- штраф за согласование, и лемма говорит, что штраф всегда уплачен автоматически.

Следствие при $F = cal(C)(B, -)$ переводит смысл в форму: естественные преобразования $cal(C)(A, -) -> cal(C)(B, -)$ --- это в точности стрелки $B -> A$ в исходной категории.

#proposition[Вложение Йонеды][
  Соответствие $B partialto cal(C)(B, -)$ продолжается до вполне верного функтора $y: cal(C)^op -> [cal(C), "Set"]$.
  Вполне верный означает, что отображение на каждом множестве параллельных стрелок взаимно однозначно: разные стрелки дают разные естественные преобразования, и каждое преобразование возникает из стрелки.
]

#proof-sketch[
  Стрелка $g: B -> A$ --- это стрелка $A -> B$ в $cal(C)^op$, и она порождает естественное преобразование $y(g)_C: cal(C)(A, C) -> cal(C)(B, C)$ предкомпозицией $f partialto f circle.stroked.tiny g$.
  Лемма Йонеды при $F = cal(C)(B, -)$ превращает соответствие $g partialto y(g)$ в биекцию со множеством $cal(C)(B, A)$: обратное направление восстанавливает $g$ как образ тождества $"id"_A$.
  Биективность на каждом множестве параллельных стрелок --- и есть полная верность.
]

Практический смысл лучше всего виден в программировании.
Объект, о котором известно только, как в него стреляют наблюдения, известен полностью: любое свойство-функтор $F$ вычисляется через стрелочный функтор.
Продолжения в функциональных языках --- та же идея в коде: тип $forall B. (A -> B) -> F (B)$ взаимно однозначен с $F (A)$, и это не совпадение, а лемма Йонеды, прочитанная в категории типов.

#remark[Почему это работает][
  Заметим, что вся сила леммы держится на тождественной стрелке $"id"_A$: элемент $x in F (A)$ и семейство функций "из стрелок" определяют друг друга через $x = alpha_A ("id"_A)$.
  Универсальные свойства из предыдущей секции --- та же механика, увиденная сверху: произведение --- представимый функтор пар стрелок, а его единственность из @sec:universal --- тень единственности представляющего элемента.
]

#check[Лемма Йонеды][
  + Пусть $cal(C)$ --- тонкая категория порядка.
    Что такое $cal(C)(A, B)$ как множество, и что означает лемма Йонеды для предпорядка?

  + Для моноида-категории $tilde(M)$ множество $cal(C)(tilde(M), tilde(M))$ --- это сам $M$.
    Во что превращается лемма Йонеды для функтора из $tilde(M)$ в $"Set"$ --- множества с действием моноида?

  + Восстановите по элементу $x in F (A)$ значение компоненты $alpha_B$ на стрелке $f: A -> B$.
    Почему без функториальности $F$ построение бы сломалось?

  + Объясните без формул, почему тип-продолжение $(A -> B) -> F (B)$ для всех $B$ "знает" столько же, сколько $F (A)$.
]

== Сопряжённые функторы <sec:adjunctions>

Свободный моноид над множеством $A$ --- список --- устроен расточительно: он хранит все последовательности.
Забывающий функтор идёт обратно, и композиция $U circle.stroked.tiny L$ теряет информацию.
Но пара функторов связана чем-то большим, чем композиция: гомоморфизмы из свободного моноида $A^*$ в моноид $M$ --- это в точности функции из $A$ в носитель $M$.
Гомоморфизм собирается из значений на буквах, и собирается единственным образом.
Такое соответствие двух множеств стрелок и есть сопряжение --- центральное понятие теории категорий, к которому в конце концов сводятся и пределы, и монады, и почти все универсальные конструкции.

#definition[Сопряжённые функторы][
  Функторы $F: cal(C) -> cal(D)$ и $G: cal(D) -> cal(C)$ образуют *сопряжение* $F dashv G$, если для любых объектов $X in cal(C)$, $Y in cal(D)$ задана биекция
  $ phi_(X, Y): cal(D)(F (X), Y) -> cal(C)(X, G (Y)), $
  естественная по $X$ и по $Y$.
  Функтор $F$ называют _левым сопряжённым_, $G$ --- _правым_.
]

#figure(
  adjunction-bijection,
  caption: [Сопряжение --- биекция между множествами стрелок $F(X) -> Y$ и $X -> G(Y)$, согласованная со всеми остальными стрелками.],
) <fig:adjunction>

Единица и коединица сопряжения получаются из биекции на тождественных стрелках и понадобятся в секции о монадах.
Единица $eta_X = phi_(X, F(X))("id"_(F(X)))$ идёт $X -> G F (X)$, а коединица $epsilon_Y = phi^(-1)_(G(Y), Y)("id"_(G(Y)))$ идёт $F G (Y) -> Y$.

Первый пример уже разобран руками.

#example[Свободный сопряжён к забывающему][
  Для $L dashv U$ между множествами и моноидами биекция переводит гомоморфизм $h: A^* -> M$ в функцию $x partialto h([x])$ на одноэлементных списках.

  Обратное направление продолжает функцию $f: A -> U (M)$ до гомоморфизма: пустой список --- в нейтральный элемент, конкатенация --- в операцию моноида.
  Сопряжение говорит: _продолжение существует и единственно_, --- и это в точности универсальное свойство свободной структуры, с которым мы уже встречались в @sec:functors.
]

Сопряжение между функторами на предпорядках --- конструкция, которую курс уже использовал молча.

#example[Связки Галуа][
  Пусть $cal(C)$ и $cal(D)$ --- тонкие категории предпорядков, а функторы между ними --- монотонные отображения.
  Биекция множеств стрелок в тонкой категории --- это эквивалентность "стрелка есть" на "стрелка есть", и сопряжение $F dashv G$ принимает вид
  $ F (x) <= y quad <=> quad x <= G (y). $

  Это определение связки Галуа из главы об абстрактной интерпретации (@chap:abstract-interp): пара $alpha, gamma$, связывающая конкретный и абстрактный домены, с условием $alpha(X) prec.eq d <=> X subset.eq gamma(d)$.
  Весь статический анализ программ построен на этом сопряжении: приближение $alpha$ и конкретизация $gamma$ --- левый и правый сопряжённые между решётками состояний, и корректность переноса свойств --- следствие сопряжённости.
]

Третье сопряжение --- самое близкое программисту.

#example[Каррирование][
  В категории множеств (и в категории типов) биекция
  $ cal(D)(A times B, C) tilde.equiv cal(D)(A, C^B) $
  переводит функцию двух аргументов в функцию, возвращающую функцию.
  Знак $C^B$ --- _экспоненциал_: объект, изображающий функции из $B$ в $C$ как единый объект.

  Биекция естественна по всем трём переменным, поэтому $- times B dashv (-)^B$: взятие произведения слева сопряжено к экспоненцированию.
  Декартово замкнутая категория --- категория с конечными произведениями и всеми экспоненциалами.
  Категория типов функционального языка декартово замкнута, и каррирование из главы о лямбда-исчислении (@chap:lambda) --- проявление этого сопряжения.
]

#proposition[Правый сопряжённый сохраняет пределы][
  Пусть $F dashv G$ и в $cal(D)$ существует предел $lim D$ диаграммы $D$.
  Тогда $G (lim D)$ --- предел диаграммы $G D$ в $cal(C)$.
]

#proof-sketch[
  Конус над $G D$ с вершиной $X$ --- это семейство стрелок $X -> G(D_i)$, биективно соответствующее семейству $F (X) -> D_i$, то есть конусу над $D$ с вершиной $F (X)$.
  Универсальный конус $lim D$ пропускает его через себя единственным образом, и биекция сопряжения переводит это разложение обратно в разложение через $G (lim D)$.
  Единственность сохраняется биекцией.
]

Почему про сопряжения говорят, что они "возникают везде": почти каждая универсальная конструкция этой главы --- тень некоторого сопряжения.
Произведения --- правые сопряжённые к диагональному функтору, пределы --- тоже, свободные структуры --- левые сопряжённые к забывающим, а лемма Йонеды --- сопряжение в маске.
Даже связка Галуа из анализа программ и экзистенциальный квантор в логике --- сопряжения, читающиеся в подходящих категориях.

#check[Сопряжённые функторы][
  + Проверьте естественность биекции $L dashv U$: что происходит с гомоморфизмом $A^* -> M$ при предкомпозиции функцией $k: A' -> A$?

  + Выпишите единицу сопряжения $- times B dashv (-)^B$: в какую стрелку переходит тождественная функция на $A times B$?

  + Почему связка Галуа из @chap:abstract-interp обязана сохранять точные грани одной из своих сторон?
    Какая именно сторона и почему?

  + Предположим, $F dashv G$ и $F' dashv G$.
    Покажите, используя биекцию, что $F tilde.equiv F'$ естественным образом.
]

== Монады и композиция эффектов <sec:monads>

Программистская функция из $A$ в $B$ редко бывает просто функцией.
Она может отказать (`Option`), вернуть несколько ответов (`Vec`), обратиться к вводу-выводу, изменить состояние, ждать.
У всех этих историй общая скелетная схема: тип результата оборачивается, и композиция таких функций требует склеивать обёртки.
Монада --- категорная формализация этой склейки, до того удачная, что стала конструкцией языков программирования#footnote[B. Milewski. "Category Theory for Programmers". 2019.].

Начнём с отказа, самого простого эффекта.
Парсер числа в строке --- функция `&str -> Option<i32>`, а валидатор --- `i32 -> Option<Email>`.
Как компоновать парсер с валидатором, чтобы отказ любого звена останавливал цепочку, видно из кода.

```rust
// Композиция Клейсли: отказ любого звена отказывает всю цепочку.
fn and_then<T, U, V>(
    parse: impl Fn(T) -> Option<U>,
    validate: impl Fn(U) -> Option<V>,
) -> impl Fn(T) -> Option<V> {
    move |x| parse(x).and_then(|y| validate(y))
}
// and_then(p, v)(x) = None, если p(x) = None --- короткое замыкание
```

За этим кодом стоит категория.
Объекты --- типы, стрелки из $A$ в $B$ --- функции $A -> "Option"(B)$.
Композиция стрелок --- склейка из кода выше, тождество из $A$ в $A$ --- функция $x partialto "Some"(x)$.
Ассоциативность и тождественность несложно проверить напрямую --- и они не случайны: перед нами _категория Клейсли_ монады `Option`, построенная по общему рецепту, который появится сейчас.

#definition[Монада][
  *Монада* на категории $cal(C)$ --- это функтор $T: cal(C) -> cal(C)$ вместе с естественными преобразованиями
  - единицей $eta: "Id" -> T$ (в каждой компоненте $eta_A: A -> T (A)$) и
  - умножением $mu: T circle.stroked.tiny T -> T$ (в каждой компоненте $mu_A: T (T (A)) -> T (A)$),

  удовлетворяющими законам:
  $
    mu circle.stroked.tiny T mu = mu circle.stroked.tiny mu T quad "и" quad mu circle.stroked.tiny T eta = mu circle.stroked.tiny eta T = "id".
  $
]

Для монады `Option` единица оборачивает значение в `Some`, а умножение `join` сплющивает `Option<Option<T>>` в `Option<T>`: внешний `None` отказывает всё, внешний `Some(x)` оставляет внутреннее.
Для списка единица --- одноэлементный список, умножение --- конкатенация списка списков.
Законы монады гарантируют, что склейки не зависят от расстановки скобок и что лишние обёртки ничего не меняют.

#example[Категория Клейсли][
  Категория Клейсли $cal(C)_T$ монады $T$ имеет те же объекты, что $cal(C)$, а стрелки из $A$ в $B$ --- стрелки $A -> T (B)$ исходной категории.
  Композиция определена через единицу и умножение:
  $
    g circle.stroked.tiny_T f = mu_B circle.stroked.tiny T (g) circle.stroked.tiny f, quad "id"_A = eta_A.
  $

  Для `Option` это в точности `and_then` из кода, для списка --- `flat_map`.
  Законы монады --- необходимое и достаточное условие того, что эта композиция ассоциативна и тождественна.
]

Откуда берутся монады в природе?
Почти всегда --- из сопряжений, как в следующем примере, и это ещё одна причина, по которой сопряжения названы центральным понятием.

#example[Монада списка из сопряжения][
  Композиция $T = U circle.stroked.tiny L$ свободного и забывающего функторов из @sec:functors переводит множество $A$ в множество букв всех списков над $A$.
  Единица $eta: "Id" -> T$ --- вставка элемента в одноэлементный список, умножение $mu: T T -> T$ --- конкатенация списка списков.

  Обе структуры естественны, и законы монады выполняются: конкатенация списка списков списков не зависит от порядка сплющивания, а одноэлементные списки нейтральны.
  Сопряжение $L dashv U$ породило монаду --- общий механизм, работающий для любой пары сопряжённых функторов.
]

#figure(
  monad-from-adjunction,
  caption: [Сопряжение $F dashv G$ сворачивается в монаду $T = G circle.stroked.tiny F$ на исходной категории.],
) <fig:monad-from-adj>

#remark[Монада --- моноид в категории эндофункторов][
  Знаменитую фразу "монада --- это просто моноид в категории эндофункторов" стоит разобрать без мемов.
  Эндофункторы $cal(C) -> cal(C)$ образуют категорию с композицией как "умножением".
  Моноид из @chap:algebra --- это объект с ассоциативной операцией и нейтральным элементом.
  В категории функторов ту же роль играют $mu$ и $eta$.
  Законы монады --- моноидные законы, прочитанные построчно: $mu$ ассоциативно, $eta$ нейтрально.
  Шутка остаётся шуткой, но за ней --- точное утверждение: монады продолжают линию моноидов в новый мир, и моноид из одной главы стал строительным блоком другой.
]

Практическая отдача монад --- дисциплина эффектов.
Функция с типом $A -> "Option"(B)$ честно объявляет возможность отказа, $A -> "Vec"(B)$ --- множественность, $A -> "State"(s, B)$ --- доступ к состоянию.
Тип --- контракт, и категория Клейсли --- система композиции контрактов, в которой эффекты не теряются и не дублируются.
Оператор `?` в Rust, `and_then` в идиоматичном коде, `>>=` в Haskell --- синтаксис поверх одной и той же диаграммы.

#check[Монады][
  + Проверьте закон тождества для монады `Option`: `join(Some(x)) = x` и `join` после `unit` --- тождественная функция.

  + Что означает ассоциативность умножения для списка списков списков?
    Приведите пример из трёх уровней вложенности.

  + Выпишите компоненты $eta$ и $mu$ монады, порождённой сопряжением из @sec:adjunctions для свободной группы вместо свободного моноида.

  + Почему для монады `Vec` композиция Клейсли --- это `flat_map`, а не `map`?
    Что сломалось бы при замене?
]

== \* Стринг-диаграммы <sec:string>

Стрелочная запись держит диаграмму в одном измерении: композиция идёт вдоль направления стрелок.
Для функторов двух аргументов --- $F (A, B)$, "бифункторов" --- удобнее второе измерение.
Запишем объекты вертикальными _проводами_, а стрелки --- узлами на них: операция $m: A times B -> C$ рисуется узлом с двумя входящими проводами и одним исходящим.

#figure(
  string-diagram,
  caption: [Стринг-диаграмма: узлы --- операции, провода --- объекты, композиция идёт снизу вверх.],
) <fig:string>

Композиция морфизмов --- вертикальное сцепление узлов, композиция по второму аргументу --- горизонтальное.
Категория с операцией $times$, согласованной с композицией (моноидальная категория), --- точный аналог моноида, "поднятого" на уровень категорий, и стринг-диаграммы --- её родная нотация.
Равенство диаграмм с точностью до непрерывной деформации проводов --- тень законов категории, и в моноидальном мире перестановки проводов сами становятся объектами теории.

Для программиста провода --- потоки данных, а узлы --- операции.
Потоковые графы обработки и dataflow-пайплайны --- стринг-диаграммы, встреченные в индустрии.
Теория категорий даёт им алгебру: когда два пайплайна эквивалентны --- вопрос о деформации диаграммы, а не о пошаговой симуляции.

== \* Алгебры и коалгебры <sec:algebras-coalgebras>

Свёртка списка с нулём и шагом --- приём из главы об алгебраических структурах (@chap:algebra) --- до сих пор оставалась просто идиомой кода.
Категорный взгляд превращает её в теорему о начальном объекте.

Фиксируем функтор $F$ на категории.
_F-алгеброй_ называется объект $X$ со стрелкой $F (X) -> X$: алгебра --- способ сворачивать "F-образные конструкции над $X$" в элемент $X$.
Для списка подходит $F (X) = 1 + A times X$: конструкция --- либо пустой маркер, либо пара (голова, хвост).
Алгебра --- пара операций: что вернуть для пустого и как обработать голову с хвостом, то есть в точности аргументы `fold`.

#definition[Начальная алгебра][
  *Начальной F-алгеброй* называется начальный объект в категории F-алгебр: алгебра $alpha: F (mu F) -> mu F$, из которой в любую алгебру $beta: F (X) -> X$ ведёт единственный гомоморфизм.
]

Этот единственный гомоморфизм из $mu F$ в $X$ называют *катаморфизмом*: свёртка структуры по шагу $beta$.
Для списка это `fold`, для дерева --- свёртка по вершинам: имя меняется, механизм один.

#figure(
  initial-algebra,
  caption: [Список --- начальная алгебра $1 + A times (-)$: свёртка в любую структуру существует и единственна.],
) <fig:initial-alg>

Начальная алгебра функтора $1 + A times (-)$ --- список $A^*$: его алгебра --- конструкторы `nil` и `cons`, а катаморфизм --- свёртка по этим конструкторам.
Единственность из универсального свойства --- доказательство того факта, что две свёртки с одинаковыми нулём и шагом совпадают: раньше это проверялось индукцией по списку, теперь --- читается со стрелки.
Рекурсивные типы в функциональных языках --- неподвижные точки функторов, и компилятор с их универсальным свойством разбирается за нас.

Двойственная конструкция смотрит в другую сторону: _F-коалгебра_ --- объект $X$ со стрелкой $X -> F (X)$, способ _разворачивать_ элемент в структуру.
Коалгебра функтора $2 times (-)^A$ --- автомат: элемент-состояние разворачивается в пару "принимающее ли это состояние" и "функция перехода по букве"#footnote[J. J. M. M. Rutten. "Universal coalgebra: a theory of systems". Theoretical Computer Science, 2000.].
_Финальная_ коалгебра собирает все возможные поведения, а уникальное отображение в неё --- минимизация: два состояния, неразличимые наблюдениями, склеиваются (@chap:automata).
Алгебры задают, как строить данные из частей, а коалгебры --- как наблюдать системы в работе.
Индукция и коиндукция --- двойственные взгляды на одно и то же определение, и теория автоматов --- её самый близкий программистам приют.

== \* Метрики, вероятности и нечёткость <sec:enriched>

Определение категории требует, чтобы стрелки каждого Hom-множества составляли множество --- обычную структуру без внутреннего устройства.
А если потребовать больше: пусть $cal(C)(A, B)$ живёт в другой категории, например в категории чисел?
Такие категории называются _обогащёнными_, и самым красивым примером служит метрика.

Зафиксируем категорию чисел $[0, infinity]$ с одним "морфизмом" $a -> b$ при $a >= b$ и моноидальной операцией сложения: морфизмы компонуются сложением, нейтральный морфизм --- ноль.
Обогащённая категория над ней задаётся функцией $d(A, B) in [0, infinity]$ на парах объектов, где композиция и тождество читаются как неравенства:
$d(A, A) = 0$ и $d(A, C) <= d(A, B) + d(B, C)$.
Второе --- неравенство треугольника: категорное определение выдало аксиомы метрики почти бесплатно.
Взгляд Ловер на метрические пространства как на обогащённые категории#footnote[F. W. Lawvere. "Metric spaces, generalized logic, and closed categories". Rendiconti del Seminario Matematico e Fisico di Milano, 1973.] перекидывает мост между анализом и алгеброй: пополнение пространства оказывается копределом, а сжимающие отображения --- функторами.
Аксиомы при этом дают квазиметрику: симметрия и разделимость из обогащения не следуют и требуют отдельных условий.

Нечёткие множества из последней главы курса (@chap:fuzzy) встают в ту же рамку.
Заменим $[0, infinity]$ на отрезок $[0, 1]$ с t-нормой: "композиция" степеней принадлежности $a, b$ --- это $t(a, b)$.
Обогащённая категория над $[0, 1]$ --- структура с _порогами_: степень $d(A, B)$ того, что "$A$ переходит в $B$", удовлетворяет треугольному неравенству $d(A, C) <= t(d(A, B), d(B, C))$.
Функция принадлежности $mu: U -> [0, 1]$ из главы о нечёткости задаёт обогащённый предпучок над этой категорией, а выбор t-нормы --- выбор моноидальной структуры на значениях.
Вероятности присоединяются третьим жестом: в марковских категориях морфизмы несут распределения, условные вероятности становятся композициями, а теорема Байеса --- свойством обратной стрелки.

Одна секция --- три сюжета из разных концов математики.
Общий механизм: Hom-объекты с внутренней структурой, категорные аксиомы --- согласование структуры с композицией.
Это и есть категорный стиль работы: не изобретать язык для каждой области заново, а находить уже готовый.

== \* Топосы и внутренняя логика <sec:topos>

Категория множеств умеет в логику.
Подмножество $S subset.eq A$ --- это то же самое, что характеристическая функция $A -> {0, 1}$ из главы о множествах (@chap:sets), и логические операции над подмножествами --- поточечные операции над нулями и единицами.
Пересечение --- минимум значений, объединение --- максимум, дополнение --- переворот.
Вопрос: какие категории умеют делать то же самое, даже если "элементов" в них нет?

Ответ требует двух ингредиентов.
Декартова замкнутость из примера о каррировании даёт произведения и экспоненциалы --- "функции как объекты".
А логику даёт _классификатор подобъектов_: объект $Omega$ вместе с естественной биекцией между подобъектами любого объекта $A$ и стрелками $A -> Omega$.
В $"Set"$ это в точности соответствие "подмножество $arrow.l.r$ характеристическая функция": $Omega = {0, 1}$.
Категория с конечными произведениями, экспоненциалами и классификатором подобъектов называется _топосом_.

Классификатор превращает логику в геометрию.
Стрелка $A -> Omega$ --- это "высказывание об $A$ с доказательствами": в $"Set"$ оно принимает значение 1 ровно на элементах подмножества.
Операции логики становятся стрелками $Omega -> Omega$: конъюнкция $Omega times Omega -> Omega$, отрицание $Omega -> Omega$, импликация $Omega times Omega -> Omega$.
Они задаются не таблицей, а устройством категории, и таблица получается как следствие.

И вот поворот: алгебра операций на $Omega$ --- это алгебра Гейтинга из интуиционистской главы курса (@chap:intuitionism).
Импликация Гейтинга удовлетворяет $p and (p -> q) -> q$, но закон исключённого третьего $p or (not p)$ не обязан выполняться: в общем топосе у $Omega$ больше двух "степеней истинности", и отрицание возвращает не всюду ложь.
Интуиционистская логика --- не ослабление классической из осторожности: это в точности логика, которая живёт внутри произвольного топоса.
Классика возвращается там, где $Omega$ булева --- как в $"Set"$ или в топосах пучков над дискретным пространством.

Примеры топосов показывают масштаб конструкции.
Категория множеств --- простейший топос, его внутренняя логика классична.
Категория пучков над топологическим пространством --- топос, чья логика чувствует геометрию пространства: истинность может меняться от точки к точке, и "существует" означает "существует в окрестности".
Двойственность Стоуна из @chap:boolean в этой рамке --- представление булевых алгебр пучками над стоуновым пространством: теорема о структурах становится предложением о топосах.

Для программиста след топоса --- теория типов с зависимыми типами и доказательные ассистенты.
Предикат-семейство типов, высказывание-тип с доказательствами-элементами: логика и типы населяют одну структуру, и соответствствие Карри--Говарда из @chap:lambda, продолженное Ламбеком до категорий, --- её первая буква#footnote[J. C. Baez, M. Stay. "Physics, Topology, Logic and Computation: A Rosetta Stone". New Structures for Physics, 2011.].

== Итоги главы

Глава началась с совпадений: произведение множеств, прямое произведение автоматов, конъюнкция, НОД --- разные главы, один рисунок с парой проекций и универсальным свойством.
Ответ оказался языком: категория --- объекты, стрелки и композиция, --- в котором "быть произведением" является свойством положения среди стрелок, доказывается один раз и переносится в любую категорию.
Язык честно отработал на всём пройденном материале.
Моноиды и порядки оказались категориями в двух крайних режимах, связка Галуа из анализа программ --- сопряжением, свёртка списка --- катаморфизмом начальной алгебры, а каррирование и нечёткость встали в общий ряд конструкций.

Три уровня языка сложились в пирамиду.
Категории описывают отдельные миры, функторы переносят между мирами, сохраняя композицию, а естественные преобразования сравнивают переносы.
На этой тройке держатся универсальные свойства и пределы, лемма Йонеды с её "объект --- сеть отношений", сопряжения и монады.
Каждый уровень оплачен примерами: булеан и прообраз, свободный список и забывающий функтор, `Option` и `Vec` в роли монад.

Для программиста итог конкретнее.
Типы и функции образуют категорию, полиморфные конструкции --- функторы и естественные преобразования, алгебраические типы --- начальные алгебры, эффекты --- монады, а тип с эффектом --- стрелка Клейсли.
Категорный язык не заменяет практику кода, но даёт ей словарь, в котором `map`, `and_then`, `flat_map` и `fold` перестают быть идиомами и становятся теоремами.

#check[Проверьте себя][
  + Что общего у моноида и частично упорядоченного множества как категорий и чем они различаются?

  + Сформулируйте универсальное свойство произведения и объясните, почему НОД ему удовлетворяет в категории делимости.

  + Что утверждает лемма Йонеды и как она объясняет фразу "объект определяется своими отношениями"?

  + Почему связка Галуа из абстрактной интерпретации --- частный случай сопряжения функторов?

  + Как монада делает композицию функций с эффектами ассоциативной?
    Что в ней играет роль нейтрального элемента?

  + Что общего между свёрткой списка и минимизацией автомата?
]

#whats-next

Категорный аппарат не заканчивается на этой главе --- он только начат.
Моноидальные категории и стринг-диаграммы ведут в квантовые вычисления и теорию сетей, высшие категории --- в топологию и гомотопическую теорию типов, пучки и топосы --- в геометрию и логику.
Прикладная волна применяет тот же язык к базам данных, вероятностям и машинному обучению.
Разница между читателем и исследователем теперь лишь в запасе примеров: словарь у обоих один.

== Упражнения

#tasklist("m36")[
  _Категории и примеры._
  + Проверьте, что $(ZZ_n, +, 0)$ задаёт категорию с одним объектом.
    Выпишите таблицу композиции стрелок для $n = 4$ и найдите изоморфизмы.

  + Постройте свободную категорию графа с рёбрами $A -> B$, $B -> C$, $A -> C$.
    Сколько стрелок из $A$ в $C$?
    Какие пары стрелок компонуемы?

  + Докажите, что в тонкой категории порядка каждая стрелка --- мономорфизм и эпиморфизм одновременно.

  + \* Найдите категорию, в которой стрелка является мономорфизмом и эпиморфизмом, но не изоморфизмом.
    Укажите объекты, стрелки и проверьте все три свойства.
    #metadata((ex-id: "m36:t4"))

  _Функторы и естественность._
  + Проверьте, что взятие носителя переводит категорию $"Mon"$ в $"Set"$: что происходит с тождественным гомоморфизмом и композицией?

  + Для функции $f: {1, 2} -> {a, b}$, $f(1) = a$, $f(2) = a$ вычислите образ множества ${1, 2}$ и прообраз множества ${b}$.
    Проверьте на этом примере контравариантность прообраза для композиции с $g: {a, b} -> {x, y}$, $g(a) = x$.

  + Докажите, что семейство $alpha_A (x) = {x}$ из $A$ в $cal(P)(A)$ естественно: выпишите квадрат и проверьте обе функции на элементах.

  + \* Функтор $cal(C)(A, -)$ переводит объект в множество стрелок.
    Докажите, что он сохраняет произведения: $cal(C)(A, B times C) tilde.equiv cal(C)(A, B) times cal(C)(A, C)$.
    #metadata((ex-id: "m36:t8"))

  _Универсальные свойства и Йонеда._
  + Найдите терминальные и начальные объекты в категории $"Set"^op$.
    Сравните с $"Set"$.

  + Проверьте универсальное свойство НОД на паре $(12, 18)$: любой общий делитель делит НОД, и как из этого следует единственность.

  + Постройте соответствие леммы Йонеды для функтора $cal(P)$ булеана на одноэлементном множестве $A = {1}$: перечислите естественные преобразования $cal(C)(A, -) -> cal(P)$ и укажите их образы в $cal(P)(A)$.

  + \* Докажите, что $cal(P)(A) tilde.equiv cal("Set")(A, 2)$: булеан --- представимый функтор.
    Какой объект его представляет?
    #metadata((ex-id: "m36:t11"))

  _Сопряжения и монады._
  + Выпишите биекцию сопряжения $L dashv U$ явно для моноида $({e, s}, star)$ из двух элементов и множества $A = {a}$: сколько гомоморфизмов $A^* -> tilde(M)$ и сколько функций $A -> {e, s}$?

  + Проверьте, что $(alpha, gamma)$ из главы об абстрактной интерпретации со знаковой областью задаёт связку Галуа: $alpha(X) prec.eq d <=> X subset.eq gamma(d)$ для $X = {-2, 3}$, $d = plus$.

  + Для монады `Option` проверьте оба закона тождества и ассоциативность умножения перебором форм значений `Option<Option<Option<i32>>>`: `None`, `Some(None)`, `Some(Some(None))`, `Some(Some(Some(x)))`.

  + \* Пусть $T$ --- монада списка.
    Что такое категория Клейсли для $T$?
    Выпишите композицию стрелок $A -> T (B)$ и $B -> T (C)$ через `flat_map` и проверьте ассоциативность на примере списков длины 2.
    #metadata((ex-id: "m36:t14"))
]

#project[Конечные категории под микроскопом][
  Реализуйте библиотеку конечных категорий и проверьте на ней главные теоремы --- от аксиом до леммы Йонеды --- конечным перебором.
  Категория представляется списком объектов и таблицей композиции стрелок, поэтому каждое утверждение главы превращается в проверяемый эксперимент.

  #project-stage[Категории из графов, порядков и моноидов][
    Структуры: категория из моноида (объект один, стрелки --- элементы), тонкая категория из отношения порядка и свободная категория ориентированного графа (стрелки --- пути, композиция --- конкатенация путей).
    Проверка аксиом: тотальность композиции, ассоциативность и тождества --- циклом по таблице.
    Убедитесь, что нетранзитивное отношение честно проваливает проверку.
  ]
  #project-stage[Функторы и естественность перебором][
    Функтор --- пара отображений (объекты, стрелки) с проверкой законов.
    Постройте функторы между своими категориями: забывающий, коллапс свободной категории в цепочку, удвоение моноида.
    Естественные преобразования перечисляются декартовым произведением Hom-множеств с фильтром по коммутативности квадратов.
  ]
  #project-stage[Лемма Йонеды][
    Для объекта $A$ постройте Hom-функтор $cal(C)(A, -)$ как функтор в конечные множества.
    Перечислите все естественные преобразования $cal(C)(A, -) -> F$ для нескольких $F$ и сравните с элементами $F (A)$: биекция из леммы должна проявиться численно, включая соответствие $alpha partialto alpha_A ("id"_A)$.
  ]
  #project-stage[Клейсли в коде][
    Реализуйте композицию Клейсли для `Option` и `Vec` и проверьте ассоциативность и тождества на наборах примеров, включая отказы на середине цепочки.
    Сравните количество successful проходов у `Vec`-цепочек с теорией списков.
  ]

  #project-outcome[
    библиотека с проверкой аксиом, таблицей перечисленных естественных преобразований и численным подтверждением биекции Йонеды на трёх категориях: моноид $ZZ_4$, цепочка $0 < 1 < 2$, свободная категория графа из двух рёбер.
  ]
]
