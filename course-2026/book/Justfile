[private]
default:
    @just --list

# Собрать книгу в book.pdf
build:
    typst compile --root .. book.typ

# Стилевой аудит глав книги (.dev/scripts/audit.py)
lint:
    python3 ../.dev/scripts/audit.py
