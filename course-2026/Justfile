compile := "typst compile"

[private]
@default:
    just --list

book:
    {{ compile }} notes/book.typ

problems:
    {{ compile }} notes/problems.typ

lint:
    cd notes && uv run --project ../.dev/1_active/text-checker python -m textcheck check --lang ru --config ../.textcheck.toml --severity warning --format rich m*.typ
