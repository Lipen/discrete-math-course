compile := "typst compile --root ."

[private]
@default:
    just --list

book:
    {{ compile }} notes/book.typ

problems:
    {{ compile }} notes/problems.typ

lec-logic:
    {{ compile }} lectures/lec-logic.typ

lec-sets:
    {{ compile }} lectures/lec-sets.typ

lec-relations:
    {{ compile }} lectures/lec-relations.typ

lec-functions:
    {{ compile }} lectures/lec-functions.typ

lec-cardinals:
    {{ compile }} lectures/lec-cardinals.typ

lec-orders:
    {{ compile }} lectures/lec-orders.typ

lec-graphs:
    {{ compile }} lectures/lec-graphs.typ

lec-boolean:
    {{ compile }} lectures/lec-boolean.typ

lec-circuits:
    {{ compile }} lectures/lec-circuits.typ

lec-codes:
    {{ compile }} lectures/lec-codes.typ

lec-sat:
    {{ compile }} lectures/lec-sat.typ

course-intro:
    {{ compile }} lectures/course-intro.typ

lectures: lec-logic lec-sets lec-relations lec-functions lec-cardinals lec-orders lec-graphs lec-boolean lec-circuits lec-codes lec-sat course-intro

lint:
    cd notes && uv run ../tools/audit.py
