// s2, лекция 14 --- Бином и включение-исключение (неделя 10--16 мая).
#import "theme.typ": *
#import "diagrams/ghosts.typ": (
  catalan-ghost, choose-ghost, coeff-extract-ghost, counting-tree-ghost,
  necklace-ghost, ogf-ghost, pigeonhole-ghost, ramsey-ghost, recurrence-ghost,
  venn3-ghost,
)
#import "diagrams/m17.typ": pascal-triangle, venn-inclusion-exclusion
#import "diagrams/m18.typ": catalan-lattice, decision-tree
#show: slides.with(
  title: [Бином и включение-исключение],
  subtitle: [Дискретная математика --- лекция 14, 10--16 мая],
  date: "Весна 2027",
  authors: "Константин Чухарев",
  module: "combinatorics",
)

= Лекция 14. Бином и включение-исключение

#lecture-slide(
  14,
  [Бином и включение-исключение],
  week: [10--16 мая],
  teaser: [
    Бином Ньютона, треугольник Паскаля и включение-исключение.
    Последнее посчитает то, что не складывается напрямую.
  ],
)

= Перестановки и сочетания

#focus-slide(
  ghost: choose-ghost,
  epigraph: [Делить каждую из рассматриваемых трудностей на столько частей, сколько требуется, чтобы их лучше разрешить.],
  epigraph-author: [Рене Декарт],
)

== Перестановки

#definition[Перестановка][
  Упорядочение $n$ различных объектов.

  Число перестановок: $n! = 1 dot 2 dot dots.c dot n$, соглашение $0! = 1$.
]

Факториал растёт стремительно: $5! = 120$, $10! = 3 628 800$, $20! approx 2.43 dot 10^18$.

#note[
  Формула Стирлинга: $n! tilde.op sqrt(2 pi n) (n / e)^n$.

  Для $n = 20$ погрешность --- полпроцента, а в анализе алгоритмов такой точности хватает всегда.
]

Соглашение $0! = 1$ кодирует единственность пустой перестановки --- без него перестали бы работать рекуррента и бином.

== Матрица выбора

Перестановки, размещения и сочетания различаются ответами на два вопроса: важен ли порядок и разрешены ли повторения.

#table(
  columns: (auto, auto, auto),
  align: (left, center, center),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([], [*Повторения запрещены*], [*Повторения разрешены*]),
  [*Порядок важен*], [$P(n, k) = n! / (n - k)!$], [$n^k$],
  [*Порядок не важен*], [$binom(n, k)$], [$binom(n + k - 1, k)$],
)

Одна таблица вместо четырёх формул: каждая клетка --- ответ на два вопроса.

Звёзды и перегородки уже считали правую нижнюю клетку.

== Размещения

#definition[Размещение (_$k$-permutation_)][
  Выбор $k$ из $n$ с учётом порядка:
  $ P(n, k) = n dot (n - 1) dots.c (n - k + 1) = n! / (n - k)!. $
]

Правило произведения в чистом виде: на первое место $n$ кандидатов, на второе $n - 1$, на последнее $n - k + 1$.

#example[
  Пьедестал (золото, серебро, бронза) из 10 участников: $P(10, 3) = 10 dot 9 dot 8 = 720$.
]

== Сочетания

#definition[Сочетание][
  Выбор $k$ из $n$ без учёта порядка:
  $ binom(n, k) = n! / (k! (n - k)!) = P(n, k) / k!. $
]

Каждое сочетание даёт $k!$ размещений --- перестановок выбранных элементов, --- поэтому делим.

#example[
  Комитет из 3 человек из 10 кандидатов: $binom(10, 3) = 120$.
]

Рукопожатия в компании из $n$: наивный подсчёт даёт $n^2$, но пара $A$--$B$ совпадает с $B$--$A$, а рукопожатий с собой нет --- $(n^2 - n) / 2 = binom(n, 2)$.

== Сочетания с повторениями

#definition[Сочетания с повторениями][
  Выбор $k$ элементов из $n$ типов без учёта порядка: $binom(n + k - 1, k)$.
]

Формула --- те же звёзды и перегородки: $k$ звёздочек и $n - 1$ перегородка между типами.

То же число --- решений уравнения $x_1 + dots.c + x_n = k$ в неотрицательных целых.

#example[
  Три шарика мороженого из 5 сортов: $binom(3 + 5 - 1, 3) = binom(7, 3) = 35$.
]

== Перестановки с повторениями

#definition[Перестановки с повторениями][
  Если среди $n$ объектов $n_1$ копий первого типа, $n_2$ --- второго и так далее, число перестановок
  $ n! / (n_1! thin n_2! thin dots.c thin n_k !) $
  --- мультиномиальный коэффициент.
]

Перестановок различных объектов было бы $n!$, но копии одного типа неотличимы --- каждый тип делит ответ на $n_i !$.

#example[
  Анаграммы слова БАЛАЛАЙКА: 9 букв, из них А --- 4 раза, Л --- 2: $9! / (4! dot 2!) = 7 560$.
]

= Бином и включение-исключение

#focus-slide(
  ghost: venn3-ghost,
  epigraph: [Последний шаг разума --- признать, что есть бесконечное число вещей, его превосходящих.],
  epigraph-author: [Блез Паскаль],
)

== Бином Ньютона

#theorem[Бином Ньютона][
  $ (x + y)^n = sum_(k = 0)^n binom(n, k) x^(n - k) y^k. $
]

Слагаемое $x^(n - k) y^k$ получается, когда буква $y$ выбирается ровно в $k$ из $n$ скобок --- $binom(n, k)$ способами.

#example[
  Подстановка $x = y = 1$: $sum_(k = 0)^n binom(n, k) = 2^n$ --- число всех подмножеств.
]

#example[
  Подстановка $x = 1, y = -1$: $sum_(k = 0)^n (-1)^k binom(n, k) = 0$ при $n >= 1$.
]

== Треугольник Паскаля

#align(center)[#pascal-triangle]

Каждая строка --- биномиальные коэффициенты $binom(n, k)$ при $n = 0, 1, 2, dots$

Строка зеркальна: $binom(n, k) = binom(n, n - k)$ --- выбрать $k$ включаемых или $n - k$ исключаемых, это одно подмножество.

== Тождество Паскаля

#theorem[Тождество Паскаля][
  $ binom(n, k) = binom(n - 1, k - 1) + binom(n - 1, k). $
]

#proof[
  Докажем разбором случаев: входит ли фиксированный элемент в выборку.

  Если входит --- осталось выбрать $k - 1$ из $n - 1$ остальных: $binom(n - 1, k - 1)$ способов.

  Если не входит --- все $k$ выбираются из $n - 1$: $binom(n - 1, k)$ способов.
]

Каждое число треугольника --- сумма двух над ним.

== Хоккейная клюшка и Вандермонд

#note[
  - Хоккейная клюшка: $sum_(i = k)^n binom(i, k) = binom(n + 1, k + 1)$ --- подмножества размера $k + 1$ из $\{0, dots.c, n\}$ группируются по наибольшему элементу.
  - Свёртка Вандермонда: $binom(m + n, r) = sum_(k) binom(m, k) binom(n, r - k)$ --- выбрать $r$ из $m$ математиков и $n$ физиков, взяв $k$ из первых.
]

#example[
  $binom(2, 2) + binom(3, 2) + binom(4, 2) + binom(5, 2) = 1 + 3 + 6 + 10 = 20 = binom(6, 3)$.
]

#important[
  Тождество доказывается комбинаторно: обе стороны считают одно и то же множество двумя маршрутами.

  Такой подсчёт объясняет, _почему_ тождество верно, --- раскрытие скобок этого не даёт.
]

== Бином в анализе алгоритмов

Вложенный цикл «каждый с каждым» делает $binom(n, 2) = n (n - 1) / 2$ сравнений --- это $O(n^2)$.

Полный перебор подмножеств --- $sum_(k = 0)^n binom(n, k) = 2^n$ масок, триллионы уже при $n = 40$.

#theorem[Взвешенная сумма][
  $ sum_(k = 0)^n k binom(n, k) = n 2^(n - 1). $
]

#proof[
  Посчитаем пары «подмножество и отмеченный в нём элемент» двумя способами.

  Для каждого размера $k$ пар даёт $k binom(n, k)$, метку выбираем $n$ способами, а остальные $n - 1$ элементов решают независимо: $n 2^(n - 1)$ пар.
]

Средний размер подмножества --- $n / 2$, и это видно из формулы.

== Пути на решётке

Сколько путей из $(0, 0)$ в $(n, n)$ шагами вправо и вверх?

Всего шагов $2n$, из них ровно $n$ вправо --- расставляем буквы «вправо» по позициям: $binom(2n, n)$.

Тот же ответ даёт динамическое программирование: `d[i][j] = d[i-1][j] + d[i][j-1]`.

#important[
  Число путей в графе без циклов считается одним проходом по топологическому порядку --- тот же приём, что в обходах и Дейкстре из начала семестра.

  Комбинаторика даёт готовую формулу там, где динамика даёт алгоритм.
]

== Включение-исключение: два множества

#theorem[
  $ |A union B| = |A| + |B| - |A inter B|. $
]

Правило суммы ломается на пересекающихся множествах: элементы пересечения посчитаны дважды.

Поправка --- вычесть пересечение один раз.

#example[
  Из 40 студентов 20 знают Python, 15 --- Rust, 7 знают оба.

  Знают хотя бы один язык: $20 + 15 - 7 = 28$.
]

== Три множества

#align(center)[#venn-inclusion-exclusion]

$
  |A union B union C| = |A| + |B| + |C| - |A inter B| - |A inter C| - |B inter C| + |A inter B inter C|.
$

Знаки чередуются: одиночные множества плюс, пары минус, тройка плюс.

Тройное пересечение вычиталось трижды и прибавлялось трижды --- его нужно вернуть ещё раз.

== Общая формула

#theorem[Включение-исключение (_inclusion-exclusion_)][
  $
    |union.big_(i = 1)^n A_i| = sum |A_i| - sum_(i < j) |A_i inter A_j| + dots.c + (-1)^(k + 1) sum |A_(i_1) inter dots.c inter A_(i_k)| - dots.c
  $
]

#proof[
  Докажем, проверив вклад одного элемента: пусть $x$ принадлежит ровно $k$ множествам. Он посчитан $binom(k, 1)$ раз в одиночных суммах, $binom(k, 2)$ --- в парных, и так далее со знаками через один:
  $
    binom(k, 1) - binom(k, 2) + dots.c + (-1)^(k + 1) binom(k, k) = 1,
  $
  то есть ровно один раз, в скольких бы множествах он ни лежал.
]

== Почему знаки чередуются?

Выражение $binom(k, 1) - binom(k, 2) + dots.c$ --- это $1 - (1 - 1)^k$: сумма строки бинома с чередующимися знаками.

Биномиальная алгебра из этой же лекции превращает формулу включений-исключений из трюка в теорему.

#important[
  Одна формула --- два взгляда: комбинаторный подсчёт вкладов и биномиальное тождество.
]

== Делители

#example[Сколько чисел от 1 до 100 делятся на 2, 3 или 5?][
  $floor(100 / 2) = 50$, $floor(100 / 3) = 33$, $floor(100 / 5) = 20$.

  Пары: $floor(100 / 6) = 16$, $floor(100 / 10) = 10$, $floor(100 / 15) = 6$.

  Тройка: $floor(100 / 30) = 3$.

  Итого: $50 + 33 + 20 - 16 - 10 - 6 + 3 = 74$.
]

Пересечения множеств стали делимостью на произведения --- включение-исключение умеет считать и в теории чисел.

== Беспорядки

#example[Беспорядки (_derangements_)][
  Переписать $n$ писем в $n$ конверты так, чтобы ни одно письмо не попало в свой конверт:
  $ !n = n! sum_(i = 0)^n ((-1)^i) / (i!) approx (n!) / e. $
]

$A_i$ --- перестановки, где письмо $i$ на своём месте: $|A_i| = (n - 1)!$, пары дают $(n - 2)!$ и так далее --- включение-исключение собирает ответ.

Доля беспорядков стремится к $1 / e approx 0.368$: уже $!6 = 265$ из $6! = 720$.

== Лекция 14: что мы поняли?

#important[
  - Матрица выбора: порядок важен или нет, повторения разрешены или нет --- четыре формулы от $n^k$ до $binom(n + k - 1, k)$.
  - Бином Ньютона переводит выбор подмножеств в коэффициенты, треугольник Паскаля устроен тождеством $binom(n, k) = binom(n - 1, k - 1) + binom(n - 1, k)$.
  - Тождества (сумма строки $2^n$, клюшка, Вандермонд) доказываются двойным подсчётом: обе стороны считают одно множество.
  - Пути на решётке считаются формулой $binom(2n, n)$ и одной строкой динамики --- это одна и та же рекурсия.
  - Включение-исключение исправляет двойной счёт чередующимися знаками: каждый элемент выживает ровно один раз.
]

== Вопросы для самопроверки

+ Почему $binom(n, k) = P(n, k) / k!$?

+ Сколько анаграмм у слова МАТЕМАТИКА?

+ Докажите $sum_(k = 0)^n binom(n, k) = 2^n$ двумя способами.

+ Что означает тождество Вандермонда в терминах выбора из двух групп?

+ Сколько чисел от 1 до 30 не делятся ни на 2, ни на 3, ни на 5?

+ Почему доля беспорядков среди перестановок стремится к $1 / e$?

== Чтение к следующей паре

#note[
  - m18 --- "Перестановки, размещения, сочетания", "Биномиальные коэффициенты и тождества", "Принцип включений-исключений".
  - Флеш-опросник --- в начале лекции 15.
  - Контрольная 4 (комбинаторика и производящие функции) --- на следующей неделе, на отдельной паре.
]
