// s2, лекция 9 --- Машина Тьюринга (неделя 5--11 апреля).
#import "theme.typ": *
#import "diagrams/ghosts.typ": (
  churchturing-ghost, diagonalization-ghost, enumerator-ghost, halting-ghost,
  multitape-ghost, oracle-ghost, reduction-ghost, rice-ghost, tape-ghost,
  universal-ghost,
)
#import "diagrams/m25.typ": turing-machine
#import "diagrams/m25.typ": reduction-halt-empty, tm-configuration

#show: slides.with(
  title: [Машина Тьюринга],
  subtitle: [Дискретная математика --- лекция 9, 5--11 апреля],
  date: "Весна 2027",
  authors: "Константин Чухарев",
  module: "turing",
)

= Лекция 9. Машина Тьюринга

#lecture-slide(
  9,
  [Машина Тьюринга],
  week: [5--11 апреля],
  teaser: [
    Лента, головка, таблица переходов: этого хватает, чтобы вычислить всё вычислимое.
    Напишем первые программы --- инкремент, сдвиг, копирование.
  ],
)

= Машина Тьюринга

#focus-slide(
  ghost: tape-ghost,
  epigraph: [Вычисление обычно выполняется записыванием определённых символов на бумаге.],
  epigraph-author: [Алан Тьюринг],
)

== Чего не хватило автомату?

В первом семестре язык $a^n b^n$ поставил конечные автоматы на место.

Лемма о накачке и теорема Майхилла--Нерода доказали: этот язык нерегулярен.

Причина честная: у автомата только конечное число состояний, и бесконечный счёт в них не помещается.

#important[
  Люди считают: карандаш, бумага, стирание и переписывание.

  Модель вычисления обязана уметь то же самое --- запоминать неограниченно много.
]

== Человек за столом

Тьюринг в 1936 году описал вычисление так, как его делает человек с карандашом:

- конечное число "состояний души" --- то, что человек помнит
- бумага, разбитая на клетки --- то, куда он смотрит
- простые шаги --- стереть, написать, сдвинуть взгляд.

#important[
  Никакой магии: только символы, клетки и правила.

  Всё остальное вычисление получает из их комбинаций.
]

== Устройство машины

#align(center)[#turing-machine]

Три части с чёткими ролями: бесконечная лента --- память, головка --- взгляд, конечное управление --- программа.

== Семёрка машины Тьюринга

#definition[Машина Тьюринга][
  Семёрка $M = (Q, Gamma, Sigma, delta, q_0, q_"accept", q_"reject")$:

  - $Q$ --- конечное множество состояний.
  - $Gamma$ --- алфавит ленты, содержит входной алфавит $Sigma$ и пробел.
  - $delta: Q times Gamma -> Q times Gamma times {L, R}$ --- частичная функция переходов.
  - $q_0$ --- начальное состояние, $q_"accept"$ и $q_"reject"$ --- принимающее и отвергающее.
]

Состояния конечны, лента --- нет: весь неограниченный счёт живёт на ленте.

== Один шаг

#definition[Переход][
  На паре (состояние, символ) правило $delta$ даёт тройку (новое состояние, новый символ, сдвиг):

  - прочитать символ под головкой
  - записать новый
  - сдвинуть головку влево или вправо
  - сменить состояние.
]

#important[
  $delta$ частична: правила для текущей пары может не быть.

  Остановка вне финальных состояний по соглашению считается отверганием.
]

== Конфигурация

#definition[Конфигурация][
  Мгновенное описание работы --- запись $u q v$:

  - $u$ --- содержимое ленты слева от головки
  - $q$ --- текущее состояние
  - $v$ --- лента от головки вправо, включая читаемую ячейку.
]

Начальная конфигурация на входе $w$ --- это $q_0 w$: головка на первом символе.

#pagebreak()

== Анатомия конфигурации

#align(center)[#tm-configuration]

Состояние стоит над читаемой ячейкой, $u$ --- всё слева, $v$ --- от головки до последнего непустого символа.

== Три исхода

#definition[Исходы работы][
  - МТ *принимает* вход, если достигает $q_"accept"$.
  - МТ *отвергает*, если достигает $q_"reject"$.
  - МТ может *зациклиться* --- не достигнуть ни того, ни другого.
]

#important[
  Третий исход --- не каприз определения, а суть модели.

  Программа, которая крутится вечно, должна описываться честно.
]

== Что значит решить язык?

#definition[Распознаваемость и разрешимость][
  Язык $L$ *распознаваем*, если есть МТ, принимающая $w$ в точности тогда, когда $w in L$.

  На $w in.not L$ машина может отвергнуть или зациклиться.

  Язык $L$ *разрешим*, если есть МТ, которая всегда останавливается: принимает $w in L$, отвергает $w in.not L$.
]

#important[
  Разрешимый язык распознаваем, а обратное --- нет.

  Пример распознаваемого, но неразрешимого языка появится через две лекции.
]

== Пример: слова, заканчивающиеся на ноль

#example[Семёрка в деле][
  Язык слов над ${0, 1}$, заканчивающихся на $"0"$.

  Состояния: $q_0$ --- "последний символ 1", $q_1$ --- "последний символ 0".

  Переходы:

  - $(q_0, 1) -> (q_0, 1, R)$
  - $(q_0, 0) -> (q_1, 0, R)$
  - $(q_1, 0) -> (q_1, 0, R)$
  - $(q_1, 1) -> (q_0, 1, R)$
  - $(q_1, "пробел") -> (q_"accept", "пробел", R)$
]

== Трасса и разрешимость

#example[Трасса на $"10"$][
  $
    q_0 10 -> 1 q_0 0 -> 10 q_1 -> q_"accept" 10.
  $

  Последний шаг: $q_1$ видит пробел и принимает.
]

#important[
  Машина делает ровно $abs(w)$ шагов и всегда останавливается.

  Значит, язык разрешим --- конечная память тут справляется без ленты.
]

== Приёмы программирования

Примеры ниже собираются из повторяющихся приёмов.

#table(
  columns: (auto, 1fr),
  align: (left, left),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([*Приём*], [*Идея*]),
  [Запоминание через состояние],
  ["вижу символ $a$" --- перейди в состояние "помню $a$" и неси его, пока не понадобится],

  [Маркеры],
  [обработанные символы заменяются на $X$: "уже учтено" отделяется от "ещё нет"],

  [Проход к краю и возврат],
  ["иди вправо, пока не пробел", шаг влево, переключение --- цикл с условием на краю],
)

#note[
  Счёт на МТ --- это всегда расстановка и пересчёт маркеров.

  Память машины растёт не числом состояний, а занятой частью ленты.
]

== Машина как процессор

#important[
  Приёмы --- прообраз настоящего процессора:

  - состояние играет роль счётчика команд
  - маркер --- флага
  - проход по ленте --- цикла.
]

Программа на МТ --- конечная таблица, и её можно записать строкой символов.

К чему это ведёт --- в конце лекции.

= Программы на ленте

#focus-slide(
  ghost: $sharp$,
  epigraph: [Программы должны писаться для того, чтобы их читали люди, и лишь попутно --- чтобы их исполняли машины.],
  epigraph-author: [Гарольд Абельсон и Джеральд Сассман],
)

== Инверсия битов

#example[Первый алгоритм][
  Заменить каждый 0 на 1 и каждую 1 на 0.

  Одно состояние, три правила:

  - $(q_0, 0) -> (q_0, 1, R)$
  - $(q_0, 1) -> (q_0, 0, R)$
  - $(q_0, "пробел") -> (q_"accept", "пробел", R)$
]

#important[
  Эта машина вычисляет функцию, а не распознаёт язык: на ленте остаётся результат.

  Разрешимость --- про ответ "да или нет", вычислимость --- про выход.
]

== Инкремент двоичного числа

#example[Прибавить единицу][
  Число на ленте, младший бит слева.

  Пока читаем 1 --- пишем 0 и уносим перенос вправо.

  Первая 0 меняется на 1, и работа закончена.

  - $(q_0, 1) -> (q_0, 0, R)$
  - $(q_0, 0) -> (q_"accept", 1, R)$
  - $(q_0, "пробел") -> (q_"accept", 1, R)$
]

Арифметика на ленте --- один цикл с одним состоянием.

== Инкремент: трасса

#example[Трасса на $"11"$][
  + $q_0 "11"$: читаем младшую 1, пишем 0, перенос вправо.
  + $0 q_0 1$: снова 1, пишем 0, перенос вправо.
  + $00 q_0$: пробел --- пишем 1 и принимаем.

  На ленте $"001"$: ровно $3 + 1$.
]

#important[
  Все единицы исчерпаны --- машина дописала новый разряд.

  Лента сама расширяет число: ничего "перевыделять" не нужно.
]

== Та самая $a^n b^n$

#example[Вычёркивание пар][
  Язык $a^n b^n$, не по силам конечным автоматам в первом семестре.

  Стратегия: многократно вычеркнуть один $a$ слева и одну $b$ справа.

  Трасса на $"aabb"$:

  $ "Xa bb" -> "Xa bX" -> "XX bX" -> "XXXX" -> "принять". $
]

#important[
  Конечному автомату не хватило состояний на счёт, а машине хватает ленты.

  Сравнить два счётчика --- значит стереть их попарно.
]

== Палиндромы

#example[Сравнение концов][
  Алгоритм для палиндромов над ${a, b}$:

  + Запомнить первый символ --- перейти в состояние "видел $a$" или "видел $b$" --- и стереть его.
  + Дойти до последнего нестёртого символа.
  + Совпал --- стереть и вернуться к началу, нет --- отвергнуть.
  + Всё стёрто --- принять.
]

#important[
  МТ видит оба конца строки --- ценой многократных проходов.

  Запоминание символа через состояние --- тот же приём, что у автомата, но возвраты делает головка.
]

== Композиция машин

#definition[Композиция][
  Если $M_1$ переводит ленту из $u$ в $v$, а $M_2$ --- из $v$ в $w$, состояние останова $M_1$ отождествляется с начальным состоянием $M_2$.
]

Из простых блоков собираются большие программы:

- "найти край"
- "скопировать"
- "стереть"

#note[
  Удвоение унарного числа, затем инверсия битов --- составная машина $1^n -> 0^(2n)$.

  Таблица композиции --- объединение таблиц с перенумерацией состояний.
]

== Программа --- конечный текст

#important[
  Таблица переходов --- конечный объект: её можно выписать строкой символов.

  Значит, программу можно положить на ленту и читать её как данные.

  Одна машина, которая делает это по правилам, исполнила бы любую программу.
]

Как такая машина устроена --- тема следующей лекции.

== Лекция 9: что мы поняли?

#important[
  - МТ --- бесконечная лента плюс конечное управление: неограниченная память при финитном описании.
  - Конфигурация $u q v$ --- полный снимок работы, вычисление --- цепочка конфигураций.
  - Три исхода: принять, отвергнуть, зациклиться.
    Разрешимость --- остановка с ответом на каждом входе.
  - Программы пишутся приёмами: символ в состоянии, маркеры, проходы, композиция.
  - Язык $a^n b^n$, не по силам автоматам, разрешается вычёркиванием пар --- лента заменяет счётчик.
]

== Вопросы для самопроверки

+ Почему $a^n b^n$ нерегулярен, но разрешим машиной Тьюринга?

+ Назовите все семь компонентов семёрки МТ и роль каждого.

+ Что записывает конфигурация $u q v$ и как выглядит начальная конфигурация для входа $"10"$?

+ Чем распознаваемость языка отличается от разрешимости?

+ Как машина запоминает символ, если у неё нет переменных?

+ Опишите машину инкремента двоичного числа: состояния и переходы.

+ Что значит скомпоновать две машины в одну?

== Чтение к следующей паре

#note[
  - m25 --- "Определение машины Тьюринга", "Примеры машин Тьюринга".
  - Флеш-опросник --- в начале лекции 10.
  - Теормин 1 (графы и автоматы, устно) --- на отдельной паре этой недели.
]
