#import "theme.typ": *
#show: slides.with(
  title: [Матроиды],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)

#import "diagrams/m13.typ": greedy-counterexample, matroid-sources

= Жадный алгоритм
#focus-slide(
  epigraph: [Всё должно быть настолько просто, насколько возможно, но не проще.],
  epigraph-author: [Альберт Эйнштейн],
)

== Задача выбора

#definition[Задача выбора][
  Даны конечное множество $E$, весовая функция $w: E -> RR_(>=0)$ и семейство $cal(F) subset.eq 2^E$ допустимых подмножеств. Нужно найти $A in cal(F)$ с максимальным весом $w(A) = sum_(e in A) w(e)$.
]

#important[
  Жадный алгоритм перебирает элементы по убыванию веса и добавляет очередной элемент, если множество остаётся допустимым. Вся трудность задачи сводится к проверке допустимости.
]

== Жадный не всегда оптимален

#example[Размен монет][
  Монеты 25, 10 и 1 цент.

  Выдать 30 центов: жадный алгоритм берёт 25 + пять по 1 --- шесть монет.

  Оптимум --- три по 10.
]

#important[
  Жадный алгоритм прост, но в общем случае неоптимален.

  В каких задачах он оптимален?
]

#note[
  Крускал и Хаффман --- жадные алгоритмы, и оба оптимальны.

  Причина оптимальности Крускала --- структура матроида.
]

== Три задачи

#example[Максимальный лес][
  В треугольнике рёбра имеют веса 5, 4 и 3, а допустимыми считаются леса.

  Жадный алгоритм берёт 5 и 4 --- вес 9, оптимально.
]

#example[Базис максимального веса][
  Три вектора плоскости: $v_1 = (1, 0)$ веса $5$, $v_2 = (0, 1)$ веса $4$, $v_3 = (1, 1)$ веса $3$. Допустимы независимые наборы.

  Жадный алгоритм берёт $v_1$ и $v_2$ --- вес $9$. $v_3$ отбрасывает: все три вектора зависимы.
]

#example[Набор ограниченного размера][
  Элементы 5, 3, 4, 2. Размер не более двух.

  Жадный алгоритм берёт 5 и 4 --- вес 9.
]

#important[Во всех трёх задачах действует одно из трёх правил допустимости: ацикличность, независимость или ограничение размера, и общая причина успеха жадного алгоритма --- в этой структуре.]

== Наследственность

#definition[Наследственность][
  Подмножество допустимого допустимо: выбросил элементы --- не испортил.
]

#example[
  Леса графа: подмножество леса --- лес.

  Выбросили ребро --- цикл не появится.
]

== Свойство замены

#definition[Свойство замены][
  Для любых $A, B in cal(I)$ с $|A| < |B|$ найдётся $x in B setminus A$, для которого $A union {x} in cal(I)$.
]

#example[Замена в треугольнике][
  В треугольнике с рёбрами $a$, $b$, $c$ возьмём леса $A = {a}$ и $B = {b, c}$.

  $|A| = 1 < 2 = |B|$, и ребро $b in B setminus A$ можно добавить к $A$: набор ${a, b}$ ацикличен.
]

#important[Наследственность и замена --- два свойства матроида.]

= Определение матроида
#focus-slide()

== Матроид

#definition[Матроид][
  Пара $(E, cal(I))$ --- *матроид*, если выполнены:
  - *Наследственность*: $A in cal(I)$, $B subset.eq A$ влечёт $B in cal(I)$.
  - *свойство замены* (_exchange property_): для любых $A, B in cal(I)$ с $|A| < |B|$ существует $x in B setminus A$, для которого $A union {x} in cal(I)$; кроме того, $emptyset in cal(I)$.

  Множества из $cal(I)$ --- *независимые*.
]

#important[
  Свойство замены --- из линейной алгебры.

  Из базиса можно выбросить вектор и добавить другой.
]

#note[
  Наследственность без замены --- не матроид.
]

== Проверка аксиом: $U_(2,3)$

#example[
  $U_(2,3)$: независимы подмножества ${1, 2, 3}$ размера не более 2.

  Наследственность: подмножество размера $<= 2$ тоже размера $<= 2$.

  Замена: если $|A| < |B| <= 2$, у $B$ есть элемент вне $A$ --- добавить его безопасно.
]

== База и ранг

#definition[
  *База* --- максимальное по включению независимое множество.

  *Ранг* --- размер базы.
]

#important[
  Все базы матроида имеют одинаковый размер: иначе для баз $A$, $B$ с $|A| < |B|$ свойство замены дало бы независимое множество $A union {x}$, строго большее базы $A$.

  В линейном матроиде база --- базис пространства.
  В графическом --- остовное дерево.
]

#example[
  В треугольнике базы --- пары рёбер.

  Все три ребра зависимы.
]

== Графический

#definition[Графический][
  Основное множество --- рёбра графа, а независимыми считаются леса. Минимальное зависимое множество --- это цикл, графовый аналог нулевого вектора.
]

#example[
  В дереве независимо любое подмножество рёбер.

  В графе с циклом сам цикл --- минимальное зависимое множество.
]

== Линейный

#definition[Линейный][
  Основное множество --- векторы пространства, а независимыми считаются линейно независимые наборы.
]

#example[Векторы плоскости][
  Векторы $v_1 = (1, 0)$, $v_2 = (0, 1)$, $v_3 = (1, 1)$.

  Независимы пустое множество, одиночные векторы и все пары.

  Все три зависимы: $v_3 = v_1 + v_2$.
  Базы --- пары векторов.
]

== Униформный

#definition[Униформный $U_(k,n)$][
  Подмножества размера не более $k$.
]

#example[Униформный матроид $U_(2,4)$][
  Независимы все подмножества размера не более двух.

  Базы --- двухэлементные подмножества. Любое трёхэлементное подмножество зависимо.
]


#align(center, box(width: 6.5cm, height: 3cm)[#place(center + horizon, scale(
  matroid-sources,
  x: 60%,
  y: 60%,
))])

#important[
  Три источника --- один матроид.

  Проверка аксиом --- наследственность и замена.
]

= Корректность жадного
#focus-slide()

== Теорема Радо--Эдмондса

#theorem[Теорема Радо--Эдмондса][
  Пусть $(E, cal(I))$ --- матроид.

  Для любой неотрицательной весовой функции жадный алгоритм находит независимое множество максимального веса.
]

#proof[
  Все базы имеют одинаковый размер $r$. Жадный возвращает базу.

  Пусть $G = {g_1, ..., g_r}$ --- база, которую вернул жадный алгоритм, а $B = {b_1, ..., b_r}$ --- оптимальное решение; так как веса неотрицательны, оптимум можно считать базой того же размера $r$. Оба множества отсортированы по убыванию веса.

  Пусть $i$ --- первый индекс с $w(g_i) < w(b_i)$.
  К множеству $G_(i-1) = {g_1, ..., g_(i-1)}$ и $B_i = {b_1, ..., b_i}$ применимо свойство замены: $|G_(i-1)| = i - 1 < i = |B_i|$, значит, существует $x in B_i setminus G_(i-1)$ с $G_(i-1) union {x}$ независимым.

  Элементы $B_i$ отсортированы, поэтому $w(x) >= w(b_i) > w(g_i)$. Жадный рассмотрел $x$ раньше $g_i$. В этот момент его текущее множество --- подмножество $G_(i-1)$, а $G_(i-1) union {x}$ независимо, поэтому по наследственности $x$ можно было добавить --- противоречие.
]

#important[Оптимальность не требует отдельного доказательства: достаточно двух аксиом.]

== Обратное утверждение

Без свойства замены жадный может ошибаться.

#definition[Система независимости][
  Наследственное семейство подмножеств.

  Матроид --- система независимости со свойством замены.
]

== Контрпример

#example[
  ${emptyset}$, ${a}$, ${b}$, ${c}$, ${b, c}$ --- наследственное семейство без свойства замены.

  $A = {a}$, $B = {b, c}$: $|A| < |B|$, но ни $b$, ни $c$ нельзя добавить к $a$.
]

#align(center, box(width: 4.8cm, height: 2.8cm)[#place(center + horizon, scale(
  greedy-counterexample,
  x: 50%,
  y: 50%,
))])

#important[Пусть вес $a$ равен 5, а веса $b$ и $c$ --- по 4. Жадный алгоритм берёт ${a}$ (вес 5), оптимум --- ${b, c}$ (вес 8). Замена отделяет оптимум от ошибки.]

== Применения

#note[
  *Крускал* --- жадный алгоритм на графическом матроиде.

  *Базис максимального веса* --- на линейном.

  *Расписания* --- матроид расписаний.
]

#important[
  Разные алгоритмы --- один жадный на разных матроидах.

  Крускал находит максимальный лес. Задача о минимальном остове --- сменой знака весов.
]

== Что мы поняли

#important[
  - Жадный алгоритм прост, но оптимален лишь при особой структуре семейства допустимых.
  - Матроид задают две аксиомы: наследственность и свойство замены.
  - Теорема Радо--Эдмондса: на матроиде жадный находит множество максимального веса для любой весовой функции.
  - Один жадный алгоритм на разных матроидах: Крускал на графическом, базис максимального веса на линейном.
]

== Вопросы для самопроверки

- Сформулируйте задачу выбора и жадный алгоритм.

- Почему жадный алгоритм неоптимален для размена монет?

- Проверьте наследственность и свойство замены для лесов.

- Дайте определение матроида.

- Почему все базы матроида имеют одинаковый размер?

- Приведите пример матроида и его базы.

- Сформулируйте теорему Радо--Эдмондса.

- Почему без свойства замены жадный может ошибаться?
  Приведите контрпример.
