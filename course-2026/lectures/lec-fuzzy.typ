#import "theme.typ": *
#show: slides.with(
  title: [Нечёткие множества],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)

= Нечёткие множества
#focus-slide(
  epigraph: [По мере роста сложности точные утверждения теряют смысл, а осмысленные --- точность.],
  epigraph-author: [Лотфи Заде],
)

== Парадокс кучи

Одно зерно --- не куча.
Добавление одного зерна к не-куче не создаёт кучу.
Миллион зёрен --- куча.
Парадокс кучи (сорит) известен со времён Эвбулида Милетского.

#example[Высокий человек][
  Пусть высоким считается человек ростом от 180 см.
  При росте 180 см человек высок, а при 179.9 см --- нет.
  Один миллиметр решает принадлежность к множеству.
]

Резкая граница --- свойство самого понятия.
Дело не в точности измерений.
Для любого порога найдутся элементы, которые интуиция относит к другой категории.
"Молодой возраст", "жарко", "большое число" --- понятия без резкой границы.

#important[
  Бинарная классификация таких понятий произвольна.
  Градуальная принадлежность --- способ говорить о них точно.
]

== Градуальная принадлежность

Классическое множество задаётся характеристической функцией $chi_A: U -> {0, 1}$.
Элемент либо принадлежит множеству, либо нет.

#definition[Нечёткое множество][
  *Нечёткое множество* $A$ на универсуме $U$ --- функция принадлежности
  $
    mu_A: U -> [0, 1],
  $
  сопоставляющая элементу $x$ степень принадлежности $mu_A (x)$.
  Значение $mu_A (x) = 0$ --- "точно не принадлежит".
  Значение $mu_A (x) = 1$ --- "точно принадлежит".
  Промежуточные значения --- частичная принадлежность.
]

Классическое множество --- частный случай нечёткого: функция принадлежности принимает только значения $0$ и $1$.

== Пример: степени принадлежности

#important[
  Лотфи Заде в 1965 году предложил градуальную принадлежность.
  Тридцатилетний человек --- "молодой" со степенью $0.6$ и "средний" со степенью $0.3$.
]

== Функция принадлежности

#example[Нечёткое множество "высокий рост"][
  $U = [140, 210]$ (рост в сантиметрах).
  $
    mu_"high" (x) = cases(
      0 & "при" x <= 160,
      (x - 160) / 30 & "при" 160 < x < 190,
      1 & "при" x >= 190
    )
  $
  При росте 175 см: $mu_"high" (175) = (175 - 160) / 30 = 0.5$.
  При росте 185 см: $mu_"high" (185) approx 0.83$.
]

Форма функции принадлежности --- инженерное решение.
Её строят по опросу экспертов: сто банкиров сочли "хорошим" кредитный балл 750 (все), 700 (половина), 650 (никто).
Доли согласия дают точки $(750, 1), (700, 0.5), (650, 0)$.

#note[
  Ходовые формы --- треугольная, трапецеидальная и гауссова.
  Соседние функции принадлежности перекрываются.
  Перекрытие отражает разброс мнений экспертов.
]

== Нечёткость и вероятность

#important[
  Степень принадлежности $0.7$ --- не вероятность.
]

Вероятность отвечает на вопрос "как часто?" и предполагает повторение: бросок монеты, шум в канале.
Нечёткость отвечает на вопрос "в какой мере?" и повторения не требует.
После броска монеты неопределённость исчезает: выпал орёл или решка.
Рост человека уже измерен, а неопределённость остаётся в классификации: насколько он "высокий".

#note[
  Вероятность --- неполнота статистическая, нечёткость --- неполнота лингвистическая.
  Аксиоматика разная, применения разные.
]

== Операции Заде

#definition[Пересечение][
  $mu_(A inter B)(x) = min(mu_A (x), mu_B (x))$ --- принадлежность обоим множествам ограничена меньшей из двух степеней.
]

#definition[Объединение][
  $mu_(A union B)(x) = max(mu_A (x), mu_B (x))$.
]

#definition[Дополнение][
  $mu_(not A)(x) = 1 - mu_A (x)$.
]

== Пример: высокий и средний рост

#example[Высокий и средний рост][
  $A$ --- "высокий рост", $B$ --- "средний рост".
  При росте 175 см: $mu_A (175) = 0.5$, $mu_B (175) = 0.5$.
  Пересечение: $mu_(A inter B)(175) = min(0.5, 0.5) = 0.5$.
  Объединение: $mu_(A union B)(175) = max(0.5, 0.5) = 0.5$.
]

== Законы де Моргана и потерянное третье

#theorem[Законы де Моргана][
  Для любых нечётких множеств $A$, $B$ и любого $x$:
  $
    mu_(not (A union B))(x) = mu_(not A inter not B)(x), \
    mu_(not (A inter B))(x) = mu_(not A union not B)(x).
  $
]

#proof[
  Для $a, b in [0, 1]$ верно $1 - max(a, b) = min(1 - a, 1 - b)$.
  При $a >= b$ обе части равны $1 - a$.
]

Операции изменились, поэтому законы пришлось проверять заново.

== Потерянное третье

#important[
  Закон исключённого третьего исчезает:
  $
    mu_(A union not A)(x) = max(mu_A (x), 1 - mu_A (x)).
  $
  При $mu_A (x) = 0.6$ получаем $0.6$ --- меньше $1$.
]

#note[
  Закон противоречия тоже исчезает: $mu_(A inter not A)(x) = min(mu_A (x), 1 - mu_A (x)) > 0$ при промежуточных степенях.
  Человек ростом 175 см --- "высокий" со степенью $0.5$ и "не высокий" со степенью $0.5$.
  Противоречия здесь нет: обе классификации верны в разной мере.
]

== t-нормы

Операции Заде --- один из возможных выборов.
Пересечение степеней --- функция, и требования к ней диктует интуиция: порядок факторов не важен, скобки не важны, рост степени не уменьшает результат, граничные значения не искажают его.

#definition[t-норма][
  *t-норма* --- функция $I: [0, 1]^2 -> [0, 1]$ с аксиомами:
  + коммутативность: $I(a, b) = I(b, a)$;
  + ассоциативность: $I(a, I(b, c)) = I(I(a, b), c)$;
  + монотонность: $a <= a'$ и $b <= b'$ влекут $I(a, b) <= I(a', b')$;
  + граничные условия: $I(a, 1) = a$, $I(a, 0) = 0$.
]

== s-нормы

#definition[s-норма][
  *s-норма* (конорма) --- функция $U: [0, 1]^2 -> [0, 1]$ с теми же аксиомами, кроме граничных: $U(a, 1) = 1$, $U(a, 0) = a$.
]

t-норма моделирует "и", s-норма --- "или".
Дополнение остаётся стандартным: $mu_(not A)(x) = 1 - mu_A (x)$.

== Три пары норм

#table(
  columns: (auto, auto, auto, auto),
  stroke: (x, y) => if y == 0 { (bottom: 0.6pt) },
  table.header([*Название*], [*t-норма*], [*s-норма*], [*Значение $I(0.6, 0.7)$*]),
  [Заде], [$min(a, b)$], [$max(a, b)$], [$0.6$],
  [Вероятностная], [$a dot b$], [$a + b - a dot b$], [$0.42$],
  [Лукасевича], [$max(0, a + b - 1)$], [$min(1, a + b)$], [$0.3$],
)

#note[
  Нормы различаются идемпотентностью.
  $min$ идемпотентна: $I(a, a) = a$. Повторные свидетельства не наказываются.
  Произведение идемпотентности лишено: $I(a, a) = a^2$. Согласие двух неуверенных мнений снижает уверенность.
  Норма Лукасевича нильпотентна: при $a + b <= 1$ пересечение обнуляется.
]

#important[
  Выбор t-нормы --- выбор семантики "и" в задаче.
  Два датчика одной величины: $min$ оставляет $0.7$, произведение даёт $0.49$.
]

== Носитель, ядро, высота

#definition[Носитель][
  *Носитель* --- классическое множество ${x in U mid(|) mu_A (x) > 0}$.
]

#definition[Ядро][
  *Ядро* --- множество ${x in U mid(|) mu_A (x) = 1}$.
]

#definition[Высота][
  *Высота* --- число $max_(x in U) mu_A (x)$.
]

#example[Характеристики "высокого роста"][
  Носитель --- $(160, 210]$: все росты с положительной степенью.
  Ядро --- $[190, 210]$: росты со степенью $1$.
  Высота --- $1$.
]

== $alpha$-срезы

#definition[$alpha$-срез][
  *$alpha$-срез* --- классическое множество
  $
    A_alpha = {x in U mid(|) mu_A (x) >= alpha}.
  $
]

#example[
  Для "высокого роста" срез уровня $alpha = 0.5$ --- росты от 175 см: $[175, 210]$.
  Срез уровня $alpha = 0$ --- весь универсум.
  Срез уровня $alpha = 1$ --- ядро $[190, 210]$.
]

Срезы переводят нечёткое множество на язык классических: по одному множеству на каждый уровень.
Семейство срезов вложено: $alpha_1 <= alpha_2$ влечёт $A_(alpha_1) supset.eq A_(alpha_2)$.

== Теорема декомпозиции

#theorem[Теорема декомпозиции][
  Нечёткое множество $A$ однозначно восстанавливается по своим $alpha$-срезам:
  $
    mu_A (x) = "sup" {alpha in [0, 1] mid(|) x in A_alpha}
  $
  для каждого $x in U$.
]

#proof[
  Условие $x in A_alpha$ по определению среза означает $mu_A (x) >= alpha$.
  Уровни, при которых $x$ принадлежит срезу, образуют интервал $[0, mu_A (x)]$.
  Супремум этого интервала равен его правому концу.
]

== Срезы: мост между мирами

#important[
  Нечёткое множество --- семейство вложенных классических множеств, по одному на каждый уровень уверенности.
  Срезы --- мост между градуальной и бинарной принадлежностью.
]

== Нечёткая логика

Нечёткая логика переносит градуальность на высказывания: истина --- число из $[0, 1]$.

#definition[Связки][
  - $mu_(P and Q) = min(mu_P, mu_Q)$ --- "и";
  - $mu_(P or Q) = max(mu_P, mu_Q)$ --- "или";
  - $mu_(not P) = 1 - mu_P$ --- отрицание;
  - $mu_(P imply Q) = min(1, 1 - mu_P + mu_Q)$ --- импликация Лукасевича.
]

== Лингвистическая переменная

#definition[Лингвистическая переменная][
  *Лингвистическая переменная* --- переменная, значения которой --- нечёткие множества на числовой шкале.
  "Температура" принимает значения "холодно", "прохладно", "комфортно", "тепло", "жарко".
]

#note[
  Связки обобщают таблицы истинности: при степенях из ${0, 1}$ возвращается классическая логика.
]

== Правила Мамдани

#example[Нечёткий контроллер кондиционера][
  Правила (Мамдани, 1975):
  + если температура "жарко", то охлаждение "сильное";
  + если температура "тепло", то охлаждение "слабое";
  + если температура "комфортно", то охлаждение "выключено".
]

#definition[Firing strength][
  *Firing strength* правила --- степень истинности его условия.
]

#example[Кондиционер при 27 $degree$ C][
  При температуре $27 degree C$: $mu_"жарко" (27) = 0.4$, $mu_"тепло" (27) = 0.7$.
  Правило "жарко" срабатывает со степенью $0.4$, правило "тепло" --- со степенью $0.7$.
  Результат --- взвешенная комбинация "сильного" и "слабого" охлаждения.
]

== Нечёткие правила против if-else

#note[
  Чёткий контроллер --- лестница if-else: правка одного порога тянет переписывание веток.
  В нечёткой версии достаточно сдвинуть одну функцию принадлежности.
]

#important[
  Правила читает эксперт: "если жарко, то сильно" --- без расшифровки таблиц порогов.
]

== Конвейер вывода

Нечёткий вывод --- конвейер из четырёх шагов:
+ фаззификация: вход превращается в степени принадлежности;
+ агрегация: каждое правило получает firing strength $f_i$;
+ композиция: выход правила срезается на уровне $f_i$: $mu_i^"out" (y) = min(mu_("consequent"_i) (y), f_i)$;
+ дефаззификация: объединённый результат превращается в число.

#note[
  Метод Ларсена отличается композицией: $mu_i^"out" (y) = mu_("consequent"_i) (y) dot f_i$.
  Мамдани даёт срезанную функцию с плато, Ларсен --- масштабированную.
]

== Дефаззификация

После композиции результат --- нечёткое множество.
Регулятору нужно одно число: заслонке, насосу, реле подаётся точка на оси.

Методы дефаззификации:
+ *Centroid* (центр тяжести): $y^** = (integral y dot mu(y) dif y) / (integral mu(y) dif y)$;
+ *Mean of Max*: среднее точек, где $mu(y)$ максимальна;
+ *Bisector*: вертикаль, делящая площадь под кривой пополам.

#example[Трапециевидная функция][
  Трапеция с вершинами $(2, 0), (2.5, 1), (3, 1), (4.5, 0)$.
  Centroid даёт $approx 3.06$, Mean of Max --- $2.75$, Bisector --- $3.0$.
]

#note[
  Центр тяжести усредняет по всей кривой.
  Mean of Max берёт самое уверенное значение.
  Bisector устойчив к длинным хвостам.
  Выбор метода --- дизайн-решение.
]

== Метро Сендай

Метро Сендай (Япония, 1987) --- первый крупный промышленный нечёткий контроллер.
Система Hitachi управляла разгоном и торможением поездов по правилам опытных машинистов.
Плавность хода выросла, экономия энергии составила около 10 процентов.

#note[
  В 1960-е и 1970-е годы "fuzzy thinking" было почти ругательством.
  Инженерные успехи --- от стиральных машин до метро --- вернули термину репутацию.
]

#important[
  Конвейер Мамдани работает там, где механизм сложен, а опыт есть: климат-контроль, ABS, автофокус.
]

== Где ещё работает нечёткость

#note[
  Кластеризация: fuzzy c-means --- объект принадлежит каждому кластеру со своей степенью.
  Принятие решений: нечёткие веса критериев в многокритериальном выборе.
  Обработка языка: квантификаторы "несколько", "много", "почти все" --- нечёткие множества на шкале доли.
]

#important[
  Нечёткие множества --- третий способ работать с числами от $0$ до $1$: рядом с булевой алгеброй и вероятностью.
  Классическое множество возвращается, как только степеням принадлежности запрещены промежуточные значения.
]

== Что мы поняли

- Парадокс кучи показывает: для многих понятий любая бинарная граница произвольна.
- Функция принадлежности $mu_A: U -> [0, 1]$ измеряет степень соответствия понятию.
- Операции Заде --- поточечные $min$, $max$ и $1 - x$; выбор "и" --- выбор t-нормы.
- $alpha$-срезы и теорема декомпозиции связывают нечёткие множества с классическими.
- Лингвистические переменные и правила Мамдани складываются в конвейер, который превращает вывод в число.
- Метро Сендай показало: нечёткие контроллеры работают в промышленности.

== Вопросы для самопроверки

- Чем нечёткость отличается от вероятности?
- Что такое $alpha$-срез, и как восстановить множество по семейству срезов?
- Почему для нечётких множеств не выполняется закон исключённого третьего?
- Что такое t-норма, и чем пересечение по $min$ отличается от пересечения по произведению?
- Как firing strength правила участвует в выводе Мамдани?
- Почему регулятору нельзя подать агрегированную функцию принадлежности, и что делает дефаззификация?
