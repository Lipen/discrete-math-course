#import "theme.typ": *
#show: slides.with(
  title: [Формальная верификация],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)
#import "diagrams/m32.typ": ctl-tree

= Формальная верификация
#focus-slide(
  epigraph: [Тестирование программы может убедительно продемонстрировать наличие ошибок, но безнадёжно несостоятельно для демонстрации их отсутствия.],
  epigraph-author: [Эдсгер Дейкстра],
)

== Что значит "программа корректна"

Программа прошла миллион тестов --- и всё равно может упасть на миллион первом.
Миллион входов --- конечное подмножество бесконечного множества возможных запусков.
Свойство вида "программа не упадёт ни на одном входе" --- утверждение обо всех элементах бесконечного множества.
Один контрпример его опровергает, но ни одно конечное число примеров не подтверждает.

Корректность --- отношение между текстом программы и требованием к нему.
Прежде чем доказывать, требование нужно записать.
Невнятное "работает правильно" превращается в точное утверждение.

#important[
  Утверждение о программе --- это формула, а проверка программы --- доказательство этой формулы.
]

== Тройки Хоара

#definition[Тройка Хоара][
  Тройка Хоара $ {P} space S space {Q} $ означает: если предусловие $P$ выполнено перед оператором $S$, то постусловие $Q$ выполнено после его завершения --- при условии, что $S$ вообще завершается.
]

#note[
  Оговорка о завершении фиксирует _частичную_ корректность: тройка не требует, чтобы оператор остановился.
  Тотальную корректность --- завершение плюс постусловие --- рассмотрим дальше.
]

== Тройка как контракт

#example[Корректная тройка][
  $
    {x = 5} space x := x + 1 space {x = 6}
  $
  До присваивания $x = 5$.
  После присваивания $x = 6$.
]

Предусловие кодирует предположения, постусловие --- гарантии.
Вместе они образуют контракт: если вызывающая сторона удовлетворяет $P$, процедура обеспечивает $Q$.

== Пример: модуль числа

#example[Модуль числа][
  $
    {x >= 0} space "if" x > 0 "then" y := x "else" y := -x space {y = |x|}
  $
  При $x > 0$ записывается $x$.
  При $x = 0$ записывается $0$.
  Тогда $y = -x = 0 = |x|$.
  Случай $x < 0$ исключён предусловием.
  К этой тройке вернёмся на слайде про условный оператор.
]

== Аксиома присваивания

#definition[Аксиома присваивания][
  Тройка $ {P[x := E]} space x := E space {P} $ верна для любого постусловия $P$.
  Здесь $P[x := E]$ --- формула $P$, в которой каждое свободное вхождение $x$ заменено выражением $E$.
]

#note[Тонкость подстановки][
  Подстановка $P[x := E]$ заменяет все свободные вхождения $x$ одновременно.
  Выражение $E$ вычисляется в старом состоянии, до присваивания.
  Поэтому аксиома читается "назад": из постусловия она восстанавливает предусловие.
]

== Прямой и обратный проход

_Прямой проход_ --- рассуждение от предусловия к постусловию.
После присваивания $x := E$ новое значение $x$ равно значению $E$ до присваивания.
Подставляя это значение в известные утверждения, продвигаем их вперёд по программе.
Обратный инструмент --- слабейшее предусловие: оно восстанавливает предусловие из постусловия.

#example[Удвоение числа][
  Программа `y := 2 * x` удваивает число из $x$.
  Тройка Хоара: $ {x = a} space y := 2 dot x space {y = 2 a} $.
  Прямой проход: предусловие даёт $x = a$.
  После присваивания $y = 2 dot x$.
  Из $x = a$ получаем $y = 2 dot a = 2 a$.
  Обратный проход: аксиома присваивания из постусловия даёт $(y = 2 a)[y := 2 dot x]$.
  Это $2 dot x = 2 a$.
  Из предусловия $x = a$ оно следует сразу.
]

== Правило композиции

#definition[Правило композиции][
  Из тройки $ {P} space S_1 space {R} $
  и тройки $ {R} space S_2 space {Q} $
  следует тройка $ {P} space S_1 ⨟ S_2 space {Q} $.
  Промежуточное условие $R$ склеивает два оператора в последовательность.
]

== Пример: композиция в действии

#example[Композиция в действии][
  Докажем тройку $ {x = 3} space x := x + 1 ⨟ x := x dot 2 space {x = 8} $.
  Идём от постусловия назад.
  Для второго присваивания промежуточное условие: $R = (x = 8)[x := x dot 2]$.
  То есть $x dot 2 = 8$.
  Значит, $x = 4$.
  Для первого присваивания аксиома даёт $(x = 4)[x := x + 1]$.
  То есть $x + 1 = 4$.
  Значит, $x = 3$.
  Промежуточное условие $R equiv x = 4$ склеивает операторы, и тройка доказана.
]

== Правило следствия

#definition[Правило следствия][
  Из тройки $ {P'} space S space {Q'} $,
  из посылки $P -> P'$
  и посылки $Q' -> Q$
  следует тройка $ {P} space S space {Q} $.
  Предусловие можно усилить, постусловие --- ослабить.
]

== Слабейшее предусловие

#definition[Слабейшее предусловие][
  Слабейшее предусловие $"wp"(S, Q)$ --- самая широкая формула $P$.
  Для неё верна тройка $ {P} space S space {Q} $.
  Для присваивания: $"wp"(x := E, Q) = Q[x := E]$.
  Для последовательности: $"wp"(S_1 ⨟ S_2, Q) = "wp"(S_1, "wp"(S_2, Q))$.
  Для условного оператора: $"wp"("if" B "then" S_1 "else" S_2, Q) = (B and "wp"(S_1, Q)) or (not B and "wp"(S_2, Q))$.
]

Обратный проход, вычисляющий $"wp"$, --- стандартный способ генерации условий верификации.

== Правило условного оператора

#definition[Правило условного оператора][
  Из тройки $ {P and B} space S_1 space {Q} $
  и тройки $ {P and not B} space S_2 space {Q} $
  следует тройка $ {P} space "if" B "then" S_1 "else" S_2 space {Q} $.
]

== Пример: возвращаемся к модулю

#example[Возвращаемся к модулю][
  Ветка "если": $x >= 0 and x > 0 arrow.r.double x = |x|$.
  После присваивания $y := x$.
  Тогда $y = |x|$.
  Ветка "иначе": $x >= 0 and not (x > 0) arrow.r.double x = 0$.
  После присваивания $y := -x$.
  Тогда $y = -0 = 0 = |x|$.
  По правилу условного оператора тройка с модулем доказана.
]

== Правило цикла

#definition[Правило цикла][
  Из тройки $ {I and B} space S space {I} $
  следует тройка $ {I} space "while" B "do" S space {I and not B} $.
  Это частичная корректность: завершение цикла не гарантировано.
]

#note[
  Цикл --- единственный оператор, где доказательство идёт по индукции.
  Инвариант $I$ --- то, что сохраняется каждой итерацией.
]

== Инвариант цикла

#definition[Инвариант цикла][
  Формула $I$ --- инвариант цикла $"while" B "do" S$, если:
  + Тело цикла сохраняет $I$: верна тройка $ {I and B} space S space {I} $.
  + $I and not B$ влечёт постусловие.
  Инвариант должен выполняться перед первой итерацией.
  Это база индукции.
]

#note[Как угадать инвариант][
  Инвариант начинают с постусловия и ослабляют, пока его не начнёт сохранять тело цикла.
  В линейном поиске постусловие "найден или $i = n$" слишком точное для середины итерации.
  Ослабляем до "все позиции до $i$ не содержат $x$": тело проверяет ровно текущую позицию.
  К ослабленному постусловию добавляют границы вроде $0 <= i <= n$ --- защита от выхода за пределы массива.
]

== Линейный поиск

#example[Инвариант цикла: линейный поиск][
  Линейный поиск ищет $x$ в массиве $A[0..n-1]$.
  Если элемент не найден, возвращается $n$.
  ```
  i := 0
  while i < n and A[i] != x do
    i := i + 1
  ```
  Инвариант: $I equiv 0 <= i <= n and forall j in {0, ..., i-1} space A[j] != x$.
  Все позиции до $i$ проверены и не содержат $x$.

  *База:* перед циклом $i = 0$.
  Диапазон ${0, ..., -1}$ пуст, и $forall j in emptyset$ истинно вакуумно.
]

== Линейный поиск: шаг и выход

#example[Шаг индукции и выход][
  *Шаг:* пусть инвариант и условие цикла истинны.
  Условие цикла: $i < n and A[i] != x$.
  Тело увеличивает счётчик на единицу.
  Условие цикла даёт $i + 1 <= n$.
  По инварианту все позиции до $i$ не содержат $x$. Условие цикла даёт $A[i] != x$.
  Значит, все позиции до $i + 1$ не содержат $x$, и инвариант сохраняется.

  *Выход:* отрицание условия цикла: $not (i < n and A[i] != x) equiv i >= n or A[i] = x$.
  С учётом инварианта $i <= n$.
  Из $i >= n$ получаем $i = n$: элемент не найден.
  Случай $A[i] = x$: элемент найден.
]

== Верификация через индукцию

Доказательство сохранения инварианта телом цикла --- шаг индукции.
База индукции --- инвариант, установленный перед первой итерацией.

Инструменты верификации работают так:
+ Программист записывает предусловия, постусловия и инварианты циклов.
+ Инструмент генерирует _условия верификации_ --- логические формулы, истинность которых гарантирует корректность программы.
+ SMT-солверы проверяют эти формулы, решая, выполнимо ли отрицание каждой из них.
+ Ответ `unsat` означает: отрицание невыполнимо, условие верификации истинно.

#important[
  Человек придумывает инварианты, машина проверяет следствия.
]

== Условия верификации

#example[От программы к условию верификации][
  Программа обменивает значения двух переменных:
  ```
  t := x
  x := y
  y := t
  ```
  Докажем тройку $ {x = a and y = b} space t := x ⨟ x := y ⨟ y := t space {x = b and y = a} $.
  Вычисляем слабейшее предусловие обратным проходом.
  $"wp"(y := t, x = b and y = a) = x = b and t = a$.
  $"wp"(x := y, x = b and t = a) = y = b and t = a$.
  $"wp"(t := x, y = b and t = a) = y = b and x = a$.
  Слабейшее предусловие --- в точности предусловие тройки, поэтому программа корректна.
]

== От программы к SMT-запросу

Каждое присваивание --- равенство между свежей константой и старым значением.
Условие верификации говорит: если выполнены предусловие и все равенства переходов, выполнено и постусловие.
Отрицание условия передаём солверу на языке SMT-LIB:

```smt2
(assert (not (=> (and (= x0 a) (= y0 b)
                      (= t0 x0) (= x1 y0) (= y1 t0))
                  (and (= x1 b) (= y1 a)))))
(check-sat)
```

Солвер отвечает `unsat`: отрицание невыполнимо, тройка верна.
Если бы солвер ответил `sat`, он предъявил бы модель --- контрпример, на котором тройка нарушается.

== Контрпримеры и тестирование

#example[Неверная тройка][
  Тройка $ {x = 5} space x := x + 1 space {x = 7} $ ложна.
  Из предусловия $x = 5$.
  После присваивания получаем $x = 6$.
  Отрицание её условия верификации выполнимо: солвер находит модель, на которой постусловие не выполнено.
]

Падающий тест --- контрпример к утверждению "программа корректна на этом входе".
Тестирование может продемонстрировать наличие ошибок, но никогда их отсутствие.
Один контрпример опровергает $forall$, но никакое количество примеров его не доказывает.

== Тестирование против доказательства

- Тестирование --- поиск контрпримера.
- Доказательство --- демонстрация того, что контрпримеров не существует.

И то, и другое необходимо: тестирование находит лёгкие ошибки, доказательство --- глубокие.

== Терминируемость и тотальная корректность

Частичная корректность не требует завершения.
Тройка $ {P} space S space {Q} $ утверждает: если $S$ завершается, то $Q$ верно.
Тотальная корректность требует и завершения, и постусловия.

#definition[Тотальная тройка][
  Тотальная тройка $[P] space S space [Q]$ означает: если $P$ верно перед выполнением, то $S$ завершается и после него верно $Q$.
]

== Вариант цикла

#definition[Вариант цикла][
  Функция $f$ со значениями во вполне упорядоченном множестве (обычно в натуральных числах) --- _вариант цикла_, если она строго убывает на каждой итерации.
  Вполне упорядоченное множество не содержит бесконечно убывающих цепочек.
  Рано или поздно вариант достигает минимума, и условие выхода срабатывает.
]

== Правило цикла с вариантом

#definition[Правило цикла с вариантом][
  Из тройки $ {I and B and f = z} space S space {I and f < z} $
  следует $[I] space "while" B "do" S space [I and not B]$.
  Здесь $f$ --- вариант цикла.
  А $z$ --- свежая переменная, фиксирующая значение $f$ до итерации.
]

#example[Вариант цикла][
  Программа `while x > 0 do x := x - 1` завершается.
  Вариант $f = x$.
  На каждой итерации $x$ уменьшается на 1.
  Бесконечная убывающая цепочка $x > x - 1 > x - 2 > dots$ невозможна.
]

== Model checking против дедуктивной верификации

Тройки Хоара --- дедуктивная верификация: доказательство по правилам вывода.
Model checking --- другой ответ: автоматический перебор состояний конечной модели.

#definition[Model checking][
  Строится конечная модель системы --- граф состояний и переходов.
  Свойство записывается в темпоральной логике, например в CTL.
  $"AG" p$ --- "во всех состояниях любого пути выполнено $p$".
  $"EF" p$ --- "существует путь, где когда-нибудь выполнено $p$".
  Алгоритм обходит состояния модели и проверяет свойство точно.
  При нарушении выдаётся контрпример --- путь, на котором свойство не выполнено.
]

== Сравнение подходов

#align(center)[#ctl-tree]

#note[
  Сравнение подходов:
  - Model checking точен на конечных моделях и полностью автоматичен.
  - Дедуктивная верификация работает с бесконечными состояниями, но требует инвариантов от человека.
  - Model checking отвечает "да" или "нет" с контрпримером, дедуктивная --- доказательством.
]

== Инструменты верификации

Труд верификации делится между человеком и машиной.
Инварианты придумывает человек, условия верификации проверяет SMT-солвер.

- Dafny --- верификатор аннотированных программ на собственном языке.
- Why3 --- портал, передающий условия верификации разным солверам.
- Frama-C --- анализ кода на языке C.
- Z3 --- SMT-солвер, проверяющий условия верификации на языке SMT-LIB.

#important[
  Разметка программы утверждениями и проверка следствий машиной --- рабочий процесс современной верификации.
  Падающий тест --- контрпример к утверждению о корректности.
  Тестирование и доказательство работают вместе: тесты находят лёгкие ошибки, доказательство закрывает глубокие.
]

== Что мы поняли

- Тройка Хоара --- контракт: предусловие, оператор, постусловие.
- Аксиома присваивания читается "назад": из постусловия восстанавливает предусловие.
- Слабейшее предусловие --- точный обратный инструмент для генерации условий верификации.
- Инвариант превращает шаг цикла в шаг индукции: база, шаг, выход.
- Условия верификации проверяют SMT-солверы: отрицание условия должно быть невыполнимо.
- Частичная корректность --- постусловие после завершения, тотальная --- ещё и завершение.
- Вариант цикла доказывает завершение: функция, убывающая на каждой итерации.
- Model checking перебирает состояния конечной модели, дедуктивная верификация доказывает по правилам.

== Вопросы для самопроверки

- Что такое тройка Хоара, и почему она задаёт частичную корректность?
- Как аксиома присваивания читается "назад"?
- Что такое слабейшее предусловие, и как оно вычисляется для присваивания и последовательности?
- Какие условия проверяются у инварианта цикла?
- Почему инвариант линейного поиска содержит границу $0 <= i <= n$?
- Что отвечает SMT-солвер корректной программе, и почему?
- Чем тотальная корректность отличается от частичной?
- Чем model checking отличается от дедуктивной верификации?
