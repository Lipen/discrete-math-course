// s2, лекция 5 --- Детерминированные автоматы (неделя 8--14 марта).
#import "theme.typ": *
#import "diagrams/ghosts.typ": (
  dfa-ghost, distinguish-ghost, minimize-ghost, nerode-ghost, nfa-ghost,
  pumping-ghost, regex-ghost, subset-ghost, words-ghost,
)
#import "diagrams/m23.typ": (
  dfa-01, dfa-even-ones, dfa-redundant, dfa-subset-01, nfa-01-eps, nfa-ends-01,
  nfa-third-one, thompson-star-one,
)
#show: slides.with(
  title: [Детерминированные автоматы],
  subtitle: [Дискретная математика --- лекция 5, 8--14 марта],
  date: "Весна 2027",
  authors: "Константин Чухарев",
  module: "automata",
)

= Лекция 5. Детерминированные автоматы

#lecture-slide(
  5,
  [Детерминированные автоматы],
  week: [8--14 марта],
  teaser: [
    Конечный набор состояний и таблица переходов --- этого хватает, чтобы распознавать языки.
    Введём конфигурации и язык автомата, построим первые распознаватели.
  ],
)

= Языки и слова

#focus-slide(
  ghost: words-ghost,
  epigraph: [Язык есть орган, образующий мысль.],
  epigraph-author: [Вильгельм фон Гумбольдт],
)

== Что читает машина?

Программа всегда читает строку.

Число записано цифрами, граф --- перечнем рёбер, программа --- текстом.

Всё, что машина читает, --- строка.

#example[
  Задачи одного и того же вида:
  - редактор находит в документе все номера телефонов;
  - компилятор режет текст программы на ключевые слова и числа;
  - протокол связи решает, корректно ли выглядит принятое сообщение.
]

#note[
  Во всех трёх задачах машина читает строку слева направо и отвечает "да" или "нет".

  Начнём с самого простого устройства, способного на такой ответ.
]

== Алфавит, слово, язык

#definition[
  - *Алфавит* --- конечное множество символов $Sigma$.
  - *Слово* --- конечная последовательность символов алфавита.
    Длина слова $w$ обозначается $|w|$.
    *Пустое слово* $epsilon$ --- единственное слово длины $0$.
  - *Язык* --- произвольное множество слов: $L subset.eq Sigma^*$.
]

#example[
  Над алфавитом ${0, 1}$ слово $"1001"$ имеет длину $4$.

  Язык слов, заканчивающихся на $"01"$, содержит $"1001"$ и не содержит $"1000"$.
]

#important[
  Язык --- множество слов.
  Распознать его --- уметь отвечать для любого слова, принадлежит ли оно.
]

== Пустое слово и пустой язык

Три множества слов легко перепутать.

#table(
  columns: (auto, auto),
  align: (left, left),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([*Язык*], [*Что содержит*]),
  [$emptyset$], [ни одного слова],
  [${epsilon}$], [одно слово длины $0$],
  [$Sigma^*$], [все слова алфавита],
)

#important[
  Язык ${epsilon}$ и язык $emptyset$ различны: в первом есть слово, во втором слов нет вовсе.

  Пустое слово в языке $L$ означает то же, что ноль элементов в множестве чисел: это содержательный факт.
]

== Сколько слов над алфавитом?

#theorem[Подсчёт слов][
  Если $|Sigma| = k$, то слов длины ровно $n$ --- ровно $k^n$.
]

#proof[
  Докажем подсчётом позиций.

  Слово длины $n$ --- это $n$ независимых выборов из $k$ символов.

  Всего выборов $k dot k dots.c k = k^n$.
]

#example[
  Двоичный алфавит, слова длины не больше $2$:
  $ 1 + 2 + 4 = 7. $

  Общее число слов любой длины бесконечно, но счётно: слова нумеруются по длине, внутри длины --- по порядку.
]

#important[
  $Sigma^*$ --- счётное множество: объединение счётного числа конечных множеств.

  Это тот же приём, которым нумеруются пары и рациональные числа.
]

#pagebreak()

== Все ли языки можно описать?

Язык --- подмножество $Sigma^*$.

Выбрать язык --- значит выбрать подмножество счётного множества.

#theorem[Подсчёт языков][
  Над непустым алфавитом языков ровно $2^(aleph_0)$ --- континуум.
]

#proof[
  Докажем подсчётом подмножеств.
  Языки --- булеан $Sigma^*$, равномощный бесконечным двоичным последовательностям, а их несчётно много по диагональному аргументу Кантора.
]

#important[
  Конечных строк над $Sigma$ --- счётное число, а языков --- континуум, и почти ни один язык нельзя описать конечным текстом. Курс изучает языки, которые описать _можно_.
]

== Операции над языками

#definition[
  - *Конкатенация*: $L_1 L_2 = {u v mid(|) u in L_1, v in L_2}$.
  - *Степень*: $L^0 = {epsilon}$, $L^(n+1) = L^n L$.
  - *Звезда Клини*: $L^* = union.big_(n >= 0) L^n$ --- ноль и более повторений.
]

#example[
  ${"a", "ab"} dot {"c"} = {"ac", "abc"}$.

  ${"0", "1"}^2$ --- четыре слова длины $2$.

  ${"a"}^*$ --- слова $"a", "aa", "aaa", ...$ и пустое слово.
]

#note[
  Звезда Клини --- сжатая запись цикла: сколько угодно раз пройти по одному и тому же куску.
]

== Алгебра языков

Языки --- множества, поэтому над ними работают все операции множеств:
$
  L_1 union L_2, quad L_1 inter L_2, quad overline(L) = Sigma^* setminus L.
$

Законы де Моргана из логики высказываний переносятся на языки дословно:
$
  overline(L_1 union L_2) = overline(L_1) inter overline(L_2).
$

#important[
  Комбинаторика языков опирается на знакомый фундамент: операции --- это связки, включение --- импликация, булеан даёт верхнюю границу.

  Нового здесь только одна операция: конкатенация, у числовых множеств аналога нет.
]

= Детерминированные автоматы

#focus-slide(
  ghost: dfa-ghost,
  epigraph: [Только разнообразие может поглотить разнообразие.],
  epigraph-author: [Уильям Росс Эшби],
)

== Три гипотезы о конце слова

Хотим автомат для языка слов, заканчивающихся на $"01"$.

Что нужно помнить о прочитанном префиксе?

- Ничего полезного для конца --- состояние $q_0$.
- Заканчивается на $0$: возможно, это предпоследний символ --- состояние $q_1$.
- Заканчивается на $"01"$ --- цель достигнута --- состояние $q_2$.

#important[
  Гипотезы о будущем превращаются в состояния автомата.

  Переход --- это обновление гипотезы при чтении очередного символа.
]

== Детерминированный конечный автомат

#definition[ДКА][
  Пятёрка $M = (Q, Sigma, delta, q_0, F)$:
  - $Q$ --- конечное множество состояний;
  - $Sigma$ --- алфавит;
  - $delta: Q times Sigma -> Q$ --- всюду определённая функция переходов;
  - $q_0 in Q$ --- начальное состояние;
  - $F subset.eq Q$ --- принимающие состояния.
]

#example[
  Таблица переходов --- задание $delta$ в конечном виде.
]

#important[
  Детерминизм: следующее состояние известно заранее --- вся память автомата есть номер состояния.
]

== Автомат как размеченный орграф

У автомата есть графовая внешность:

#table(
  columns: (auto, auto),
  align: (left, left),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([*В автомате*], [*В орграфе*]),
  [состояния], [вершины],
  [переходы], [дуги],
  [метки переходов], [цвета дуг],
  [начальное состояние], [выделенная вершина],
  [принимающие состояния], [выделенное подмножество вершин],
)

#important[
  Вычисление --- путь из выделенной вершины, метки пути складываются в слово.

  Язык автомата --- метки путей, кончающихся в принимающих вершинах.

  Это тот же граф, с которым работала теория графов: достижимость, циклы, обходы.
]

== Пример: чётное число единиц

#align(center)[#dfa-even-ones]

#example[
  Два состояния: $q_0$ --- чётное число единиц, $q_1$ --- нечётное.

  Символ $0$ состояние не меняет, символ $1$ переключает.

  Прогон $"011"$: $q_0 -> q_0 -> q_1 -> q_0$ --- принято.

  Прогон $"01"$: $q_0 -> q_0 -> q_1$ --- отвергнуто.
]

#note[
  Пустое слово принимается: ноль единиц --- чётное число.
]

== Расширенная функция переходов

#definition[Расширенная функция переходов][
  $hat(delta): Q times Sigma^* -> Q$ протягивает $delta$ на целые слова:
  - *Базис*: $hat(delta)(q, epsilon) = q$.
  - *Шаг*: $hat(delta)(q, a w) = hat(delta)(delta(q, a), w)$.
]

#example[
  На автомате чётности:
  $
    hat(delta)(q_0, "011") = hat(delta)(delta(q_0, 0), "11") = hat(delta)(q_0, "11") = q_0.
  $

  Слово $"011"$ принято.
]

#important[
  Принять слово --- значит проверить одно неравенство: $hat(delta)(q_0, w) in F$.
]

== Язык автомата

#definition[
  $ L(M) = {w in Sigma^* mid(|) hat(delta)(q_0, w) in F}. $

  Язык называется *регулярным*, если он распознаётся некоторым ДКА.
]

#example[
  Автомат чётности распознаёт язык слов с чётным числом единиц.

  Три состояния из гипотез распознают язык слов, заканчивающихся на $"01"$.
]

#important[
  Два автомата эквивалентны, если распознают один и тот же язык.

  Класс регулярных языков --- это то, что описывают автоматы с конечной памятью.
]

== Автомат в программе

#note[
  Принятие слова --- цикл по символам, один шаг --- один табличный переход.
  ```rust
  let mut q = start;
  for &sym in word {
      q = trans[q][sym];
  }
  accept[q]
  ```
  Массив `trans` --- это и есть таблица $delta$.

  Каждый символ обрабатывается за $O(1)$: такая распознающая часть называется *конечным автоматом* и в программировании тоже.
]

== Слова, заканчивающиеся на "01"

#align(center)[#dfa-01]

#example[
  Три состояния: ожидание, "заканчивается на $0$", "заканчивается на $"01"$".

  Прогон $"1001"$: $q_0 -> q_0 -> q_1 -> q_1 -> q_2$ --- принято.

  Прогон $"1000"$: $q_0 -> q_0 -> q_1 -> q_1 -> q_1$ --- отвергнуто.
]

#important[
  Три состояния хватает на любую длину: в памяти лишь важное для будущего.
]

== Что у автомата в памяти?

Состояние --- это сжатая история прочитанного префикса.

- Язык "заканчивается на $"01"$" --- три состояния: достаточно помнить последний символ или два.
- Язык "чётное число единиц" --- два состояния: достаточно помнить чётность.
- Язык "в слове поровну нулей и единиц" --- нужно помнить разность, а она не ограничена.

#important[
  Конечность памяти --- граница модели.

  Насколько она жёсткая, станет ясно, когда появится инструмент доказательства невозможности.

  Такой инструмент появится через две лекции.
]

== Лекция 5: что мы поняли?

#important[
  - Всё, что читает машина, --- строка: алфавит, слово, язык $subset.eq Sigma^*$.
  - Слов счётное число, языков --- континуум: конечным описанием описана малая доля языков.
  - ДКА --- пятёрка $(Q, Sigma, delta, q_0, F)$, вся память --- номер состояния.
  - Язык автомата собирается расширенной функцией $hat(delta)$ и условием $hat(delta)(q_0, w) in F$.
  - Автомат --- размеченный орграф: распознавание --- путь от выделенной вершины.
]

== Вопросы для самопроверки

+ Над алфавитом ${0, 1}$ сколько слов длины $3$? А длины не больше $3$?

+ Постройте ДКА для языка слов над ${0, 1}$, содержащих подстроку $"10"$.

+ Почему $emptyset$ и ${epsilon}$ --- разные языки? Какой из них распознаёт ДКА с недостижимым принимающим состоянием?

+ Слово $"ab"$ длины $2$ переводит автомат из $q_0$ в $q_2$.
  Чему равно $hat(delta)(q_0, "ab")$ и как это вычислить по шагам?

== Чтение к следующей паре

#note[
  - m23 --- "Алфавит, слова и формальные языки", "Детерминированные конечные автоматы".
  - Флеш-опросник --- в начале лекции 6.
]
