#import "theme.typ": *
#show: slides.with(
  title: [Контекстно-свободные языки],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)

#import "diagrams/m23.typ": chomsky-hierarchy, parse-tree-a3b3

= Грамматики
#focus-slide(
  epigraph: [Бесцветные зелёные идеи яростно спят.],
  epigraph-author: [Ноам Хомский],
)

== КС-грамматика

Регулярные языки --- простейшая ступень.
КС-грамматики снимают ограничение.

#definition[КС-грамматика (_context-free grammar_)][
  Четвёрка $G = (V, Sigma, P, S)$:
  - $V$ --- нетерминалы.
  - $Sigma$ --- терминалы.
  - $P$ --- правила $A -> alpha$, где $A in V$ --- один нетерминал, а $alpha in (V union Sigma)^*$ --- строка из терминалов и нетерминалов.
  - $S$ --- стартовый нетерминал.

  $ L(G) = {w in Sigma^* mid(|) S ->^* w}. $
]

== Порождение

#important[
  Грамматика описывает язык порождением.

  Правила конечны, слова --- бесконечно много.
]

== $a^n b^n$

#example[
  $ S -> a S b mid(|) epsilon. $

  Вывод: $S -> a S b -> a a S b b -> a a a S b b b -> a^3 b^3$.

  Не регулярен, но контекстно-свободен.
]

#important[
  На каждом шаге вывода слева появляется одна $a$, а справа --- одна $b$.

  Нужна память о числе --- конечному автомату не под силу.
]

== Язык Дика

#example[
  Сбалансированные скобки:
  $ S -> ( S ) S mid(|) epsilon. $

  Слово $(())()$ выводится так: раскрываем пары скобок и завершаем правилом $S -> epsilon$.

  Вложенность --- всюду:
  - программы,
  - выражения,
  - разметка.
]

#important[
  Распознать конечным автоматом нельзя.

  Нужна память о глубине.
]

== Грамматика предложений

#example[
  Правила над словами:
  $
    S -> "NP" "VP", \
    "NP" -> "Det" N mid(|) N, quad "VP" -> V "NP" mid(|) V, \
    "Det" -> "старый", quad N -> "кот" mid(|) "мышь", quad V -> "ловит" mid(|) "бежит".
  $

  Левый вывод: $S -> "NP" "VP" -> "Det" N "VP" -> "старый" "кот" "ловит" "мышь"$.

  Слово "кот старый" не выводится: определение стоит только перед именем.
]

#important[
  Язык --- не все строки над алфавитом, а фразы со структурой.

  Структуру задают правила, и она наследуется выводом.
]

= Автоматы с магазинной памятью
#focus-slide()

== PDA

#definition[PDA (_pushdown automaton_)][
  Семёрка $M = (Q, Sigma, Gamma, delta, q_0, Z_0, F)$:
  - $Q$ --- состояния, $Sigma$ --- входной алфавит.
  - $Gamma$ --- алфавит стека, $Z_0 in Gamma$ --- донный символ.
  - $delta: Q times (Sigma union {epsilon}) times Gamma -> cal(P)(Q times Gamma^*)$.
  - $q_0$ --- начальное, $F subset.eq Q$ --- принимающие.

  Переход $(q', gamma) in delta(q, a, A)$: из $q$, прочитав $a$, увидев $A$ на вершине, перейти в $q'$, заменив $A$ строкой $gamma$.
]

#important[
  Стек --- память о глубине вложенности.

  Недетерминизм --- как в НКА: существует путь.
]

== Принятие слова

#definition[Конфигурация и принятие][
  Конфигурация --- тройка $(q, w, gamma)$: состояние, остаток входа, стек.

  - *По принимающему состоянию*: путь к $(q, epsilon, gamma)$ с $q in F$.
  - *По пустому стеку*: путь к $(q, epsilon, epsilon)$.

  Оба критерия эквивалентны.
]

== Распознавание $a^n b^n$

#example[
  - Фаза $q_0$: на каждую $a$ кладём $A$ в стек.
  - Первая $b$: фаза $q_1$, на каждую $b$ снимаем $A$.
  - Принятие по пустому стеку.

  Прогон $"a a b b"$ (дно --- $Z_0$):
  $
    (q_0, a a b b, Z_0) -> (q_0, a b b, A Z_0) -> (q_0, b b, A A Z_0) \
    -> (q_1, b, A Z_0) -> (q_1, epsilon, Z_0) -> (q_1, epsilon, epsilon).
  $

  $a b a$ отвергается: в фазе $q_1$ символ $a$ читать нельзя.
]

#important[
  Глубина стека --- сколько $a$ ещё не нашли пару.
]

== КСГ и PDA равносильны

#theorem[КСГ и PDA][
  Язык порождается КС-грамматикой тогда и только тогда, когда распознаётся МП-автоматом (PDA, автоматом с магазинной памятью).
]

#proof[
  От грамматики к автомату: автомат имитирует левый вывод, храня непрочитанную часть вывода в стеке.
  Верхний символ стека --- терминал: снимаем его и сверяем с входной буквой.
  Верхний символ --- нетерминал $A$: выбираем правило $A -> beta$, снимаем $A$ и кладём $beta$.
  Автомат принимает, когда стек пуст и вход прочитан, и допускаемые им слова ровно те, что выводятся грамматикой.
]

== КСГ и PDA: от автомата к грамматике

#important[
  КСГ $->$ PDA: автомат имитирует левый вывод.

  PDA $->$ КСГ: нетерминалы кодируют переходы со стеком.
]

#proof[
  От автомата к грамматике: кодируем состояния и стек нетерминалами вида $[p X q]$ --- переместиться из $p$ в $q$, удалив из стека $X$.
  Переход из $p$ в $r$, снимающий $X$ и кладущий $Y_1 ... Y_m$ при чтении $a$, даёт правило $[p X q] -> a [r Y_1 q_1] [q_1 Y_2 q_2] ... [q_(m-1) Y_m q]$ со вспомогательными состояниями $q_1, ..., q_(m-1)$.
  Индукцией по числу переходов показывается, что такой нетерминал выводит строку тогда и только тогда, когда автомат проходит соответствующий путь.
]

= Структура КС-языков
#focus-slide()

== Дерево разбора

#definition[Дерево разбора][
  Корнем служит $S$, внутренние вершины помечены нетерминалами, а листья --- терминалами.

  Дети вершины $A$ слева направо --- правая часть применённого правила $A -> alpha$.

  Метки листьев слева направо дают слово $w$.
]

#example[
  Слово $"a a b b"$ из $S -> a S b mid(|) epsilon$:
  корень $S$ раскрывается в $a S b$, внутренний $S$ --- снова в $a S b$, самый внутренний --- в $epsilon$.

  Дерево показывает: первая $a$ спарена с последней $b$, вторая --- с предпоследней.
]

== Дерево: $a^3 b^3$

#align(center)[#parse-tree-a3b3]

#note[
  Дерево растёт слева направо: корень $S$ слева, листья --- справа.

  На каждой стрелке --- применённое правило: три раскрытия $S -> a S b$ и завершение $S -> epsilon$.

  Пустой лист $epsilon$ символов не даёт: листья $a, a, a, epsilon, b, b, b$ складываются в слово $a^3 b^3$.
]

== Видимая вложенность

#important[
  Строка прячет, какая скобка закрывает какую.

  Дерево разбора делает вложенность видимой.
]

== Неоднозначность

#definition[Неоднозначная грамматика][
  Грамматика неоднозначна, если некоторое слово языка имеет более одного дерева разбора.
]

#important[
  Два дерева --- два смысла одной строки.

  Неоднозначность приоритетов устраняют уровнями правил: слагаемые собираются из множителей.
]

== Пример: два дерева

#example[
  $ E -> E "+" E mid(|) E "*" E mid(|) (E) mid(|) "id". $

  Слово $"id" "+" "id" "*" "id"$ --- два дерева:
  - Склеить сначала $id + id$: чтение $(id + id) * id$.
  - Склеить сначала $id * id$: чтение $id + (id * id)$.

  При числах значения расходятся: $(2 + 3) dot 4 = 20$, но $2 + (3 dot 4) = 14$.
]

== Лемма о накачке

#theorem[Лемма о накачке для КС-языков (_pumping lemma_)][
  Если $L$ контекстно-свободен, то есть $p >= 1$: любое $w in L$ длины $|w| >= p$ разбивается как $w = u v x y z$:
  - $|v y| > 0$;
  - $|v x y| <= p$;
  - $u v^i x y^i z in L$ для всех $i >= 0$.
]

#important[
  Накачиваются две подстроки одновременно и симметрично.

  Регулярный случай --- одна подстрока (цикл). Здесь две (стек растёт и убывает).
]

== $a^n b^n c^n$ не КС

#example[
  Возьмём $w = a^p b^p c^p$.

  Подстрока $v x y$ длины $<= p$ не захватывает и $a$, и $c$: между ними стоят все $b$.

  Так как $v x y$ не содержит одновременно $a$ и $c$, при $i = 0$ или $i = 2$ одна из букв перестаёт накачиваться, и равенство количеств нарушается.

  Значит, $a^n b^n c^n$ не контекстно-свободен.
]

#important[
  Один стек --- один счётчик.

  Два независимых счётчика уводят за пределы класса.
]

= Разбор
#focus-slide()

== Обратная задача

КС-грамматика задаёт язык порождением.
Разбор --- обратная задача: по строке найти её вывод.

#definition[Разбор][
  Дана строка $w$ и грамматика $G$. Определить, лежит ли $w$ в $L(G)$, и если да --- построить дерево разбора.

  Именно это делает компилятор: текст программы превращается в синтаксическое дерево.
]

#example[
  Для $S -> a S b mid(|) epsilon$ и строки $"aabb"$: $S -> a S b -> a a S b b -> a a b b$ --- вывод, он же путь в дереве разбора.
]

== Направления разбора

- Нисходящий разбор начинает со стартового символа и раскрывает продукции.
- Восходящий разбор сворачивает правые части продукций к стартовому символу.

#important[
  Трудность --- выбор: какую продукцию применить, где и что свернуть.
]

== LL и LR

#definition[Классы LL($k$) и LR($k$)][
  - Грамматика LL($k$) разбирается нисходяще с подглядыванием на $k$ символов вперёд.
  - Грамматика LR($k$) разбирается восходяще.
  - Большинство грамматик языков программирования --- LR (или их упрощение LALR).
]

#important[
  Для LR-грамматик существуют генераторы парсеров:
  - yacc,
  - bison,
  - ANTLR.

  LL- и LR-языки --- собственные подклассы КС-языков.

  Недетерминизм --- цена выразительности: не всякая КС-грамматика разбирается детерминированно.
]

== Рекурсивный спуск

#definition[Рекурсивный спуск][
  Простейший нисходящий парсер состоит из одной функции на каждый нетерминал и пишется вручную.

  Функция для $A$ пробует правые части правил $A -> alpha$ по очереди.
]

#note[
  Так устроены парсеры компиляторов, написанные вручную.
]

= Иерархия Хомского
#focus-slide()

== Ступени

#align(center)[#chomsky-hierarchy]

#note[
  - *Регулярные*: конечные автоматы.
  - *Контекстно-свободные*: PDA.
  - *Контекстно-зависимые*: линейно-ограниченные автоматы.
  - *Рекурсивно перечислимые*: машины Тьюринга.
]

#important[Каждая ступень строго больше: $a^n b^n$ --- КС, но не регулярный.]

== Разрешимость

#important[
  Подъём по иерархии оплачивается потерей алгоритмических гарантий.

  - Для регулярных языков разрешимы все стандартные вопросы: эквивалентность, пустота, включение.
  - Для КС-языков разрешима пустота, но эквивалентность --- уже нет.
  - Для рекурсивно перечислимых неразрешима даже пустота (следующая лекция).
]

== Контекстно-зависимые

#definition[КЗ-грамматика (_context-sensitive grammar_)][
  Правила вида $alpha A beta -> alpha gamma beta$: нетерминал $A$ заменяется на непустую $gamma$, только если вокруг стоят $alpha$ и $beta$.

  Замена чувствительна к контексту --- отсюда название.
]

#example[
  Грамматика для $a^n b^n c^n$:
  $
    S -> a S B C mid(|) a B C, quad C B -> B C, \
    a B -> a b, quad b B -> b b, quad b C -> b c, quad c C -> c c.
  $

  - Правило $C B -> B C$ переставляет соседние $B$ и $C$.
  - Остальные превращают их в $b$ и $c$.
]

== Монотонность

КЗ-правило не укорачивает строку: правая часть длиннее левой на $|gamma| - 1 >= 0$ символов.

В выводе слова $w$ ни одна промежуточная строка не длиннее $|w|$.
Строк подходящей длины над конечным алфавитом --- конечное число.

#important[
  Распознавание перебором заведомо завершается: кандидатов конечное число.
]

#note[
  Монотонные грамматики (все правила $alpha -> beta$ с $|beta| >= |alpha|$) порождают в точности контекстно-зависимые языки.
]

== Линейно-ограниченный автомат

Стек доступен только с вершины, а контекстно-зависимым языкам нужна память с произвольным доступом.

#definition[Линейно-ограниченный автомат][
  Линейно-ограниченный автомат --- машина Тьюринга, длина ленты которой ограничена линейной функцией от длины входа.

  Распознаёт в точности контекстно-зависимые языки.
]

#example[
  ЛОА распознаёт $a^n b^n c^n$:
  + Стирает крайние $a$ и $c$ попарно.
  + Сверяет оставшиеся $b$, умещаясь на ленте длины $O(n)$.
]

== Мост к машине Тьюринга

#important[
  Мост к следующей лекции: полная машина Тьюринга --- та же лента без ограничения длины.

  КЗ-языки разрешимы, но распознавание дороже: перебор растёт экспоненциально с длиной слова.
]

== КС-языки на практике

#important[
  - КС-грамматики --- синтаксис языков программирования.
  - Парсеры строят деревья разбора.
  - Язык Дика --- вложенность скобок в коде.
]

#note[
  Нотация Бэкуса--Наура (BNF) --- синтаксическая запись КС-грамматик.

  Парсеры --- алгоритмы построения дерева разбора.
]

#important[
  Иерархия Хомского --- карта выразительности.

  От регулярных к рекурсивно перечислимым.
]

== Что мы поняли

#important[
  - КС-грамматика описывает язык порождением: конечные правила дают бесконечно много слов.
  - Стек --- память о глубине вложенности. КС-языки --- ровно те, что распознаются МП-автоматом (PDA).
  - Дерево разбора делает вложенность видимой. Два дерева у одного слова --- два смысла.
  - Один стек --- один счётчик: ${a^n b^n c^n}$ требует двух независимых счётчиков и выходит за пределы КС.
]

== Вопросы для самопроверки

- Определите КС-грамматику.

- Запишите грамматику для ${a^n b^n}$.

- Что такое язык Дика?

- Почему $a^n b^n$ распознаётся PDA, но не ДКА?

- Сформулируйте эквивалентность КСГ и PDA.

- Когда грамматика неоднозначна?

- Сформулируйте лемму о накачке для КС-языков.

- Почему ${a^n b^n c^n}$ не контекстно-свободен?

- Опишите иерархию Хомского.
