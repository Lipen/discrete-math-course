// M12 --- Графы: универсальная структура для моделирования связей.
#import "common-notes.typ": *
#import "notation.typ": *
#import "diagrams/m06.typ": (
  bfs-tree, bipartite, bridge-cut, directed-graph, eulerian, graph-coloring,
  k33, k5, petersen, planar, simple-graph, spanning-tree, tree,
)
#import "@preview/cetz:0.5.2" as cetz

= Графы <chap:graphs>

Отношения между объектами можно описать словами.
Можно нарисовать таблицу смежности --- она скажет, кто с кем связан.
Этого достаточно, чтобы передать информацию.
Но этого недостаточно, чтобы _увидеть_ структуру.

Проблема в том, что таблица смежности не различает изоморфные графы.
Два набора данных --- скажем, сеть знакомств и схема маршрутов городского транспорта --- могут иметь идентичную матрицу смежности при разной геометрии связей.
Таблица фиксирует наличие отношения, но теряет его форму.
А форма --- то, как именно устроена связность целого, --- часто и есть главное.

Граф решает эту проблему одним шагом: оставляет только вершины и рёбра.
Вершина --- точка без размера и положения, ребро --- связь без формы и длины.
Двух типов объектов достаточно для задач, остающихся открытыми десятилетиями, и теорем, требующих компьютерного перебора.
Короткое определение не обещает простых следствий.

В предыдущей главе мы изучали порядки --- структуры, выстраивающие элементы в иерархии: один больше, другой меньше, третий несравним.
Диаграмма Хассе --- это уже граф: вершины, соединённые рёбрами, только специального вида.
Теперь мы делаем следующий шаг: от частичных порядков к произвольным отношениям, представленным точками и линиями.

Из точек и линий строятся интернет (маршрутизаторы и кабели), социальные сети (люди и связи), карты дорог, зависимости программ, электрические цепи.
Сформулировать гамильтонов цикл можно в одну строку --- "посетить каждую вершину ровно один раз и вернуться"; решить для произвольного графа --- нельзя за полиномиальное время (при стандартном предположении $P eq.not "NP"$).

#history-note[Головоломка, ставшая математикой: рождение теории графов][
  В 1736 году Леонард Эйлер представил решение задачи о кёнигсбергских мостах: можно ли пройти по всем семи мостам города, не проходя ни по одному дважды?
  Эйлер показал, что это невозможно, и в процессе рассуждения абстрагировался от географии: участки суши стали вершинами, мосты --- рёбрами.
  Статья "Solutio problematis ad geometriam situs pertinentis" считается первой работой по теории графов, хотя сам термин "граф" появится лишь полтора века спустя.

  Сэр Уильям Роуэн Гамильтон в 1857 году изобрёл "Икосианскую игру" --- головоломку о поиске цикла, проходящего через все вершины додекаэдра ровно один раз, и начал изучение того, что мы теперь называем гамильтоновыми циклами.
  Артур Кэли в 1889 году опубликовал работу о перечислении деревьев --- формула Кэли $n^(n-2)$ для числа помеченных деревьев стала одним из первых нетривиальных результатов теории графов.
  Тогда же он применил графы к химии: деревья моделировали структурные формулы органических соединений.

  Густав Кирхгоф в 1845 году, разрабатывая законы электрических цепей, независимо пришёл к графовым понятиям: циклы в графе цепи соответствуют законам Кирхгофа.
  Денеш Кёниг в 1936 году опубликовал "Theorie der endlichen und unendlichen Graphen" --- первую монографию, где теория графов была представлена как самостоятельная математическая дисциплина с собственной системой понятий и результатов.
  За двести лет головоломка о мостах превратилась в универсальный язык дискретных структур.
]

#note[После этой главы вы будете уметь:
  + Определять основные параметры графа: степень, диаметр, компоненты связности;
  + Различать классы графов (деревья, двудольные, планарные, эйлеровы) по их характеристикам;
  + Применять формулу Эйлера $V - E + F = 2$ для проверки планарности;
  + Находить эйлеров цикл в графе (если существует);
  + Оценивать хроматическое число и применять раскраску к задачам распределения ресурсов;
  + Выбирать структуру данных для представления графа (матрица смежности vs списки смежности).
]

== Основные определения

Любой граф начинается с двух списков: перечня вершин и перечня рёбер.
Всё остальное --- степени, связность, циклы, раскраски --- вырастает из этих двух списков и правил их взаимодействия.
В этом разделе мы закладываем аксиоматический фундамент: что считать графом, какие бывают графы и как их представлять в памяти компьютера.

=== Графы и их разновидности

Граф --- это множество точек (вершин) и соединяющих их линий (рёбер).
Это определение настолько общее, что графы оказываются применимы повсюду: от моделирования социальных сетей до представления автоматов и пространств состояний программ.

#definition[Граф][
  *Графом* называется пара $G = (V, E)$, где $V$ --- множество вершин, $E$ --- множество рёбер.
  Граф называется *неориентированным*, если его рёбра --- неупорядоченные пары ${u, v}$ (связь симметрична).
  Граф называется *ориентированным* (орграфом), если его рёбра (дуги) --- упорядоченные пары $(u, v)$ (связь направлена).
]

#example[Граф треугольник][
  $V = {1, 2, 3}$, $E = {{1, 2}, {2, 3}, {3, 1}}$ --- неориентированный граф-треугольник.
  Полный граф $K_n$ имеет все $n(n-1)/2$ возможных рёбер между $n$ вершинами.
  $K_3$ --- треугольник, $K_4$ --- тетраэдр, $K_5$ --- минимальный непланарный полный граф (рисунок @fig:k5).
]

#note[Почему именно ($V$, $E$)?][
  Почему определение графа именно такое --- множество вершин и множество рёбер, и больше ничего?
  Можно было бы добавить веса, ориентацию, атрибуты вершин --- и все эти расширения действительно полезны на практике.
  Но минимальное определение фиксирует единственную существенную абстракцию: бинарное отношение на конечном множестве.
  Всё остальное --- веса, ориентация, разметка --- надстраивается поверх этой базы.
  Альтернативный путь --- определять граф сразу как матрицу смежности, но это привязывает к конкретному представлению; пара ($V$, $E$) остаётся инвариантом, не зависящим от реализации.
]

Под землёй лес --- это граф.

Деревья не растут изолированно.
Между корнями --- микоризная сеть: грибные гифы соединяют берёзу с дубом, сосну с елью.
По этим каналам идут вода, минералы, химические сигналы.
Материнское дерево узнаёт свой саженец и направляет ему больше углерода --- через общие грибные нити.

Это не метафора.
Это граф в прямом смысле: вершины --- деревья, рёбра --- гифы.
Та же структура, что и интернет, что и социальная сеть --- только вершины и рёбра.

Базовая пара $(V, E)$ --- это минимальный инвариант, но на практике одного определения недостаточно.
Разные задачи требуют разных вариантов графа.
Петли и кратные рёбра не нужны при моделировании социальных сетей (человек не дружит сам с собой, и между двумя людьми либо есть дружба, либо нет), но необходимы при моделировании электрических цепей (где между двумя узлами может идти несколько проводников) и автоматов (где петля означает переход в то же состояние).
Аналогично, ориентация рёбер критична для моделирования потоков данных и безразлична для моделирования симметричных отношений.
Выбор типа графа --- это первый проектировочный шаг: он определяет, какие вопросы можно задать и какие алгоритмы применимы.

В зависимости от того, разрешены ли петли (рёбра из вершины в себя) и кратные рёбра (несколько рёбер между одной парой вершин), выделяют несколько типов графов:

#definition[Специальные типы графов][
  - *Простой граф*: не содержит петель и кратных рёбер. Каждая пара вершин соединена не более чем одним ребром.
  - *Мультиграф*: разрешены петли и кратные рёбра. Полезен для моделирования, например, нескольких дорог между городами.
  - *$k$-регулярный граф*: каждая вершина имеет степень ровно $k$. Все вершины "одинаковы" по числу соседей.
]

#example[Типы графов][
  - Простой граф: граф Петерсена (рисунок @fig:petersen) --- нет петель и кратных рёбер.
  - Мультиграф: граф кёнигсбергских мостов (рисунок @fig:eulerian) может иметь несколько рёбер между одной парой вершин, что соответствует нескольким мостам между участками суши.
  - $k$-регулярный граф: граф Петерсена 3-регулярен; $K_4$ (полный граф на 4 вершинах) 3-регулярен; $C_n$ (цикл) 2-регулярен.
]

#figure(
  petersen,
  caption: [Граф Петерсена.],
) <fig:petersen>

Граф Петерсена на рисунке @fig:petersen 3-регулярен: каждая вершина имеет ровно три соседа.
Степень вершины --- одна из базовых характеристик графа, измеряющая локальную плотность связей.

#definition[Степень вершины][
  *Степенью* вершины $v$ называется количество инцидентных ей рёбер, обозначается $"deg"(v)$.
  В ориентированном графе различают:
  - *Полустепень исхода* $"deg"^+(v)$ --- количество исходящих дуг,
  - *Полустепень захода* $"deg"^-(v)$ --- количество входящих дуг.
]

#example[Степени в графе Петерсена][
  Граф Петерсена (рисунок @fig:petersen) 3-регулярен: каждая вершина имеет степень ровно $3$.
  В графе-звезде из 5 вершин: центр имеет степень $4$, каждый лист --- степень $1$.
  В ориентированном графе @fig:directed-graph: $"deg"^+(1) = 1$, $"deg"^-(1) = 1$ (одна дуга исходит к 2, одна входит от 3).
]

Лемма о рукопожатиях --- простейший пример метода двойного счёта.
Интуитивно: каждое ребро добавляет по единице к степени каждой из двух своих конечных вершин.

#theorem[Лемма о рукопожатиях][
  $sum_(v in V) "deg"(v) = 2|E|$.
  Следствие: число вершин нечётной степени всегда чётно.
]

#proof[
  Каждое ребро инцидентно ровно двум вершинам (своим концам).
  При суммировании степеней всех вершин каждое ребро учитывается дважды --- по разу для каждой из двух вершин, которые оно соединяет.
  Следовательно, сумма степеней равна удвоенному числу рёбер.

  Для следствия: сумма степеней всех вершин чётна (равна $2|E|$).
  Разобьём вершины на два множества: с чётной степенью и с нечётной.
  Сумма степеней вершин с чётной степенью чётна.
  Значит, сумма степеней вершин с нечётной степенью тоже должна быть чётной.
  Но сумма нечётных чисел может быть чётной только если количество слагаемых чётно.
]

Двойной счёт --- один из основных инструментов теории графов: многие тождества со степенями доказываются фразой "посчитаем рёбра двумя способами".

#figure(
  simple-graph,
  caption: [Неориентированный граф.],
) <fig:simple-graph>

Теперь, когда граф определён математически, возникает практический вопрос: как представить его в памяти компьютера?
Математическое определение --- пара множеств $(V, E)$ --- не диктует конкретную структуру данных.
А выбор структуры данных критически влияет на скорость алгоритмов: проверка смежности двух вершин, перебор соседей, добавление и удаление рёбер --- все эти операции имеют разную стоимость в разных представлениях.
Основных вариантов три, и выбор между ними сводится к одному вопросу --- насколько граф плотный.

=== Представления графов в памяти

Выбор представления графа критически влияет на производительность алгоритмов.
Три основных способа --- компромисс между памятью и скоростью типичных операций.

- *Список рёбер*: хранить все рёбра как пары вершин. Память: $O(|E|)$. Проверка смежности двух вершин: $O(|E|)$ (нужно просмотреть все рёбра). Подходит для алгоритмов, перебирающих все рёбра (Краскал).

- *Матрица смежности*: матрица $n times n$, где $A_(i j) = 1$, если есть ребро из $i$ в $j$. Память: $O(n^2)$. Проверка смежности: $O(1)$. Свойство: $(A^k)_(i j)$ равно количеству маршрутов длины $k$ из $i$ в $j$.

- *Список смежности*: для каждой вершины --- список её соседей. Память: $O(n + |E|)$. Проверка смежности: $O("deg"(v))$. Наилучший вариант для разреженных графов ($|E| << n^2$), стандартный выбор для большинства приложений.

== Пути, циклы, связность

Определив структуру графа, мы переходим к его динамическим свойствам: как можно "ходить" по графу и насколько граф "целостен".

#definition[Маршрут, путь, цикл][
  *Маршрутом* (walk) называется последовательность вершин $v_0, v_1, ..., v_k$, где ${v_(i-1), v_i} in E$ для каждого $i$.
  Длина маршрута равна $k$ (количество пройденных рёбер).

  Маршрут называется *путём* (path), если все его вершины различны (посещаем каждую не более одного раза).
  Маршрут называется *циклом* (cycle), если он замкнут ($v_0 = v_k$, $k >= 3$) и все промежуточные вершины различны.
]

#example[Маршруты, пути, циклы][
  В графе-треугольнике (вершины $1, 2, 3$, полный набор рёбер):
  - $1, 2, 1, 3$ --- маршрут длины $3$, но не путь (вершина $1$ повторяется).
  - $1, 2, 3$ --- путь (все вершины различны).
  - $1, 2, 3, 1$ --- цикл длины $3$.
  - $1, 2, 3, 2, 1$ --- замкнутый маршрут, но не цикл (вершина $2$ повторяется в середине).
]

#definition[Связность и расстояния][
  Граф называется *связным*, если между любой парой его вершин существует путь. *Компонентой связности* называется максимальный по включению связный подграф.
  Ребро называется *мостом*, если его удаление увеличивает число компонент связности.
  Вершина называется *точкой сочленения*, если её удаление нарушает связность.

  *Расстоянием* $d(u, v)$ называется длина кратчайшего пути между $u$ и $v$. *Диаметром* графа называется максимальное расстояние между его вершинами: $max_(u, v) d(u, v)$. *Эксцентриситетом* вершины называется максимальное расстояние от неё до любой другой вершины.
]

#example[Расстояния и диаметр][
  В графе-цепочке из пяти вершин ($1-2-3-4-5$): $d(1, 5) = 4$ (кратчайший путь: $1, 2, 3, 4, 5$), $d(1, 3) = 2$.
  Диаметр равен $4$ --- максимальное расстояние между вершинами.
  Эксцентриситет вершины 3 равен $2$ (до 1 и 5), вершины 1 --- $4$ (до 5).

  В графе-звезде (центр 1, листья $2, 3, 4, 5$): $d(2, 3) = 2$ (через центр), диаметр $2$, эксцентриситет центра --- $1$, листьев --- $2$.
]

#figure(
  bridge-cut,
  caption: [Мост и точка сочленения.],
) <fig:bridge-cut>

#note[Сильная связность (ориентированные графы)][
  В орграфах *сильная связность* требует существования ориентированных путей в обоих направлениях между любой парой вершин.
  Компоненты сильной связности (SCC) вычисляются алгоритмом Косарайю или Тарьяна за $O(V + E)$ с помощью DFS.
]

#figure(
  directed-graph,
  caption: [Ориентированный граф.
    Вершины 1, 2, 3 образуют одну компоненту сильной связности --- каждая достижима из каждой.],
) <fig:directed-graph>

Дерево --- простейший нетривиальный класс графов: у графа без рёбер нет структуры, у дерева --- минимальная структура, поддерживающая связность.
Для этого класса известно пять эквивалентных определений.
Каждое высвечивает свой аспект: комбинаторный (число рёбер), топологический (связность без циклов), алгоритмический (единственность пути).
Множественность эквивалентных определений --- признак хорошо определённой структуры: каждое удобно в своей ситуации.
В комбинаторике, теории алгоритмов и базах данных деревья возникают повсеместно потому, что моделируют иерархию.

== Деревья

#definition[Дерево][
  Граф называется *деревом*, если он связный и ациклический.
  Граф называется *лесом*, если он является дизъюнктным объединением деревьев.
]

#example[Деревья и не-деревья][
  Граф-цепочка из $n$ вершин ($1-2-\...-n$) --- дерево: связен и имеет $n-1$ рёбер, циклов нет.
  Граф-цикл $C_n$ не является деревом: содержит цикл.
  Звезда (центр и $k$ листьев) --- дерево.
  Лес из двух цепочек $1-2-3$ и $4-5$ имеет $3+2=5$ вершин и $2+1=3$ ребра, $5-2=3$ компоненты связности.
]

#figure(
  tree,
  caption: [Дерево.],
) <fig:tree>

#theorem[Эквивалентные характеризации дерева][
  Для графа с $n$ вершинами следующие утверждения эквивалентны:
  + Граф связный и ациклический (является деревом).
  + Граф связный и имеет $n-1$ рёбер.
  + Граф ациклический и имеет $n-1$ рёбер.
  + Между любой парой вершин существует единственный простой путь.
  + Граф связный, и каждое ребро является мостом.
]

#proof-sketch[
  $(1) => (2)$: граф связный и ациклический $=>$ граф связный и имеет $n-1$ рёбер.
  Индукция по $n$.
  Дерево имеет лист (вершину степени 1).
  Удалив лист и инцидентное ему ребро, получим дерево на $n-1$ вершинах, которое по индукции имеет $(n-1)-1 = n-2$ рёбер.
  Добавляем удалённое ребро --- получаем $n-1$.

  $(2) => (3)$: граф связный и имеет $n-1$ рёбер $=>$ граф ациклический и имеет $n-1$ рёбер.
  Если бы существовал цикл, удаление ребра из цикла оставило бы граф связным с $n-2$ рёбрами.
  Но связный граф на $n$ вершинах требует не менее $n-1$ рёбер --- противоречие.
  Значит, циклов нет.

  $(3) => (4)$: граф ациклический и имеет $n-1$ рёбер $=>$ между любой парой вершин существует единственный простой путь.
  Если граф несвязен и состоит из $c$ компонент, каждая компонента --- дерево, и общее число рёбер $n - c$.
  При $|E| = n-1$ получаем $c = 1$, то есть граф связен.
  Если бы между $u$ и $v$ существовали два разных простых пути, их симметрическая разность содержала бы цикл --- противоречие с ацикличностью.

  $(4) => (5)$: между любой парой вершин существует единственный простой путь $=>$ граф связный, и каждое ребро является мостом.
  Удаление любого ребра ${u, v}$ уничтожает единственный путь между $u$ и $v$ --- граф теряет связность.
  Следовательно, каждое ребро --- мост.

  $(5) => (1)$: граф связный, и каждое ребро является мостом $=>$ граф связный и ациклический (дерево).
  Если бы существовал цикл, никакое ребро цикла не было бы мостом (удаление ребра цикла оставляет альтернативный путь).
  Значит, циклов нет --- граф ацикличен.
  Вместе со связностью это даёт дерево.
]

Структура ветвления повторяется на всех масштабах.

Дельта реки, бронхи лёгкого, нейрон с дендритами, граф вызовов рекурсивной функции --- всё это деревья.
Пять эквивалентных определений не случайны.
Они отражают разные способы увидеть одну структуру.
"Связный и ациклический" --- топология.
"$n$ вершин, $n-1$ рёбер" --- комбинаторика.
"Единственный путь между любой парой" --- алгоритмический взгляд.

Каждое из пяти определений высвечивает свой аспект дерева --- комбинаторный, топологический, алгоритмический, --- и выбор определения зависит от задачи.

#corollary[Листья][
  Любое дерево с $n >= 2$ имеет хотя бы два листа (вершины степени 1).
]

#proof[
  Пусть $T$ --- дерево с $n >= 2$ вершинами.
  По характеризации дерева и лемме о рукопожатиях сумма степеней равна $2|E| = 2(n-1) = 2n - 2$.

  Предположим, что листьев меньше двух.
  Тогда либо их нет вовсе, либо ровно один.
  Если листьев нет, каждая вершина имеет степень $>= 2$, и сумма степеней $>= 2n > 2n - 2$ --- противоречие.
  Если лист ровно один, то остальные $n-1$ вершин имеют степень $>= 2$, и сумма степеней $>= 1 + 2(n-1) = 2n - 1 > 2n - 2$ --- противоречие.
  Следовательно, дерево имеет хотя бы два листа.
]

Листья --- крайние точки дерева; их существование используется в алгоритмах обхода и доказательствах по индукции.

Дерево может быть подграфом более крупного графа --- тогда оно служит скелетом, сохраняющим связность.

#definition[Остовное дерево][
  *Остовным деревом* графа называется его подграф, включающий все вершины и являющийся деревом.
  Любой связный граф имеет остовное дерево.
]

#example[Остовные деревья][
  $K_3$ (треугольник) --- полный граф на трёх вершинах.
  Удаление любого из трёх рёбер даёт остовное дерево (цепочку из трёх вершин).

  $C_4$ (цикл из четырёх вершин): удаление любого из четырёх рёбер даёт остовное дерево.
  Рисунок @fig:spanning-tree показывает остовное дерево для графа из шести вершин.
]

#figure(
  spanning-tree,
  caption: [Остовное дерево.],
) <fig:spanning-tree>

Остовное дерево сохраняет связность графа минимальным числом рёбер.
Естественный вопрос: сколько вообще существует деревьев на заданном наборе помеченных вершин?

#theorem[Формула Кэли#footnote[Cayley A. "A theorem on trees", Quarterly Journal of Mathematics, 1889.]][
  Количество помеченных деревьев на $n$ вершинах равно $n^(n-2)$.
]

#proof-sketch[
  Построим биекцию между помеченными деревьями на $n$ вершинах и последовательностями длины $n-2$ из чисел ${1, 2, ..., n}$ (кодами Прюфера).
  Таких последовательностей ровно $n^(n-2)$.

  Из дерева в код: на каждом шаге находим лист с наименьшей меткой, записываем метку его соседа, удаляем лист.
  Повторяем $n-2$ раза.

  Из кода в дерево: восстанавливаем последовательность удалённых листьев, затем строим дерево, соединяя удалённые листья с соответствующими элементами кода.

  Биективность: каждой последовательности соответствует ровно одно дерево и наоборот.
  Следовательно, число помеченных деревьев равно числу последовательностей.
]

Формула Кэли считает все помеченные деревья, не различая их по весам.
Если рёбрам приписаны веса, возникает оптимизационная задача: найти остов минимального суммарного веса.

Дан взвешенный связный граф; минимальное остовное дерево (MST) минимизирует сумму весов рёбер.
Два классических жадных алгоритма:
- *Краскал*: сортировать рёбра по весу, добавлять легчайшее, не создающее цикла (система непересекающихся множеств).\ $O(E log E)$.
- *Прим*: растить дерево от произвольной стартовой вершины, на каждом шаге добавлять легчайшее ребро до внешней вершины (очередь с приоритетом).\ $O((V+E) log V)$.

== Эйлеровы и гамильтоновы графы

#definition[Эйлеров граф][
  Цикл называется *эйлеровым*, если он проходит каждое ребро графа ровно один раз и возвращается в начало.
  Граф называется *эйлеровым*, если он содержит эйлеров цикл.
  Маршрут называется *эйлеровой цепью*, если он проходит каждое ребро ровно один раз, а его начало и конец различны.
]

#example[Эйлеровы графы][
  $K_5$ эйлеров: каждая вершина имеет степень $4$ (чётно).
  $K_4$ не эйлеров: степень каждой вершины $3$ (нечётно), но имеет эйлерову цепь.
  Граф на рисунке @fig:eulerian не эйлеров: четыре вершины имеют нечётную степень, что соответствует задаче о кёнигсбергских мостах.
]

#theorem[Критерий Эйлера#footnote[Euler L. "Solutio problematis ad geometriam situs pertinentis" (Решение задачи, относящейся к геометрии положения), Commentarii academiae scientiarum Petropolitanae, 1741. Первая работа по теории графов --- задача о кёнигсбергских мостах.]][
  Связный неориентированный граф является эйлеровым тогда и только тогда, когда все степени чётны.
  Имеет эйлерову цепь тогда и только тогда, когда ровно две вершины имеют нечётную степень.
]

#proof-sketch[
  ($arrow.double.r$) При каждом проходе через вершину эйлеров цикл использует два инцидентных ей ребра --- одно для входа, одно для выхода.
  Поскольку цикл замкнут и проходит каждое ребро ровно один раз, степень каждой вершины чётна.

  ($arrow.double.l$) Индуктивное построение алгоритмом Хирхольцера.
  Начинаем с произвольной вершины и идём по непосещённым рёбрам, пока не зайдём в тупик.
  Поскольку все степени чётны, тупик возможен только в стартовой вершине.
  Полученный цикл $C$ заметаем; если остались непосещённые рёбра, находим в $C$ вершину с непосещёнными рёбрами и запускаем из неё новый обход.
  Вставляем новый цикл в $C$ в этой вершине.
  Повторяем, пока не исчерпаем все рёбра.

  Для эйлеровой цепи: если ровно две вершины нечётной степени, соединим их фиктивным ребром.
  В полученном графе все степени чётны --- строим эйлеров цикл и удаляем фиктивное ребро, получая эйлерову цепь.
]

Эйлер доказал невозможность обхода кёнигсбергских мостов, но необходимое и достаточное условие существования эйлерова цикла сформулировал не он.
Это сделал Карл Хирхольцер в 1873 году --- спустя почти полтора века после статьи Эйлера.
Хирхольцер также дал первый алгоритм построения эйлерова цикла, который, в современной терминологии, работает за $O(E)$ и лежит в основе всех практических реализаций.
Постановка задачи и её полное решение могут разделяться веками.

#raven[
  В доказательстве критерия Эйлера чётность степеней работает непрерывно --- на каждом шаге алгоритма.
  Связность используется однажды: гарантирует что оставшиеся рёбра достижимы из построенного цикла.
  Странно, что условие, использованное однажды, необходимо...
]

=== Задача китайского почтальона

Дан связный взвешенный граф; требуется найти кратчайший замкнутый маршрут, проходящий каждое ребро хотя бы один раз.
Если все степени чётны, задача сводится к эйлерову циклу и решается за $O(E)$.

Если есть вершины нечётной степени, их чётное число (по лемме о рукопожатиях).
Алгоритм: найти паросочетание минимального веса на подграфе вершин нечётной степени (задача о назначениях, решаемая за $O(V^3)$ алгоритмом Эдмондса), добавить рёбра паросочетания как дублируемые --- после этого все степени становятся чётными, и в полученном мультиграфе строится эйлеров цикл.
Итоговая сложность --- $O(V^3)$.

Та же задача возникает при планировании маршрутов уборочной техники и инспекции трубопроводов: нужно покрыть все сегменты сети, минимизируя повторные проходы.
#footnote[Kwan M.-K. "Graphic programming using odd or even points", Chinese Mathematics, 1962. Эдмондс и Джонсон (1973) свели задачу к паросочетанию, установив полиномиальную разрешимость.]

#figure(
  eulerian,
  caption: [Кёнигсбергские мосты.],
) <fig:eulerian>

Эйлеров цикл и его вариации разрешимы за полиномиальное время.
Гамильтонов цикл --- внешне похожая задача (посетить каждую вершину вместо каждого ребра), но её сложностной статус иной.

#definition[Гамильтонов граф][
  Цикл называется *гамильтоновым*, если он посещает каждую вершину графа ровно один раз и возвращается в начало.
  Граф называется *гамильтоновым*, если он содержит гамильтонов цикл.
  Необходимого и достаточного условия, проверяемого за полиномиальное время, не известно --- задача NP-полна.
  Существуют достаточные условия (теоремы Дирака, Оре), но они не являются необходимыми.
]

#example[Гамильтоновы и негамильтоновы графы][
  Граф Петерсена (рисунок @fig:petersen) гамильтонов --- в нём есть цикл, посещающий все 10 вершин ровно один раз.
  $K_n$ при $n >= 3$ всегда гамильтонов.
  Граф-звезда из 4 и более вершин не гамильтонов: центр вынужден повторяться при попытке обойти все листья.
]

#history-note[Икосианова игра][
  В 1857 году сэр Уильям Роуэн Гамильтон изобрёл "Икосианскую игру": деревянный додекаэдр, на вершинах которого были нанесены названия городов.
  Задача --- обойти все вершины по рёбрам, посетив каждую ровно один раз и вернувшись в начало.
  Гамильтон продал идею лондонскому производителю игр за 25 фунтов.
  Игра коммерчески провалилась --- викторианская публика не оценила математическую головоломку, --- но породила понятие гамильтонова цикла, прочно вошедшее в теорию графов и теорию сложности.
  Деревянный додекаэдр за 25 фунтов оказался одной из самых выгодных инвестиций в математику XIX века.
]

#remark[Эйлер против Гамильтона: одно слово --- пропасть][
  Эйлеров цикл проходит по каждому ребру ровно один раз.
  Гамильтонов цикл посещает каждую вершину ровно один раз.
  Разница в одном слове --- "ребро" вместо "вершина".
  Формулировки почти идентичны.

  Но задача проверки эйлеровости решается за $O(E)$ операций --- достаточно проверить чётность степеней и связность.
  А задача проверки гамильтоновости NP-полна.
  Разница не количественная, а качественная: одна задача принадлежит классу P, другая --- к числу труднейших в NP.

  Причина --- в структуре условий.
  Чётность степени --- локальное свойство: чтобы проверить, является ли вершина частью эйлерова цикла, достаточно посмотреть на её соседей.
  Решение в одной части графа не конфликтует с решением в другой.
  Гамильтонов цикл требует глобального выбора: включив одно ребро в цикл, вы исключаете возможность включить два других.
  Каждое локальное решение немедленно ограничивает весь остальной граф.

  Разница в одном слове меняет природу задачи.
  Проверка эйлеровости использует только локальную информацию о степенях и не требует перебора.
  Проверка гамильтоновости требует учёта взаимодействия всех выбранных рёбер --- и становится NP-полной.
]

#theorem[Дирак, 1952][
  Если в графе $G$ с $n >= 3$ вершинами $"deg"(v) >= n/2$ для каждой вершины $v$, то $G$ --- гамильтонов.
]

#proof[
  Пусть $P = v_1, ..., v_k$ --- максимальный простой путь в $G$.
  По максимальности все соседи $v_1$ и $v_k$ лежат на $P$: иначе путь можно было бы удлинить.

  По условию Дирака $"deg"(v_1) >= n/2$ и $"deg"(v_k) >= n/2$.
  Определим множества индексов:
  $S = {i mid(|) 1 <= i < k, v_1$ смежна с $v_(i+1)}$,
  $T = {i mid(|) 1 <= i < k, v_k$ смежна с $v_i}$.
  Тогда $|S| >= n/2$, $|T| >= n/2$, и оба множества --- подмножества ${1, ..., k-1}$.

  По принципу включения-исключения:
  $ |S inter T| = |S| + |T| - |S union T| >= n/2 + n/2 - (k-1) = n - (k-1) $.
  Поскольку $k <= n$, имеем $|S inter T| >= 1$.
  Значит, существует индекс $i$ такой, что $v_1$ смежна с $v_(i+1)$, а $v_k$ --- с $v_i$.

  Построим цикл $C = v_1, ..., v_i, v_k, v_(k-1), ..., v_(i+1), v_1$ длины $k$.

  Если $k = n$, то $C$ --- гамильтонов цикл.
  Если $k < n$, то из условия Дирака следует связность графа (сумма степеней любой пары несмежных вершин $>= n$ при $n >= 3$), поэтому существует вершина $w$ вне $C$, смежная с некоторой вершиной $C$.
  Добавив $w$ к $C$, получим путь длины $k+1$ --- противоречие с максимальностью $P$.
  Следовательно, $k = n$, и $G$ гамильтонов.
]

Теорема Дирака требует, чтобы _каждая_ вершина имела высокую степень.
Через восемь лет Ойстин Оре заметил, что условие можно ослабить: достаточно контролировать суммы степеней _несмежных_ пар.

#theorem[Оре, 1960][
  Если в графе $G$ с $n >= 3$ вершинами для каждой пары несмежных вершин $u$, $v$ выполнено $"deg"(u) + "deg"(v) >= n$, то $G$ --- гамильтонов.
]

#proof[
  Пусть $P = v_1, ..., v_k$ --- максимальный простой путь в $G$.
  По максимальности все соседи $v_1$ и $v_k$ лежат на $P$.

  Рассмотрим два случая.

  *Случай 1:* $v_1$ и $v_k$ смежны.
  Тогда $P$ замыкается в цикл $v_1, ..., v_k, v_1$ длины $k$.

  *Случай 2:* $v_1$ и $v_k$ несмежны.
  Тогда по условию Оре $"deg"(v_1) + "deg"(v_k) >= n$.
  Как и в доказательстве Дирака, определим $S = {i mid(|) v_1$ смежна с $v_(i+1)}$, $T = {i mid(|) v_k$ смежна с $v_i}$.
  Имеем $|S| = "deg"(v_1)$, $|T| = "deg"(v_k)$, и $S union T subset.eq {1, ..., k-1}$, откуда $|S union T| <= k - 1$.

  Тогда $|S inter T| = |S| + |T| - |S union T| >= n - (k - 1)$.
  Поскольку $k <= n$, имеем $|S inter T| >= 1$, и существует $i$ с $v_1 ~ v_(i+1)$ и $v_k ~ v_i$.
  Строим цикл $C = v_1, ..., v_i, v_k, v_(k-1), ..., v_(i+1), v_1$ длины $k$.

  В обоих случаях получаем цикл $C$ длины $k$.
  Если $k = n$, то $C$ --- гамильтонов цикл.
  Если $k < n$, то условие Оре влечёт связность $G$ (для любой пары несмежных вершин сумма степеней $>= n$ гарантирует наличие общего соседа или смежность), и существует вершина вне $C$, смежная с $C$ --- противоречие с максимальностью $P$.
  Следовательно, $k = n$, и $G$ гамильтонов.
]

Условие Оре слабее условия Дирака: если каждая вершина имеет степень $>= n/2$, то сумма степеней любой пары (смежной или нет) не меньше $n$, так что теорема Дирака --- прямое следствие теоремы Оре.
Тем не менее, формулировка Дирака часто удобнее для проверки: достаточно посчитать степени вершин, не вникая в то, какие пары смежны.

=== Задача коммивояжёра (TSP)

Задача коммивояжёра в общей постановке NP-трудна, но это не остановило разработку практических методов.
Современный рекорд точного решения --- 85 900 городов (Concorde, 2006), достигнутый методом branch-and-cut: задача формулируется как целочисленная линейная программа, решается её линейная релаксация, а затем отсекаются нецелочисленные решения добавлением отрезающих плоскостей --- линейных неравенств, которым удовлетворяют все гамильтоновы циклы, но которые нарушаются текущим дробным решением.

Для метрического TSP (расстояния удовлетворяют неравенству треугольника) алгоритм Кристофидеса (1976) даёт 1.5-приближение за полиномиальное время: строится минимальное остовное дерево, на вершинах нечётной степени в MST находится совершенное паросочетание минимального веса, объединение даёт эйлеров мультиграф, из его эйлерова цикла строится гамильтонов цикл shortcut'ами (пропуская повторно посещённые вершины).
#footnote[Dantzig G., Fulkerson R., Johnson S. "Solution of a large-scale traveling-salesman problem", J. Operations Research Society of America, 1954. Christofides N. "Worst-case analysis of a new heuristic for the travelling salesman problem", Report 388, Carnegie-Mellon, 1976.]

== Двудольные графы

#definition[Двудольный граф][
  Граф называется *двудольным*, обозначается $G = (X, Y, E)$, если его вершины можно разбить на два непересекающихся множества $X$ и $Y$, так что каждое ребро соединяет вершину из $X$ с вершиной из $Y$.
]

#example[Двудольные графы][
  $C_4$ (цикл из 4 вершин) двудолен: $X = {1, 3}$, $Y = {2, 4}$, каждое ребро идёт между долями.
  $K_(3,3)$ --- полный двудольный граф с $|X| = |Y| = 3$ (рисунок @fig:k33).
  $C_3$ (треугольник) не двудолен: при попытке раскрасить две вершины в разные цвета третья соседствует с обеими.
  Деревья всегда двудольны: BFS-раскраска по чётности уровня даёт разбиение.
]

#theorem[Критерий двудольности][
  Граф является двудольным тогда и только тогда, когда в нём нет циклов нечётной длины.
]

#proof[
  Пусть граф двудолен с долями $X$ и $Y$.
  Рассмотрим произвольный цикл $v_1, v_2, ..., v_k, v_1$.
  Вершины цикла чередуются между долями: если $v_1 in X$, то $v_2 in Y$, $v_3 in X$, и так далее.
  Чтобы вернуться в $X$ на последнем шаге, число шагов должно быть чётным.
  Следовательно, $k$ чётно.

  Обратно: пусть в графе нет циклов нечётной длины.
  Запустим BFS из произвольной вершины и назначим уровень каждой вершины --- расстояние от стартовой.
  Раскрасим вершины чётных уровней в один цвет, нечётных --- в другой.
  Если бы ребро соединяло вершины одного цвета, то вместе с путями от стартовой вершины оно образовало бы цикл нечётной длины.
  Противоречие.
  Значит, каждое ребро соединяет вершины разных цветов --- граф двудолен.

  Проверка двудольности и построение разбиения выполняются за $O(V+E)$ одним обходом.
]

#figure(
  bipartite,
  caption: [Двудольный граф.],
) <fig:bipartite>

Двудольная структура естественно подводит к задаче о паросочетании: как установить взаимно-однозначное соответствие между элементами двух долей?

#definition[Паросочетание][
  *Паросочетанием* называется множество рёбер, не имеющих общих вершин.
  Паросочетание называется *совершенным*, если оно покрывает все вершины графа.
]

#example[Паросочетания][
  В двудольном графе $K_(3,2)$ (три вершины слева, две справа, все возможные рёбра): максимальное паросочетание состоит из двух рёбер --- по одному на каждую вершину правой доли.
  Совершенного паросочетания нет: нечётное число вершин.
  В $K_(3,3)$ совершенное паросочетание существует: можно выбрать три ребра, попарно не имеющие общих вершин, покрывающие все шесть вершин.
]

Существование паросочетания, покрывающего долю $X$, не гарантировано: если у нескольких вершин из $X$ общие соседи, рёбер может не хватить.
Теорема Холла даёт точное условие.

#theorem[Теорема Холла о свадьбах#footnote[Hall P. "On representatives of subsets", Journal of the London Mathematical Society, 1935.]][
  В двудольном графе $(X union Y, E)$ паросочетание, покрывающее $X$, существует тогда и только тогда, когда $forall S subset.eq X: |N(S)| >= |S|$.
]

#proof-sketch[
  ($arrow.double.r$) Необходимость: если $M$ --- паросочетание, покрывающее $X$, то для любого $S subset.eq X$ рёбра из $M$, инцидентные вершинам $S$, идут в различные вершины $Y$.
  Значит, $|N(S)| >= |M inter delta(S)| = |S|$.

  ($arrow.double.l$) Достаточность доказывается от противного.
  Пусть $M$ --- максимальное по размеру паросочетание, но $|M| < |X|$.
  Построим увеличивающий путь алгоритмом поиска чередующихся путей (аналогично доказательству Кёнига): множество $Z subset.eq X$ вершин, не покрытых $M$ и достижимых чередующимися путями.
  Условие Холла гарантирует $|N(Z)| >= |Z|$, но из максимальности $M$ следует строгая достижимость $N(Z)$ --- противоречие.
  Следовательно, $M$ покрывает $X$.
]

== Планарность

Планарность --- геометрическое ограничение с прямыми инженерными следствиями.
При проектировании печатных плат проводники не могут пересекаться: пересечение означает короткое замыкание.
При проектировании СБИС (сверхбольших интегральных схем) пересечения проводов на одном слое физически невозможны --- для этого пришлось бы добавлять дополнительные слои металлизации, что удорожает производство.
Даже при визуализации графов пересечения рёбер мешают восприятию: планарная укладка читается быстрее и точнее.
Поэтому вопрос "является ли граф планарным" --- это вопрос о том, можно ли реализовать схему на одном слое, можно ли развести провода без перемычек и можно ли нарисовать граф без визуального шума.

#definition[Планарный граф][
  Граф называется *планарным*, если он может быть изображён на плоскости без пересечений рёбер.
  Области, ограниченные рёбрами планарного графа, называются *гранями*.
]

#example[Планарные и непланарные графы][
  $K_4$ (полный граф на 4 вершинах) планарен: его можно изобразить как треугольник с вершиной внутри, соединённой со всеми тремя углами, без пересечений.
  Любое дерево планарно --- достаточно отводить ветви в стороны.
  $K_5$ (рисунок @fig:k5) не планарен: при любой попытке укладки два ребра пересекутся.
  $K_(3,3)$ (рисунок @fig:k33) также не планарен.
  Рисунок @fig:planar показывает планарную укладку графа.
]

#remark[Почему планарность --- свойство структуры, а не рисунка?][
  Определение планарности заслуживает пристального взгляда.
  Граф называется планарным, если _существует_ его укладка на плоскости без пересечения рёбер.
  Слово "существует" --- экзистенциальный квантор.
  Оно ничего не говорит о том, как проверить планарность по внутренним свойствам самого графа.
  Перед нами неконструктивное определение: мы знаем, что значит "быть планарным", но не знаем, как распознать планарность, не прибегая к перебору укладок.

  Теорема Куратовского устраняет этот разрыв.
  Планарность эквивалентна отсутствию подграфов, гомеоморфных $K_5$ или $K_(3,3)$.
  Два запрещённых подразбиения --- и вопрос решён.
  Определение, сформулированное через внешнее представление (укладку), получает внутреннюю характеризацию (запрещённые подструктуры).

  Это не изолированный факт о графах.
  Это паттерн: свойство, определённое через существование некоторого представления, оказывается эквивалентным запрету конечного набора конфигураций.
  Внешнее определение --- внутренняя структура.
  Теорема Куратовского завершает этот переход: планарность, определённая через укладку, получает внутреннюю характеризацию через запрещённые подструктуры.
]

Формула Эйлера связывает число вершин, рёбер и граней любого планарного графа простым соотношением, из которого следуют все основные ограничения на планарность.

#theorem[Формула Эйлера#footnote[Euler L. "Elementa doctrinae solidorum" (Элементы учения о телах), 1758. Доказательство для многогранников; позже обобщено на планарные графы.]][
  Для связного планарного графа: $V - E + F = 2$, где $F$ --- число граней (включая внешнюю).
]

#proof-sketch[
  Индукция по числу рёбер $E$.

  *База:* $E = 0$.
  Связный граф с $V$ вершинами и без рёбер --- это изолированная вершина ($V = 1$).
  Тогда $F = 1$ (только внешняя грань). $1 - 0 + 1 = 2$.

  *База для дерева:* Если граф --- дерево, то $E = V - 1$ и $F = 1$ (дерево не разделяет плоскость).
  Тогда $V - (V-1) + 1 = 2$.

  *Шаг:* Пусть граф не является деревом, то есть содержит цикл.
  Удалим одно ребро из цикла.
  Число вершин не изменилось, число рёбер уменьшилось на 1, а две грани, разделённые удалённым ребром, слились в одну --- число граней также уменьшилось на 1.
  Значение $V - E + F$ не изменилось.
  Продолжаем, пока граф не станет деревом, для которого формула уже проверена.
]

Та же формула связывает вершины, рёбра и грани любого выпуклого многогранника.
Для куба: $8 - 12 + 6 = 2$.
Для тетраэдра: $4 - 6 + 4 = 2$.
Для додекаэдра: $20 - 30 + 12 = 2$.
Эйлер опубликовал эту формулу в 1758 году, ещё до того, как понятие графа было формализовано, --- он мыслил в терминах многогранников.
Одно уравнение для многогранников и плоских графов --- следствие топологической эквивалентности: сфера, в которую можно "раздуть" любой выпуклый многогранник, и плоскость, на которую проецируется планарный граф, гомеоморфны.

#corollary[Ограничение на число рёбер][
  Для простого планарного графа с $V >= 3$: $E <= 3V - 6$.
  Следствие: любой простой планарный граф имеет вершину степени $<= 5$.
]

#proof[
  Пусть $G$ --- простой планарный граф с $V >= 3$.
  Каждая грань ограничена не менее чем тремя рёбрами (в простом графе нет петель и кратных рёбер, поэтому грань не может быть ограничена одним или двумя рёбрами).
  При подсчёте суммы длин границ всех граней каждое ребро учитывается не более двух раз (оно разделяет не более двух граней).
  Следовательно, $3F <= 2E$, откуда $F <= 2E \/ 3$.

  Подставляем в формулу Эйлера $V - E + F = 2$:
  $V - E + 2E \/ 3 >= 2$, откуда $V - E \/ 3 >= 2$, и $E <= 3V - 6$.

  Для следствия: сумма степеней вершин равна $2E <= 6V - 12$.
  Если бы все вершины имели степень $>= 6$, сумма была бы $>= 6V > 6V - 12$ --- противоречие.
  Значит, существует вершина степени $<= 5$.
]

Эти оценки --- необходимые условия планарности:
- $K_5$ нарушает $E <= 3V - 6$ ($E=10$, $3 dot 5 - 6 = 9$) и потому не планарен.
- $K_(3,3)$ этому неравенству удовлетворяет, но не планарен по другой причине: в графе без треугольников $E <= 2V - 4$, а $K_(3,3)$ имеет $E=9 > 2 dot 6 - 4 = 8$.
Это уже не следствие формулы Эйлера, а более тонкое необходимое условие, мотивирующее теорему Куратовского.

#figure(
  k5,
  caption: [Полный граф $K_5$.],
) <fig:k5>

#figure(
  k33,
  caption: [Полный двудольный граф $K_(3,3)$.],
) <fig:k33>

#figure(
  planar,
  caption: [Планарный граф.],
) <fig:planar>

Ограничения на число рёбер дают необходимые условия планарности.
Теорема Куратовского даёт необходимое и достаточное условие --- через запрещённые подструктуры.

#definition[Подразбиение графа][
  *Подразбиением* ребра ${u, v}$ называется замена его на путь $u - w - v$ добавлением новой вершины $w$ степени 2.
  Граф $H$ является *подразбиением* графа $G$, если $H$ можно получить из $G$ последовательным подразбиением рёбер (возможно, нулевым числом шагов).
  Отношение "$H$ --- подразбиение $G$" называют также *гомеоморфизмом* графов.
]

#example[Подразбиения][
  Разобьём одно ребро $K_5$: возьмём ребро ${1, 2}$ и заменим его на путь $1 - x - 2$, где $x$ --- новая вершина степени 2.
  Полученный граф с $6$ вершинами --- подразбиение $K_5$ и, по теореме Куратовского, не планарен.
  Аналогично, замена всех трёх рёбер треугольника внутри $K_(3,3)$ на пути с промежуточными вершинами даёт подразбиение $K_(3,3)$ --- тоже непланарный граф.
]

Интуитивно: если нельзя нарисовать $K_5$ или $K_(3,3)$ на плоскости, то добавить лишние вершины на рёбра этих графов и обойти запрет тоже не получится.

#theorem[Теорема Куратовского#footnote[Kuratowski K. "Sur le problème des courbes gauches en topologie", Fundamenta Mathematicae, 1930.]][
  Граф планарен тогда и только тогда, когда он не содержит подграфа, гомеоморфного $K_5$ или $K_(3,3)$ (то есть полученного из $K_5$ или $K_(3,3)$ подразбиением рёбер).
]

#proof-sketch[
  Доказательство опирается на характеризацию через 3-связные компоненты и лемму о существовании вершины, удаление которой сохраняет 3-связность.
  Полное доказательство см. в К. Куратовский, "Sur le problème des courbes gauches en topologie", Fundamenta Mathematicae, 1930.
]

== Раскраска графов

#definition[Вершинная раскраска][
  Раскраска вершин графа называется *правильной $k$-раскраской*, если смежные вершины получают разные цвета. *Хроматическим числом* графа $G$ называется минимальное $k$, для которого существует правильная $k$-раскраска, обозначается $chi(G)$.
]

#example[Хроматические числа][
  $chi(C_4) = 2$ (цикл чётной длины, двудолен).
  $chi(C_5) = 3$ (цикл нечётной длины, не двудолен).
  $chi(K_n) = n$ (в полном графе все вершины попарно смежны, каждой нужен свой цвет).
  $chi("дерева") = 2$ для любого дерева с $n >= 2$ (все деревья двудольны).
  Рисунок @fig:coloring показывает правильную раскраску графа.
]

Хроматическое число измеряет, насколько граф далёк от двудольного: $chi(G) = 2$ у двудольных графов (имеющих хотя бы одно ребро), $chi(K_n) = n$ у полных.
Простейшая верхняя оценка получается жадным алгоритмом: $chi(G) <= Delta(G) + 1$, где $Delta(G)$ --- максимальная степень.
Эта оценка достигается на полных графах и циклах нечётной длины, но для большинства графов жадный алгоритм может использовать меньше цветов --- точное значение найти NP-трудно.

Общая оценка через максимальную степень оставляет значительный зазор.
Для планарных графов верно более точное утверждение.

#theorem[Теорема о четырёх красках][
  Любой планарный граф 4-раскрашиваем.
  Доказательство (Аппель и Хакен, 1976) --- первое крупное математическое доказательство с применением компьютера: 1936 конфигураций проверены программно; в 2005 году Гонтье формализовал доказательство в Coq.
]

Теорема о четырёх красках --- первая крупная теорема, доказанная с использованием компьютера.
Аппель и Хакен (1976) свели задачу к проверке 1936 конфигураций, каждая из которых была проанализирована программой.
Споры вызвал философский вопрос о том, считать ли проверку человеком необходимой частью математической истины.
Консенсуса нет до сих пор, но компьютерные доказательства стали привычным инструментом --- правда, не без сопротивления.
В 2005 году Гонтье формализовал доказательство в системе Coq, устранив зависимость от конкретной программы, но не от вычислительной природы аргумента.

Теорема о четырёх красках относится к раскраске вершин.
Аналог для рёберной раскраски даёт теорема Визинга.

#theorem[Теорема Визинга#footnote[Vizing V. G. "On an estimate of the chromatic class of a p-graph", Diskretnyi Analiz, 1964 (на русском: Визинг В. Г. "Об оценке хроматического класса p-графа").]][
  Для рёберной раскраски: $Delta(G) <= chi'(G) <= Delta(G) + 1$.
]

#proof-sketch[
  Нижняя оценка тривиальна: рёбра, инцидентные вершине максимальной степени, требуют различных цветов.

  Для верхней: индукция по числу рёбер.
  Удаляем ребро $e = {u, v}$, раскрашиваем оставшийся граф $Delta(G) + 1$ цветами.
  Если у $u$ и $v$ имеется общий свободный цвет --- красим $e$ в него.
  Иначе строим чередующийся путь из цветов, отсутствующих у $u$ и $v$, и перекрашиваем рёбра вдоль этого пути, освобождая общий цвет для $e$.
]

#figure(
  graph-coloring,
  caption: [Раскраска графа.],
) <fig:coloring>

Раскраска графа решает задачу распределения регистров в компиляторе.
Переменные, используемые одновременно, не могут занимать один регистр --- конфликт.
Граф интерференции регистров: вершины --- переменные программы; ребро между $u$ и $v$, если $u$ и $v$ "живы" в одной точке программы (их время жизни пересекается).

Задача: назначить $k$ доступных регистров вершинам графа так, чтобы смежные вершины получили разные регистры.
Это в точности задача $k$-раскраски --- NP-трудная в общем случае.
На практике компиляторы используют эвристики: алгоритм Чайтина (1982) итеративно удаляет вершины со степенью $< k$, складывая их в стек; когда удалять больше нечего, раскрашивает в обратном порядке; если на каком-то шаге все оставшиеся вершины имеют степень $>= k$, одна из них "вытесняется" в память (spill) --- для неё резервируется ячейка стека вместо регистра, и процесс повторяется заново.

Та же модель --- назначение радиочастот: вершины --- передатчики, ребро --- недопустимые помехи (географически близкие или работающие на соседних частотах), цвета --- доступные частотные каналы.
Та же задача возникает при составлении расписания экзаменов: вершины --- экзамены, ребро --- наличие общих студентов, цвета --- временные слоты.#footnote[Chaitin G. et al. "Register allocation via coloring", Computer Languages, 1981.
  Chaitin G.
  "Register allocation & spilling via graph coloring", SIGPLAN Notices, 1982.
]

== Алгоритмы на графах

Графовые алгоритмы --- это рабочие инструменты computer science.
В этом разделе мы разберём основные алгоритмы как конструкции: конкретный граф, пошаговая трассировка, объяснение корректности.

=== Поиск в ширину (BFS)

BFS (Breadth-First Search) обходит граф "по слоям": сначала вершины на расстоянии 1 от источника, затем на расстоянии 2, и так далее.
Он находит кратчайшие пути в невзвешенном графе --- графе без весов на рёбрах или, что эквивалентно, с единичными весами.

Алгоритм использует очередь: на каждом шаге извлекаем вершину из головы очереди и добавляем в хвост всех её непосещённых соседей, помечая их как посещённые.
Порядок извлечения гарантирует обход по слоям: сначала все вершины слоя $k$, затем все вершины слоя $k+1$.

#algorithm[Поиск в ширину][
  + Инициализировать очередь $Q$ с начальной вершиной $s$. Пометить $s$ как посещённую.
  + Пока $Q$ не пуста:
    - Извлечь вершину $u$ из головы $Q$.
    - Для каждого соседа $v$ вершины $u$: если $v$ не посещена, пометить $v$ как посещённую, установить $"parent"(v) = u$ и добавить $v$ в хвост $Q$.
]

Родительские ссылки, записанные во время обхода, образуют _BFS-дерево_ --- дерево кратчайших путей от $s$ до всех достижимых вершин.

#example[Трассировка BFS][
  Рассмотрим граф:
  #cetz.canvas({
    import cetz.draw: *

    let n-fill = oklch(88%, 0.03, 250deg)
    let n-border = oklch(60%, 0.08, 250deg)
    let n-text = oklch(25%, 0.02, 260deg)
    let edge = oklch(35%, 0.02, 265deg)

    let dx = 1.5
    let dy = 1.5

    set-style(fill: n-fill, stroke: 0.8pt + n-border, radius: 2pt)
    for (i, (x, y)) in (
      (0, 0),
      (1, 0),
      (2, 0),
      (0, -1),
      (1, -1),
      (2, -1),
    ).enumerate() {
      circle((x * dx, y * dy), radius: 0.35, anchor: "center")
      content((x * dx, y * dy), text(size: 9pt, n-text, str(i + 1)))
    }

    set-style(stroke: 0.6pt + edge)
    // Рёбра
    line((0, 0), (dx, 0))
    line((dx, 0), (2 * dx, 0))
    line((0, -dy), (dx, -dy))
    line((dx, -dy), (2 * dx, -dy))
    line((0, 0), (0, -dy))
    line((dx, 0), (dx, -dy))
    line((2 * dx, 0), (2 * dx, -dy))
  })

  Старт из вершины 1. Очередь: $[1]$.
  - Извлекаем 1. Соседи: ${2, 4}$. Оба не посещены. $Q = [2, 4]$, $"parent"(2) = 1$, $"parent"(4) = 1$.
  - Извлекаем 2. Соседи: ${1, 3, 5}$. 1 посещена, 3 и 5 --- нет. $Q = [4, 3, 5]$, $"parent"(3) = 2$, $"parent"(5) = 2$.
  - Извлекаем 4. Соседи: ${1, 5}$. 1 посещена, 5 уже в очереди --- пропускаем.
  - Извлекаем 3. Соседи: ${2, 6}$. 2 посещена, 6 --- нет. $Q = [5, 6]$, $"parent"(6) = 3$.
  - Извлекаем 5. Соседи: ${2, 4, 6}$. 2 и 4 посещены, 6 уже в очереди.
  - Извлекаем 6. Все соседи посещены. $Q$ пуста --- конец.

  Кратчайшие расстояния: $d(1, 1) = 0$, $d(1, 2) = d(1, 4) = 1$, $d(1, 3) = d(1, 5) = 2$, $d(1, 6) = 3$.
  Родительские ссылки дают кратчайшие пути: $1 -> 2 -> 3 -> 6$.
]

BFS работает за $O(V + E)$: каждая вершина попадает в очередь ровно один раз, каждое ребро просматривается не более двух раз (по разу с каждой стороны).

#figure(
  bfs-tree,
  caption: [BFS-дерево.],
) <fig:bfs-tree>

=== Поиск в глубину (DFS)

DFS (Depth-First Search) идёт "вглубь": рекурсивно исследует первого непосещённого соседа, затем его непосещённого соседа, и так далее, пока не упрётся в вершину без непосещённых соседей --- и тогда возвращается (backtrack) к предыдущей развилке.

В отличие от BFS, который явно хранит очередь, DFS естественно выражается рекурсией.
Стек вызовов играет роль структуры данных: текущий путь от стартовой вершины хранится в стеке рекурсии.

#algorithm[Поиск в глубину][
  + Для каждой непосещённой вершины $u$: вызвать $"DFS"(u)$.
  + $"DFS"(u)$:
    - Пометить $u$ как посещённую. Записать $"pre"(u)$ (время входа).
    - Для каждого соседа $v$ вершины $u$: если $v$ не посещена, установить $"parent"(v) = u$ и вызвать $"DFS"(v)$.
    - Записать $"post"(u)$ (время выхода).
]

Времена $"pre"$ и $"post"$ --- это номера шагов, на которых вершина была впервые посещена и полностью обработана.
Они дают структурную информацию о графе: для дерева/прямого ребра $"pre"(u) < "pre"(v) < "post"(v) < "post"(u)$; для обратного ребра $"pre"(v) < "pre"(u) < "post"(u) < "post"(v)$ (ребро ведёт к предку); для перекрёстного ребра $"post"(v) < "pre"(u)$ (ребро ведёт в уже полностью обработанную ветвь).

DFS лежит в основе алгоритмов топологической сортировки (обратный postorder --- это топологический порядок вершин DAG), поиска компонент сильной связности (двухпроходный алгоритм Косарайю), обнаружения циклов (обратное ребро в DFS-дереве означает цикл) и поиска мостов (рёбер, удаление которых нарушает связность).

Сложность DFS --- $O(V + E)$: каждая вершина посещается один раз, каждое ребро просматривается дважды (по разу с каждого конца).

=== Алгоритм Дейкстры

BFS находит кратчайшие пути в невзвешенном графе.
В взвешенном графе расстояние --- сумма весов рёбер, а не их количество.
BFS не справляется: путь из двух рёбер может быть короче пути из одного, если веса разные.

Алгоритм Дейкстры обобщает BFS на графы с неотрицательными весами.
Вместо очереди используется очередь с приоритетом (min-heap): на каждом шаге извлекается вершина с минимальным известным расстоянием, и расстояния до её соседей обновляются (релаксируются).

#algorithm[Дейкстры][
  + Инициализировать расстояния: $"dist"(s) = 0$, $"dist"(v) = oo$ для $v eq.not s$. Добавить $(0, s)$ в min-heap.
  + Пока куча не пуста:
    - Извлечь $(d, u)$ --- вершину $u$ с минимальным расстоянием $d$.
    - Если $d > "dist"(u)$ --- пропустить (устаревшая запись).
    - Для каждого соседа $v$ вершины $u$ с весом ребра $w(u, v)$:
      если $"dist"(u) + w(u, v) < "dist"(v)$, то $"dist"(v) = "dist"(u) + w(u, v)$, $"parent"(v) = u$, добавить $("dist"(v), v)$ в кучу.
]

Почему Дейкстра корректен?
Когда вершина $u$ извлекается из кучи, $"dist"(u)$ уже является длиной кратчайшего пути до $u$.
Причина: все рёбра имеют неотрицательный вес, поэтому любой другой путь до $u$ проходит через вершину, всё ещё находящуюся в куче с расстоянием $>= "dist"(u)$ --- и добавление положительного веса не может уменьшить это расстояние.
Это индукция по порядку извлечения вершин из кучи.

Именно неотрицательность весов критична.
Если есть ребро отрицательного веса, Дейкстра может выдать неверный ответ: вершина, извлечённая раньше, могла бы быть достигнута короче через отрицательное ребро, проходящее через вершину, извлечённую позже.
Для графов с отрицательными весами (но без отрицательных циклов) служит алгоритм Беллмана--Форда.

#example[Трассировка Дейкстры][
  Граф: вершины ${A, B, C, D}$.
  Рёбра: $A-B$ (вес 2), $A-C$ (вес 5), $B-C$ (вес 1), $B-D$ (вес 4), $C-D$ (вес 1).

  Старт из $A$.
  - Куча: $[(0, A)]$. Извлекаем $A$. Релаксируем: $"dist"(B) = 2$, $"dist"(C) = 5$.
  - Куча: $[(2, B), (5, C)]$. Извлекаем $B$. Релаксируем: $"dist"(C) = min(5, 2+1) = 3$, $"dist"(D) = min(oo, 2+4) = 6$.
  - Куча: $[(3, C), (5, C_"old"), (6, D)]$. Извлекаем $C$ (3). $5 > 3$ --- пропускаем устаревшую запись $C$. Релаксируем: $"dist"(D) = min(6, 3+1) = 4$.
  - Куча: $[(4, D), (6, D_"old")]$. Извлекаем $D$ (4). $6 > 4$ --- пропускаем устаревшую.

  Кратчайшие расстояния от $A$: до $B$ --- 2, до $C$ --- 3, до $D$ --- 4.
  Кратчайший путь до $D$: $A -> B -> C -> D$ (суммарный вес $2 + 1 + 1 = 4$).
]

Сложность Дейкстры с бинарной кучей --- $O((V+E) log V)$: каждая вершина извлекается из кучи один раз ($O(V log V)$), каждое ребро может вызвать операцию обновления ключа в куче ($O(E log V)$).

=== Минимальное остовное дерево: Краскал и Прим

Задача: в взвешенном связном графе найти остовное дерево (подграф, включающий все вершины и являющийся деревом) минимального суммарного веса.
Это не то же самое, что кратчайшие пути: остовное дерево минимизирует сумму весов рёбер дерева, а не расстояния от источника.

*Алгоритм Краскала* сортирует все рёбра по весу и добавляет легчайшее ребро, не создающее цикла.
Принадлежность вершин одной компоненте связности проверяется системой непересекающихся множеств (union-find): операция $"find"(v)$ возвращает представителя компоненты, $"union"(u, v)$ объединяет компоненты.

#algorithm[Краскала][
  + Отсортировать все рёбра по возрастанию веса.
  + Инициализировать $V$ компонент связности (каждая вершина --- отдельная компонента).
  + Для каждого ребра $(u, v)$ в порядке возрастания веса:
    - Если $"find"(u) eq.not "find"(v)$ (вершины в разных компонентах) --- добавить ребро в остов и выполнить $"union"(u, v)$.
  + Остановиться, когда добавлено $V-1$ рёбер.
]

Почему Краскал корректен?
Кратчайшая версия: на каждом шаге алгоритм выбирает легчайшее ребро, не создающее цикла.
Если бы оптимальное остовное дерево не содержало этого ребра, добавление ребра к оптимальному дереву создало бы цикл, содержащий другое ребро с весом не меньше данного; замена дала бы остовное дерево не хуже.
Это классическое доказательство "жадность работает" через свойство замены.

*Алгоритм Прима* растёт из одной вершины: на каждом шаге добавляется легчайшее ребро, соединяющее уже построенное дерево с внешней вершиной.
По духу это Дейкстра на задаче MST: вместо расстояний от источника обновляются минимальные веса рёбер до строящегося дерева.

Оба алгоритма жадные и оба доказуемо оптимальны.
Краскал удобнее для разреженных графов ($E = O(V)$), Прим --- для плотных ($E = Theta(V^2)$).

=== Другие алгоритмы на графах

*Беллман--Форд* ($O(V E)$) решает задачу кратчайших путей с отрицательными весами (но без отрицательных циклов).
Он выполняет $V-1$ раундов релаксации _всех_ рёбер; после $k$ раундов кратчайшие пути, состоящие не более чем из $k$ рёбер, найдены корректно.

*Топологическая сортировка* DAG (алгоритм Кана) многократно удаляет вершину-исток (без входящих рёбер) и выводит её.
Эквивалентно: вершины выводятся в порядке убывания $"post"$ при обходе DFS (обратный postorder).

*Компоненты сильной связности* (Косарайю, Тарьян) раскладывают орграф на максимальные подграфы, в которых каждая вершина достижима из любой другой.
Алгоритм Косарайю выполняет два прохода DFS: первый собирает вершины в порядке выхода; второй запускается на обращённом графе в обратном порядке выхода первого прохода.

*Семейство алгоритмов SCC* раскладывает орграф на базовые строительные блоки; конденсация орграфа (граф компонент) всегда является DAG.

=== Сетевые потоки

Задача о максимальном потоке: дана сеть --- ориентированный граф с источником $s$ и стоком $t$, где каждое ребро имеет пропускную способность $c(u, v)$.
Требуется найти максимальный поток из $s$ в $t$ --- функцию $f: E -> RR$, такую что $0 <= f(u, v) <= c(u, v)$ и в каждой вершине (кроме $s$ и $t$) входящий поток равен исходящему (закон сохранения потока).

#definition[Сеть и поток][
  *Сетью* называется ориентированный граф $G = (V, E)$ с выделенными источником $s$ и стоком $t$, и функцией пропускных способностей $c: E -> RR^+$.
  *Потоком* называется функция $f: E -> RR$, удовлетворяющая:
  - Ограничению пропускной способности: $0 <= f(u, v) <= c(u, v)$.
  - Закону сохранения: $sum_(u) f(u, v) = sum_(w) f(v, w)$ для всех $v in V setminus {s, t}$.
  *Величина потока*: $|f| = sum_(v) f(s, v) - sum_(u) f(u, s)$ --- чистый поток, выходящий из источника.
]

#example[Сеть и поток][
  Сеть из четырёх вершин: источник $s$, сток $t$, промежуточные вершины $a$ и $b$.
  Рёбра и пропускные способности: $s -> a$ (5), $s -> b$ (3), $a -> t$ (3), $b -> t$ (4), $a -> b$ (2).

  Один из возможных потоков: $f(s, a) = 3$, $f(s, b) = 2$, $f(a, t) = 2$, $f(a, b) = 1$, $f(b, t) = 3$.
  Проверим закон сохранения потока:
  - В $a$: входящий $3$, исходящие $2 + 1 = 3$.
  - В $b$: входящие $2 + 1 = 3$, исходящий $3$.
  Величина потока: $|f| = f(s, a) + f(s, b) = 3 + 2 = 5$.

  Эта же сеть используется в трассировке алгоритма Форда--Фалкерсона ниже.
]

Алгоритм Форда--Фалкерсона (1956) ищет увеличивающий путь --- путь из $s$ в $t$ в _остаточной сети_, где каждое ребро имеет остаточную пропускную способность $c_f(u, v) = c(u, v) - f(u, v)$ (плюс обратные рёбра для возможности "отменить" часть потока).
Пока такой путь существует, вдоль него пропускается дополнительный поток, равный минимальной остаточной пропускной способности на пути.

#algorithm[Форда--Фалкерсона][
  + Начать с нулевого потока: $f(u, v) = 0$ для всех рёбер.
  + Пока существует увеличивающий путь $P$ из $s$ в $t$ в остаточной сети:
    - Найти минимальную остаточную пропускную способность $Delta = min_(e in P) c_f(e)$.
    - Пропустить $Delta$ единиц потока вдоль $P$: для каждого ребра $(u, v)$ на пути $f(u, v) += Delta$, для обратного ребра $f(v, u) -= Delta$.
  + Когда увеличивающих путей нет --- текущий поток максимален.
]

Время работы зависит от способа поиска увеличивающего пути.
С BFS (алгоритм Эдмондса--Карпа) --- $O(V E^2)$.
С более сложными структурами (динамические деревья) --- $O(V E log V)$.

#theorem[Max-flow min-cut#footnote[Ford L. R., Fulkerson D. R. "Maximal flow through a network", Canadian Journal of Mathematics, 1956. Также известна как теорема Форда--Фалкерсона.]][
  Величина максимального потока равна минимальной пропускной способности разреза.
  *Разрезом* $(S, T)$ называется разбиение вершин на два множества: $s in S$, $t in T$.
  Пропускная способность разреза: $c(S, T) = sum_(u in S, v in T) c(u, v)$.
]

#proof-sketch[
  Величина любого потока не превышает пропускной способности любого разреза: весь поток из $S$ в $T$ проходит через рёбра разреза, и по ограничению пропускной способности $f(u, v) <= c(u, v)$.
  Следовательно, максимальный поток $<=$ минимальный разрез.

  Алгоритм Форда--Фалкерсона останавливается, когда не существует увеличивающего пути.
  В этот момент множество $S$ всех вершин, достижимых из $s$ в остаточной сети, образует разрез.
  Все рёбра из $S$ в $T$ насыщены (поток равен пропускной способности), а все рёбра из $T$ в $S$ пусты (поток равен 0).
  Величина потока в точности равна пропускной способности этого разреза --- следовательно, поток максимален, а разрез минимален.
]

#example[Трассировка Форда--Фалкерсона][
  Сеть: вершины ${s, a, b, t}$.
  Рёбра с пропускными способностями: $s -> a$ (3), $s -> b$ (2), $a -> b$ (1), $a -> t$ (2), $b -> t$ (3).

  *Итерация 1.* Остаточные способности равны исходным. Увеличивающий путь: $s -> a -> t$.
  Минимальная остаточная пропускная способность: $Delta = min(3, 2) = 2$.
  Поток: $f(s, a) = 2$, $f(a, t) = 2$. Остаточные: $c_f(s, a) = 1$, $c_f(a, s) = 2$, $c_f(a, t) = 0$, $c_f(t, a) = 2$.

  *Итерация 2.* Путь $s -> b -> t$: $Delta = min(2, 3) = 2$.
  Поток: $f(s, b) = 2$, $f(b, t) = 2$. Остаточные: $c_f(s, b) = 0$, $c_f(b, s) = 2$, $c_f(b, t) = 1$, $c_f(t, b) = 2$.

  *Итерация 3.* Путь $s -> a -> b -> t$: используем обратное ребро $a -> b$ (остаточная способность 1).
  $Delta = min(1, 1, 1) = 1$.
  Поток: $f(s, a) = 3$, $f(a, b) = 1$, $f(b, t) = 3$.

  Итоговый поток: $|f| = f(s, a) + f(s, b) = 3 + 2 = 5$.
  Минимальный разрез: $S = {s, a}$, $T = {b, t}$ с пропускной способностью $c(s, b) + c(a, t) + c(a, b) = 2 + 2 + 1 = 5$. Совпадает с величиной потока.
]

Интуиция: поток из $s$ в $t$ ограничен "бутылочным горлышком" --- минимальным разрезом.
Никакой поток не может превысить пропускную способность любого разреза (весь поток из $S$ в $T$ проходит через рёбра разреза), и всегда существует поток, достигающий этого максимума.

Применения --- от транспортной логистики (максимальный грузопоток через сеть дорог) до двудольных паросочетаний (сведение задачи о максимальном паросочетании к максимальному потоку добавлением источника, соединённого с левой долей, и стока, соединённого с правой).

=== Паросочетания в двудольных графах

Теорема Холла (раздел о двудольных графах) даёт необходимое и достаточное условие существования паросочетания, покрывающего долю $X$.
Алгоритмически задача о максимальном паросочетании в двудольном графе решается сведением к максимальному потоку: соединяем источник $s$ с каждой вершиной левой доли ребром пропускной способности 1; каждую вершину правой доли --- со стоком $t$ ребром пропускной способности 1; каждое ребро исходного графа направляем слева направо с пропускной способностью 1.
Максимальный поток в такой сети даёт максимальное паросочетание.

#algorithm[Куна (венгерский алгоритм для двудольных графов)][
  + Для каждой вершины $u$ левой доли:
    - Пытаемся найти увеличивающий чередующийся путь, начинающийся в $u$, с помощью DFS (или BFS).
    - Чередующийся путь --- это путь, в котором рёбра попеременно не принадлежат и принадлежат текущему паросочетанию.
    - Если путь найден --- инвертируем принадлежность рёбер на этом пути (не-паросочетанные становятся паросочетанными и наоборот). Размер паросочетания увеличивается на 1.
  + Повторяем для всех вершин левой доли.
]

Алгоритм Куна работает за $O(V E)$ --- для каждой из $|X|$ вершин запускается DFS, просматривающий все рёбра.
Этого достаточно для большинства практических задач при умеренных размерах графа.

== Применения графов

Графы --- рабочий инструмент дискретной математики и computer science.
Вот несколько приложений.

=== Социальные сети

Социальный граф: вершины --- люди, рёбра --- дружеские связи, подписки или взаимодействия.

Центральность измеряет "влиятельность" вершины.
Степень центральности (degree centrality) --- количество связей: сколько людей знает данный человек.
Промежуточность (betweenness centrality) вычисляется так: для каждой пары вершин $(s, t)$ находятся все кратчайшие пути между ними; центральность вершины $v$ --- доля этих путей, проходящих через $v$.
Вершина с высокой промежуточностью --- "мост" между сообществами, контролирующий поток информации.

Выделение сообществ (community detection): задача разбить граф на плотные подграфы с редкими связями между ними.
Алгоритм Гирван--Ньюмана (2002) итеративно удаляет рёбра с наибольшей промежуточностью, постепенно разбивая граф на компоненты-сообщества --- каждое удаление увеличивает число компонент связности, и на каждом шаге пересчитывается промежуточность оставшихся рёбер.
Альтернативный подход --- максимизация модулярности (modularity): меры того, насколько плотность рёбер внутри предполагаемых сообществ превышает ожидаемую плотность в случайном графе с теми же степенями вершин.#footnote[Girvan M., Newman M.
  "Community structure in social and biological networks", PNAS, 2002.
  Newman M.
  "Networks: An Introduction", Oxford, 2010.
]

=== Маршрутизация в сетях

Интернет: маршрутизаторы --- вершины, каналы связи --- рёбра с весами (задержка или стоимость соединения).

Протокол OSPF (Open Shortest Path First) решает задачу внутридоменной маршрутизации: каждый маршрутизатор вычисляет кратчайшие пути до всех остальных в своей автономной системе.
Механизм: каждый маршрутизатор хранит полную карту сети (link-state database) --- взвешенный граф своей области.
При изменении топологии (отказ канала, добавление маршрутизатора) обновлённая информация рассылается лавинно (flooding) всем участникам.
Затем каждый маршрутизатор независимо запускает алгоритм Дейкстры: поддерживается множество посещённых вершин и min-heap граничных расстояний; на каждом шаге извлекается ближайшая непосещённая вершина, и расстояния до её соседей обновляются, если найден более короткий путь.
Результат --- дерево кратчайших путей от данного маршрутизатора до всех остальных; пакеты направляются по следующему шагу этого дерева.

Междоменная маршрутизация (BGP) принципиально иная: автономные системы обмениваются информацией о доступных префиксах с учётом политик (какие пути предпочтительны по коммерческим соглашениям), а не только расстояний.
BGP образует граф автономных систем, где поиск пути может быть осложнён противоречивыми политиками.#footnote[Moy J.
  "OSPF Version 2", RFC 2328, 1998.
  Dijkstra E.W.
  "A note on two problems in connexion with graphs", Numerische Mathematik, 1959.
]

=== Разработка компиляторов

Компилятор использует несколько графовых представлений программы на разных этапах трансляции.

Граф потока управления (CFG): вершины --- базовые блоки (последовательности инструкций без ветвлений), рёбра --- возможные переходы (условные и безусловные ветвления, рёбра циклов).
CFG строится на этапе промежуточного представления (IR) и служит основой для оптимизаций: удаление недостижимого кода, вынесение инвариантных вычислений из циклов, анализ доминирования (вершина $u$ доминирует над $v$, если любой путь от входа к $v$ проходит через $u$; дерево доминаторов строится за почти линейное время алгоритмом Ленгауэра--Тарьяна).

Граф вызовов (call graph): вершины --- функции, ребро от $f$ к $g$, если $f$ вызывает $g$.
Используется для обнаружения рекурсии, вычисления побочных эффектов и оптимизации встраиванием (inlining): если функция вызывается из единственного места и достаточно мала, её тело подставляется в точку вызова, экономя накладные расходы на вызов.

Распределение регистров сводится к раскраске графа интерференции: вершины --- переменные, "живые" одновременно; $k$ регистров --- $k$ цветов; NP-трудная задача решается эвристиками (алгоритм Чайтина с вытеснением, описанный в разделе о раскраске графов).#footnote[Aho A., Lam M., Sethi R., Ullman J.
  "Compilers: Principles, Techniques, and Tools", 2nd ed., 2006.
]

=== Model checking

Model checking --- метод автоматической верификации конечных систем: программа или аппаратный модуль моделируется как размеченная система переходов (граф, где вершины --- состояния, рёбра --- переходы, а вершины и рёбра размечены утверждениями о значениях переменных).

Верифицируемое свойство записывается формулой темпоральной логики (LTL или CTL).
Например, свойство "запрос рано или поздно получает ответ" --- формула $square ("request" -> diamond "response")$, означающая: "всегда, если пришёл запрос, то когда-нибудь в будущем последует ответ".

Проверка сводится к поиску на графе пространства состояний:
- Инварианты (свойства вида "всегда $P$") проверяются обходом всех достижимых состояний --- если достигнуто состояние с $not P$, свойство нарушено.
- Свойства живости (liveness, "когда-нибудь $P$") проверяются поиском цикла, в котором $P$ никогда не выполняется --- такой цикл называется контрпримером (counterexample) и предъявляется пользователю как диагностика.
- Для CTL-формул строится рекурсивный обход графа с пометкой состояний, в которых выполняется подформула, от атомарных утверждений к составным.

Практическая трудность --- взрыв пространства состояний: $n$ булевых переменных дают $2^n$ состояний.
Современные model checker'ы (NuSMV, SPIN) справляются с $10^(20)$ и более состояниями благодаря символьной верификации: множества состояний представляются не перечислением, а булевыми формулами в виде BDD (Binary Decision Diagrams) --- компактных ациклических графов, кодирующих булевы функции; операции над множествами (объединение, пересечение, проверка пустоты) выполняются как операции над BDD.#footnote[Clarke E., Grumberg O., Peled D.
  "Model Checking", MIT Press, 1999.
  Baier C., Katoen J.-P.
  "Principles of Model Checking", MIT Press, 2008.
]

=== PageRank и поисковые системы

Всемирная паутина --- ориентированный граф: страницы --- вершины, гиперссылки --- дуги.

Алгоритм PageRank (Брин и Пейдж, 1998) моделирует поведение случайного пользователя: с вероятностью $d$ (обычно $0.85$) он переходит по случайной исходящей ссылке текущей страницы, а с вероятностью $1-d$ --- "телепортируется" на случайную страницу из всей сети (фактор телепортации предотвращает застревание в страницах без исходящих ссылок).

Формально, вектор PageRank $r$ удовлетворяет системе линейных уравнений:
$
  r(i) = (1 - d)/N + d dot sum_(j -> i) r(j) / "out"(j),
$
где $"out"(j)$ --- количество исходящих ссылок со страницы $j$, а $N$ --- общее число страниц.
Это уравнение баланса марковской цепи: стационарное распределение случайного блуждания.

Вычисление --- итерационный процесс (метод степеней, power iteration).
Начальное приближение: $r_0(i) = 1/N$.
На каждом шаге $t$ применяется переход по формуле выше, порождая $r_(t+1)$.
Итерации продолжаются, пока изменение $norm(r_(t+1) - r_t)$ не станет меньше заданного порога.
Метод сходится к единственному решению, потому что фактор телепортации делает марковскую цепь эргодичной (из любого состояния достижимо любое).

На графе из миллиардов вершин это соответствует распределённому умножению разреженной матрицы на вектор --- основная вычислительная нагрузка поисковых систем.#footnote[Brin S., Page L.
  "The anatomy of a large-scale hypertextual Web search engine", Computer Networks, 1998.
]


== Упражнения

+ Сколько рёбер в полном графе $K_n$? А в полном двудольном $K_{m,n}$?

+ Нарисуйте все неизоморфные деревья с 5 вершинами. Сколько их?

+ Примените BFS к графу: вершины ${a, b, c, d, e}$, рёбра $a b$, $a c$, $b d$, $b e$, $c d$. Начните с $a$.

+ Докажите, что дерево с $n$ вершинами имеет ровно $n-1$ рёбер.

+ (Продвинутое) Проверьте, является ли граф Петерсена гамильтоновым. Если да --- укажите цикл.



#pagebreak(weak: true)
#align(center)[
  #set text(0.9em)
  #line(length: 30%, stroke: 1pt + luma(80%))
  #v(0.6em)
  #text(weight: "bold")[Что дальше?]
  #v(0.4em)
  От графов --- структур связей --- мы переходим к алгебре нулей и единиц.
  В главе @chap:boolean мы изучим булевы функции, критерий функциональной полноты Поста, нормальные формы и методы минимизации --- математический фундамент цифровой схемотехники.
  #v(0.4em)
  #line(length: 30%, stroke: 1pt + luma(80%))
]
