// Решения к мета-разделу 05: Коды и SAT (m10-m11).
#import "macros.typ": pb-solution
#import "../notation.typ": *

#pb-solution("code:hamming-distance")[
  + $1100101$ и $1010110$: различаются позиции 2, 3, 6, 7 --- расстояние 4;
  + $0001111$ и $1110000$: все 7 позиций различны --- расстояние 7;
  + $1010101$ и $0101010$: все 7 позиций различны --- расстояние 7.
]

#pb-solution("code:code-distance")[
  Попарные расстояния:
  + $00000$-$00111$: 3; $00000$-$11001$: 3; $00000$-$11110$: 4;
  + $00111$-$11001$: 4; $00111$-$11110$: 3;
  + $11001$-$11110$: 3.

  Минимум --- 3, значит $d = 3$.
  Код обнаруживает до $d - 1 = 2$ ошибок и исправляет до $floor((d-1)\/2) = 1$ ошибки.
]

#pb-solution("code:hamming-encode")[
  Данные: $d_1 = 1$, $d_2 = 1$, $d_3 = 0$, $d_4 = 1$.
  Проверочные биты:
  $p_1 = d_1 xor d_2 xor d_4 = 1 xor 1 xor 1 = 1$;
  $p_2 = d_1 xor d_3 xor d_4 = 1 xor 0 xor 1 = 0$;
  $p_3 = d_2 xor d_3 xor d_4 = 1 xor 0 xor 1 = 0$.

  Кодовое слово $(p_1, p_2, d_1, p_3, d_2, d_3, d_4) = (1, 0, 1, 0, 1, 0, 1)$, то есть $1010101$.
]

#pb-solution("code:syndrome")[
  Принято $r = (0, 1, 1, 1, 0, 0, 1)$: $p_1 = 0$, $p_2 = 1$, $d_1 = 1$, $p_3 = 1$, $d_2 = 0$, $d_3 = 0$, $d_4 = 1$.
  Синдром:
  $s_1 = p_1 xor d_1 xor d_2 xor d_4 = 0 xor 1 xor 0 xor 1 = 0$;
  $s_2 = p_2 xor d_1 xor d_3 xor d_4 = 1 xor 1 xor 0 xor 1 = 1$;
  $s_3 = p_3 xor d_2 xor d_3 xor d_4 = 1 xor 0 xor 0 xor 1 = 0$.

  Синдром $(s_1, s_2, s_3) = (0, 1, 0)$ --- двоичная запись 2, ошибка в позиции 2.
  Переворачиваем бит 2: $(0, 0, 1, 1, 0, 0, 1)$.
  Информационные биты (позиции 3, 5, 6, 7): $d = (1, 0, 0, 1)$.
]

#pb-solution("code:bsc-capacity")[
  При $p = 0.5$: $H(0.5) = 1$, значит $C = 1 - 1 = 0$.
  Канал симметрично перемешивает биты: принятый бит не зависит от переданного, информации не передаётся.

  При $p = 1$: $H(1) = 0$, значит $C = 1 - 0 = 1$.
  Бит всегда переворачивается --- детерминированно, поэтому получатель восстанавливает передачу обратным переворотом, и канал фактически безошибочен.
]

#pb-solution("code:huffman")[
  Строим дерево, объединяя две наименьшие частоты:
  + $E$ (0.05) и $D$ (0.10) -> $0.15$;
  + $0.15$ и $C$ (0.20) -> $0.35$;
  + $B$ (0.25) и $0.35$ -> $0.60$;
  + $A$ (0.40) и $0.60$ -> $1.00$.

  Кодовые слова (одна из возможных расстановок): $A$: $0$, $B$: $10$, $C$: $110$, $D$: $1110$, $E$: $1111$.

  Средняя длина: $0.40 dot 1 + 0.25 dot 2 + 0.20 dot 3 + 0.10 dot 4 + 0.05 dot 4 = 0.40 + 0.50 + 0.60 + 0.40 + 0.20 = 2.10$.
  Равномерный код на 5 символов требует 3 бита на символ.
  Код Хаффмана короче: 2.10 вместо 3.
]

#pb-solution("code:perfect-hamming")[
  Шар радиуса 1 в ${0, 1}^7$ состоит из кодового слова и 7 его соседей на расстоянии 1 --- объём $1 + 7 = 8$.
  Кодовых слов у $(7, 4)$ --- $2^4 = 16$, и $16 dot 8 = 128 = 2^7$.
  Шары не пересекаются (расстояние 3) и полностью заполняют всё пространство --- код совершенный.

  Совершенные двоичные коды, исправляющие одну ошибку, --- коды Хэмминга с параметрами
  $n = 2^r - 1$, $k = n - r$, $d = 3$ (например, $r = 3$ даёт $(7, 4)$).
]

#pb-solution("code:linear-check")[
  Проверяем замкнутость относительно XOR:
  $0101 xor 1010 = 1111 in C$;
  $0101 xor 1111 = 1010 in C$;
  $1010 xor 1111 = 0101 in C$; нулевое слово в $C$.
  Множество замкнуто --- код линейный, размерность $k = "log"_2 4 = 2$.

  Базис ${0101, 1010}$; в систематическом виде $G = mat(1, 0, 1, 0; 0, 1, 0, 1) = [I_2 | P]$, где $P = mat(1, 0; 0, 1)$.
  Проверочная матрица $H = [P^top | I_2] = mat(1, 0, 1, 0; 0, 1, 0, 1)$ --- совпадает с $G$ (код самодуальный).

  Кодовое расстояние: минимальный вес ненулевого слова --- $d = 2$ (у $0101$ и $1010$ вес 2).
]

#pb-solution("code:generator-matrix")[
  Кодовые слова --- все линейные комбинации строк $G = mat(1, 1, 0, 1; 0, 1, 1, 0)$:
  $0000$, $1101$, $0110$, $1101 xor 0110 = 1011$.

  Приводим к систематическому виду: $G' = mat(1, 0, 1, 1; 0, 1, 1, 0) = [I_2 | P]$, $P = mat(1, 1; 1, 0)$.
  Проверочная матрица $H = [P^top | I_2] = mat(1, 1, 1, 0; 1, 0, 0, 1)$.

  Кодовое расстояние: минимальный вес ненулевого слова --- $d = 2$ (у слова $0110$ вес 2).
]

#pb-solution("code:cyclic")[
  Умножаем: $(1 + x + x^3)(1 + x + x^2 + x^4) = x^7 + 1$ (в $"GF"(2)$ удвоенные члены обнуляются).
  Значит, $g(x)$ делит $x^7 - 1$.

  Кодовое слово: $c(x) = m(x) g(x) = (1 + x)(1 + x + x^3) = 1 + x^2 + x^3 + x^4$.
  Коэффициенты от старшей степени: $(x^6, ..., x^0) = (0, 0, 1, 1, 1, 0, 1)$ --- кодовое слово $0011101$.
]

#pb-solution("code:rs-singleton")[
  Для $"RS"(7, 3, 5)_8$: $n = 7$, $k = 3$, $d = 5$.
  Граница Синглтона $d <= n - k + 1 = 5$ достигается --- $d = 5$.

  Код исправляет $t = floor((d-1)\/2) = 2$ символьные ошибки.
  Если ошибки затрагивают отдельные биты внутри разных символов, число исправляемых бит зависит от распределения: символ в $"GF"(8)$ --- это 3 бита, и пока поражено не более двух символов, код справляется, даже если внутри каждого бит перевёрнут не один.
  Три символа с ошибками в разных битах код уже не исправит.
]

#pb-solution("code:hamming-bound")[
  Шары радиуса $t = 2$ вокруг кодовых слов не пересекаются.
  Объём одного шара: $1 + n + binom(n, 2) = 1 + 10 + 45 = 56$.
  Граница: $|C| dot 56 <= 2^10 = 1024$, откуда $|C| <= 1024\/56 approx 18.28$.

  Максимум --- $18$ кодовых слов.
]

#pb-solution("code:capacity-053")[
  $C = 1 - H(p) = 0.53$, значит $H(p) = 0.47$.
  Подбором: при $p = 0.10$, $H(0.10) = -0.10 "log"_2 0.10 - 0.90 "log"_2 0.90 approx 0.469 approx 0.47$.
  Значит, $p approx 0.10$.

  Теорема Шеннона гарантирует надёжную связь при любой скорости $R < C = 0.53$, например при $R = 0.5$ (половина бита данных на переданный бит).
]

#pb-solution("code:no-code-5-4")[
  Сложение с фиксированным словом (XOR) --- биекция на ${0, 1}^5$, сохраняющая расстояния Хэмминга.
  Поэтому можно считать, что одно кодовое слово --- $00000$.
  Остальные три слова должны быть на расстоянии $>= 4$ от $00000$, то есть иметь вес 4 или 5.

  Слов веса 5 ровно одно --- $11111$; слов веса 4 --- пять (в каждой ровно один нуль).
  Расстояние между двумя словами веса 4 равно 2 (они различаются в позициях своих нулей), а расстояние от $11111$ до слова веса 4 равно 1.
  Значит, среди оставшихся слов нет даже двух на расстоянии $>= 4$.

  Противоречие: код с $d = 4$ на 5 позициях не может содержать 4 слова.
]

#pb-solution("code:gauss-binomial")[
  Число двумерных подпространств в $"GF"(2)^4$:
  $"Gauss"(4, 2, 2) = [(2^4 - 1)(2^4 - 2)]\/[(2^2 - 1)(2^2 - 2)] = (15 dot 14)\/(3 dot 2) = 210\/6 = 35$.

  Перечень удобно получить по единственному приведённому ступенчатому виду базиса:
  + $mat(1, 0, a, b; 0, 1, c, d)$ --- 16 подпространств;
  + $mat(1, a, 0, b; 0, 0, 1, c)$ --- 8;
  + $mat(1, a, b, 0; 0, 0, 0, 1)$ --- 4;
  + $mat(0, 1, 0, a; 0, 0, 1, b)$ --- 4;
  + $mat(0, 1, a, 0; 0, 0, 0, 1)$ --- 2;
  + $mat(0, 0, 1, 0; 0, 0, 0, 1)$ --- 1.

  Итого $16 + 8 + 4 + 4 + 2 + 1 = 35$ двумерных подпространств.
]

#pb-solution("code:gf16")[
  В поле $"GF"(2)[x] \/ (x^4 + x + 1)$ выполнено $alpha^4 = alpha + 1$ (так как $x^4 equiv x + 1$ по модулю $x^4 + x + 1$).
  Возводим степени:
  $alpha^5 = alpha dot alpha^4 = alpha (alpha + 1) = alpha^2 + alpha$;
  $alpha^(10) = (alpha^5)^2 = (alpha^2 + alpha)^2 = alpha^4 + alpha^2 = (alpha + 1) + alpha^2 = alpha^2 + alpha + 1$;
  $alpha^(15) = alpha^(10) dot alpha^5 = (alpha^2 + alpha + 1)(alpha^2 + alpha) = alpha^4 + alpha = (alpha + 1) + alpha = 1$.

  Так как $alpha^(15) = 1$ и никакая меньшая степень не даёт 1, элемент $alpha$ примитивен (порядок 15), и поле построено корректно.
]

#pb-solution("code:repetition-perfect")[
  Код-повторение исправляет $t = floor((n-1)\/2)$ ошибок.
  Граница Хэмминга превращается в равенство, когда шары радиуса $t$ вокруг $00 dots 0$ и $11 dots 1$ покрывают всё пространство без наложения.

  Для нечётного $n = 2t + 1$ это верно: каждое слово ближе к одному из двух кодовых слов (правило большинства), и
  $2 sum_(i=0)^t binom(2t+1, i) = 2 dot 2^(2t) = 2^(n)$.
  Например, при $n = 5$: $2(1 + 5 + 10) = 32 = 2^5$.

  Для чётного $n$ равенства нет: слово с ровно $n\/2$ единицами находится на расстоянии $n\/2$ от обоих слов.
  Ответ: код-повторение совершенен тогда и только тогда, когда $n$ нечётно.
]

#pb-solution("code:rs-distance-proof")[
  Кодовое слово --- значения многочлена степени $< k$ в точках $alpha^0, alpha^1, ..., alpha^(n-1)$ (все ненулевые элементы поля).
  Два различных кодовых слова соответствуют двум многочленам $m_1, m_2$ степени $< k$.
  Их разность $m_1 - m_2$ --- ненулевой многочлен степени $< k$, поэтому имеет не более $k - 1$ корней.

  Значит, совпадают не более $k - 1$ позиций, а различаются не менее $n - (k - 1) = n - k + 1$ позиций.
  Расстояние $d >= n - k + 1$; вместе с границей Синглтона $d <= n - k + 1$ получаем равенство.
]

#pb-solution("sat:cnf-sat")[
  + $(x or y) and (not x or not y)$: выполнима, модель $x = 1, y = 0$.
  + $(x) and (not x)$: невыполнима --- требует $x = 1$ и $x = 0$ одновременно.
  + $(x or y or z) and (not x) and (not y) and (not z)$: невыполнима --- единичные дизъюнкты дают $x = y = z = 0$, но первый дизъюнкт при этом ложен.
  + $(x or y) and (not x or y) and (not y)$: невыполнима --- из $(not y)$ следует $y = 0$, тогда $(x or 0)(not x or 0) = x and not x = 0$.
]

#pb-solution("sat:implication-graph")[
  Дизъюнкт $(l_1 or l_2)$ даёт импликации $not l_1 -> l_2$ и $not l_2 -> l_1$:
  + $(x_1 or not x_2)$: $not x_1 -> not x_2$, $x_2 -> x_1$;
  + $(not x_1 or x_3)$: $x_1 -> x_3$, $not x_3 -> not x_1$;
  + $(not x_2 or not x_3)$: $x_2 -> not x_3$, $x_3 -> not x_2$.

  Вершин 6 ($x_1, not x_1, x_2, not x_2, x_3, not x_3$), рёбер 6.
  Формула выполнима (например, $x_1 = 0$, $x_2 = 0$, $x_3$ любое).
]

#pb-solution("sat:four-clause")[
  Дизъюнкты дают импликации:
  $(x or y)$: $not x -> y$, $not y -> x$;
  $(not x or y)$: $x -> y$, $not y -> not x$;
  $(x or not y)$: $not x -> not y$, $y -> x$;
  $(not x or not y)$: $x -> not y$, $y -> not x$.

  В графе $not x -> y -> x$ и $x -> not y -> not x$: все четыре литерала $x, not x, y, not y$ лежат в одной компоненте сильной связности.
  Переменная и её отрицание в одной КСС --- формула невыполнима.
  (И прямо: формула запрещает все четыре набора значений $(x, y)$.)
]

#pb-solution("sat:unsat-2-clauses")[
  Пример: $(x or x) and (not x or not x)$ --- формула требует $x = 1$ и $x = 0$.

  Импликации: $(x or x)$ даёт $not x -> x$; $(not x or not x)$ даёт $x -> not x$.
  В графе импликаций есть цикл $not x -> x -> not x$: $x$ и $not x$ в одной КСС.
  По критерию 2-SAT формула невыполнима.
]

#pb-solution("sat:solve-2sat")[
  Импликации:
  $(x_1 or x_2)$: $not x_1 -> x_2$, $not x_2 -> x_1$;
  $(not x_1 or x_3)$: $x_1 -> x_3$, $not x_3 -> not x_1$;
  $(not x_2 or not x_3)$: $x_2 -> not x_3$, $x_3 -> not x_2$;
  $(not x_2 or x_4)$: $x_2 -> x_4$, $not x_4 -> not x_2$;
  $(not x_4 or not x_3)$: $x_4 -> not x_3$, $x_3 -> not x_4$.

  Ни одна переменная не лежит в одной КСС со своим отрицанием --- формула выполнима.
  Конденсация --- DAG; назначение в обратном топологическом порядке даёт, например,
  $x_1 = 0$, $x_2 = 1$, $x_3 = 0$, $x_4 = 1$.

  Проверка: все пять дизъюнктов истинны.
]

#pb-solution("sat:3coloring-k3")[
  Три группы дизъюнктов:
  + каждая вершина имеет хотя бы один цвет: $(x_(v,1) or x_(v,2) or x_(v,3))$ для $v = 1, 2, 3$ --- 3 дизъюнкта;
  + вершина имеет не более одного цвета: $(not x_(v,c) or not x_(v,c'))$ для всех $v$ и пар $c < c'$ --- $3 dot 3 = 9$ дизъюнктов;
  + смежные вершины разных цветов: $(not x_(u,c) or not x_(v,c))$ для всех рёбер $u v$ и цветов $c$ --- $3 dot 3 = 9$ дизъюнктов.

  Итого $3 + 9 + 9 = 21$ дизъюнкт.
  Формула выполнима: $K_3$ раскрашивается тремя цветами (каждой вершине --- свой цвет).
  Это подтверждает, что хроматическое число $K_3$ равно 3.
]

#pb-solution("sat:dpll")[
  Решение $x_1 = 1$:
  unit propagation: из $(not x_1 or x_3)$ получаем $x_3 = 1$; из $(not x_2 or not x_3)$ --- $x_2 = 0$; дизъюнкты $(x_1 or x_2)$ и $(x_1 or not x_3)$ уже удовлетворены.

  Присваивание $x_1 = 1$, $x_2 = 0$, $x_3 = 1$ удовлетворяет все четыре дизъюнкта --- формула выполнима.
  Дерево поиска --- одна ветвь без возвратов: решение $x_1 = 1$ уже приводит к модели, второе решение $x_2 = 1$ не понадобилось.
]

#pb-solution("sat:exactly-two")[
  «Хотя бы две истинны»: в каждой тройке переменных не все ложны ---
  $(x_1 or x_2 or x_3)$, $(x_1 or x_2 or x_4)$, $(x_1 or x_3 or x_4)$, $(x_2 or x_3 or x_4)$ --- 4 дизъюнкта.
  «Не более двух»: в каждой тройке не все истинны ---
  $(not x_1 or not x_2 or not x_3)$, $(not x_1 or not x_2 or not x_4)$, $(not x_1 or not x_3 or not x_4)$, $(not x_2 or not x_3 or not x_4)$ --- 4 дизъюнкта.

  Конъюнкция обеих групп --- 8 дизъюнктов, без вспомогательных переменных.
]

#pb-solution("sat:vertex-cover")[
  Сведение 3-SAT к вершинному покрытию.
  Для каждой переменной $x_i$ --- пара вершин $x_i$, $not x_i$ с ребром между ними (покрыть можно только выбором одной из двух).
  Для каждого дизъюнкта $(l_1 or l_2 or l_3)$ --- треугольник из трёх вершин $l_1', l_2', l_3'$, и каждую вершину $l_j'$ соединяем ребром с вершиной-литералом $l_j$.

  Положим $k = m + 2c$, где $m$ --- число переменных, $c$ --- число дизъюнктов.
  В каждой переменной-паре покрытие берёт одну вершину ($m$), в каждом дизъюнкт-треугольнике --- две из трёх ($2c$), итого $k$.

  Если формула выполнима, берём в покрытие литералы, истинные в модели (по одному из каждой пары), а в каждом треугольнике --- две вершины, не соответствующие ложному литералу.
  Обратно: если покрытие размера $k$ есть, из каждой пары переменных выбранная вершина задаёт значение; в каждом треугольнике одна вершина не покрыта внутри, но покрыта ребром из пары --- соответствующий литерал истинен, дизъюнкт выполнен.
  Эквивалентность установлена, задача NP-полна.
]

#pb-solution("sat:cdcl")[
  Граф импликаций: решения $x_1 = 1$ (уровень 1) и $x_2 = 0$ (уровень 2); по (reason) $(not x_1 or x_3)$ из $x_1 = 1$ следует $x_3 = 1$; по $(x_2 or x_4)$ из $x_2 = 0$ следует $x_4 = 1$; конфликт в $(not x_3 or not x_4)$.

  Первый UIP --- точка на текущем уровне решения (уровень 2), через которую проходит любой разрез конфликта: это $x_4$.

  Выученный дизъюнкт: разрешаем конфликтный $(not x_3 or not x_4)$ с причиной $x_4$ --- дизъюнктом $(x_2 or x_4)$, устраняя $x_4$:
  $(not x_3 or not x_4)$ и $(x_2 or x_4)$ дают $(x_2 or not x_3)$.

  После выучивания $(x_2 or not x_3)$ решатель делает нехронологический возврат на уровень 1.
  На уровне 1 уже присвоены $x_1 = 1$ и $x_3 = 1$, значит литерал $not x_3$ ложен, и дизъюнкт становится единичным: $x_2 = 1$.
]

#pb-solution("sat:hamiltonian-sat")[
  Переменные $p_(v, i)$: «вершина $v$ стоит на $i$-й позиции цикла» ($v in V$, $i = 1, ..., n$).
  Группы дизъюнктов:
  + каждая вершина на хотя бы одной позиции: $(p_(v,1) or ... or p_(v,n))$ --- $n$ дизъюнктов;
  + каждая вершина не более чем на одной позиции: $(not p_(v,i) or not p_(v,j))$ для $i < j$;
  + каждая позиция занята: $(p_(v_1,i) or ... or p_(v_n,i))$ --- $n$ дизъюнктов;
  + каждая позиция занята не более чем одной вершиной: $(not p_(u,i) or not p_(v,i))$ для $u != v$;
  + соседние по циклу вершины смежны: для каждой позиции $i$ и каждого не-ребра $(u, v)$ запрещаем $p_(u,i) and p_(v, i+1)$: дизъюнкт $(not p_(u,i) or not p_(v, i+1))$.

  Размер: $O(n^2)$ переменных и $O(n^3)$ дизъюнктов.
  Наивное перечисление всех $n!$ перестановок порождает экспоненциально много дизъюнктов; позиционная кодировка линейно/полиномиально описывает цикл и избегает этого.
]
