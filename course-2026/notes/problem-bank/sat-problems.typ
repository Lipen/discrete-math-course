// Раздел: SAT и NP-полнота (m11).
// Задачи соответствуют разделу «== Упражнения» главы m11.
#import "macros.typ": problem, project, topic-header
#import "../notation.typ": *

#topic-header("SAT и NP-полнота", "m11")[
  Выполнимость КНФ, 2-SAT, DPLL, NP-полнота.
]

// ── Сложные ──

#problem(
  id: "sat:cnf-sat",
  title: [Выполнимость КНФ-формул],
  difficulty: "basic",
  topics: ("SAT", "КНФ"),
)[
  Какие из следующих КНФ-формул выполнимы?
  Для выполнимых --- укажите модель; для невыполнимых --- объясните причину.
  + $(x or y) and (not x or not y)$;
  + $(x) and (not x)$;
  + $(x or y or z) and (not x) and (not y) and (not z)$;
  + $(x or y) and (not x or y) and (not y)$.
]

// ── Сложные ──

#problem(
  id: "sat:implication-graph",
  title: [Граф импликаций для 2-SAT],
  difficulty: "basic",
  topics: ("SAT", "2-SAT", "граф импликаций"),
)[
  Переведите каждый дизъюнкт 2-SAT в пару импликаций и постройте граф импликаций для формулы:
  $
    (x_1 or not x_2) and (not x_1 or x_3) and (not x_2 or not x_3).
  $
  Сколько вершин и рёбер в графе?
]

// ── Сложные ──

#problem(
  id: "sat:four-clause",
  title: [Невыполнимость по графу импликаций],
  difficulty: "basic",
  topics: ("SAT", "2-SAT", "компоненты сильной связности"),
)[
  Является ли формула $(x or y) and (not x or y) and (x or not y) and (not x or not y)$ выполнимой?
  Переведите дизъюнкты в импликации, постройте граф и найдите КСС.
]

// ── Сложные ──

#problem(
  id: "sat:unsat-2-clauses",
  title: [Невыполнимая 2-SAT с двумя дизъюнктами],
  difficulty: "basic",
  topics: ("SAT", "2-SAT", "компоненты сильной связности"),
)[
  Приведите пример невыполнимой 2-SAT-формулы, содержащей ровно два дизъюнкта.
  Покажите на графе импликаций, что некоторая переменная и её отрицание лежат в одной КСС.
]

// ── Сложные ──

#problem(
  id: "sat:solve-2sat",
  title: [Решение 2-SAT по конденсации],
  difficulty: "medium",
  topics: ("SAT", "2-SAT", "конденсация"),
)[
  Решите 2-SAT-формулу с помощью графа импликаций:
  $
    (x_1 or x_2) and (not x_1 or x_3) and (not x_2 or not x_3) and (not x_2 or x_4) and (not x_4 or not x_3).
  $
  Найдите все компоненты сильной связности, постройте конденсацию, присвойте значения в обратном топологическом порядке.
]

// ── Сложные ──

#problem(
  id: "sat:3coloring-k3",
  title: [3-раскраска треугольника как SAT],
  difficulty: "medium",
  topics: ("SAT", "кодирование", "раскраска"),
)[
  Закодируйте задачу 3-раскраски графа-треугольника $K_3$ в виде КНФ-формулы.
  Используйте переменные $x_(v, c)$ (вершина $v$ имеет цвет $c$).
  Выпишите все дизъюнкты.
  Выполнима ли формула?
  Что ответ говорит о хроматическом числе $K_3$?
]

// ── Сложные ──

#problem(
  id: "sat:dpll",
  title: [Работа DPLL с unit propagation],
  difficulty: "medium",
  topics: ("SAT", "DPLL"),
)[
  Промоделируйте работу DPLL (с unit propagation) на формуле:
  $
    (x_1 or x_2) and (not x_1 or x_3) and (not x_2 or not x_3) and (x_1 or not x_3).
  $
  Используйте порядок решений: $x_1 = 1$, затем $x_2 = 1$ (если потребуется).
  Нарисуйте дерево поиска; в каждом узле укажите частичное присваивание и результат unit propagation.
]

// ── Сложные ──

#problem(
  id: "sat:exactly-two",
  title: ["Ровно две переменные истинны" в КНФ],
  difficulty: "medium",
  topics: ("SAT", "кодирование"),
)[
  Закодируйте условие "ровно две переменные из $x_1, x_2, x_3, x_4$ истинны" в КНФ.
  Подсчитайте число дизъюнктов.
  Указание: скомбинируйте ограничения "хотя бы две" и "не более двух"; можно ввести вспомогательные переменные.
]

// ── Сложные ──

#problem(
  id: "sat:vertex-cover",
  title: [NP-полнота задачи о вершинном покрытии],
  difficulty: "advanced",
  topics: ("NP-полнота", "сведение 3-SAT"),
)[
  Докажите, что задача о вершинном покрытии NP-полна: сведите 3-SAT к задаче "существует ли вершинное покрытие размера не более $k$?".
  Опишите конструкцию: как по 3-КНФ-формуле $phi$ построить граф $G$ и число $k$ так, что $G$ имеет вершинное покрытие размера $k$ тогда и только тогда, когда $phi$ выполнима.
]

// ── Сложные ──

#problem(
  id: "sat:cdcl",
  title: [Анализ конфликта и первый UIP],
  difficulty: "advanced",
  topics: ("SAT", "CDCL", "UIP"),
)[
  Дан частичный вывод: решение $x_1 = 1$; решение $x_2 = 0$; unit propagation из $(not x_1 or x_3)$ даёт $x_3 = 1$; unit propagation из $(x_2 or x_4)$ даёт $x_4 = 1$; конфликт в дизъюнкте $(not x_3 or not x_4)$.
  Нарисуйте граф импликаций.
  Примените анализ конфликта: найдите разрез, соответствующий первому UIP, и выпишите выученный дизъюнкт.
]

// ── Сложные ──

#problem(
  id: "sat:hamiltonian-sat",
  title: [Гамильтонов цикл как SAT],
  difficulty: "advanced",
  topics: ("SAT", "кодирование", "гамильтонов цикл"),
)[
  Закодируйте задачу поиска гамильтонова цикла в графе с $n$ вершинами как SAT.
  Опишите переменные и основные группы дизъюнктов.
  Оцените размер КНФ относительно $n$.
  Почему наивное кодирование (через перечисление всех перестановок) порождает экспоненциально много дизъюнктов и как этого избежать?
]

// ── Найдите ошибку в доказательстве ──

#problem(
  id: "sat:err-2sat",
  title: [2-SAT NP-полна],
  difficulty: "advanced",
  topics: ("SAT", "2-SAT", "NP-полнота", "ошибка в доказательстве"),
)[
  Некто утверждает, что задача 2-SAT NP-полна, и приводит такое доказательство.

  Сведём 3-SAT к 2-SAT.
  Заменим каждый дизъюнкт $(l_1 or l_2 or l_3)$ в 3-КНФ парой дизъюнктов $(l_1 or l_2)$, $(l_2 or l_3)$.
  Невыполнимость исходного дизъюнкта сохраняется: тройка $(l_1 or l_2 or l_3)$ ложна тогда и только тогда, когда все три литерала $l_1, l_2, l_3$ ложны, а в этом случае оба дизъюнкта $(l_1 or l_2)$ и $(l_2 or l_3)$ тоже ложны.
  Следовательно, исходная 3-КНФ невыполнима тогда и только тогда, когда полученная 2-КНФ невыполнима.
  Сведение полиномиальное (линейное число дизъюнктов), значит 2-SAT NP-полна.

  Где ошибка?
]

#problem(
  id: "sat:err-greedy",
  title: [Жадный алгоритм решает SAT за полиномиальное время],
  difficulty: "advanced",
  topics: ("SAT", "жадный алгоритм", "P vs NP", "ошибка в доказательстве"),
)[
  Некто утверждает, что $"SAT" in P$, и предлагает жадный алгоритм.

  Рассмотрим произвольную КНФ-формулу над переменными $x_1, x_2, ..., x_n$.
  Будем перебирать переменные по порядку от $x_1$ до $x_n$.
  Для переменной $x_i$ пробуем оба значения: 0 и 1.
  Выбираем то, при котором выполнено больше дизъюнктов.
  Если число выполненных дизъюнктов одинаково --- берём 0.
  После обработки всех $n$ переменных получаем полное присваивание.
  Если формула была выполнима, алгоритм найдёт выполняющее присваивание.
  Алгоритм полиномиален: $n$ итераций, на каждой проверяем не более $m$ дизъюнктов.
  Значит $"SAT" in P$.

  Где ошибка?
]

#problem(
  id: "sat:err2-one-var",
  title: [SAT с одной переменной экспоненциален],
  difficulty: "advanced",
  topics: ("SAT", "сложность", "перебор", "ошибка"),
)[
  Рассмотрим следующее «доказательство» того, что даже простейший SAT экспоненциален.
  Докажем, что проверка выполнимости КНФ даже с одной переменной требует перебора $2^n$ вариантов.
  Пусть формула содержит $n$ вхождений переменной $x$ (возможно, с отрицаниями).
  Каждое вхождение --- это литерал $x$ или $not x$.
  Для проверки выполнимости нужно перебрать все комбинации значений этих $n$ литералов, а их $2^n$.
  Даже при $n = 50$ это астрономическое число, не поддающееся перебору.
  Следовательно, даже простейший SAT-задачи требуют экспоненциального времени.

  Где ошибка?
]

#project(
  id: "sat:proj-cdcl",
  title: [Скелет CDCL-решателя],
  chapter-refs: ("m11",),
)[
  Реализуйте минимальный SAT-решатель на 100--150 строк: единичное распространение (unit propagation), выбор переменной по эвристике, конфликт-анализ с выученным дизъюнктом и нехронологическим возвратом (backjumping).
  Для небольших формул он должен решать SAT быстрее наивного перебора.

  Заготовка:

  ```rust
  pub struct Solver {
      n: usize,                     // число переменных
      clauses: Vec<Vec<i32>>,       // литеры: ±(var+1)
      assign: Vec<Option<bool>>,
  }

  impl Solver {
      pub fn new(
          n: usize,
          clauses: Vec<Vec<i32>>,
      ) -> Self { /* TODO */ }

      /// Единичное распространение;
      /// возвращает ошибку при конфликте.
      fn propagate(&mut self) -> Result<(), ()> { /* TODO */ }

      /// CDCL; возвращает выполняющее присваивание или None.
      pub fn solve(&mut self) -> Option<Vec<bool>> { /* TODO */ }
  }
  ```

  Что нужно сдать:
  Файл `lib.rs` и тесты: 2-SAT, раскраска маленького графа в КНФ, выполнимая и невыполнимая формулы.

  Критерии оценки:
  Корректность на маленьких формулах, единичное распространение работает, выученные дизъюнкты добавляются.
]
