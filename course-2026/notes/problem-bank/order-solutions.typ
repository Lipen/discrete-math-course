// Решения к разделу Отношения порядка (m06).
#import "macros.typ": pb-solution
#import "../notation.typ": *

#pb-solution("ord:hasse-divisors")[
  Покрытия (рёбра диаграммы Хассе):
  $1$ покрывается $2$ и $3$;
  $2$ покрывается $4$ и $6$;
  $3$ покрывается $6$;
  $4$ и $6$ покрываются $12$.
  Диаграмма --- две ветви из $1$, через $2$ и через $3$, сходящиеся в $12$.
  Минимальные элементы: ${1}$.
  Максимальные элементы: ${12}$.
  Наименьший элемент: $1$.
  Наибольший элемент: $12$.
]

#pb-solution("ord:two-minimal")[
  Пример: множество ${a, b, c}$ с отношениями $a prec c$ и $b prec c$, при этом $a$ и $b$ несравнимы.
  Это частичный порядок: рефлексивность, антисимметрия и транзитивность выполняются, конфликтов нет.
  Минимальные элементы --- $a$ и $b$: меньше них нет ничего.
  Наименьшего элемента нет: ни $a$, ни $b$ не меньше всех (они несравнимы между собой), а $c$ больше $a$ и $b$.
]

#pb-solution("ord:distributive-lattice")[
  Супремум двух подмножеств --- их объединение, инфимум --- пересечение.
  Множество $cal(P)({1, 2, 3})$ с включением --- решётка: любая пара имеет и супремум, и инфимум.
  Дистрибутивность выполняется, потому что для множеств верны тождества
  $A inter (B union C) = (A inter B) union (A inter C)$
  и
  $A union (B inter C) = (A union B) inter (A union C)$.
  Оба равенства --- стандартные свойства объединения и пересечения, проверяются принадлежностью элемента.
  Значит, $cal(P)({1, 2, 3})$ --- дистрибутивная решётка (более того, булева: у неё $2^3 = 8$ элементов).
]

#pb-solution("ord:topological-sort")[
  Индукция по числу элементов.
  База: одноэлементное ЧУМ сортируется тривиально.
  Шаг: в конечном ЧУМ есть минимальный элемент $m$.
  Действительно, возьмём любой элемент; если он не минимален, есть строго меньший, и конечным спуском мы дойдём до минимального.
  Удалим $m$.
  По индукционному предположению остаток допускает линейное продолжение.
  Поставим $m$ первым, затем --- это продолжение.
  Так как $m$ минимален, ничего не должно стоять перед ним, а отношения между остальными элементами сохранены.
  Линейное продолжение получено.
]

#pb-solution("ord:divisibility-lattice")[
  Супремум пары $a, b$ --- наименьшее общее кратное $"НОК"(a, b)$: оно делится на оба числа и делит любое общее кратное.
  Инфимум --- наибольший общий делитель $"НОД"(a, b)$: он делит оба числа и делится на любой общий делитель.
  Оба элемента существуют и единственны для любой пары натуральных чисел.
  Значит, $(NN^+, |)$ --- решётка: супремум и инфимум есть у каждой пары.
]

#pb-solution("ord:course-topo")[
  Построим линейный порядок, в котором каждый курс идёт раньше всех, от кого он зависит.
  Один из вариантов: Программирование, Алгебра, Дискретная математика, Алгоритмы, Логика, Линейная алгебра.
  Проверка зависимостей:
  + Алгебра $->$ Дискретная математика: Алгебра (2-й) раньше Дискретной математики (3-й);
  + Алгебра $->$ Линейная алгебра: Алгебра (2-й) раньше Линейной алгебры (6-й);
  + Программирование $->$ Алгоритмы: Программирование (1-й) раньше Алгоритмов (4-й);
  + Дискретная математика $->$ Алгоритмы: Дискретная математика (3-й) раньше Алгоритмов (4-й);
  + Дискретная математика $->$ Логика: Дискретная математика (3-й) раньше Логики (5-й).

  Все рёбра направлены вперёд, значит это корректная топологическая сортировка.
]

#pb-solution("ord:knaster-tarski")[
  В полной решётке есть наименьший элемент $bot$ (инфимум всей решётки).
  Построим цепь $bot, f(bot), f(f(bot)), ...$.
  Так как $bot <= f(bot)$ (ничего нет меньше $bot$), монотонность даёт $f(bot) <= f(f(bot))$, и по индукции $bot <= f(bot) <= f(f(bot)) <= dots.c$.
  Решётка конечна, поэтому цепь стабилизируется: $f^k (bot) = f^(k+1) (bot)$ для некоторого $k$.
  Тогда $f(f^k (bot)) = f^(k+1) (bot) = f^k (bot)$, то есть $f^k (bot)$ --- неподвижная точка.
  Существование доказано.
]

#pb-solution("ord:count-posets")[
  Точные значения: $n = 1$: $1$; $n = 2$: $3$; $n = 3$: $19$; $n = 4$: $219$.
  (Это последовательность OEIS A001035.)

  Для $n = 2$: на паре ${a, b}$ возможны три варианта: элементы несравнимы, $a < b$, $b < a$.

  Для $n = 3$ разобьём по числу сравнимых пар:
  + несравнимы все: $1$ порядок;
  + одна сравнимая пара (цепь из двух элементов и изолированный элемент): выбрать пару $binom(3, 2) = 3$ способами и направление --- $2$ способами, итого $6$;
  + две сравнимые пары («галка»: один элемент больше двух остальных или меньше двух остальных): выбрать вершину $3$ способами и ориентацию $2$ способами, итого $6$;
  + все сравнимы (линейный порядок): $3! = 6$.

  Итого $1 + 6 + 6 + 6 = 19$.
  Для $n = 4$ значение $219$ получается аналогичным перечислением (или из таблицы OEIS A001035).
]
