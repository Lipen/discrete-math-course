// M13 --- Конечные автоматы и регулярные языки: простейшая вычислительная модель.
#import "common-notes.typ": *
#import "notation.typ": *
#import "diagrams/m13.typ": dfa-01, nfa-00-11

= Конечные автоматы и регулярные языки

#chapter-overview[
  Конечные автоматы --- простейшая модель вычислений: машины с фиксированной конечной памятью, читающие входную строку один раз слева направо.
  Несмотря на простоту, они определяют в точности регулярные языки, повсеместно встречающиеся в программировании: каждый поиск по регулярному выражению, каждый лексер, каждый валидатор ввода --- это конечный автомат в действии.
  Эта глава охватывает ДКА, НКА, их эквивалентность, регулярные выражения, лемму о накачке и минимизацию ДКА.
]

== Детерминированные конечные автоматы (ДКА)

=== Определение ДКА

Детерминированный конечный автомат --- это математическая модель устройства, которое в каждый момент находится в одном из конечного числа состояний.
На вход подаётся строка символов; автомат читает её слева направо, переходя из состояния в состояние в соответствии с фиксированной таблицей переходов.
В конце строки автомат либо принимает её (если оказался в принимающем состоянии), либо отвергает.

#definition[ДКА][
  *Детерминированным конечным автоматом* (ДКА) называется пятёрка $M = (Q, Sigma, delta, q_0, F)$, где:
  - $Q$ --- конечное множество состояний,
  - $Sigma$ --- конечный алфавит (множество допустимых символов),
  - $delta: Q times Sigma -> Q$ --- всюду определённая функция переходов: по текущему состоянию и прочитанному символу однозначно определяется следующее состояние,
  - $q_0 in Q$ --- начальное состояние,
  - $F subset.eq Q$ --- множество принимающих (допускающих) состояний.
]

Функция переходов естественно расширяется на строки: прочитав строку $w$, автомат из состояния $q$ переходит в состояние, определяемое последовательным применением $delta$ к символам строки.

#definition[Расширенная функция переходов и язык ДКА][
  *Расширенная функция переходов* $hat(delta): Q times Sigma^* -> Q$ определяется рекурсивно:
  - $hat(delta)(q, epsilon) = q$ (пустая строка не меняет состояния),
  - $hat(delta)(q, a w) = hat(delta)(delta(q, a), w)$ для $a in Sigma$, $w in Sigma^*$.

  *Языком, распознаваемым автоматом $M$*, называется множество всех строк, которые переводят автомат из начального в принимающее состояние:
  $L(M) = {w in Sigma^* mid(|) hat(delta)(q_0, w) in F}$.
]

#example[ДКА для строк, заканчивающихся на $"01"$][
  Рассмотрим язык над алфавитом ${0, 1}$, состоящий из всех строк, заканчивающихся на $"01"$.

  Идея построения: состояния хранят информацию о последних прочитанных символах, достаточную для определения того, заканчивается ли строка на $"01"$:
  - $q_0$: последние символы не образуют префикса $"01"$ (или строка пуста).
  - $q_1$: строка заканчивается на 0 (потенциальное начало $"01"$).
  - $q_2$: строка заканчивается на $"01"$ (принимающее состояние).
]

#figure(
  dfa-01,
  caption: [ДКА, распознающий строки над алфавитом ${0, 1}$, заканчивающиеся на $"01"$.
    Принимающее состояние $q_2$ обозначено двойной границей.],
) <fig:dfa-01>

=== Свойства замкнутости

Имея автоматы для языков $L_1$ и $L_2$, можно построить автоматы для их булевых комбинаций.
Ключевая конструкция --- _прямое произведение_ автоматов: состояния нового автомата --- это пары $(q, r)$, где $q$ --- состояние первого автомата, $r$ --- второго.
Переходы определяются покомпонентно.

#proposition[Замкнутость относительно булевых операций][
  Имея ДКА для $L_1$ и $L_2$, можно построить ДКА для:
  - *Объединения* $L_1 union L_2$: принимающие состояния --- все пары, в которых хотя бы одна компонента принимающая: $F = (F_1 times Q_2) union (Q_1 times F_2)$.
  - *Пересечения* $L_1 inter L_2$: принимающие состояния --- пары, в которых обе компоненты принимающие: $F = F_1 times F_2$.
  - *Дополнения* $overline(L_1)$: меняем местами принимающие и непринимающие состояния. Это работает только для ДКА (всюду определённая $delta$ гарантирует, что каждая строка либо принимается, либо отвергается).
]

Таким образом, класс регулярных языков образует _булеву алгебру_ --- он замкнут относительно объединения, пересечения и дополнения.

== Недетерминированные конечные автоматы (НКА)

=== Определение НКА

Детерминизм ДКА иногда заставляет плодить экспоненциально много состояний.
Например, язык "третий с конца символ равен 1" требует $2^3 = 8$ состояний в ДКА, но всего 4 состояния в недетерминированном автомате.
Недетерминизм --- это не физическая реальность, а математическое удобство: автомат "угадывает" правильный путь.

#definition[НКА][
  *Недетерминированным конечным автоматом* (НКА) называется пятёрка $M = (Q, Sigma, delta, q_0, F)$, где:
  - $Q$, $Sigma$, $q_0$, $F$ имеют тот же смысл, что и в ДКА,
  - $delta: Q times (Sigma union {epsilon}) -> cal(P)(Q)$ --- функция переходов, возвращающая _множество_ возможных следующих состояний.

  Из состояния по символу (или спонтанно, без чтения символа, через $epsilon$-переход) автомат может перейти в любое из нескольких состояний.
]

#note[
  НКА принимает строку, если существует _хотя бы один_ путь вычисления из начального состояния в принимающее, следуя переходам.
  Пути, ведущие в непринимающие состояния или застревающие (нет перехода по очередному символу), игнорируются.
  Достаточно существования одного успешного пути.
]

#example[НКА для строк, содержащих $"00"$ или $"11"$][
  Построим НКА для языка строк, содержащих подстроку $"00"$ или $"11"$.

  Из начального состояния $q_0$ автомат "угадывает", какой из шаблонов встретится:
  - По 0 переходит в $q_1$ (ожидание второй подряд 0).
  - По 1 переходит в $q_2$ (ожидание второй подряд 1).
  Если угадано правильно, автомат достигает принимающего состояния.
  Обе ветви работают независимо.
]

#figure(
  nfa-00-11,
  caption: [НКА, распознающий строки над алфавитом ${0, 1}$, содержащие $"00"$ или $"11"$.
    Две независимые ветви "угадывают" шаблон.],
) <fig:nfa-00-11>

#example[НКА для "третий с конца символ равен 1"][
  Этот язык иллюстрирует экспоненциальный разрыв между НКА и ДКА.
  НКА: угадывает момент, когда до конца строки осталось ровно 3 символа, затем проверяет, что текущий символ равен 1, и что после него идут ровно два любых символа.
  Требуется 4 состояния.

  Минимальный ДКА для того же языка требует $2^3 = 8$ состояний: автомат должен "помнить" последние 3 прочитанных символа.
]

=== Конструкция подмножеств (НКА $->$ ДКА)

Несмотря на кажущуюся большую мощность НКА, они распознают в точности те же языки, что и ДКА.
Конструкция подмножеств (Рабина--Скотта) превращает любой НКА в эквивалентный ДКА ценой экспоненциального роста числа состояний.

#theorem[Эквивалентность ДКА и НКА][
  Для любого НКА существует ДКА, распознающий тот же язык.
]

#proof[
  Пусть дан НКА $N = (Q, Sigma, delta, q_0, F)$.
  Построим ДКА $D$, состояниями которого являются _подмножества_ состояний $N$:
  $D = (cal(P)(Q), Sigma, delta', {q_0}^\*, F')$, где ${q_0}^\*$ --- это $epsilon$-замыкание множества ${q_0}$ (все состояния, достижимые из $q_0$ по $epsilon$-переходам).

  Функция переходов ДКА: для множества $S subset.eq Q$ и символа $a$,
  $
    delta'(S, a) = epsilon "{-closure}"(union.big_(q in S) delta(q, a)).
  $
  То есть: из каждого состояния $S$ берём все возможные переходы по $a$, собираем результаты и замыкаем по $epsilon$.

  Принимающие состояния ДКА: $F' = {S subset.eq Q mid(|) S inter F eq.not nothing}$ --- множества, содержащие хотя бы одно принимающее состояние НКА.

  По построению, $D$ симулирует _все_ возможные пути $N$ одновременно.
  Число состояний ДКА не превышает $2^(|Q|)$, и этот экспоненциальный рост неизбежен в худшем случае.
]

#proposition[ДКА и НКА --- сравнение][
  #table(
    columns: 3,
    align: (left, left, left),
    stroke: (x, y) => if y == 0 { (bottom: 0.4pt) },
    table.header([*Свойство*], [*ДКА*], [*НКА*]),
    [Переход],
    [$delta(q, a)$ = одно состояние],
    [$delta(q, a)$ = множество состояний],

    [$epsilon$-переходы], [Запрещены], [Разрешены (спонтанные)],
    [Требуемое число состояний],
    [Потенциально экспоненциальное],
    [Часто линейное или полиномиальное],

    [Распознавание],
    [Единственный путь вычисления],
    [Хотя бы один путь ведёт к принятию],

    [Реализация],
    [Табличный, простой цикл],
    [Возврат или симуляция подмножеств],

    [Сложность построения],
    [Сложнее (явно обрабатывать все случаи)],
    [Проще (недетерминизм помогает)],

    [Дополнение],
    [Тривиально (поменять $F$)],
    [Требует предварительной детерминизации],
  )
]

== Регулярные выражения

Регулярные выражения --- это алгебраический язык для описания регулярных языков.
Они используются в каждом текстовом редакторе, в каждом поисковом движке, в каждом лексере компилятора.

#definition[Регулярное выражение][
  *Регулярным выражением* над алфавитом $Sigma$ называется выражение, построенное по следующим правилам:
  - $nothing$ обозначает пустой язык.
  - $epsilon$ обозначает язык, содержащий только пустую строку: ${epsilon}$.
  - $a$ (где $a in Sigma$) обозначает язык ${a}$ (строка из одного символа).
  - $R_1 | R_2$ обозначает объединение языков: $L(R_1) union L(R_2)$.
  - $R_1 R_2$ обозначает конкатенацию: ${u v mid(|) u in L(R_1), v in L(R_2)}$.
  - $R^*$ обозначает итерацию (звезду Клини): ноль или более повторений $R$.

  Приоритет операций (от высшего к низшему): $*$, конкатенация, $|$.
]

#theorem[Теорема Клини][
  Язык регулярен (распознаётся ДКА или НКА) тогда и только тогда, когда он может быть описан регулярным выражением.
]

#proof-sketch[
  Доказательство состоит из двух преобразований:

  *Регулярное выражение $->$ НКА*: структурной индукцией по построению регулярного выражения.
  Для каждого правила (пустой язык, пустая строка, символ, объединение, конкатенация, звезда) строится соответствующий НКА с одним начальным и одним принимающим состоянием, которые затем соединяются в общую схему.

  *ДКА $->$ регулярное выражение*: методом исключения состояний.
  Превращаем ДКА в обобщённый автомат с регулярными выражениями на рёбрах, затем последовательно удаляем все состояния, кроме начального и принимающих, обновляя выражения на оставшихся рёбрах.
  В конце на ребре от начального к принимающему состоянию остаётся искомое регулярное выражение.
]

#proposition[Свойства замкнутости регулярных языков][
  Регулярные языки замкнуты относительно:
  - *Булевых операций*: объединение, пересечение, дополнение, разность (конструкции на ДКА).
  - *Конкатенации и звезды Клини* (по определению регулярных выражений).
  - *Обращения* $L^R$: обратить все переходы НКА, начальное и принимающее состояния поменять местами.
  - *Гомоморфизма и обратного гомоморфизма* (замены символов алфавита на строки и наоборот).
  - *Взятия префикса, суффикса и подстроки*.
]

=== Лемма о накачке

Как доказать, что язык _не_ является регулярным?
Для этого служит лемма о накачке (pumping lemma) --- необходимое условие регулярности.
Если язык этому условию не удовлетворяет, он не может быть регулярным.

#theorem[Лемма о накачке для регулярных языков][
  Если язык $L$ регулярен, то существует целое число $p >= 1$ (длина накачки) такое, что для любой строки $w in L$ с длиной $|w| >= p$ существует разбиение $w = x y z$, удовлетворяющее трём условиям:
  - $|y| > 0$ (накачиваемая часть непуста),
  - $|x y| <= p$ (накачиваемая часть находится в первых $p$ символах),
  - $x y^i z in L$ для всех $i >= 0$ (подстроку $y$ можно повторить любое число раз или удалить, и строка останется в языке).
]

#proof-sketch[
  Пусть ДКА, распознающий $L$, имеет $p$ состояний.
  Рассмотрим строку $w$ длины $>= p$.
  При её чтении автомат проходит через $|w| + 1 >= p + 1$ состояние.
  По принципу Дирихле, среди этих состояний есть повторяющееся --- некоторое состояние $s$ посещается дважды.

  Пусть $x$ --- префикс до первого вхождения $s$, $y$ --- подстрока между двумя вхождениями $s$, $z$ --- оставшийся суффикс.
  Тогда $y$ можно "накачать": повторить любое число раз (автомат будет ходить по циклу $s -> ... -> s$) или удалить, и конечное состояние не изменится.
]

#example[Доказательство нерегулярности языка ${0^n 1^n mid(|) n >= 0}$][
  Предположим, что $L = {0^n 1^n mid(|) n >= 0}$ регулярен, и пусть $p$ --- его длина накачки.
  Возьмём строку $w = 0^p 1^p in L$, её длина $2p >= p$.

  По лемме о накачке, $w = x y z$, где $|x y| <= p$ и $|y| > 0$.
  Так как $|x y| <= p$, подстрока $x y$ целиком лежит в первых $p$ символах строки, то есть состоит только из нулей.
  Следовательно, $y = 0^k$ для некоторого $k > 0$.

  Тогда строка $x y^2 z = 0^(p+k) 1^p$ должна принадлежать $L$.
  Но в этой строке больше нулей, чем единиц --- противоречие с определением $L$.
  Следовательно, $L$ не регулярен.
]

Лемма о накачке даёт необходимое, но не достаточное условие регулярности.
Существуют нерегулярные языки, удовлетворяющие условию леммы о накачке, поэтому с её помощью нельзя _доказать_ регулярность --- только _опровергнуть_ её.

#example[Другие нерегулярные языки][
  - Язык палиндромов над алфавитом ${a, b}$ не регулярен (рассуждение аналогично $0^n 1^n$).
  - Язык ${a^(2^n) mid(|) n >= 0}$ не регулярен: накачка дала бы длины, не являющиеся степенями двойки.
  - Язык ${a^n b^n c^n mid(|) n >= 0}$ не регулярен (требует двух накачек, что выходит за пределы возможностей конечного автомата).
]

== Минимизация ДКА

Среди всех ДКА, распознающих данный язык, существует единственный (с точностью до изоморфизма) минимальный ДКА.
Минимизация важна на практике: меньше состояний --- быстрее проверка, меньше памяти для хранения таблицы переходов.

Идея минимизации: объединить состояния, которые невозможно различить по дальнейшему поведению.

#definition[Эквивалентность состояний][
  Состояния $p$ и $q$ называются *эквивалентными* (или неразличимыми), обозначается $p tilde.op q$, если для любой строки $w in Sigma^*$ выполняется:
  $hat(delta)(p, w) in F$ тогда и только тогда, когда $hat(delta)(q, w) in F$.

  Другими словами: из $p$ и $q$ принимаются в точности одни и те же строки.
]

Алгоритм минимизации систематически находит _различимые_ пары состояний и объединяет неразличимые.

#proposition[Алгоритм Мура (заполнение таблицы)][
  1. Нарисовать таблицу всех неупорядоченных пар $(p, q)$ с $p < q$.
  2. Пометить пары, в которых ровно одно состояние принимающее (они различимы тривиально).
  3. Итеративно: для каждой непомеченной пары $(p, q)$ и каждого символа $a$ проверить пару $(delta(p, a), delta(q, a))$. Если эта пара помечена, пометить $(p, q)$ (различимость распространяется назад).
  4. Повторять, пока в таблице происходят изменения.
  5. Непомеченные пары = эквивалентные состояния.
  6. Склеить эквивалентные состояния в одно --- получить минимальный ДКА.

  Время работы: $O(|Q|^2 dot |Sigma|)$.
]

#theorem[Единственность минимального ДКА][
  Каждый регулярный язык имеет единственный минимальный ДКА с точностью до изоморфизма (переименования состояний).
]

Минимальный ДКА --- это каноническое представление регулярного языка.
Проверка эквивалентности двух регулярных языков сводится к сравнению их минимальных ДКА: языки равны тогда и только тогда, когда минимальные ДКА изоморфны.


== Применения

#remark[
  Конечные автоматы --- это не абстрактная теория, а повседневный рабочий инструмент:

  *Лексеры.* Каждый компилятор или интерпретатор начинается с лексического анализатора (flex, lex, ANTLR), который превращает поток символов в поток токенов.
  Спецификации токенов записываются как регулярные выражения, которые затем компилируются в ДКА.
  Полученный ДКА чрезвычайно эффективен: каждый входной символ обрабатывается за константное время.

  *Сетевые протоколы.* Состояния TCP-соединения (CLOSED, LISTEN, SYN-SENT, ESTABLISHED и т.д.) образуют конечный автомат.
  Каждый пакет --- это символ, вызывающий переход; корректность протокола означает достижение желаемых состояний и избегание нежелательных.

  *Model checking.* Аппаратное и программное обеспечение моделируются в виде конечных автоматов (или размеченных систем переходов).
  Спецификации записываются в темпоральной логике (LTL, CTL), а model checker исчерпывающе проверяет, удовлетворяет ли автомат спецификации.
  За эту работу Кларк, Эмерсон и Сифакис получили премию Тьюринга в 2007 году.
  Современные model checker'ы оперируют астрономическими пространствами состояний ($10^20$ и более) благодаря методам символьной верификации.
]
