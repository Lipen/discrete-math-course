// s2, лекция 12 --- Разрешимость и теорема Райса (неделя 26 апреля --- 2 мая).
#import "theme.typ": *
#import "diagrams/ghosts.typ": (
  churchturing-ghost, diagonalization-ghost, enumerator-ghost, halting-ghost,
  multitape-ghost, oracle-ghost, reduction-ghost, rice-ghost, tape-ghost,
  universal-ghost,
)
#import "diagrams/m25.typ": turing-machine
#import "diagrams/m25.typ": reduction-halt-empty, tm-configuration

#show: slides.with(
  title: [Разрешимость и теорема Райса],
  date: "Весна 2027",
  authors: "Константин Чухарев",
  module: "turing",
  lecture: (
    num: 12,
    week: [27--28 апреля],
    teaser: [
      Сведения переносят неразрешимость с задачи на задачу.
      Теорема Райса закроет весь класс нетривиальных свойств языков.
    ],
  ),
)

= Разрешимость и перечислимость

#focus-slide(
  ghost: enumerator-ghost,
  epigraph: [Мы должны знать. Мы будем знать.],
  epigraph-author: [Давид Гильберт],
)

== Разрешитель против распознавателя

#definition[Два класса машин][
  - *Разрешитель* всегда останавливается и отвечает да или нет.
  - *Распознаватель* принимает слова своего языка, а вне его может зациклиться.
]

Язык разрешим --- есть разрешитель, язык перечислим --- есть распознаватель.

Разрешимость требует двух правильных ответов, распознаваемость --- только одного.

== Примеры: разрешимо и распознаваемо

#example[Два языка][
  - $a^n b^n$ разрешим: машина вычёркивает пары и всегда останавливается.
  - $"HALT"$ распознаваема: универсальная машина симулирует $M$ на $w$, но зацикливание симуляции не даёт ответа "нет".
]

#note[
  Вычислимо --- не значит практически решаемо.

  Разрешимая задача может требовать астрономического времени: граница "в принципе" и граница "на практике" --- разные границы.
]

== Перечислимость

#definition[Перечислимость][
  Язык $L$ *перечислим*, если есть МТ, которая печатает все элементы $L$ --- каждый хотя бы раз и ничего besides.

  Эквивалентно: есть распознаватель $L$.
]

Распознаватель и перечислитель --- две маски одной силы: запустить машину на всех входах параллельно и печатать принятые.

Перечислимые языки --- то, что машина способна хоть как-то предъявить.

== С двух сторон

#theorem[Перечислимость плюс ко-перечислимость][
  $L$ разрешим тогда и только тогда, когда перечислимы $L$ и $overline(L)$.
]

#proof[
  Докажем в обе стороны.

  Если $L$ разрешим, оба распознавателя получают готовые ответы разрешителя.

  Обратно: пусть есть распознаватели $M$ для $L$ и $N$ для $overline(L)$.

  Запустим их параллельно, чередуя шаги: свой язык имеет ровно один из них, он и остановится --- его приём и есть ответ.
]

#important[
  Дополнение $"HALT"$ даже не перечислимо --- не всё честно сформулированное доступно машине хотя бы наполовину.
]

= Теорема Райса

#focus-slide(
  ghost: rice-ghost,
  epigraph: [Остерегайтесь ошибок в приведённом выше коде: я лишь доказал его корректность, но не исполнил его.],
  epigraph-author: [Дональд Кнут],
)

== Свойство языка или свойство машины?

Свойство $P$ описаний машин называется экстенсиональным, если оно зависит только от языка:

$ L(M_1) = L(M_2) quad => quad (la M_1 ra in P iff la M_2 ra in P). $

Свойство нетривиально, если есть машины обоих сортов: одна с $P$, другая без.

#note[
  "Язык пуст" --- экстенсионально, "не более 100 состояний" --- свойство текста, а не языка.

  Всякий вопрос "что программа вычисляет?" экстенсионален, всякий "как она устроена?" --- нет.
]

== Теорема Райса

#theorem[Теорема Райса][
  Всякое нетривиальное экстенсиональное свойство распознаваемых языков неразрешимо.

  Иначе: язык ${la M ra mid(|) L(M) "удовлетворяет" P}$ неразрешим для каждого нетривиального $P$.
]

Единственные разрешимые свойства --- тривиальные: выполненные для всех языков или ни для одного.

После Райса вопрос "неразрешимо ли?" заменяется вопросом "тривиально ли?".

== Идея доказательства

#proof[Набросок][
  Докажем сведением $"HALT"$ к $P$.

  Без ограничения общности $emptyset$ не удовлетворяет $P$ --- иначе рассуждаем про дополнение свойства.

  По нетривиальности есть машина $A$ с $L(A) in P$.

  Строим $M'$: на любом входе $x$ она симулирует $M$ на $w$, а при остановке симуляции работает как $A$.

  $M$ остановилась на $w$ --- значит, $L(M') = L(A)$ и свойство есть.
  $M$ зациклилась --- значит, $L(M') = emptyset$ и свойства нет.

  Разрешитель $P$ дал бы разрешитель $"HALT"$ --- противоречие.
]

== Что закрыто Райсом?

#example[Неразрешимые свойства языков][
  - Пуст ли $L(M)$? Конечен ли он?
  - Регулярен ли $L(M)$? Контекстно-свободен?
  - Верно ли $L(M) = Sigma^*$? Содержит ли $L(M)$ слово $"01"$?
]

Все --- нетривиальные свойства языка --- все неразрешимы одной теоремой.

#note[
  Граница: "останавливается ли $M$ на $epsilon$" --- свойство поведения, а не языка, Райсу не подчиняется.

  Но и оно неразрешимо --- отдельным сведением из прошлой лекции.
]

== Инженерный итог

#important[
  Статический анализ, антивирусы, верификаторы проверяют экстенсиональные свойства --- и обречены на приближения.

  Точность и разрешимость несовместимы: инструмент либо молчит на части программ, либо врёт.

  Поэтому анализаторы выдают "да", "нет" и "не знаю" --- третий ответ обязан существовать.
]

= За пределами машин Тьюринга

#focus-slide(
  ghost: oracle-ghost,
  epigraph: [Любая достаточно развитая технология неотличима от магии.],
  epigraph-author: [Артур Кларк],
)

== Оракулы

#definition[МТ с оракулом][
  МТ с дополнительной лентой-оракулом: записав строку и перейдя в состояние запроса, машина за один шаг узнаёт, принадлежит ли строка языку $A$.
]

Оракул --- чёрный ящик, решающий $A$: подпрограмма, отвечающая мгновенно.

#example[Оракул для $"HALT"$][
  Даже он не решает тотальность: вопрос "для всех ли $x$ машина останавливается?" требует бесконечной серии запросов --- по одному на каждый вход.
]

== Лестница иерархии

Оракул для $"HALT"$ открывает часть задач --- и порождает новую проблему остановки: уже для машин с оракулом.

Тот же шаг повторяется наверху --- иерархия не кончается.

#note[
  Кванторная глубина --- мост к логике первого порядка из первого семестра:

  - $exists t$: "остановится за $t$ шагов" --- перечислимость
  - $forall t$: "не остановится никогда" --- ко-перечислимость
  - $forall x exists t$ --- следующий уровень, тотальность.
]

== Гипервычисления

Гипервычисления --- спекуляции о машинах, выходящих за тезис: бесконечное ускорение, оракулы в физике, точные действительные числа за один шаг.

#important[
  Тезис Чёрча--Тьюринга --- не теорема, и физика не обязана подчиняться определению.

  Но за девяносто лет ни одна реализация не вышла за класс тьюринг-вычислимого: гипотез хватает, работающих машин --- нет.
]

== Что с этим делать инженеру?

#important[
  Если задача неразрешима на МТ, её не решит ни один язык программирования.

  Значит: искать разрешимые фрагменты, ограничивать класс входов, строить приближения.

  Умение распознать неразрешимость --- экономит месяцы охоты за несуществующим алгоритмом.
]

== Лекция 12: что мы поняли?

#important[
  - Разрешитель против распознавателя: разрешимость требует ответа на каждом входе, перечислимость --- только предъявления своих слов.
  - Перечислимость с двух сторон даёт разрешимость.
    Дополнение $"HALT"$ показывает, что даже перечислимости может не быть.
  - Теорема Райса: всякое нетривиальное свойство языка машины неразрешимо --- доказательство сводит остановку, подменяя язык машины по ответу решателя.
  - Выход за пределы машин Тьюринга --- оракулы и лестница иерархии: каждая ступень открывает задачи и рождает свою неразрешимость.
  - Тезис Чёрча--Тьюринга остаётся определением, проверяемым опытом, --- гипервычисления пока не вышли из спекуляций.
]

== Вопросы для самопроверки

+ Чем разрешитель отличается от распознавателя и какой класс языков у каждого?

+ Почему $"HALT"$ перечислима, а её дополнение --- нет?

+ Сформулируйте и докажите: перечислимость $L$ и $overline(L)$ даёт разрешимость.

+ Сформулируйте теорему Райса и проверьте, применима ли она к свойству "у машины ровно три состояния".

+ Воспроизведите идею доказательства Райса: как строится машина $M'$?

+ Почему оракул для $"HALT"$ не решает тотальность?

+ Что означает "тезис --- не теорема" и что могло бы его пошатнуть?

== Чтение к следующей паре

#note[
  - m26 --- "Теорема Райса", "За пределами машин Тьюринга".
  - Флеш-опросник --- в начале лекции 13.
  - Контрольная 3 (машина Тьюринга и разрешимость) --- на отдельной паре на следующей неделе.
]
