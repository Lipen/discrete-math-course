#import "theme.typ": *
#show: slides.with(
  title: [Логика первого порядка],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)

= Логика первого порядка
#focus-slide(
  epigraph: [Всякий хороший математик хотя бы наполовину философ, и всякий хороший философ хотя бы наполовину математик.],
  epigraph-author: [Готлоб Фреге],
)

== От высказываний к структуре

Логика высказываний видит утверждение как атом: "Сократ смертен" --- это просто $p$, внутреннее устройство недоступно.

Но утверждение "каждое натуральное число либо чётно, либо нечётно" в логике высказываний не выразить.
Пришлось бы писать бесконечную конъюнкцию: "0 чётно или нечётно, и 1 чётно или нечётно, и 2..."

#important[
  Нужен язык, который проникает внутрь утверждения:
  - объекты;
  - их свойства и отношения;
  - слова "все" и "существует".
]

== Синтаксис: термы и формулы

Формулы логики первого порядка строятся явно, как в языке программирования.

- _Термы_ --- обозначают объекты: переменные $x, y, z$, константы $0, 1$, применения функций $S(x)$, $x + y$.
- _Атомарные формулы_ --- предикаты от термов: $P(x)$, $x < y$, $"чётно"(x)$.
- Из атомов булевыми связками ($and$, $or$, $not$, $->$) и кванторами ($forall$, $exists$) собираются составные формулы.

#definition[Формула][
  - Всякий атом --- формула.
  - Если $phi$ и $psi$ --- формулы, то $not phi$, $phi and psi$, $phi or psi$, $phi -> psi$ --- формулы.
  - Если $phi$ --- формула, а $x$ --- переменная, то $forall x space phi$ и $exists x space phi$ --- формулы.
]

== Предикаты

Предикат --- это шаблон утверждения с дыркой-переменной.
Он становится высказыванием, когда переменную заменяют конкретным значением.

#example[Предикат][
  - $P(x)$ = "$x > 5$" над областью $NN$: $P(7)$ истинно, $P(3)$ ложно.
  - $P(x, y)$ = "$x < y$" над $RR$ --- двухместный предикат, отношение.
]

#definition[Предметная область][
  Предметная область (domain of discourse) --- множество значений, которые пробегает переменная.
]

Квантор связывает переменную и превращает шаблон в высказывание.

== Кванторы

#definition[Квантор всеобщности][
  $forall x space P(x)$ --- "$P(x)$ для _каждого_ $x$ из области".
]

#definition[Квантор существования][
  $exists x space P(x)$ --- "$P(x)$ хотя бы для _одного_ $x$ из области".
]

#example[Кванторы над $NN$][
  - $forall x space x >= 0$ --- истинно.
  - $exists x space x < 0$ --- ложно.
  - $forall x exists y space y > x$ --- истинно (натуральные числа не ограничены).
]

== Кванторы над конечной областью

#note[
  Над конечной областью $A = {a_1, dots, a_n}$ кванторы --- лишь сокращение:
  $
    forall x space P(x) & equiv and.big_(x in A) P(x) & equiv P(a_1) and dots and P(a_n) \
    exists x space P(x) & equiv or.big_(x in A) P(x)  &   equiv P(a_1) or dots or P(a_n)
  $
  Подлинная сила кванторов --- в бесконечных областях.
]

== Отрицание кванторов

#theorem[Законы де Моргана для кванторов][
  $
    not forall x space P(x) & equiv exists x space not P(x) \
    not exists x space P(x) & equiv forall x space not P(x)
  $
]

- Отрицание "все любят пиццу" --- "существует тот, кто не любит пиццу".
- Отрицание "существует единорог" --- "всё является не-единорогом".

#important[
  Отрицание "протаскивается" через квантор, меняя его тип:
  - $forall$ превращается в $exists$,
  - $exists$ --- в $forall$.
]

== Порядок кванторов

Порядок кванторов критичен: он определяет, может ли внутренняя переменная зависеть от внешней.

#example[Порядок кванторов][
  - $P(x, y)$ = "$y$ --- мать $x$".
  - $forall x exists y space P(x, y)$ --- "у каждого есть мать" (истинно): для каждого $x$ свой $y$.
  - $exists y forall x space P(x, y)$ --- "существует мать всех" (ложно): один общий $y$ для всех $x$.
]

- $forall x exists y$ соответствует вложенному циклу: внутренний $y$ зависит от внешнего $x$.
- $exists y forall x$ --- значению, предвычисленному до цикла.

== Ограниченные кванторы

Квантификация почти всегда идёт по ограниченному множеству, и здесь таится частая ошибка.

#definition[Ограниченный квантор][
  - $forall x in A space P(x)$ сокращает $forall x space (x in A -> P(x))$.
  - $exists x in A space P(x)$ сокращает $exists x space (x in A and P(x))$.
]

#important[
  $forall$ использует импликацию, а $exists$ --- конъюнкцию.
]

Почему?
- "Все $A$ суть $P$": если объект в $A$, то он в $P$ --- про объекты вне $A$ ничего не утверждается.
- "Существует $A$ со свойством $P$": объект одновременно в $A$ и в $P$.

== Перевод на язык логики предикатов

Основной навык --- переводить с естественного языка и обратно, точно сохраняя смысл.

#example[Массив отсортирован][
  "Массив $A$ отсортирован по возрастанию":
  $ forall i in {0, dots, n-2} space A[i] <= A[i+1] $
]

#example[Предел на языке кванторов][
  $limits(lim)_(x -> a) f(x) = L$:
  $ forall epsilon > 0 space exists delta > 0 space forall x space (0 < abs(x - a) < delta -> abs(f(x) - L) < epsilon) $
  Три квантора и два неравенства в одном предложении --- вот сила логики предикатов.
]

== Семантика: модели

Формула получает значение в _модели_ --- интерпретации языка.

#definition[Модель][
  Модель задаёт:
  - предметную область $D$ (множество объектов);
  - интерпретацию каждой константы (элемент $D$), каждого функционального символа (функция на $D$), каждого предиката (подмножество $D^k$).
]

#example[Модель][
  Формула $forall x space (P(x) -> Q(x))$ в модели с $D = {"люди"}$, $P$ = "смертен", $Q$ = "говорит" означает "всякий смертный говорит" --- истинность зависит от того, все ли люди говорят.
]

== Выполнимость и общезначимость

#definition[Выполнимость и общезначимость][
  - Формула _выполнима_, если истинна хотя бы в одной модели.
  - _Общезначима_, если истинна во всех моделях.
  - Формулы $phi$ и $psi$ _эквивалентны_, если $phi iff psi$ общезначимо.
]

== Правила вывода для кванторов

Для доказательств формул с кванторами нужны правила ввода и удаления.

#definition[Правила кванторов][
  - $forall$E: из $forall x space P(x)$ вывести $P(c)$ для любого имени $c$.
  - $forall$I: из $P(c)$ для _произвольного_ $c$ вывести $forall x space P(x)$.
  - $exists$I: из $P(c)$ вывести $exists x space P(x)$.
  - $exists$E: из $exists x space P(x)$ и подвывода $P(c) -> B$ (со свежим $c$) вывести $B$.
]

Ограничение на собственные переменные: $c$ для $forall$I и $exists$E должно быть свежим --- не встречаться в посылках и в заключении.

Подробности --- в лекции о натуральной дедукции.

== Многосортная логика и SMT

Часто объекты разных видов смешиваются, и полезно типизировать переменные.

#definition[Многосортная логика][
  У каждой переменной есть _сорт_: $x : "Nat"$, $a : "Array"$.

  Квантор пробегает по сорту: $forall x : "Nat" space x >= 0$.
]

Сорты --- это ограниченные кванторы, поднятые в синтаксис: ошибка в типе ловится до семантики, как в компиляторе.

#important[
  Многосортная логика --- основа SMT-солверов: решателей, проверяющих выполнимость формул над теориями --- арифметикой, массивами, битовыми векторами.
]

== Что мы поняли?

#important[
  - Логика первого порядка проникает внутрь высказывания: термы, предикаты, кванторы.
  - $forall$ и $exists$ превращают шаблон-предикат в высказывание; порядок кванторов меняет смысл.
  - Отрицание кванторов --- де Морган для $forall$/$exists$; ограниченный $forall$ --- импликация, ограниченный $exists$ --- конъюнкция.
  - Формула получает значение в модели; правила вывода для кванторов доказывают формулы о всех или о существующих объектах.
  - Многосортная логика типизирует переменные и лежит в основе SMT.
]

== Вопросы для самопроверки

- Запишите формулой: "каждый студент, изучающий дискретную математику, умеет программировать", и её отрицание.
- Чем $forall x exists y space P(x, y)$ отличается от $exists y forall x space P(x, y)$?
  Приведите пример, где первая истинна, а вторая ложна.
- Почему в $forall x in A$ стоит импликация, а в $exists x in A$ --- конъюнкция?
- Сформулируйте отрицание формулы: $forall x space (P(x) -> exists y space Q(x, y))$.
- Что задаёт модель языка первого порядка? Когда формула общезначима?
