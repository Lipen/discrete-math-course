#import "theme.typ": *
#show: slides.with(
  title: [Абстрактная интерпретация],
  subtitle: "Дискретная математика",
  date: "Осень 2026",
  authors: "Константин Чухарев",
)
#import "diagrams/m08.typ": sign-lattice

= Абстрактная интерпретация
#focus-slide(
  epigraph: [Все модели неверны, но некоторые полезны.],
  epigraph-author: [Джордж Бокс],
)

== Почему тестов недостаточно

Программа, делящая на введённое число, прошла тесты на входах $x = 5$, $x = -2$, $x = 7$.
Все три запуска успешны --- и всё равно ничего не доказано.
На четвёртом входе, $x = 0$, программа упадёт с ошибкой деления на ноль.
Тестирование показывает наличие ошибок, но никогда не доказывает их отсутствия: ошибка может прятаться на любом следующем входе.

Перебрать все входы невозможно.
Вход --- 64-битное целое: возможных значений $2^64$, больше, чем секунд в возрасте Вселенной.
Для программы, читающей список чисел, входов бесконечно много.

#important[
  Доказывать свойство нужно для всех запусков сразу, не выполняя программу ни на одном из них.
]

== Аппроксимация вместо точного ответа

Точный автоматический ответ недостижим: свойства произвольной программы неразрешимы.
Остаётся приближённый ответ --- и его оказывается достаточно.
Точный ответ "да" или "нет" гарантировать нельзя.
Достаточно разрешить третий ответ, "не знаю", и задача перестаёт быть безнадёжной.

Простейший корректный анализ отвечает "не знаю" на любой вопрос: он не ошибается, но и не сообщает ничего полезного.
Вся дальнейшая работа --- ужимать этот "не знаю" до точного ответа там, где это возможно.

#important[
  Аппарат для приближённого ответа уже знаком: знаковая решётка, супремум и неподвижная точка.
  Вместо точных значений анализ работает с их грубыми обобщениями --- _абстракциями_.
  Цена --- неточность: анализ может сообщить о возможной ошибке там, где на деле всё в порядке.
  Корректность при этом сохраняется: ни одна реальная ошибка не пропадает.
]

== Знаки вместо чисел

Идея проста: заменить точные значения их грубыми обобщениями.
Чтобы доказать, что знаменатель не равен нулю, достаточно знать, что $x$ не бывает нулём.
Вместо числа возьмём его _знак_: положительное, отрицательное или ноль.
Добавим два особых значения --- "неизвестно" и "недостижимо".
Таких обобщений конечное число, и программа "выполняется" на них за конечное число шагов.

#example[
  Для доказательства отсутствия деления на ноль в программе `y := 10 / x` достаточно знать знак $x$.
  Домен знаков различает случаи: при $x != 0$ деление на ноль невозможно.
  При $x = 0$ --- возможно.
]

== Домен знаков

#definition[Домен знаков][
  Домен знаков состоит из пяти элементов:
  - $top$ --- "знак неизвестен" (значение может быть любым),
  - $plus$ --- "значение положительно",
  - $0$ --- "значение равно нулю",
  - $minus$ --- "значение отрицательно",
  - $bot$ --- "значение невозможно" (переменная здесь не определена).
]

#align(center)[#sign-lattice]

Элементы упорядочены по точности: $bot$ --- самое точное (значения нет), $top$ --- самое грубое (значение любое).
Нагляднее всего порядок виден на решётке: $bot prec.eq {minus, 0, plus} prec.eq top$.

== Информационный порядок

#definition[Информационный порядок][
  Говорим, что $a prec.eq b$ ("$a$ не грубее $b$"), если любой конкретный смысл $a$ содержится в конкретном смысле $b$.
  Например, $plus prec.eq top$: "положительное" точнее, чем "любое".
  И $bot prec.eq plus$: "недостижимо" точнее, чем "положительное".
]

#note[Почему порядок "наоборот"][
  Здесь $prec.eq$ означает "не грубее": элемент ниже по порядку несёт больше информации.
  Чем ниже спускаемся по решётке, тем точнее знаем состояние программы.
]

== Абстракция и конкретизация

Каждое абстрактное значение описывает множество конкретных значений.
Значение $plus$ описывает все положительные числа, $top$ --- все целые, $0$ --- только ноль.

#definition[Конкретизация][
  Пусть $cal(C)$ --- множество конкретных состояний программы, а $D$ --- абстрактный домен.
  *Конкретизация* $gamma: D -> cal(P)(cal(C))$ отображает абстрактное значение в множество конкретных состояний, которые оно представляет.
]

#definition[Абстракция][
  *Абстракция* $alpha: cal(P)(cal(C)) -> D$ отображает множество конкретных состояний в наиболее точное абстрактное значение, его покрывающее:
  $ alpha(X) = inter.sq {d in D mid(|) X subset.eq gamma(d)}. $
  Для домена знаков $alpha({5, 7}) = plus$.
  Конкретизация: $gamma(plus) = {x in ZZ mid(|) x > 0}$.
]

== Связка Галуа

#note[Связка Галуа][
  Пара $(alpha, gamma)$ образует _связку Галуа_:
  для любых $X subset.eq cal(C)$ и $d in D$
  $
    alpha(X) prec.eq d arrow.l.r.double X subset.eq gamma(d).
  $
  Обе стороны утверждают одно: абстрактное значение $d$ описывает всё множество $X$.
  Абстракция даёт наиболее точное описание, конкретизация --- все значения, которые $d$ представляет.
]

== Абстрактный домен

#definition[Абстрактный домен][
  *Абстрактный домен* --- решётка $(D, prec.eq, bot, top, union.sq, inter.sq)$, где:
  - $prec.eq$ --- информационный порядок ("не грубее"),
  - $bot$ --- наименьший элемент ("недостижимо"),
  - $top$ --- наибольший элемент (все значения),
  - $union.sq$ --- объединение: $a union.sq b$ описывает всё, что описывают $a$ и $b$,
  - $inter.sq$ --- пересечение: $a inter.sq b$ --- наиболее точное значение, совместимое с обоими.
  Объединение соответствует слиянию веток программы, пересечение --- усилению требования.
]

== Абстрактные состояния

#note[
  Для программы с $n$ переменными абстрактное состояние --- вектор из $n$ абстрактных значений.
  Например, $(minus, plus, 0, dots)$.
  Порядок поэлементный: $a prec.eq b arrow.l.r.double forall i. a_i prec.eq b_i$.
  Конечный текст программы описывается конечным множеством абстрактных состояний.
]

== Переносящие функции

Конкретная семантика оператора превращает множество состояний в множество состояний.
Например, оператор $x := x + 1$ переводит множество значений в сдвинутое множество.
То есть $X + 1 = {v + 1 mid(|) v in X}$.

#definition[Переносящая функция][
  *Переносящая функция* --- отображение $[| s |]^sharp : D -> D$, которое вычисляет абстрактное состояние после оператора $s$ по абстрактному состоянию до оператора.
  Интуиция: "запустить оператор на абстрактных значениях".
]

== Пример: присваивание в домене знаков

#example[Присваивание в домене знаков][
  Пусть $x := x + 1$.
  Абстрактный результат:
  - $plus -> plus$ --- положительное плюс единица остаётся положительным;
  - $minus -> top$ --- отрицательное плюс единица может стать нулём;
  - $0 -> plus$;
  - $top -> top$;
  - $bot -> bot$.
  Правило $minus -> top$ --- цена абстракции: знак не различает $-1$ и $-5$.
  Он не видит, что $-1 + 1 = 0$.
]

== Корректность переноса

Переносящая функция обязана быть _корректной_: она не имеет права терять ни одно реальное поведение.

#definition[Корректность переноса][
  Переносящая функция $[| s |]^sharp$ _корректна_, если для любого абстрактного состояния $d$
  $
    [| s |] (gamma(d)) subset.eq gamma([| s |]^sharp (d)),
  $
  где $[| s |]$ --- конкретная семантика оператора $s$ на множествах состояний.
  Перенос добавляет лишь невозможные поведения и не теряет настоящие.
]

#definition[Монотонность][
  Перенос $[| s |]^sharp$ _монотонен_, если из $d_1 prec.eq d_2$ следует
  $[| s |]^sharp (d_1) prec.eq [| s |]^sharp (d_2)$.
  Без монотонности итерации по циклу могут не сходиться.
]

== Сложение в домене знаков

#example[Сложение в домене знаков][
  Операция $z := x + y$ требует абстрактной таблицы сложения знаков.
  Правила:
  - $plus plus.square plus = plus$;
  - $minus plus.square minus = minus$;
  - $x plus.square 0 = x$ для любого знака $x$;
  - $plus plus.square minus = top$ --- сумма знаков может быть любой;
  - $top plus.square x = top$ при $x != bot$;
  - $bot plus.square x = bot$.
  Правило $plus plus.square minus = top$ --- источник неточности: знак не хранит величин.
  Он не видит, что $7 + (-4) = 3 > 0$.
  Корректность сохраняется: абстрактный результат $top$ покрывает реальный.
]

== Интервальный домен

Программа суммирует элементы массива:
```
s := 0
for i in 0..n do
  s := s + a[i]
```
Индекс $i$ не должен выйти за границы массива.
Чтобы доказать это, достаточно знать, что $i$ лежит в отрезке $[0, n-1]$.

#definition[Интервальный домен][
  Абстрактное значение --- интервал $[l, h]$ либо особый элемент $bot$.
  Здесь $l, h in ZZ union {-oo, +oo}$.
  Конкретизация --- все целые числа из интервала $[l, h]$.
  $top = [-oo, +oo]$.
  Порядок по включению: $[a, b] prec.eq [c, d] arrow.l.r.double [a, b] subset.eq [c, d]$.
  Объединение --- выпуклая оболочка: $[a, b] union.sq [c, d] = ["min"(a, c), "max"(b, d)]$.
  Пересечение --- наиболее точный интервал: $["max"(a, c), "min"(b, d)]$ (пусто, если нижняя граница больше верхней).
]

== Интервалы и связи между переменными

Сложение интервалов поэлементное: $[a, b] + [c, d] = [a + c, b + d]$.

#note[
  Интервальный домен теряет связи между переменными.
  Если в каждой точке программы $y = x - 1$, точное множество пар лежит на прямой.
  Интервальный анализ видит лишь прямоугольник $x in [2, 5], y in [1, 4]$ и не замечает, что $y$ определён значением $x$.
  Связи между переменными возвращают более богатые домены --- октагоны и полиэдры.
]

#example[Выход за границы массива][
  Пусть в программе $i in [0, 9]$, а массив имеет 10 элементов.
  Перенос обращения к элементу $A[i]$ проверяет, что индекс в границах.
  Если интервал $i$ выходит за границы --- например, $[0, 10]$ --- анализатор сообщает о возможном выходе за границы.
  Сообщение может оказаться ложным, а реальный выход за границы он не пропустит.
]

== Домен констант

#definition[Домен констант][
  Абстрактное значение --- либо конкретное целое $c$, либо особое значение $top$ ("не константа"), либо $bot$ ("недостижимо").
  Порядок: $bot prec.eq c prec.eq top$ для всех целых $c$.
]

Домен констант доказывает равенства.
После присваиваний $x := 3, y := x + 4$ выполняется равенство
$ y = 7. $
Компиляторы используют его для _распространения констант_: подставить вычисленное значение и упростить выражение.

== Пример: распространение констант

#example[Распространение констант][
  Программа:
  ```
  x := 3
  y := x + 4
  z := y * 2
  ```
  Анализ констант: $x = 3, y = 7, z = 14$.
  Компилятор заменит $z := y dot 2 -> z := 14$.
]

== Перенос условий

#definition[Перенос условий][
  Условие переносится как _фильтр_: абстрактное состояние пересекается с множеством, заданным условием.
  Например, для условия `i < n` интервал $i$ в ветке "да" становится $[l, h] -> [l, "min"(h, n-1)]$ (при известной константе $n$).
  Если $l > n-1$ --- ветка "да" недостижима.
  Ветка "нет" получает пересечение с дополнением: $[l, h] inter.sq [n, +oo]$.
  Если $n$ неизвестна (её интервал --- $top$), фильтр ничего не отсекает.
]

== Неподвижная точка цикла

Простая последовательность операторов переносится естественно: идём по программе, применяя переносящие функции.
С условными операторами объединяем ветки через $union.sq$.
Сложность --- циклы: тело цикла может выполняться сколько угодно раз.

Пусть $E$ --- состояние в голове цикла при входе.
Определим $F(d) = E union.sq [| B |]^sharp (d)$: объединение состояния на входе и результата одного прохода тела из $d$.
Состояние после любого числа проходов --- наименьшая неподвижная точка $F$:
$
  "lfp"(F) = union.sq_(k >= 0) F^k (bot).
$

Смысл: последовательность $F^k (bot)$ монотонно накапливает состояния, достижимые в голове цикла.
Объединив все $F^k (bot)$, получаем состояние после любого числа проходов.

== Итерация Клини

#theorem[Итерация Клини][
  Если $F$ монотонна и домен конечен, последовательность итераций
  $d_0 = bot, d_(k+1) = F(d_k)$
  достигает $"lfp"(F)$ за конечное число шагов.
]

#proof[Доказательство][
  Монотонность даёт $d_0 prec.eq d_1 prec.eq d_2 prec.eq dots$: последовательность не убывает.
  В конечной решётке любая неубывающая последовательность стабилизируется.
  С какого-то момента $d_(k+1) = d_k$.
  Тогда $F(d_k) = d_k$ --- неподвижная точка.
  Она наименьшая: любая неподвижная точка содержит $bot = d_0$.
  Значит, она содержит и все последующие $d_k$.
]

== Расходящаяся итерация

Домен знаков конечен --- циклы в нём анализируются перебором.
Интервальный домен бесконечен: интервалы вида $[0, n]$ с ростом $n$ никогда не стабилизируются.
Именно здесь наивная итерация ломается.

#example[Расходящаяся итерация][
  Рассмотрим цикл
  ```
  i := 0
  while i < n do
    i := i + 1
  ```
  Интервальный анализ состояния $i$ в голове цикла даёт последовательность $[0, 0], [0, 1], [0, 2], [0, 3], dots$.
  Каждая итерация расширяет интервал на единицу.
  Процесс никогда не остановится: наивной итерацией неподвижную точку не получить.
]

Условие `i < n` здесь не помогает: значение $n$ неизвестно (его интервал --- $top$), поэтому фильтр условия ничего не отсекает.

== Widening

#definition[Оператор расширения][
  Бинарная операция $nabla : D times D -> D$, для которой:
  - $a union.sq b prec.eq a nabla b$ (результат не менее груб, чем объединение),
  - для любой возрастающей последовательности $d_0 prec.eq d_1 prec.eq dots$ последовательность
    $w_0 = d_0, w_(k+1) = w_k nabla d_(k+1)$
    стабилизируется за конечное число шагов.
  Словами: оператор $nabla$ "ускоряет" рост, сбрасывая нестабильные компоненты в $top$.
]

== Widening для интервалов

#example[Widening для интервалов][
  Классический widening:
  $
    [l_1, h_1] nabla [l_2, h_2] & = [l, h], \
                              l & = cases(-oo "если" l_2 < l_1, l_1 "иначе"), \
                              h & = cases(+oo "если" h_2 > h_1, h_1 "иначе").
  $
  В примере со счётчиком $[0, 0] nabla [0, 1] = [0, +oo]$: верхняя граница выросла --- сбрасываем её в бесконечность.
  Следующая итерация: $[0, +oo] nabla [0, +oo] = [0, +oo]$ --- стабилизация.
  Неподвижная точка достигнута за два шага.
]

== Narrowing

#definition[Оператор сужения][
  Бинарная операция $triangle : D times D -> D$.
  Результат не грубее первого операнда: $a triangle b prec.eq a$.
  Повторяя сужение из уже достигнутой неподвижной точки, получаем более точную неподвижную точку.
]

#example[Narrowing в примере со счётчиком][
  Сужение идёт по условию цикла.
  При известной константе $n$ фильтр `i < n` уточняет интервал $[0, +oo] -> [0, n-1]$.
  Верхняя граница восстановлена из условия.
]

Схема "widening для сходимости, narrowing для точности" --- стандартный рецепт анализа циклов в интервальном домене.

== Цена аппроксимации

Абстрактный анализ корректен, но не точен.
Корректность здесь означает: анализатор не пропускает реальные ошибки.
Неточность означает: он может сообщить об ошибке, которой на деле нет.

#definition[Ложное срабатывание][
  Если анализатор сообщает о возможной ошибке в точке, где ошибки не бывает, --- это _ложное срабатывание_ (false positive).
  Корректный анализатор допускает ложные срабатывания, но не допускает пропусков.
]

#note[
  Абстракция рисует вокруг исполнений оболочку.
  Если оболочка не задевает "запретную" область, ошибка доказанно невозможна.
  Если задевает --- анализатор не различает реальную ошибку и ложную тревогу.
  Замалчивание в неоднозначном случае было бы пропущенной реальной ошибкой.
]

== Пример: точность и домен

#example[Точность и домен][
  Рассмотрим программу $x := 6, y := -7, z := x + y$.
  Домен знаков даёт для неё $z = top$: знак суммы не вычисляется.
  В действительности $z = -1$ --- определённое число.
  Домен констант посчитал бы $z = -1$ точно.
  Домен интервалов --- $[-1, -1]$.
  Выбор домена --- компромисс между точностью, стоимостью и свойством, которое нужно доказать.
]

== Применения

Абстрактная интерпретация --- основа индустрии статического анализа.

- Оптимизирующие компиляторы (GCC, LLVM) внутри --- анализаторы потоков данных: распространение констант, анализ диапазонов, удаление мёртвого кода.
- Astrée доказала отсутствие ошибок времени выполнения в полётном софте Airbus A340 и A380.
- Polyspace (MathWorks) верифицирует встраиваемый софт.
- Frama-C --- открытая платформа анализа кода на C.

#important[
  Абстрактная интерпретация вычисляет инварианты циклов автоматически.
  Человеку их придумывать не нужно.
]

== Символьное исполнение

#note[
  Символьное исполнение --- противоположный полюс: оно перебирает пути точно, ценой стоимости.
  На практике подходы работают вместе: анализ находит подозрительные места, символьное исполнение подтверждает их по конкретным путям.
]

== Что мы поняли?

- Тестирование не доказывает отсутствие ошибок: свойство нужно доказывать для всех запусков сразу.
- Точный анализ программ неразрешим --- работает корректная аппроксимация с третьим ответом "не знаю".
- Абстрактный домен --- решётка с информационным порядком; связь с конкретным задают $alpha$ и $gamma$.
- Переносящие функции переносят операторы в абстрактный мир и обязаны быть корректными и монотонными.
- Знаки, интервалы и константы --- домены для разных свойств.
- Неподвижная точка переносящей функции цикла --- результат анализа; widening и narrowing обеспечивают сходимость.
- Ложные срабатывания неизбежны, пропуски ошибок --- нет.

== Вопросы для самопроверки

- Почему тестирование не может доказать отсутствие ошибок?
- Что такое абстрактный домен, и зачем нужны $bot$ и $top$?
- Как связаны абстракция $alpha$ и конкретизация $gamma$?
- Почему $plus plus.square minus = top$ в домене знаков?
- Чем интервальный домен отличается от знакового?
- Почему наивная итерация для интервалов расходится, и как widening это исправляет?
- Почему ложные срабатывания неизбежны, а пропуски ошибок --- нет?
