// s1, лекция 8 --- Счётность и несчётность (неделя 26 октября --- 1 ноября).
#import "theme.typ": *
#import "diagrams/ghosts.typ": (
  cantor-ghost, closure-ghost, composition-ghost, diamond-ghost, function-ghost,
  hotel-ghost, partition-ghost, poset-ghost, quotient-ghost, relation-ghost,
)
#import "diagrams/m05.typ": equivalence-partition, rel-digraph
#import "diagrams/m06.typ": (
  function-parts, mapping-bijection, mapping-injection, mapping-surjection,
)
#import "diagrams/m07.typ": (
  aleph-beth, cantor-diagonal, cantor-line-square, qq-pairing,
)
#import "diagrams/m08.typ": (
  hasse-chain-3, hasse-divisors-12, hasse-powerset-2, hasse-powerset-3,
  sign-lattice,
)
#show: slides.with(
  title: [Счётность и несчётность],
  date: "Осень 2026",
  authors: "Константин Чухарев",
  module: "relations",
  lecture: (
    num: 8,
    week: [27--28 октября],
    teaser: [
      Равномощность: в отеле Гильберта места находятся всегда.
      Теорема Кантора покажет, что бесконечности разные --- и строго вложенные.
    ],
  ),
)

= Равномощность и отель Гильберта

#focus-slide(
  ghost: hotel-ghost,
  epigraph: [Я протестую против использования бесконечной величины как чего-то завершённого.],
  epigraph-author: [Карл Фридрих Гаусс],
)

== Сколько бесконечностей?

Натуральных чисел бесконечно много.

Действительных --- тоже.

На первый взгляд ответ одинаков: и там, и там бесконечно много.

#important[
  У бесконечности нет числа, но есть размер.

  Кантор показал: бесконечностей много, и они разного размера.
]

== Равномощность

#definition[
  Множества $A$ и $B$ *равномощны* (_equinumerous_), если существует биекция $f: A -> B$.

  Обозначение: $abs(A) = abs(B)$.
]

- Для конечных множеств это совпадает с подсчётом.
- Для бесконечных --- даёт контринтуитивные выводы.

#example[
  $NN$ и чётные числа $E = {0, 2, 4, ...}$ равномощны.

  Биекция $f(n) = 2n$, обратная --- $g(m) = m/2$.
]

#important[
  Мощность $NN$ обозначается $aleph_0$: $abs(NN) = abs(ZZ) = abs(QQ) = aleph_0$.
]

== Часть равна целому

- Для конечных множеств часть всегда меньше целого.
- Для бесконечных --- нет: $E subset NN$, но $abs(E) = abs(NN) = aleph_0$.

#important[
  Дедекинд определял бесконечность именно так: множество, равномощное своему собственному подмножеству.
]

#note[
  Галилей в 1638 году заметил, что квадратов столько же, сколько натуральных, и заключил, что к бесконечности слова "больше" и "меньше" неприменимы.

  Кантор ответил иначе: раз биекция есть, значит "столько же".
]

== Отель Гильберта

В бесконечном отеле Гильберта счётное число комнат.

Комнаты пронумерованы натуральными числами: $0, 1, 2, ...$.

Все комнаты заняты.

Свободных комнат нет.

#important[
  Администратор расселяет гостей, никого не выселяя и не подселяя двоих в одну комнату.

  После каждого заселения отель снова заполнен полностью.
]

== Один новый гость

Отель заполнен, приходит новый гость.

#example[
  + Администратор сдвигает всех на одну комнату: из $n$ --- в $n + 1$.
  + Комната $0$ освобождается, в неё заселяется гость.
]

#important[
  Отель был заполнен, добавился один гость --- отель остался заполнен.

  $ aleph_0 + 1 = aleph_0. $
]

== Автобус с бесконечным числом гостей

Прибывает автобус со счётным числом гостей $b_0, b_1, b_2, ...$.

#example[
  - Старых постояльцев --- в чётные комнаты: из $n$ --- в $2n$.
  - Новых гостей --- в нечётные: $b_k$ --- в комнату $2k + 1$.
]

#important[
  И старых, и новых гостей --- $aleph_0$.

  $ aleph_0 + aleph_0 = aleph_0. $
]

== Бесконечное число автобусов

Прибывает счётное число автобусов, в каждом --- счётное число гостей.

Гость на месте $j$ в автобусе $i$ --- это $a_(i,j)$, где $i, j in NN$.

#note[
  + Сначала текущих постояльцев расселяем из комнаты $n$ в $2n$ (чётные комнаты).
  + Освободившиеся нечётные комнаты получают пары $(i, j)$: диагональное перечисление пар даёт каждой паре свой номер.
]

#important[
  $ aleph_0 dot aleph_0 = aleph_0. $
]

== Что мы увидели?

#table(
  columns: 2,
  align: (left, center),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([*Наплыв гостей*], [*Мощность*]),
  [один гость], [$aleph_0 + 1 = aleph_0$],
  [счётный автобус], [$aleph_0 + aleph_0 = aleph_0$],
  [счётное число автобусов], [$aleph_0 dot aleph_0 = aleph_0$],
)

#important[
  Отель примет любой счётный наплыв.

  Но отступит перед несчётным: действительные числа не расселить ни по какой схеме.
]

== Счётные и несчётные

#definition[
  - Множество *счётно*, если оно конечно или равномощно $NN$.
  - Множество *несчётно*, если оно бесконечно и не равномощно $NN$.
]

#note[
  Счётное бесконечное множество можно _перечислить_ --- выписать в один бесконечный список.

  $ZZ$ и $QQ$ счётны, $RR$ --- нет.
]

== $ZZ$ счётно

#example[
  Биекция $NN -> ZZ$ перечисляет:
  $ 0, -1, 1, -2, 2, -3, 3, ... $
]

#important[
  На первый взгляд целых "вдвое больше", чем натуральных.

  Биекция всё выравнивает: $abs(ZZ) = abs(NN) = aleph_0$.
]

== $QQ$ счётно

$QQ$ плотно заполняет прямую --- и всё же счётно.

#align(center)[#qq-pairing]

== Диагональное перечисление

#note[
  Дроби перечисляются по диагоналям:
  + Каждая дробь $p/q$ встретится.
  + Пропуская повторные записи одной дроби (сократимые, например $2/4 = 1/2$), получаем перечисление без повторов.
]

#important[
  Плотность --- про расположение, мощность --- про количество.

  Это разные оси.
]

== $RR$ несчётно

#theorem[
  Интервал $(0, 1)$ несчётен, а значит и $RR$.
]

#align(center)[#cantor-diagonal]

== Диагональный аргумент

#proof[
  Предположим, $(0, 1)$ перечислимо: $(0, 1) = {r_0, r_1, r_2, ...}$.

  Каждое $r_i$ записываем десятичной дробью без хвоста из девяток.

  Строим $r$: в $i$-й цифре ставим 4, если у $r_i$ там не 4, иначе --- 5.

  Только цифры 4 и 5, поэтому у $r$ нет двусмысленной записи вида $0.0999 ... = 0.1000 ...$.

  Тогда $r in (0, 1)$ и отличается от $r_i$ в $i$-й цифре --- противоречие.

  Значит, $(0, 1)$ несчётно, а так как $(0, 1) subset RR$, то и $RR$ несчётно.
]

== Отрезок и квадрат

#theorem[
  $abs([0, 1]) = abs([0, 1]^2)$.

  Отрезок, квадрат, куб --- равномощны: для мощности размерность не имеет значения.
]

#align(center)[#cantor-line-square]

== Переплетение цифр

#proof[
  Переплетение десятичных цифр $(0.x_1 x_2 ..., 0.y_1 y_2 ...) |-> 0.x_1 y_1 x_2 y_2 ...$ --- взаимно однозначное соответствие между $[0, 1]^2$ и $[0, 1]$ с оговоркой про $0.0999 ... = 0.1000 ...$.
]

#important[
  Кантор три года искал доказательство, что биекции нет --- и нашёл биекцию.

  "Я вижу это, но не верю".
]

== Программ счётно, языков --- континуум

Каждая программа записывается конечным текстом.

Конечных строк над конечным алфавитом счётное число.

#important[
  Программ --- $aleph_0$, потому что каждая программа --- конечная строка.

  Языков, то есть подмножеств $Sigma^*$, --- континуум: $2^(aleph_0)$.

  Значит, почти все языки не вычислимы ни одной программой.
]

= Теорема Кантора и иерархия мощностей

#focus-slide(
  ghost: cantor-ghost,
  epigraph: [Из рая, который создал для нас Кантор, никто не сможет нас изгнать.],
  epigraph-author: [Давид Гильберт],
)

== Теорема Кантора

#theorem[Теорема Кантора][
  Для любого множества $A$:
  $ abs(A) < abs(power(A)). $
]

#proof[
  Рассмотрим произвольную функцию $f: A -> power(A)$ и диагональное множество $D = {a in A mid(|) a in.not f(a)}$.

  Если $a in D$, то $a in.not f(a)$ --- значит, $f(a) != D$.
  Если $a in.not D$, то $a in f(a)$ --- и снова $f(a) != D$.

  Никакой элемент не отображается в $D$: функция $f$ не сюръективна.

  Инъекция $A -> power(A)$ есть --- $a |-> {a}$, биекции нет.
]

== Лестница мощностей

Теорема Кантора порождает бесконечную иерархию:

$ aleph_0 < 2^(aleph_0) < 2^(2^(aleph_0)) < ... $

#important[
  Бесконечностей бесконечно много.

  Для конечного $A$ из $n$ элементов: $abs(power(A)) = 2^n$ --- тот же булеан, что в лекции о множествах.
]

== Кардинальная арифметика

#table(
  columns: 2,
  align: (left, center),
  stroke: (x, y) => if y == 0 { (bottom: 0.8pt) },
  table.header([*Операция*], [*Результат*]),
  [$aleph_0 + aleph_0$], [$aleph_0$],
  [$aleph_0 dot aleph_0$], [$aleph_0$],
  [$2^(aleph_0)$], [$> aleph_0$ --- новый кардинал],
)

#important[
  Сложение и умножение не меняют счётную мощность.

  Возведение в степень рождает новый кардинал.
]

== Ординалы: обзор

Кардиналы отвечают на вопрос "сколько?", а ординалы --- "какой по счёту?".

Порядковое число --- это тип вполне упорядоченного множества.

#example[
  - Натуральные числа по порядку: $0, 1, 2, 3, ...$.
  - За всеми натуральными идёт первый бесконечный ординал $omega$.
  - Дальше: $omega + 1$, $omega + 2$, ..., $omega + omega$, ...
]

#important[
  Сложение ординалов не коммутативно: $1 + omega = omega$, но $omega + 1 != omega$.

  Кардиналы прибавку не замечают ($aleph_0 + 1 = aleph_0$), а ординалы --- различают ($omega + 1 != omega$).
]

== Алефы и беты

#definition[
  _Бет-числа_ $beth_0, beth_1, beth_2, dots$ строятся через булеан:
  $ beth_0 = aleph_0, quad beth_(n+1) = 2^(beth_n). $

  _Алеф-числа_ $aleph_0, aleph_1, aleph_2, dots$ перечисляют все бесконечные кардиналы по порядку:
  $
    aleph_0 = abs(NN), quad aleph_(alpha+1) = "наименьший кардинал, больший" aleph_alpha.
  $
]

#note[
  Беты --- конструктивная лестница: каждый уровень --- булеан предыдущего.

  Алефы --- последовательная: известен порядок, но не устройство $aleph_1$.
]

#pagebreak()

#align(center)[#aleph-beth]

== Континуум-гипотеза

#definition[
  _Континуум-гипотеза_ (CH) утверждает: не существует кардинала строго между $aleph_0$ и континуумом:
  $ aleph_1 = beth_1 = 2^(aleph_0) = abs(RR). $
]

#note[
  Обобщённая континуум-гипотеза (GCH) требует совпадения иерархий на всей лестнице.

  Ни CH, ни GCH нельзя ни доказать, ни опровергнуть в ZFC --- Коэн (1963) доказал независимость.

  Это предел того, что аксиомы множеств фиксируют про размеры бесконечности.
]

== Теорема Кантора--Бернштейна--Шрёдера

Сравнение мощностей определяется через инъекции:
- $abs(A) <= abs(B)$, если существует инъекция $f: A -> B$;
- $abs(A) < abs(B)$, если $abs(A) <= abs(B)$ и биекции нет.

#theorem[Теорема КБШ][
  Если $abs(A) <= abs(B)$ и $abs(B) <= abs(A)$, то $abs(A) = abs(B)$.
]

#proof[
  Идея: от $a in A$ идём назад по $g^(-1)$, затем вперёд по $f$, чередуя шаги.
  Цепочки обрываются в $A$, в $B$ или бесконечны --- тип цепочки решает, куда отправить элемент.
]

#important[
  Равномощность доказывается без явной биекции --- достаточно инъекций в обе стороны.
]

== Применение: $abs((0, 1)) = abs([0, 1])$

#example[
  Инъекция $(0, 1) -> [0, 1]$ --- включение.

  Инъекция $[0, 1] -> (0, 1)$ --- сдвиг внутрь: $x |-> x/2 + 1/4$, образ --- отрезок $[1/4, 3/4]$.
]

#important[
  По теореме КБШ множества равномощны.

  Явную биекцию строить не пришлось.
]

== Лекция 8: что мы поняли?

#important[
  - Равномощность --- это биекция: часть может быть равномощна целому (чётные числа и $NN$).
  - Счётное бесконечное множество равномощно $NN$: отель Гильберта, $ZZ$ и $QQ$ счётны.
  - $RR$ несчётен --- диагональный аргумент Кантора.
  - Теорема Кантора: $abs(A) < abs(power(A))$ для любого $A$.
    Лестница мощностей уходит вверх без конца.
  - Ординалы считают "какой по счёту", кардиналы --- "сколько".
    Континуум-гипотеза независима от ZFC.
  - Теорема КБШ сводит равномощность к двум инъекциям.
]

== Вопросы для самопроверки

+ Докажите: множество чётных чисел равномощно $NN$.
  Приведите явную биекцию и обратную.

+ Докажите: если $A$ и $B$ счётны, то $A times B$ счётно.

+ Покажите, что множество бесконечных битовых строк ${0, 1}^oo$ несчётно.

+ Почему $aleph_0 + aleph_0 = aleph_0$, но $2^(aleph_0) > aleph_0$?

+ Докажите через теорему КБШ: $abs((0, 1)) = abs([0, 1])$.

+ Почему программ счётное число, а языков --- континуум?

+ Чем отличаются алеф- и бет-иерархии?
  Что утверждает континуум-гипотеза?

== Чтение к следующей паре

#note[
  - m07 --- "Равномощность", "Счётные и несчётные множества", "Теорема Кантора".
  - Флеш-опросник --- в начале лекции 9.
  - Контрольная 2 (отношения, порядок, функции, счётность) --- на следующей неделе, на отдельной паре.
]
